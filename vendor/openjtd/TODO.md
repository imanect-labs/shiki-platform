# TODO

## M1: Container Explorer

Goal: implement `rjtd streams <file.jtd>` as the first executable milestone.

Status: implemented for CFB entry inventory, including a rhwp-style lenient fallback for malformed FAT files.

Why this comes first:

- rjtd must follow rhwp's layered parsing order.
- rhwp starts binary parsing from the CFB container layer before header, record, body text, model, renderer, or exporter work.
- JTD structure should be observed and documented before text extraction is attempted.
- Exporters must never read raw file data directly; all later work depends on stable lower layers.

Immediate tasks:

- [x] Keep local `.jtd`, `.jtt`, and `.jttc` samples under `rjtd-testdata/local-samples/`.
- [x] Write tests before implementation.
- [x] Add a minimal CFB container reader to `rjtd-core`.
- [x] Expose stream/storage entries with normalized paths, byte sizes, and entry kind.
- [x] Add a rhwp-style lenient CFB fallback for malformed FAT samples.
- [x] Implement `rjtd streams <file.jtd>` in `rjtd-cli`.
- [x] Implement `rjtd info <file>` as a rhwp-style lightweight inventory command.
- [x] Record early observations in `openjtd-spec`.

Acceptance criteria:

- [x] Unit tests create a tiny synthetic CFB file and prove stream listing works.
- [x] CLI integration tests run `rjtd streams` against a synthetic CFB file.
- [x] Local sample files can be inspected manually with `cargo run -p rjtd-cli -- streams ../rjtd-testdata/local-samples/<name>.jtd`.
- [x] `cargo fmt --all --check`, `cargo check --workspace`, `cargo test --workspace`, and `cargo clippy --workspace --all-targets -- -D warnings` pass from `rjtd/`.

## M2: Text Extraction

Goal: implement `rjtd cat <file.jtd>` after M1 reveals the relevant stream layout.

Do not start M2 until M1 has produced documented stream observations for the local samples.

Status: structured `ParsedDocumentText` token layer implemented for observed text runs and display inline segments. Observed `.jttc` samples are opened through the `/JSCompDocument` `JustCompressedDocument` wrapper. Embedded `SsmgV.01` fragments are recovered for the current no-`/DocumentText` samples.

Priority prework:

- [x] Download the historical Ichitaro Document Filter `.oxt`.
- [x] Unpack the `.oxt` and record its file tree.
- [x] Confirm whether it contains Java, native code, or OpenOffice registry files.
- [x] Record filter/type registration metadata.
- [x] Search for stream and conversion strings without decompiling the DLL.
- [x] Investigate `/DocumentText` bytes in local samples against the stream names confirmed by the filter.
- [x] Add a read-only `rjtd dump-stream <file.jtd> <path>` helper or test utility if needed.
- [x] Implement initial `rjtd cat <file.jtd>` using `DocumentText` text-run markers.
- [x] Recover common display inline segments wrapped by `0x001D ... 0x001E`, including ruby base text and template placeholders.
- [x] Skip known phonetic annotation and template instruction inline segments in plain `cat` output.
- [x] Detect `.jttc` samples that store `/JSCompDocument` as `JustCompressedDocument`.
- [x] Decode observed `.jttc` `/JSCompDocument` `-lh5-` payloads without adding an LHA dependency.
- [x] Report invalid or unsupported compressed payloads clearly.
- [x] Confirm local sample inventory opens after malformed FAT fallback.
- [x] Recover embedded `SsmgV.01`/`TextV.01` fragments when `/DocumentText` is absent.
- [x] Parse `SsmgV.01` w[9]=`0x0001` raw-text segment format: `TextV.01` segment name (8 raw ASCII bytes) + two-word length + plain UTF-16BE, no `0x001f` paragraph markers; enables `te.jtd` text extraction.
- [x] Confirm `rjtd cat` succeeds on all 61 current local samples.
- [x] Add `rjtd text-tokens <file>` to inspect structured `ParsedDocumentText` output.
- [x] Add `rjtd text-control-context <file> [control-code]` to inspect `/DocumentText` control boundaries with neighboring map entries and nearest control distances.
- [x] Replace the plain string extractor path with a structured `ParsedDocumentText` parser layer.
- [x] Preserve skipped inline text segments as structured tokens and document-model unknown objects.
- [x] Make CLI stdout writes tolerate broken pipes for token/stream inspection pipelines.
- [x] Add `rjtd text-positions <file>` for initial `/DocumentTextPositionTables` `MarkV.01` diagnostics.
- [x] Add `rjtd text-map <file>` to map `/DocumentText` tokens to byte and UTF-16 unit ranges.
- [x] Add `rjtd text-position-context <file>` to compare `MarkV.01` offsets against byte, unit, and provisional `unit + 29` token contexts.
- [x] Add `rjtd text-position-delta-scan <file>` to score MarkV.01 UTF-16 unit delta candidates from 0 through 64.
- [x] Add `rjtd text-position-mark-header <file>` to expose the raw six bytes between `MarkV.01` and the first entry plus raw `(id, offset)` entries.
- [x] Add `rjtd text-position-mark-summary <file>` to correlate the MarkV.01 header with `/DocumentText`, `/LineMark`, `/PageMark`, and `/PaperMark` metrics.
- [x] Add `rjtd text-position-line-context <file>` to compare the MarkV.01 header and entry offsets against `/LineMark` word contexts and nearest tag rows.
- [x] Record `/DocumentTextPositionTables` observations in `openjtd-spec`.
- [x] Record the current hypothesis that `MarkV.01` offsets are closer to UTF-16 unit/internal coordinates than raw byte or extracted-character positions.
- [x] Sweep `text-position-context` output across the 5 current samples with parsed `MarkV.01` entries and record the initial id classification.
- [x] Sweep `text-position-delta-scan` across the 5 current MarkV.01 samples: 40 entries total; `delta 9`, `delta 29`, and `delta 30` tie at 31 visible text hits, while `delta 9` has more unit hits than `delta 29`.
- [x] Sweep `text-position-mark-header` across the 5 current MarkV.01 samples: marker offset is consistently 30, header prefix is `00000000`, and the last big-endian `u16` is `0x0603`, `0x0610`, or `0x061c`, so the header does not directly encode a constant 29-unit adjustment.
- [x] Sweep `text-position-mark-summary` across the 5 current MarkV.01 samples: `0x0603` appears with different page/paper counts, `0x061c` appears in samples without `/LineMark`/`/PageMark`/`/PaperMark`, and no direct document-length or page-count meaning is proven.
- [x] Sweep `text-position-line-context` across the 61 current local samples: only `46.jtd`, `a5.jtd`, and `b6.jtd` have both readable `/LineMark` and parsed `MarkV.01`; all 24 MarkV.01 entry offsets are outside the `/LineMark` word range, while the MarkV.01 header's final `u16` lands inside `/LineMark`.
- [x] Search the four-byte MarkV.01 header values `00000603`, `00000610`, and `0000061c` across representative MarkV samples and confirm the exact four-byte values only appear in `/DocumentTextPositionTables`, not in the observed layout/style streams.
- [x] Add `rjtd text-position-counts <file>` for observed non-Mark `TCntV.01` numeric tables.
- [x] Classify the 11 non-Mark position-table samples as 10 `TCntV.01` tables plus 1 empty/unreadable position-table payload despite a non-zero inventory size.
- [x] Add `rjtd text-position-count-context <file>` to compare the first two `TCntV.01` fields against `/DocumentText` byte and UTF-16 unit token ranges.
- [x] Sweep `text-position-count-context` across the 10 current `TCntV.01` samples and record that the coordinate behavior is mixed.
- [x] Add `rjtd text-position-count-tail-context <file>` to compare tail `t1/t2` fields against `/DocumentText` byte and UTF-16 unit token ranges.
- [x] Sweep `text-position-count-tail-context` across current local samples: 10 readable `TCntV.01` files, 89 rows, any byte hit 21, both byte hit 5, any unit hit 49, both unit hit 28, and both unit text hit 26; this makes UTF-16 unit coordinates a stronger but still incomplete signal for `t1/t2`.
- [x] Add `rjtd text-position-count-tail-delta-scan <file>` to scan positive `0..64` UTF-16 unit deltas over tail `t1/t2` endpoints.
- [x] Sweep `text-position-count-tail-delta-scan` across current local samples: 10 readable `TCntV.01` files, 89 rows, 178 endpoints; delta 29 and 30 tie for best unit endpoint hits at 124, delta 29 has better text endpoint hits and both-unit rows than 30, while text endpoint hits peak at delta 53 with 102, so no single offset is proven.
- [x] Add `rjtd text-position-count-tail-delta-groups <file>` to summarize tail delta scores by `(family,t0,t3,t4,t7)` pattern.
- [x] Sweep `text-position-count-tail-delta-groups` across current local samples: the 28-row `be0/0x0101/0x0100/0x0001/0x0001` group prefers delta 29 for both unit and text; the 16-row shifted `0x0101/0x0100/0x0001/0x0001` group prefers unit delta 31 and text delta 30; the 28-row `be0/0x0202/0x0100/0x0000/0x0001` group is spread across many best deltas, so a single global adjustment is unlikely.
- [x] Add `rjtd text-position-count-tail-row-deltas <file>` to expose per-row best unit/text deltas, chosen spans, tail spans, and document byte/unit lengths.
- [x] Sweep `text-position-count-tail-row-deltas` across current local samples: the major `be0/0x0202/0x0100/0x0000/0x0001` group has 28 rows across 8 files, chosen spans 398..1212, tail spans 46..72, document unit lengths 65889..146657, and 17 different row-level best unit deltas; this looks like row-local structure rather than one file-level or global correction.
- [x] Add `rjtd text-position-count-tail-row-context <file>` to place chosen start/end byte/unit contexts and best-delta tail contexts on the same row.
- [x] Sweep `text-position-count-tail-row-context` across current local samples: all `0x0202` rows total 36 rows across 10 files; in the major 28-row `be0/0x0202/0x0100/0x0000/0x0001` group, chosen start/end are often byte-context body hits or boundaries while unit-context is mostly `between`, and best-delta tail `t2` lands on text in 27/28 rows. This supports treating chosen range and tail fields as different roles rather than duplicate text coordinates.
- [x] Add `rjtd text-position-count-range-preview <file>` to summarize `/DocumentText` entries overlapped by each chosen `TCntV.01` range as byte and UTF-16 unit intervals.
- [x] Sweep `text-position-count-range-preview` across current local samples: 61 checked, 10 readable `TCntV.01` files, 89 rows; all `t0=0x0202` rows total 36 rows, and 31/36 have chosen byte ranges overlapping text. The major `be0/0x0202/0x0100/0x0000/0x0001` group has 25/28 chosen byte ranges overlapping text and 21/28 chosen unit ranges overlapping text.
- [x] Add `rjtd text-position-count-range-boundaries <file>` to expose chosen range edge alignment, first/last/previous/next `/DocumentText` entries, and overlapped control-code counts.
- [x] Sweep `text-position-count-range-boundaries` across current local samples: in the major 28-row `be0/0x0202/0x0100/0x0000/0x0001` group, chosen byte ranges overlap 535 map entries total, 513 fully contained and 22 partial; 25/28 rows include controls, dominated by `0x001c` 281 times and `0x000e` 38 times. This makes `/DocumentText` control-delimited range structure the next target.
- [x] Sweep `text-control-context` across current local samples: 61 checked, 0 errors, 60 files with controls. `0x001c` appears 51,971 times in 60 files and most often separates text/text (16,717 rows), text/control (10,329), control/text (7,191), or control/control (6,561). `0x000e` appears 6,621 times in 41 files and most often appears in control/control (3,338), text/control (1,832), or text/skipped-inline (844) contexts.
- [x] Add `rjtd text-position-count-control-ranges <file> [control-code]` to compare chosen `TCntV.01` ranges against `/DocumentText` intervals split by a selected control delimiter.
- [x] Sweep `text-position-count-control-ranges` across the 10 readable `TCntV.01` files: 89 rows all map into some interval for both `0x001c` and `0x000e`, but `0x001c` produces many more overlaps (462 byte-basis intervals, 794 unit-basis intervals; 40 byte and 37 unit multi-interval rows) than `0x000e` (135 byte, 195 unit; 25 byte and 32 unit multi-interval rows). This makes direct `0x001c => paragraph` promotion too risky.
- [x] Expose candidate control-delimited overlap summaries on decoded-false `textCountRanges` model/export/app-core JSON as `controlRangeOverlaps` without promoting them to paragraph semantics.
- [x] Expose derived decoded-false `textBoundaryCandidates` model/export/app-core JSON from `controlRangeOverlaps` as diagnostic paragraph-boundary evidence without changing parsed paragraph semantics.
- [x] Preserve multi-interval `textBoundaryCandidates` as decoded-false `tableCandidates` in model/export/app-core JSON and validation warnings, including per-interval source ranges, text previews, and line-break counts, without changing parsed blocks or table/cell fallback APIs.
- [x] Add `rjtd table-candidates <file>` to print model-derived decoded-false table candidates with interval source evidence.
- [x] Sweep `table-candidates` across current local samples: 61 checked, 0 failures, 10 files with candidates, 134 candidates total, 65 byte-basis rows, 69 unit-basis rows, max 44 intervals, and 0 candidates with interval line breaks. This keeps the layer diagnostic-only until row/cell geometry is proven.
- [x] Add `rjtd table-candidate-context <file>` to summarize each preserved table candidate's interval text shape, empty intervals, line breaks, edge context, and strict diagnostic `cell-like` flag.
- [x] Sweep `table-candidate-context` across current local samples: 61 checked, 0 failures, 10 positive files, 134 candidates, 10 strict cell-like candidates, 124 non-cell-like candidates, 54 candidates with empty intervals, 112 candidates with line breaks, max 44 intervals, and max interval text length 7,443 characters. This rejects promoting all multi-interval candidates as tables.
- [x] Add `rjtd table-cell-like-candidates <file>` to emit only strict cell-like decoded-false table candidates as a compact input for row/cell geometry analysis.
- [x] Sweep `table-cell-like-candidates` across current local samples: 61 checked, 0 failures, 5 positive files, 10 candidates, all `delimiter=0x000e`, 6 unit-basis rows, 4 byte-basis rows, max 6 intervals, and max cell text length 65 characters.
- [x] Expose strict row-like table candidates through the existing rhwp-shaped read-only app-core table APIs (`get_table_dimensions`, `get_cell_info`, `get_text_in_cell`, `get_table_signature`) as observed N-row/1-column decoded-false tables, while keeping edit APIs and non-row-like candidates on compatibility fallbacks.
- [x] Add `cellLike`, `rowLike`, and `observedTable` shape metadata to model/export/app-core `tableCandidates` JSON so downstream tools can distinguish compact row-like evidence from diagnostic-only multi-interval spans.
- [x] Sweep JSON export `tableCandidates[].observedTable` across current local samples: 61 checked, 0 failures, 5 positive files, 134 total candidates, 10 `cellLike=true`/`rowLike=true`, 10 `observedTable` objects, and 0 bad N-row/1-column shapes, matching the strict CLI subset.
- [x] Add decoded-false `columnSegments` diagnostics to row-like `tableCandidates[].intervals[]`, splitting only conservative finance-style label/value runs while keeping `observedTable.colCount=1` until geometry is proven.
- [x] Sweep JSON export `columnSegments` across current local samples: 61 checked, 0 failures, 5 positive files, 10 observed tables, 38 split rows, 181 segments, max 8 segments per row, and 0 split rows under non-observed candidates.
- [x] Extend `rjtd table-cell-like-candidates <file>` with compact `columnSegments` diagnostics and sweep the current local samples: 61 checked, 0 failures, 5 positive files, 10 candidates, 38 split rows, 181 segments, and max 8 segments per row.
- [x] Add decoded-false column segment pattern consistency diagnostics (`columnSegmentPatternConsistent` and `columnSegmentPatternMismatchRows`) to observed row-like table metadata and CLI output, without changing `observedTable.colCount=1`.
- [x] Sweep column segment pattern consistency across current local samples: 61 checked, 0 failures, 5 positive files, 10 observed/strict candidates, 5 pattern-consistent candidates, 5 pattern-inconsistent candidates, 5 mismatch rows, and 38 split rows. JSON export and CLI distributions match.
- [x] Add decoded-false `columnGridCandidate` diagnostics for the stricter subset where every row has column segments and the lexical segment pattern is consistent, while still keeping `observedTable.colCount=1`.
- [x] Sweep `columnGridCandidate` across current local samples: 61 checked, 0 failures, 5 positive files, 10 observed/strict candidates, 3 grid candidates, 7 non-grid candidates, shapes `4x4` once and `5x4` twice. JSON export and CLI distributions match with 0 missing fields.
- [x] Project `columnGridCandidate` into fallback SVG pages and `getPageLayerTree` as decoded-false diagnostic table-grid overlays, with local-sample regression coverage proving SVG and layer-tree counts match the model-owned candidates.
- [x] Add `rjtd text-boundary-candidates <file>` to print model-derived decoded-false boundary candidates with basis, delimiter, interval count, single/multi classification, source span, and `decoded=false`.
- [x] Sweep `text-boundary-candidates` across current local samples: 61 checked, 10 files with candidates, 356 candidate rows, 1,586 overlapped intervals, 222 single-interval candidates, and 134 multi-interval candidates. The largest single candidate spans 44 intervals (`0x001c`/unit) in `justsystems-20120223023609-jp-just-finance-j200403sc.jtd`, so paragraph promotion still needs a stricter rule.
- [x] Add `rjtd text-boundary-candidate-context <file>` to compare decoded-false boundary candidates against `/DocumentText` visible text, line breaks, and edge alignment.
- [x] Sweep `text-boundary-candidate-context` across current local samples: 356 candidate rows, 276 rows with at least one line break, 3,458 total line breaks, and 210 rows that start after a control gap and end on an aligned text boundary. Among `0x001c` single-interval edge-good rows, byte basis has 17 one-line-break and 16 zero-line-break rows, while unit basis has 22 one-line-break and 13 zero-line-break rows; `0x000e` often spans many line breaks and remains too coarse for direct paragraph promotion.
- [x] Add `rjtd text-boundary-candidate-agreement <file>` to pair byte/unit decoded-false boundary candidates by text-count range and delimiter, reporting edge-good flags, line-break counts, visible text previews, and match flags.
- [x] Sweep `text-boundary-candidate-agreement` across current local samples: 178 byte/unit pairs across 10 files. Exact visible-text match appears only once and that row is empty, so text equality is not a useful promotion rule. In the stricter `0x001c` single/single set, 43 pairs exist; unit-basis edge-good/non-empty/line-break<=1 keeps 33 rows, while byte-basis keeps 28 rows.
- [x] Add `rjtd text-boundary-candidate-layout-context <file>` to compare unit-basis `0x001c` single candidates with `/LineMark`, `/PageMark`, and `/PaperMark` direct index/byte contexts while keeping the rule diagnostic-only.
- [x] Sweep `text-boundary-candidate-layout-context` across current local samples: 52 unit `0x001c` single candidates across 8 files, 35 strict rule-selected rows, but 0 selected rows have start/end direct hits in `/LineMark`, `/PageMark`, or `/PaperMark`. This means candidate source units and layout mark rows are not the same coordinate space.
- [x] Add `rjtd text-boundary-layout-map <file>` to score unit-basis `0x001c` candidates against sparse `/LineMark` tag positions, `/PageMark` entry indexes/raw fields/byte boundaries, and `/PaperMark` entry indexes/byte boundaries under several global unit transform hypotheses.
- [x] Sweep `text-boundary-layout-map` across current local samples: 61 checked, 0 failures, 52 unit `0x001c` single candidates across 8 files, and 35 strict selected candidates across 4 files. Non-boundary exact hits exist, but the winning target/base/delta combinations are file-specific (`iwata_file` favors line-word-value/page-be32-field with unit-div2 shifts around -1140..-1192; finance samples favor different page-be32-field shifts). This rejects a single global layout-map transform for paragraph promotion.
- [x] Add `rjtd text-boundary-layout-map-rows <file>` to score each unit `0x001c` single candidate separately and print its linked `TCntV.01` row, local delta, nearest start/end layout points, and exact endpoint count.
- [x] Sweep `text-boundary-layout-map-rows` across current local samples: 61 checked, 0 failures, the same 52 unit `0x001c` single candidates and 35 strict selected candidates. The 32 strict selected candidates in `iwata_file` include 10 candidates with row-local `exact=2` evidence through both `line-word-value` and `page-be32-field`; the 3 strict selected finance candidates have no row-local `exact=2` evidence. This keeps strict candidates diagnostic-only and suggests the next rule must separate paragraph-like rows from non-paragraph large spans.
- [x] Add `rjtd text-boundary-paragraph-like <file>` as a diagnostic-only classifier requiring strict unit `0x001c` single candidates plus both `line-word-value` and `page-be32-field` row-local exact endpoint evidence before reporting `paragraph-like=true`.
- [x] Sweep `text-boundary-paragraph-like` across current local samples: 61 checked, 0 failures, 52 unit `0x001c` single candidates, 35 strict selected candidates, 10 paragraph-like candidates, and 25 strict selected but non-paragraph-like candidates. Only `iwata_file` produces paragraph-like candidates under this rule.
- [x] Add `rjtd text-boundary-paragraph-like-style-context <file>` to print the paragraph-like classifier together with linked `TCntV.01` tail fields, text/page style hits, view-style group hits, and byte/unit range previews.
- [x] Sweep `text-boundary-paragraph-like-style-context` across current local samples: 61 checked, 0 failures, the same 52 unit candidates, 35 strict selected candidates, 10 paragraph-like candidates, and 25 selected non-paragraph-like candidates. The 10 paragraph-like rows have no text/page style candidate hits, but all 10 hit `/DocumentViewStyles` group evidence in `iwata_file`; strict non-paragraph rows also have view-group hits (25/25), so this is not a paragraph discriminator and does not prove paragraph style attachment.
- [x] Add `rjtd text-boundary-paragraph-like-discriminators <file>` to summarize paragraph-like, strict-non-paragraph, and non-strict buckets by exact layout evidence, `TCntV.01` family/span, tail field counts, and style/view-style hits.
- [x] Sweep `text-boundary-paragraph-like-discriminators` across current local samples: 61 checked, 0 failures. Only paragraph-like rows have dual exact layout evidence (10/10); strict-non-paragraph and non-strict rows have 0/25 and 0/17. In `iwata_file`, paragraph-like rows are `be0` with nonzero `range-spans=2..8`, while strict-non-paragraph and non-strict rows have `range-spans=0..0`. This makes nonzero chosen `TCntV.01` span plus dual row-local layout exactness the next discriminator to test, still decoded-false.
- [x] Preserve that stricter discriminator as decoded-false `textParagraphBoundaryCandidates` in model/export/app-core JSON without rebuilding real paragraphs. A 61-sample JSON export sweep has 0 failures and preserves 10 candidates total, all in `ichitaro-20030316043238-success-001-success_data-iwata_file.jtd`.
- [x] Add `rjtd text-paragraph-boundary-targets <file>` to trace preserved `textParagraphBoundaryCandidates` back to concrete `/LineMark` word indexes and `/PageMark` raw row fields.
- [x] Sweep `text-paragraph-boundary-targets` across current local samples: 61 checked, 0 failures, 1 file with candidates, 10 total candidates. Among the 20 candidate endpoints, 6 line endpoints and 4 page endpoints are non-unique or missing under the current hit formatter, so exactness alone is not sufficient for real paragraph construction.
- [x] Add `rjtd text-position-count-clusters <file>` to group duplicate `TCntV.01` ranges and expose raw-tail variants.
- [x] Add `rjtd text-position-count-candidates <file>` to compare `be32@0/4` and shifted `be32@1/5` offset candidates.
- [x] Confirm 9 current `TCntV.01` samples fit `be32@0/4`, while `ichitaro-20030316043238-success-001-success_data-iwata_file.jtd` splits into 32 `be32@0/4`-plausible entries and 18 shifted `be32@1/5`-plausible entries.
- [x] Add `rjtd text-position-count-family <file>` to classify `TCntV.01` entries as `be0` or `be1-shifted` while preserving both candidate offsets and the raw tail.
- [x] Sweep `text-position-count-family` across current `TCntV.01` samples: 10 files, 89 records total, 71 `be0`, 18 `be1-shifted`; all shifted entries are `iwata_file` entries 32-49.
- [x] Add `rjtd text-position-count-fields <file>` to expand each `TCntV.01` record into the chosen range plus tail `u16be` fields without assigning final semantics.
- [x] Sweep `text-position-count-fields` across current `TCntV.01` samples: `be0` records have 10 tail `u16be` fields plus extra byte `00`; `be1-shifted` records have exactly 10 tail `u16be` fields and no extra byte.
- [x] Record current tail-field invariants: shifted records have fixed `t3=0x0100`, `t4=0x0001`, `t5=0x0000`, `t6=0x0000`, `t7=0x0001`, `t8=0x0000`, `t9=0x0000`; `be0` records share several mostly fixed fields but retain more variation.
- [x] Add `rjtd text-position-count-field-deltas <file>` to compare the chosen `TCntV.01` range span against the tail `t1..t2` span plus signed deltas.
- [x] Sweep `text-position-count-field-deltas` across current local samples: 10 readable `TCntV.01` files, 89 rows, all rows have `t2 >= t1`, but no row has `t2 - t1` equal to the chosen range span; relation counts are `be0` `gt` 27 / `lt` 44 and `be1-shifted` `gt` 15 / `lt` 3.
- [x] Add `rjtd text-position-count-layout-context <file>` to compare chosen `TCntV.01` ranges against `/LineMark` word/byte offsets plus parsed `/PageMark` and `/PaperMark` row/byte offsets.
- [x] Sweep `text-position-count-layout-context` across current local samples: the 10 readable `TCntV.01` files expose 89 rows and all chosen start/end values are outside direct `/LineMark`, `/PageMark`, and `/PaperMark` ranges under word, row, and byte interpretations.
- [x] Add `rjtd stream-meta <file> <stream-path>` to inspect CFB regular/mini stream placement.
- [x] Confirm the one empty `/DocumentTextPositionTables` payload has mini-sector start `224`, which points beyond the observed 7680-byte mini stream.
- [x] Record initial `/LineMark`, `/PageMark`, and `/PaperMark` observations in `openjtd-spec`.
- [x] Add generic `rjtd stream-words <file> <stream-path>` and `rjtd stream-word-frequencies <file> <stream-path>` diagnostics.
- [x] Compare `/LineMark` raw words against `/DocumentText` raw words and record that `0x1000`, `0x1001`, and `0x1002` look like LineMark-specific tags.
- [x] Add `rjtd line-mark-tags <file>` to print `/LineMark` `0x1000`/`0x1001`/`0x1002` tag positions with surrounding word context.
- [x] Sweep `line-mark-tags` across the 61 current local samples: 5 files contain these tags, 6 files lack `/LineMark`, 50 readable `/LineMark` streams have no such tags, and the tag totals are `0x1000` 915, `0x1001` 67, `0x1002` 554.
- [x] Confirm the first word after each `/LineMark` tag is not a unique tag-family discriminator: high-frequency next words overlap strongly across `0x1000`, `0x1001`, and `0x1002`.
- [x] Add `rjtd line-mark-text-context <file>` to compare `/LineMark` tag offsets and immediate next words against the `/DocumentText` token map.
- [x] Sweep `line-mark-text-context` across the 61 current local samples: 55 files run successfully, 6 lack `/LineMark`, all 1536 known tag rows are reported, direct LineMark byte/unit offsets hit mapped `DocumentText` entries for 587 rows, and tag-next words appear in raw `/DocumentText` for 1511 rows.
- [x] Record that direct `/LineMark` word/byte offsets are not proven `DocumentText` coordinates even though tag-next words usually occur somewhere in raw `/DocumentText`.
- [x] Add generic `rjtd stream-dwords <file> <stream-path>` and `rjtd stream-dword-frequencies <file> <stream-path>` diagnostics for u32-oriented streams.
- [x] Prove the observed `/PaperMark` row shape as a 12-byte header followed by 8-byte `(index, flags)` rows in the current large layout samples.
- [x] Add parser-backed `rjtd paper-marks <file>` diagnostics, initially keeping `/PaperMark` out of the document model until the row shape was stable.
- [x] Sweep `paper-marks` across local samples and record that 52 of 55 `/PaperMark` streams parse with the observed row shape.
- [x] Add parser-backed `rjtd page-marks <file>` diagnostics for the observed 84-byte `/PageMark` row family while preserving each row as raw bytes.
- [x] Sweep `page-marks` across local samples and record that 20 of 55 `/PageMark` streams match the observed 84-byte row family.
- [x] Add `rjtd page-mark-shape <file>` to classify `/PageMark` stream length, declared CFB size, header values, and row-shape candidates.
- [x] Identify initial `/PageMark` reject families: count-plus-one variable rows, count-plus-one with 2-byte tail/trimming, declared-size mismatch regular streams, and non-PageMark-looking payloads.
- [x] Promote count-plus-one variable and count-plus-one-trim2 `/PageMark` families into the parser as raw-preserved row families.
- [x] Confirm parser-backed `page-marks` opened 43 of 55 current `/PageMark` streams after count-plus-one family promotion.
- [x] Promote count-variable `/PageMark` family into the parser as a raw-preserved row family.
- [x] Confirm parser-backed `page-marks` now opens 45 of 55 current `/PageMark` streams.
- [x] Promote fixed84-tail `/PageMark` family into the parser with preserved trailing bytes.
- [x] Confirm parser-backed `page-marks` now opens 52 of 55 current `/PageMark` streams.
- [x] Add `rjtd stream-text-probe <file> <stream-path>` to detect ASCII/UTF-16 string candidates in suspicious raw streams.
- [x] Confirm the remaining 3 unsupported `/PageMark` payloads contain stream/object names or legacy class/control metadata rather than numeric PageMark rows.
- [x] Add `rjtd paper-mark-shape <file>` to classify `/PaperMark` stream length, declared CFB size, header values, and fixed-row candidates.
- [x] Confirm `paper-mark-shape` opens all 55 current `/PaperMark` streams and separates the 3 unsupported payloads as `non-paper-header`.
- [x] Add `rjtd stream-chain <file> <stream-path>` to inspect FAT/miniFAT sector chains for suspicious stream entries.
- [x] Confirm the remaining 3 unsupported `/PageMark` and `/PaperMark` entries have complete miniFAT chains whose payload bytes decode as CFB directory-entry fragments, OLE/ActiveX control metadata, or unrelated stream names rather than layout rows.
- [x] Add `rjtd cfb-map <file>` to inspect FAT, directory, mini FAT, and root mini-stream sector chains.
- [x] Explain the CFB directory-entry fragments in `kaisya_annai` and `shanai_lan`: their root mini-stream chains overlap the CFB directory chains, so `/PageMark` and `/PaperMark` can read directory-entry bytes through structurally complete miniFAT chains.
- [x] Add `rjtd stream-find <file> <stream-path>` to search for exact duplicate stream payloads inside the same CFB file.
- [x] Trace `kazoku_ryoko` `/PageMark` to an exact slice of `/EmbedItems/Embedding 1/JSFart2Contents` at offset 1664.
- [x] Add `rjtd cfb-dir <file>` to inspect raw CFB directory ids, sibling links, start sectors, and resolved paths.
- [x] Trace `kazoku_ryoko` `/PaperMark` to the root-level object/control stream sequence around `/EmbedFrame` and `/Figure`; its `SO` marker also appears in `/Figure` and `\x01CompObj`, and its first nonzero fields match `/EmbedItems/Embedding 2/JSFart2Contents` offset 192.
- [x] Add `rjtd stream-find-bytes <file> <hex>` to search arbitrary byte markers across all readable streams.
- [x] Reproduce the `kazoku_ryoko` `/PaperMark` object/control evidence with `stream-find-bytes`: `534f0000` appears in `/PaperMark`, `/Figure`, and `\x01CompObj`; the 20-byte coordinate-like suffix appears in `/PaperMark` and `/EmbedItems/Embedding 2/JSFart2Contents`.
- [x] Add `rjtd so-records <file>` to scan all readable streams for the `SO\0\0` object/control marker and print preserved little-endian fields plus raw bytes.
- [x] Sweep `so-records` across local samples: 4 of 61 samples expose SO records, 24 records total, and only `kazoku_ryoko` exposes an SO record through `/PaperMark`.
- [x] Add `rjtd so-record-clusters <file>` to group SO records by preserved raw bytes and report repeated locations.
- [x] Confirm `JSFart2Contents` SO records split into singleton geometry-like records and repeated default/control clusters; `kazoku_ryoko` `/PaperMark` matches the geometry-like cluster seen in the older `kazoku_ryoko` `JSFart2Contents` sample.
- [x] Add `rjtd so-record-fields <file>` to expand SO records into little-endian 32-bit fields plus signed and low/high 16-bit views.
- [x] Confirm current SO records are best treated as 9 little-endian dwords: field 0 is marker `0x00004f53`, repeated default/control records use small constants such as `0x00000100` and `0x00000064`, and singleton geometry-like records carry coordinate-like values in fields 1-4.
- [x] Add `rjtd so-record-geometry <file>` to classify SO records as `geometry-like`, `default-control`, packed subfamilies, `truncated`, or `unknown` without assigning final semantics.
- [x] Add `rjtd so-record-halves <file>` to print SO payload dwords as low/high 16-bit unsigned and signed halves.
- [x] Sweep `so-record-geometry` across local samples: 61 checked, 0 failures, 4 files with SO records, 24 records total (`geometry-like` 9, `default-control` 8, `packed-jseq3-like` 4, `packed-ffff-preamble` 2, `truncated` 1).
- [x] Split the previous generic `packed` SO bucket: `packed-jseq3-like` appears only in `JSEQ3Contents` records, while `packed-ffff-preamble` appears only as the `JSFart2Contents` offset-324 preamble before repeated geometry-like records.
- [x] Add `rjtd object-stream-candidates <file>` to classify readable CFB streams with object/image/shape/table path evidence, `SO\0\0` markers, binary image signatures, SVG text signatures, payload prefixes, and `decoded=false`.
- [x] Sweep `object-stream-candidates` across local samples: 61 checked, 43 files with candidates, 933 candidate rows across 2,253 readable streams, 27 files with object-path evidence, 42 with shape-path evidence, 17 with image-signature evidence, 4 with SO-marker evidence, 0 with SVG signatures, 0 with table-path evidence, and 0 unreadable streams.
- [x] Record the object/image stream inventory in `openjtd-spec` RFC 0008, including the conclusion that embedded JPEG recovery can start from `/EmbedItems/Embedding */Contents` while the `hyo` table sample likely requires `/DocumentText` control/layout decoding rather than named table streams.
- [x] Promote object/image/shape stream candidate evidence into model/export/app-core decoded-false `objectStreamCandidates` before wiring any non-text PDF rendering.
- [x] Sweep JSON export for model-preserved `objectStreamCandidates`: 61 checked, 0 failures, 43 positive files, 933 candidates total, 17 image-signature files, 4 SO-marker files, 0 table-path files, and 0 SVG-signature files.
- [x] Extract complete embedded image payload spans from image-signature object candidates and preserve the bytes in decoded-false `objectStreamCandidates.imagePayloads` without decoding object geometry yet.
- [x] Sweep JSON export for model-preserved `imagePayloads`: 61 checked, 0 failures, 12 payload files, 67 strict complete payloads, 35 dimensioned payloads, 629,024 bytes, JPEG 35 rows/5 files, GIF89a 31 rows/9 files, GIF87a 1 row/1 file.
- [x] Project fully decodable JPEG/PNG image payloads into fallback SVG/PDF pages as decoded-false diagnostic thumbnails by re-encoding them as PNG data URIs, while keeping `placementProven=false` and original page geometry unclaimed.
- [x] Expose image payload render-promotion gates in app-core overlay/layer/SVG diagnostics: object envelopes, source-path candidates, ownership candidate/reference counts, `ownershipProven=false`, `pageGeometryProven=false`, `paintOrderDecoded=false`, `diagnosticRenderable=true`, and `renderable=false` until ownership, geometry, and paint order are decoded.
- [x] Preserve `/VisualList` streams such as `fax02.jtt` as decoded-false `objectStreamCandidates` with `visual-list-path` evidence so form/rule/vector decoding can start from the model instead of exporter-side stream reads.
- [x] Decode the observed `fax02.jtt` `/VisualList` BMDV header as model metadata (`declaredSize=2296`, `120x169`, `8bpp`, RLE length `2216`) and project long horizontal raster runs into SVG/PDF as decoded-false `visualListRasterDiagnostic` form-rule geometry.
- [x] Promote the decoded `/VisualList` BMDV pixel buffer to an SVG/PDF PNG image layer. The renderer treats the dominant pixel value as transparent background; when a DocumentText-backed form-text projection is also present, it suppresses the darkest raster foreground value to avoid double-painting low-resolution text while preserving source-backed rule/hatch pixels. The old horizontal-run/title-band projection remains a fallback when bitmap encoding is unavailable.
- [x] Project the `fax02.jtt` VisualList title-band hatch, static `FAX:`/`TEL:` form labels, and DocumentText-derived form text slots into SVG/PDF and `getPageLayerTree` as decoded-false model-owned diagnostics gated by `/VisualList` plus document text evidence.
- [x] Preserve decoded-false image payload object envelopes with header/trailer slices and conservative `le32` declared payload-length candidates.
- [x] Sweep image payload envelopes: 61 checked, 0 failures, 67 envelopes, 20 `le32` declared length matches, all currently in `Embedding */Contents` rows.
- [x] Preserve decoded-false image envelope header field candidates: first prefix `u16/u32` values and source path candidates from `Embedding */Contents` headers.
- [x] Sweep image envelope header fields: 61 checked, 0 failures, 66 source path candidates, all in `Contents` rows (`jpg` 34, `gif` 32); first prefix word `9` appears in 59 payload rows and `4` in 6 rows.
- [x] Preserve decoded-false path-derived `ownershipCandidate` evidence for object stream candidates, including `Embedding N`, stream role, and figure/frame/layout stream families.
- [x] Sweep ownership candidates: 61 checked, 0 failures, 933 candidates, 474 with ownership, and all 67 strict image payload rows covered by an ownership candidate (`contents` 67).
- [x] Preserve decoded-false `ownershipReferences` evidence by matching `Embedding N` byte patterns from embedded image candidates into `FigureData`, `/Figure`, `/Frame`, `/LayoutBox`, `/PageMark`, and `/PaperMark` streams.
- [x] Sweep ownership references: 61 checked, 0 failures, 12 files with references, 52 embedded image candidates with references, 604 reference rows, 9,949 total byte matches, and 67 of 67 strict image payload rows covered by cross-stream reference candidates.
- [x] Add `rjtd object-ownership-references <file>` to print model-owned ownership reference matches with source stream, target stream, encoding, offset, total match count, alignment, local context hex, and le/be 16/32-bit values at the match offset.
- [x] Sweep `object-ownership-references`: 61 checked, 0 failures, 12 files with rows, 3,167 reported preview offsets; target families include `figure-data` 1,010, `frame` 897, `layout-box` 528, `page-mark` 498, `paper-mark` 151, and `figure` 83.
- [x] Record reference-context alignment evidence: reported preview offsets split as mod2 `0` 1,401 / `1` 1,872 and mod4 `0` 728 / `1` 855 / `2` 673 / `3` 1,017; `u16-be`/`u16-le` offsets expose the embedding index directly as be/le16 at the match offset, while `u32-le` exposes it in the low 16 bits.
- [x] Add `rjtd object-ownership-reference-fields <file>` to project ownership reference offsets onto candidate record strides and summarize target, encoding, stride, field offset, row indexes, source count, embedding indexes, and cross-row matches.
- [x] Sweep `object-ownership-reference-fields`: 61 checked, 0 failures, 12 files with field groups, 33,492 projected field groups. This is a projection surface, not decoded geometry; every reported offset is tested against 20 candidate strides.
- [x] Record the strongest cross-row-free stride candidates with stride >= 12: `frame/u16-le/12/5` has 102 weighted matches, `frame/u16-be/12/7` has 89, and `frame/u16-be/20/15` has 70.
- [x] Add `rjtd object-frame-reference-records <file>` to expand the strongest `/Frame` reference projections into candidate row bytes with row hex, BE/LE 16-bit fields, and BE/LE 32-bit fields.
- [x] Sweep `object-frame-reference-records`: 61 checked, 0 failures, 12 files with candidate records, 261 expanded rows. Candidate counts are `u16-le/12/5` 102, `u16-be/12/7` 89, and `u16-be/20/15` 70.
- [x] Record the dominant expanded `/Frame` row families: repeated rows include `00010000000N000000020001`-style 12-byte rows and `00000000010200380000000N` rows, but these are still row-family evidence rather than decoded placement geometry.
- [x] Add `rjtd object-frame-record-families <file>` to group expanded `/Frame` candidate rows into decoded-false diagnostic families.
- [x] Sweep `object-frame-record-families`: 61 checked, 0 failures, 12 files with families, 261 records. Family counts are `frame-index-tail-coordinate-row12` 65, `frame-index-tail-window20` 65, `frame-index-mixed-row12` 61, `frame-index-flag-row12` 41, `frame-index-tail-zero-row12` 22, `frame-index-mixed-window20` 5, and `frame-index-tail-mixed-row12` 2.
- [x] Add `rjtd object-frame-row-links <file>` to verify whether 20-byte `/Frame` windows contain matching 12-byte frame rows as suffixes.
- [x] Sweep `object-frame-row-links`: 61 checked, 0 failures, 9 files with 20-byte rows, 70 row20 windows, 65 linked same-source suffix rows, 5 unlinked. All linked rows are `frame-index-tail-window20 -> frame-index-tail-coordinate-row12`, which makes `u16-be/12/7` a stronger authoritative-row candidate than the 20-byte context window.
- [x] Promote decoded-false `/Frame` reference rows and suffix links into model/export `objectStreamCandidates[].frameReferenceRows` so future image placement remains model-first.
- [x] Sweep JSON export for model-preserved `frameReferenceRows`: 61 checked, 0 failures, positive files 12, rows 261, suffix links 65. Family counts match the CLI sweep exactly.
- [x] Add `rjtd object-image-frame-candidates <file>` to summarize each image payload source against model-owned `/Frame` row evidence, payload kinds/dimensions, row families, row20 suffix links, coordinate-looking row12 pairs, and best coordinate/payload aspect delta.
- [x] Sweep `object-image-frame-candidates`: 61 checked, 0 failures, 12 files with image payload sources, 52 image sources, 52 frame-linked sources, 0 missing-frame sources, 261 frame rows, 35 dimensioned payloads, and 13 sources with aspect candidates. Preferred diagnostic buckets are `row12-tail-coordinate` 25, `row12-tail-zero` 7, `u16-le-row12` 19, and `none` 1.
- [x] Record that `row12-tail-coordinate` is a strong placement-analysis candidate but not yet sufficient for rendering: only two sources currently have best coordinate/payload aspect deltas <= 250 permille, both in `natsu.jtd`, so aspect evidence is still sparse.
- [x] Add `rjtd object-fdm-index <file>` to inspect `/FigureData/*/FDMIndex` rows against sibling `FDMVector` segments, preserving vector offsets, kind fields, bounding-box-like fields, segment prefixes, and image signature hits as decoded-false evidence.
- [x] Sweep `object-fdm-index`: 61 checked, 0 failures, 31 files with indexes, 39 index streams, 417 parsed rows, 6 rows with images, 13 image hits, and 2 missing sibling vectors. This proves FDMIndex/FDMVector is a separate image-placement evidence path from `Embedding N` `/Frame` rows.
- [x] Add `rjtd object-fdm-index-shape <file>` to separate exact 22-byte FDMIndex tables, declared-count prefix tables with auxiliary trailing payloads, mixed declared rows, unknown-header streams, and missing vectors.
- [x] Sweep `object-fdm-index-shape`: 61 checked, 0 failures, 39 indexes, 35 `fdm-index-v1` headers, 4 unknown headers, 34 plausible declared counts, 417 raw stream rows, 252 raw invalid offsets, 147 declared-prefix rows, 43 declared-prefix invalid offsets, and 13 declared-prefix image hits. Shape counts are `row22-count-prefix` 17, `row22-exact` 14, `row22-mixed-declared` 3, `unknown-header` 3, and `missing-vector` 2.
- [x] Add `rjtd object-fdm-index-rows <file>` to print row scope (`declared`, `post-declared`, or `raw`), row role (`vector-segment`, `coordinate-like-invalid`, or `invalid-vector-offset`), BE16/i16 field views, row bytes, and segment image hits for FDMIndex analysis.
- [x] Sweep `object-fdm-index-rows`: 61 checked, 0 failures, 31 files with indexes, 39 indexes, 417 rows, 147 declared rows, 253 post-declared rows, 17 raw rows, 165 valid vector rows, 252 invalid rows, 13 image hits, and 2 missing vectors. Role counts are `vector-segment` 165, `coordinate-like-invalid` 231, and `invalid-vector-offset` 21; all 43 declared invalid rows are `coordinate-like-invalid`.
- [x] Promote decoded-false `FDMIndex` row evidence into model/export/app-core JSON as `objectStreamCandidates[].fdmIndexEntries` on the corresponding `FDMVector` candidate, limited to valid `fdm-index-v1` declared-count prefix rows so auxiliary payload bytes are not exposed as false row entries.
- [x] Sweep JSON export for model-preserved `fdmIndexEntries`: 61 checked, 0 failures, 24 files with entries, 30 candidates with entries, 147 rows, 3 files with image-linked rows, 6 image-linked rows, 13 image hits, 104 valid vector offsets, and 43 invalid/out-of-range vector offsets.
- [x] Record that `fdmIndexEntries` can identify all currently observed image-bearing FDMVector segments while reducing false auxiliary rows. The 43 invalid declared-prefix rows are now classified as coordinate-like diagnostic rows in exactly 3 files, not image-bearing vector segments, so they must remain decoded-false and must not be promoted to renderable page geometry or paint resources.
- [x] Add `rjtd object-fdm-image-candidates <file>` to summarize image-bearing FDMVector rows from model-owned `fdmIndexEntries`, including normalized bbox diagnostics, complete payload coverage, and explicit `renderable=false` until page placement is proven.
- [x] Sweep `object-fdm-image-candidates`: 61 checked, 0 failures, 3 files with candidates, 3 FDM sources, 6 image-bearing rows, 13 image hits, 0 strict complete payloads, 5 plausible bbox rows, and 0 renderable rows. The observed `FFD8FF` hits in FDMVector segments are JPEG-like byte patterns inside vector data, not valid JPEG payloads with SOF/SOS structure, so they remain signature-only decoded-false evidence.
- [x] Split the `object-fdm-image-candidates` render blocker so complete image payload rows remain `page-placement-unproven`, while signature-only FDM rows report `image-signature-without-complete-payload-role-unproven`. This keeps payload extraction failure separate from page geometry failure before any FDM image row is promoted.
- [x] Expose the same FDM image rows through app-core `getPageOverlayImages` as `unplacedDiagnostics` with `imageCount:0`, `placementProven:false`, and `renderable:false`, keeping the rhwp-shaped overlay API callable without pretending page placement is decoded.
- [x] Preserve `/Frame` fixed 60-byte records as decoded-false model/export/app-core `objectFrameRecords`, including observed object id, record kind/type, and geometry-looking fields.
- [x] Add `rjtd object-fdm-frame-links <file>` to correlate image-bearing FDMIndex rows with `/Frame` records by `fdm row index == frame object id`.
- [x] Add strict image payload dimension diagnostics: model/export JSON now carries optional `imagePayloads[].dimensions`, JPEG payload spans must pass SOF/SOS structure validation, and `object-fdm-frame-links` reports frame size, payload dimensions, dimensioned payload counts, and best frame/payload aspect delta.
- [x] Sweep `object-fdm-frame-links`: 61 checked, 0 failures, 3 positive files, 6 FDM image rows, 13 image hits, 6 frame-linked rows, 0 missing-frame rows, 0 strict complete payloads, 0 dimensioned payloads, and 0 renderable rows.
- [x] Preserve `/EmbedItems/EmbeddingInfo` rows as decoded-false model/export/app-core `objectEmbeddingFrames`, including embedding index, class name (`JSFart.Art.2`, `JSEQ.Document.3`), primary size, frame reference, and frame size.
- [x] Project `objectEmbeddingFrames` through `/Frame` row geometry into first-page SVG/PDF and `getPageLayerTree` as decoded-false `embeddingFrameDiagnostic` boxes for the PDF-reference `success_data-test` sample. The projection remains `placementProven:false` and `renderable:false`.
- [x] Preserve `JSSnapShot32` headers from `/EmbedItems/Embedding */\x03EmbeddedPress` streams as decoded-false `embeddedPressSnapshot` metadata on `objectStreamCandidates`, including width, height, body-length candidate, object-count candidate, payload-length candidate, and `renderable:false`.
- [x] Preserve `/EmbedItems/Embedding */JSEQ3Contents` streams with UTF-16LE `MATH.VAF` magic as decoded-false `jseq3Formula` metadata on `objectStreamCandidates`, including SO trailer fields and stable UTF-16LE text markers such as `Times New Roman`, `JustUnitMark`, and `JustOubunMark`.
- [x] Link `JSEQ.Document.3` embedding-frame diagnostics to matching `jseq3Formula` evidence and render decoded-false `MATH.VAF` labels into SVG/PDF output so missing formula boxes are traceable to file data.
- [ ] Decode `/Frame` geometry units, page association, paint order, and payload-to-image selection before promoting any FDM or `Embedding N` image ownership candidates into page geometry or paint resources.
- [ ] Decode `JSSnapShot32` payloads under `/EmbedItems/Embedding */EmbeddedPress` and the `JSEQ.Document.3` embedded formula/document payloads before replacing the `embeddingFrameDiagnostic` boxes with real title art, formulas, table borders, and shape paint operations.
- [x] Recover remaining text hidden behind unknown inline formatting/control records; `sample-academic.jtd` plain-text extraction is complete; the `0xff1c`/`0xff1e` values previously flagged as hidden content are U+FF1C/U+FF1E full-width angle brackets, not control codes; RFC 0009 documents the self-describing variable-length `0x001c` header record that the current parser skips correctly by stopping on `0x001c` and resuming on `0x001f`.
- [ ] Decode true `DocumentText` record boundaries and control semantics beyond the current token layer; RFC 0009 now documents the full record family with partial decode: `0x0030` b0/b1=cell edge coordinates, `0x0010` w4-subtypes, `0x0000` inline-context/cell markers, `0x0020` table-transition; `0x0010 w4=0x0026 w5=0x0005` len=17 records have 9 distinct payload patterns across `w6/w7/w8/w10`—`w6=1` with `w7=0x1ec/w8=0x1cc` is the dominant hanging-indent group (93/142); `w4=0x002e` len=13 is fully constant in single-column samples; cross-sample analysis of 246 records (11 new samples) reveals `(w8=w10=0x1cc)` appears only in sample-reference (with `w6=1`), while `(w8=w10=1/2)` appears only in sample-outline/sample-draft, suggesting (w8,w10) may discriminate by document type or may encode style IDs in short-text vs physical coordinates in long reference-statute documents; `w7` values (0/1/2/3/4/6/8/10) sweep across all 14 tested samples confirms paragraph-style-ID hypothesis: 0=body/headings, 1=body-continuation, 2=item-list, 3=provision-subheading, 4=appendix-indent, 6=title/preamble (all sample-outline/sample-draft title lines), 8=supplementary-provision heading, 10=reason heading, 492=hanging-indent-body (sample-reference only); the consistent appearance of w7=6 on the opening title line across every sample-outline/sample-draft sample strengthens the paragraph-style-ID interpretation (RFC 0009 §w7 cross-sample table updated with full 14-sample sweep); remaining undecoded: exact unit scale, `w10` in len=20, and `0x0000 len=21` w8/w9/w13 fields.
- [ ] Explain why `MarkV.01` delta candidates 9, 29, and 30 all score strongly; current evidence does not support treating `unit + 29` as a unique stable adjustment.
- [ ] Decode the meaning of the varying MarkV.01 header value `0x0603`/`0x0610`/`0x061c`; current summary, LineMark-context, and exact-byte-search evidence weakens direct page-count, document-length, global page-style-code, or direct LineMark-entry-offset interpretations.
- [ ] Decode the semantic meaning of the remaining fields inside 29-byte `TCntV.01` records; current `text-position-count-field-deltas` evidence shows `t1/t2` form an ordered range-like pair but not the same span as the chosen `start/end` range.
- [ ] Determine whether `TCntV.01` mixes byte coordinates, UTF-16 unit coordinates, and layout/object-local coordinates, or whether the current token map is missing intervening record structure; tail `t1/t2` currently leans toward UTF-16 unit coordinates and delta 29/30 improve unit hits, while `0x0202` chosen ranges now show strong byte-range text/control overlap without matching the same tail coordinate behavior.
- [ ] Decode `/DocumentText` control boundaries `0x001c` and `0x000e`; RFC 0009 proves the full record family: `0x0030` b0/b1=cell coordinates (4-unit gaps, max 268); `0x0010` w4-subtypes (`0x002e`=para, `0x008f`=table-row, `0x002a`=composite, `0xffff`=null); `0x0010 w4=0x008f` payload = 4-word sub-entries `[tag, v1, v2, v3]` terminated by `0xffff`; `n_sub_entries = n_cells − 1` holds in dominant cases; `0x23`/`0x2b`/`0x1b` tag values observed but semantics undecoded; `0x0000 len=12`=inline-segment context (w4=inline-len, w6=525 constant, w5=cell-position-sensitive style selector); `0x0000 len=21`=table-cell marker (constant block + variable w8/w9/w13) — `w8=0` with small (0/2/4/5) flag values vs `w8=1` with large cell-specific values where `w9−w13=400=(b1−b0)×3.125` invariant holds (w8=1 always precedes ruby/inline content); `0x0020 len=12`=table→paragraph transition (w4=0x0010, w7=1); `0x0010 w4=0x008f` `w8` takes only values 0x0001/0x0003 (all n_cells=12 rows have w8=0x0001; n_cells=4 rows are overwhelmingly w8=0x0003; may be a row-type flag); `0x000a` (210× in `sample-table`, present in all current samples) appears after `0x001f` or CJK text and is followed in 169/210 cases by `0x001c/0x0030` and 24/210 by `0x001c/0x0010` — behaves as a within-cell or intra-paragraph line break distinct from `0x000e` (which is confined to between-`0x0030` positions); remaining gaps: physical coordinate unit, sub-entry v3 formula and tag semantics, exact w8 rule, `w9`/`w13` in `0x0000 len=21`, `0x000a` semantic meaning.
- [ ] Find a row-local, section-local, or record-local base offset that explains the file-specific shifts exposed by `text-boundary-layout-map`.
- [ ] Explain why only 10 of the `iwata_file` strict boundary candidates have both line-word and page-field exact endpoint evidence while the remaining strict candidates and selected finance spans do not; since view-style group hits also appear on strict non-paragraph rows, treat them as default/flag-like until proven otherwise before constructing real paragraphs.
- [ ] Promote `TCntV.01` `be0` and `be1-shifted` diagnostics into explicit raw-preserving record family types once the shifted leading byte is explained by a flag, prefix, version, or preceding record boundary.
- [ ] Identify the actual coordinate target for `TCntV.01`; current evidence rejects direct `/LineMark`, `/PageMark`, and `/PaperMark` word/row/byte coordinates.
- [ ] Determine whether the out-of-range mini-sector in the empty `/DocumentTextPositionTables` sample is a stale directory entry, malformed ministream chain, or recoverable via another storage/object boundary.
- [x] Fully prove the record layout of `/PageMark` variants that are still unsupported by `page-marks`; RFC 0007 §PageMark Observation documents the three unsupported entries (`kaisya_annai`, `kazoku_ryoko`, `shanai_lan`) as stale/foreign payloads (CFB directory-entry fragments and OLE/ActiveX metadata) rather than layout rows — the miniFAT chains are complete but byte content is unrelated to PageMark row structure; no further proof is needed.
- [x] Confirm from the reference `a5.pdf` that visible page numbers begin on page 6, even pages place page number and running chapter title on the left side, odd pages place page number and document title on the right side, and the running document title matches `/AutoTextInfo` text candidates.
- [x] Expose `PageMark` first-field diagnostics as raw-preserving `flags`, `lineStart`, and `lineEnd` values so page rows can be compared against reference PDF pagination without claiming final semantics.
- [x] Add `rjtd page-layout-style-slots <file>` to inspect `/PageLayoutStyle` nested `31xx..39xx` slot groups as decoded-false raw evidence, including active `paired-slot-pairs` and `facing-pages-candidate` summary fields for future facing-page/header/footer/page-number analysis.
- [x] Check the local A/B/Ginga-style samples for facing-page evidence: `a5.jtd`, `a6.jtd`, `b6.jtd`, `46.jtd`, and `shinsyo.jtd` all expose active paired `/PageLayoutStyle` slot groups such as `0x32/0x33`, `0x34/0x35`, `0x36/0x37`, and `0x38/0x39`; page decoration layer output now explicitly marks `facingPagesCandidate:true` and `sidePolicyDecoded:false`.
- [x] Preserve `/AutoTextInfo` UTF-16BE text candidates in the document model/export JSON so running document titles can be sourced from file data instead of hardcoded sample names.
- [x] Render a decoded-false running header/page-number projection for vertical Ginga-style documents only when `/AutoTextInfo`, active paired `/PageLayoutStyle` slots, and document-text chapter-title evidence are all present; layer-tree output now carries `sidePolicy:"facing-pages-odd-right-even-left"` plus the source slot pairs, running headers render horizontally like the Ichitaro reference, and `openjtd-samples/pdf-output/a5.pdf` pages 6 and 7 were verified against the reference PDF. Page-number x placement now follows the page margin line: reference/generated page 6 x is about 57.4/54.0 pt, and page 7 x is about 358.8/360.7 pt.
- [x] Remove the a5-only front-matter gate for the same Ginga vertical projection. With reference PDFs present, `a6.jtd` and `b6.jtd` now also expose page 6 as even-left with `一、午后の授業` and page 7 as odd-right with `銀河鉄道の夜`, while `getPageLayerTree` keeps `sidePolicyDecoded:false`, `facingPagesCandidate:true`, and the `0x32/0x33` through `0x38/0x39` paired slot evidence.
- [x] Preserve the raw `/PageLayoutStyle` slot payload evidence behind decoded-false page decorations in `getPageLayerTree`: each `pageDecoration` now carries `slotEvidence` with record index/offset/label, slot id, `part05First`/`part05NonZero`, and opaque `part04..part07` hex payloads so future header/footer/page-number decoding can be traced without hardcoded geometry.
- [x] Add `rjtd page-layer-tree <file> <zero-based-page-index>` so facing-page page-number/header projections can be inspected through the model layer tree, including `side`, `sidePolicyDecoded:false`, `facingPagesCandidate:true`, `pairedSlotPairs`, and `slotEvidence`.
- [x] Fix `getPageLayerTree` JSON framing so the page-decoration evidence is machine-parseable; tests now check balanced JSON delimiters and the `root.ops` to `textSources` boundary.
- [x] Preserve `DocumentText` table-of-contents page-label candidates from skipped-inline selector `0x0101` as model/export/app-core `tocEntries` with source spans and `decoded:false`; the A5 sample now exposes labels `6,10,12,16,21,23,28,35,42` without treating the selector as fully decoded.
- [x] Render the A5 table-of-contents page from model-owned `tocEntries` instead of hardcoded labels, including leader dots, page labels, and source-span-backed ruby annotations for title text; `openjtd-samples/pdf-output/a5.pdf` page 3 was regenerated and checked against the reference PDF.
- [x] Preserve `/PageMark` rows as decoded-false model/export/app-core `pageMarks`, including header count/stride/last-index, family, entry count, row index, flags, and `lineStart`/`lineEnd` fields. The A5 sample exposes `count=74`, `stride=16`, `last=73`, `75` parsed rows, and row 5 `lineStart=23,lineEnd=40`, which aligns with the reference body start page 6.
- [x] Preserve `/PaperMark` rows as decoded-false model/export/app-core `paperMarks`, including header count/stride/last-index, entry count, row index, flags, flag hex, and raw row length. The A5 sample exposes `count=74`, `stride=12`, `last=73`, `75` parsed rows, and flags such as `0x00010010`/`0x00010011`; semantic page/paper meaning is still unclaimed.
- [x] Attach decoded-false `/PageMark` + `/PaperMark` row-alignment evidence to page decoration layer-tree entries. A5 and B6 page 6 now expose matching `pageMarkEntryIndex=5` and `paperMarkEntryIndex=5`, aligned mark indexes, matching entry counts, and the source-backed `/PaperMark` flag (`0x00010010`) while blocking render promotion until flag semantics are decoded.
- [x] Expose the same decoded-false page mark context through app-core `get_page_info` and CLI `rjtd page-info <file> <page>`. Mark-bearing pages report the source-backed `/PageMark+/PaperMark` row context, while markless fallback pages such as current `a6.jtd` report `layoutMarkEvidence:null`.
- [x] Match the A5 generated PDF page count to the Ichitaro reference by tightening the vertical fallback display-unit advance instead of clamping page count. `openjtd-samples/pdf-output/a5.pdf` now has 72 pages with the same A5 portrait MediaBox as the reference.
- [x] Project the A5/Ginga first body chapter title as a standalone vertical heading block instead of consuming it as a normal body column. Page 6 now ends at `大きな望遠鏡...何でしょう。`, and `やっぱり星だ...` starts on page 7 like the Ichitaro reference while keeping the 72-page count.
- [x] Preserve `/Font` table names through model/export JSON and prefer Mincho-compatible aliases for PDF generation. On macOS, exporter font discovery now also loads Apple MobileAsset font directories so A5 embeds `YuMin-Medium` instead of falling back to `ArialUnicodeMS`.
- [x] Guard PDF text-layer preservation in the local reference-backed export sweep: generated PDFs must retain `/ToUnicode` maps and CID font resources inside the svg2pdf Form XObject path, instead of silently degrading to path-only output.
- [ ] Decode the exact semantics of `/PageLayoutStyle` nested `31xx..39xx` slot groups, including which active slot pairs correspond to facing pages, header/footer areas, and page-number fields. Cross-sample analysis of `part06` terminal u16 values across 10 samples (5 families): `sample-outline`/`sample-draft`→0x32/0x33=1000/0x34=453/0x36=600/0x38=407; `sample-table`→0x32/0x33=800/0x34=453/0x36=407+773/0x38=411; `sample-draft-b`→0x32/0x33=2000/all others=0; `sample-table-b` and `sample-table-c` are identical→0x32/0x33=1405/0x36=0+1397/all others=0. Part06 byte 1 is 0x01 only in `sample-outline`/`sample-draft`; 0x00 in all others. `sample-reference` variants and `sample-academic` have no `/PageLayoutStyle`. Correlation with physical page dimensions requires reference PDF calibration (blocked: a5.jtd absent).
- [ ] Replace the current decoded-false running header/page-number projection with decoded layout records once source slots, page parity rules, chapter-title binding, and document-title binding are proven beyond the current a5-style evidence.
- [ ] Decode the exact table-of-contents layout semantics behind the `0x0101` page-label candidate, including leader length, title/ruby binding, and page-label alignment; the current A5 projection is reference-backed but still decoded-false.
- [ ] Replace the vertical fallback display-unit heuristic with a proven `/PageMark`/`/LineMark` mapping from original layout rows to `Document` text lines. Rows 73 and 74 look sentinel-like (`lineStart=lineEnd` with zero geometry), so they must not be blindly treated as visible pages. From 14 government/academic samples, `additive-boundary` PageMark entries have constant span `lineEnd−lineStart = rows_per_page−1` (e.g. span=29 for 30-row/page samples), meaning `lineStart`/`lineEnd` encode 0-based layout row positions — but the mapping from layout row to DocumentText unit coordinates is not decoded, and PageMark pre-allocates many more slots than the document contains text lines.
- [x] Match A5 colophon/end-matter geometry against the reference PDF with a decoded-false projection gated by Ginga colophon text and preserved layout evidence. Page 72 now suppresses running headers/page numbers, starts the colophon block in the lower half, preserves the title/date/copyright spacing, splits the note into three vertical columns, and removes the trailing control-noise line.
- [x] Replace or quarantine suspicious `46.pdf` before treating it as a full-document regression: `pypdf` extracts no text from the local reference, and a 144dpi first-page render is essentially blank/title-like while `46.jtd`/generated output contains the Ginga body text. The updated `a6.pdf` reference is usable (`114` pages with extractable Ginga text), and generated `openjtd-samples/pdf-output/a6.pdf` now also renders `114` pages with an A6 portrait MediaBox; keep this covered by the local reference-backed model test while future vertical-layout work moves back to decoded `/PageMark`/`/LineMark` mapping instead of filename calibration. Confirmed: `46.pdf` is not used as a reference PDF in any current test; `46.jtd` only appears in file-name-based page-size unit tests; no regression risk.
- [x] Strengthen `openjtd-samples/pdf-output` artifact regression coverage for Preview/PDFKit compatibility. The output-artifact test now rejects PDFKit-risky `/Group`, `/S /Transparency`, and `/SMask` constructs, and compares generated PDF page-object counts against trusted local reference PDFs while explicitly excluding the quarantined `46.pdf`. This locks the current reference-backed samples to matching page counts without relying on Chrome/PDFium behavior.
- [ ] Decode the `SO` object/control record family field semantics; current evidence suggests fields 1-4 carry geometry-like tuples in singleton records while repeated records carry default/control constants, but the exact meaning of `packed-jseq3-like` 16-bit halves remains unproven.
- [x] Add the new PDF-reference `shanai_lan` sample to the current fidelity sweep and apply an A4 landscape sample hint. The generated PDF now matches the reference as a single A4 landscape page (`841.875x595.275` pt generated vs `841.920x595.320` pt reference), while keeping the undecoded `/FigureData/main_data/FDMVector` content on the first page as diagnostics instead of spilling fallback text across multiple pages.
- [x] Use `shanai_lan` as the first FDMVector/Frame rendering target: it has one `/FigureData/main_data/FDMIndex`, 39 parsed entries, 2 image-bearing entries, 4 JPEG signatures, and 2 frame-linked image candidates (`row=23`, `row=33`). `getPageLayerTree` and SVG/PDF output now expose decoded-false `fdmFrameDiagnostic` boxes for those linked rows using the reference-backed `/Frame` projection (`x/width/height ÷ 24`, `y` direct), while keeping `placementProven:false`, `geometryDecoded:false`, and `renderable:false`.
- [x] Surface the FDM frame image-payload blocker directly on `shanai_lan` `fdmFrameDiagnostic` rows. Frame rows `23` and `33` now report `imagePayloadExtractionStatus:"signature-without-complete-payload"` and `renderPromotionBlockedReason:"image-signature-without-complete-payload-role-unproven"`, matching the raw FDMVector probe that shows the observed `FFD8FF` hits are JPEG-like vector byte patterns rather than complete image payloads.
- [x] Mirror the same FDM frame payload blocker into SVG frame-diagnostic `data-*` attributes when fallback frame boxes are emitted. Synthetic frame fixtures with complete JPEG payloads now expose `data-image-payload-extraction-status="complete-payload-in-fdm-index-segment"` and stay blocked as `fdm-frame-linked-image-payload-placement-and-paint-order-unproven`, while real `shanai_lan` layer-tree rows remain blocked as signature-only evidence. The CLI `object-fdm-frame-links` command now reports the same source-backed blocker for frame-linked complete payloads instead of the generic `page-placement-unproven`.
- [x] Generalize FDMVector/FDMIndex pairing beyond exact sibling paths. Exact `/FigureData/.../FDMVector` -> `/FigureData/.../FDMIndex` still wins when valid, but if the sibling index is absent or invalid the model now scans `/FDMIndex` streams and selects a unique content-backed match by declared-row count and valid vector offsets. Synthetic coverage proves root `/FDMIndex` can pair with nested `/FigureData/main_data/FDMVector` while an invalid decoy `/FigureData/other/FDMIndex` is rejected.
- [x] Parse the `shanai_lan` FDMVector segment command tables: each segment starts with `0x01000b60`, carries a segment length and command count, and uses a 16-bit offset table for top-level commands. The model now preserves command records and projects the 334 `0xff000a60` bbox-bearing commands as decoded-false `fdmVectorCommandDiagnostic` layer/SVG/PDF outlines with `fdmCommandBBoxReferenceProjection`.
- [x] Promote source-backed FDMVector drawing primitives for `shanai_lan`: the model now stores two-point `0xff000160` line records, top-level `0xff000660`/`0xff000960` path records, `0xff000460`/`00000460` ellipse records, and nested `00000660`/`00000960` plus nested `ff000660`/`ff000960` primitives inside `0xff000a60` compound records. `0xff000960`/`00000960` tails are preserved as cubic control-handle deltas, so globe/orbit curves render as cubic Beziers instead of angular polylines. `getPageLayerTree` exposes 1889 `fdmVectorPrimitiveProjection` ops with `geometryDecoded:true`, `primitiveKind`, `curveSegmentCount`, `styleWord`, BGR-interpreted `fillColor`/`strokeColor`, inferred `renderStrokeColor`, `renderFillKind`/`renderGradient`, `strokeWidth`, `pathClosed`, source ellipse geometry, and the reference-backed `projectionViewport`. SVG/PDF render source-backed strokes, conservative closed-path fills, source-prefix-backed linear gradients, ellipse dots/circles, and a source-bbox-gated white stroke for uncolored closed `ff000660` port-box paths inside black device fills instead of drawing the old blue bbox/path debug overlays; the latest `shanai_lan` 144dpi raster diff mean is about `24.35`, with the right globe crop RMS improved from `103.85` to `96.07` after cubic-handle projection.
- [x] Align source-backed `shanai_lan` `DocumentText` labels with the same reference-backed diagram viewport and project them from `/DocumentText` `0x001c/0x0010` text-group records plus nearest `0x001c/0x0030` line headers. The layer tree now exposes `documentTextGroupLineProjection` text runs with source byte/unit spans, `groupIndex`, `lineOffsetUnits`, `leadingUnits`, `fragmentStartUnits`, `splitFromTextRun`, `lineHeaderHex`, and `baselineFactor:0.800`; SVG/PDF output restores labels such as `ルーター`, `DHCP機能`, `NASサーバ`, bottom device labels, and the application software block. Large whitespace-separated multi-label runs are split into source-spanned fragments so bottom captions get independent x coordinates instead of depending on PDF space widths. Colors no longer use label-string exceptions: 16 exact source ranges receive property-15-backed colors from the trailing `TextV.01` style event section, while the remaining 22 slots keep the default black fill.
- [x] Attach decoded-false `/DocumentTextPositionTables` `TCntV.01` overlap evidence to each rendered `shanai_lan` label slot instead of using those ranges as anchors or color selectors. This sample has no `MarkV.01`, but has a 304-byte position-table stream with 6 `TCntV.01` rows (`3603..4078` repeated 3 times and `5082..5598`/`5083..5550`/`5194..5598`). `getPageLayerTree` records per-slot `textCountRangeEvidence` plus `textCountRangeEvidenceCount`; SVG records the same evidence as `data-*` attributes. `fillColorBasis` now distinguishes `document-text-style-property-15-text-run-candidate` from `default-text-fill`; `TCntV.01` remains comparison evidence and is not used as a color selector.
- [x] Expose `shanai_lan` label style-link evidence independently from the property-15 color candidate. `getPageLayerTree` emits `textStyleLinkEvidence` per label slot, and SVG mirrors it as `data-style-link-*` attributes: this records `TextLayoutStyle` record count `0`, `/DocumentViewStyles` group count `9`, the `TCntV.01` tail-field-f7 group candidate (`3` where present), `styleLinkProven:false`, and the remaining `document-view-style-group-link-unproven` blocker. Property-15-backed colors carry separate `documentTextProperty15ColorCandidate` evidence with `propertyRoleDecoded:false` and the `shanai-lan-text-projection` context gate.
- [x] Add decoded-false `/DocumentText` inline style-candidate evidence to the same `shanai_lan` label style-link diagnostics. The title label now exposes the immediately preceding raw inline block (`001c 0001 0007 0000 0000 0001 001d 0002 001e 0005 0000 0001 001f`) with selector `0x0001`, source span `60..86`, payload word `0x0002`, post-inline words `0005 0000 0001 001f`, and blocker `document-text-inline-control-semantics-unproven`. This block remains comparison evidence; title color authority comes from the exact-range property 15 candidate.
- [x] Add decoded-false `/DocumentText` group-header evidence to `shanai_lan` label style diagnostics. `getPageLayerTree` now records `documentTextGroupHeaderCandidate` for 35 visible label slots, and SVG mirrors it as `data-document-text-group-header-*` attributes. The blue `ファイルサーバ` label carries source span `286..324` with raw words `001c 0010 0013 0000 008f 0007 0118 0000 0052 0008 005b 0000 0068 ffff 0000 0013 0000 0010 001f`; `ｶﾗｰｼﾞｪｯﾄﾌﾟﾘﾝﾀｰ` carries the neighboring `0017` group header. These fields are now preserved for comparison, but rendering still blocks on `document-text-group-header-semantics-unproven` because the same header family also appears around non-blue labels.
- [x] Add a decoded-false aggregate `shanaiLanTextStyleEvidenceSummary` layer op for `shanai_lan` label styling. The summary records 38 text slots, 8 `/DocumentViewStyles` group-candidate slots, 35 `/DocumentText` group-header candidates, 1 inline style candidate, 16 source-property-colored slots, and 22 default-black slots. The source-property distribution is green 1 (`#008000`), blue 14 (`#000080`), and dark blue 1 (`#000066`). Header families remain comparison evidence because the same `0x0017` family contains both default-black and property-15-blue labels.
- [x] Preserve parent text-run fragment context for split `shanai_lan` labels. `getPageLayerTree` emits `textRunFragmentContext` per label, and SVG mirrors it as `data-parent-text-run-*` / `data-fragment-*` attributes. The sample has 14 slots split from multi-fragment parent runs, with a maximum fragment count of 5. The five bottom labels in parent run `byte 12060..12384` / `unit 6030..6192` all carry the same exact property-15 state and now render blue; general fragment style semantics remain undecoded.
- [x] Re-check `shanai_lan` color/style evidence and replace label-string color exceptions with the trailing `TextV.01` style event stream. A persistent-state resolver maps property changes and run lengths to exact `/DocumentText` source units. For this projection only, uniform property 15 values map as packed BGR: `0x00008000 -> #008000`, `0x00800000 -> #000080`, and `0x00660000 -> #000066`; `0xffffffff` remains automatic/default. A fragment crossing a state boundary is not promoted.
- [x] Add `multiFragmentParentRunFillMixes` to `shanai_lan` label style evidence so fragment-level style hypotheses are checked at the parent-run level. The current sample has 5 multi-fragment parent text runs, 14 split slots, and `mixedFillMultiFragmentParentRunCount:2`: runs `6010..6096` and `9494..9628` each contain one default-black and one property-15-blue fragment, while the bottom run `12060..12384` contains 5 uniformly blue fragments. Cross-sample `hyo` evidence shows property 15 also participates in non-text/control state, so its universal role remains undecoded and rendering is deliberately gated to uniform exact text ranges in the `shanai_lan` projection.
- [ ] Decode `/FigureData/main_data/FDMVector` drawing and image payload semantics for `shanai_lan` so the single-page LAN diagram can be rendered as actual diagram objects instead of diagnostic Frame boxes.
- [x] Add the source-backed `fdmIndexSegmentBboxAxisPairGate` for object geometry without changing rendering. A valid FDMIndex row is admitted only when its vector offset reaches a sibling FDMVector segment with a header bbox; the raw index fields must exactly match the segment as `left`, `right`, `top`, `bottom` before `normalizationInputSourceBacked:true` is reported. `shanai_lan` has 39/39 valid linked rows with this agreement, and the local corpus has 70/77 linked rows across the observed FDMIndex/FDMVector candidates; the unmatched seven rows stay outside the gate. The evidence remains `decoded:false`, `diagnosticOnly:true`, and `renderable:false`, with `fdm-index-axis-pair-does-not-decode-page-transform-or-object-role` blocking page placement and object-role promotion.
- [x] Decode the most common `shanai_lan` FDMVector compound prefix trailer shape as a nested child offset table rather than a style trailer. The model now preserves marker-valid `compoundChildOffsets` on parent `fdmVectorCommandDiagnostic` ops, strips non-marker child-area end words from that list, and the nested primitive parser uses those offsets before falling back to marker scanning, keeping row/parent paint order source-backed and reducing false marker risk.
- [x] Preserve source-backed compound fill semantics for `shanai_lan`: single-child `0xff000a60` compounds with parent style `0x0001` now treat the first two prefix dwords as BGR gradient stops (`strokeColor -> fillColor`) and render them as SVG/PDF linear gradients. This covers the laptop/CRT/monitor screen fills and other clip-art shaded surfaces without hardcoding the `#003366 -> #7a7acc` screen color pair.
- [x] Preserve filled path counters for `shanai_lan` text/icon masks: same-compound holes and black/white text-mask pairs are rendered as explicit counter overlays, so Router `R`/`o`/`e` interiors stay black even when the source child order would otherwise repaint them white.
- [x] Preserve source-backed line-header y-placement candidates for `shanai_lan` text slots without changing rendering. `getPageLayerTree` now emits `lineHeaderSameSegmentGroupRun` and `lineHeaderYPlacementCandidate`, and SVG mirrors the same run facts as `data-line-header-same-segment-run-*` attributes. This captures cases such as the separated `RAID`/`5` label (`offsetUnits=84`, `extentUnits=174`, groups `1..6`, `positionInRun=1`) and `CD-RW` (`offsetUnits=0`, `extentUnits=84`, groups `27..31`, `positionInRun=2`) while preserving peer counts that explain why the run cannot be used as a row anchor yet: the `RAID`/`5` run spans 5 visible text slots across 4 distinct groups, and `CD-RW` spans 4 slots across 2 groups. Render promotion remains blocked as `line-header-y-run-placement-semantics-unproven` with detail `same-segment-run-spans-multiple-visible-text-rows`.
- [x] Add a source-backed `fdmTextMaskCohortSummary` diagnostic for `shanai_lan` FDMVector closed-fill text/icon masks before changing any label placement. The summary groups black/white closed-fill primitives by FDM row and searches for right-neighbor `/DocumentText` slots without promoting alignment. It now captures the `RAID` FDM path cohort as row `5` (`22` primitives, projected bbox `465.107,84.575,34.683,76.196`) and links the nearby `/DocumentText` `5` slot as a diagnostic right-neighbor candidate (`gap=12.464px`, vertical overlap `19.950px`, center delta `-19.998px`). The same row is also split into source-bbox-adjacent components (`topTextLikeComponentCandidateCount:3`, `componentRightNeighborCandidateCount:1`), isolating the top glyph-like `RAID` component (`7` primitives, bbox `465.204,84.575,34.586,10.435`) from the server-rack rectangles; its component-level right-neighbor relation to `/DocumentText` `5` records `vertical overlap=2.310px` and `center delta=12.882px`. The component relation now also exposes a `leadingWhitespaceBridgeCandidate`: the `/DocumentText` parent run is unit `578..613`, the pre-fragment span is `578..599` (`21` units), the pre-fragment projection is `42` grid units / `155.418px`, and the FDM glyph component sits inside that projected pre-fragment corridor (`sourceBboxWithinPreFragmentProjection:true`, offset `108.368..142.954px`, `12.464px` before the `5`). Rendering stays blocked as `fdm-text-mask-document-text-alignment-unproven`, `fdm-text-mask-component-to-document-text-alignment-unproven`, `document-text-pre-fragment-fdm-mask-role-unproven`, and `fdm-text-mask-to-document-text-baseline-transform-unproven` until a real FDM-mask-to-DocumentText baseline transform is decoded.
- [x] Add non-rendering FDM text-mask promotion readiness and projection-extent diagnostics for `shanai_lan`. `getPageLayerTree` now emits one `fdmProjectionExtentSummary`; FDMIndex entry bbox-like fields are normalized as an entry-only axis pair (`x0,x1,y0,y1`) while FDMVector command bboxes keep normal left/top/right/bottom semantics. With that normalization, active command extent (`-16154,-16224,-5612,-9344`), rendered primitive source extent (`-16154,-16224,-5453,-9344`), and FDMIndex entry extent (`-16154,-16224,-5576,-9336`) are close but still disagree (`commandVsFdmIndexEntries.maxAbsDelta:36`, `renderedPrimitivesVsFdmIndexEntries.maxAbsDelta:123`), and a reference crop around `RAID` still places generated glyphs about `10.7px` left and `12.1px` high. Page-placement transform authority remains blocked as `fdm-vector-page-placement-transform-source-fields-unproven`. Text-mask cohorts/components also expose normalized `sourceBbox`; the `RAID` row 5 cohort source bbox is `-11828,-15758,-11470,-14984`, and the top glyph component source bbox is `-11827,-15758,-11470,-15652`, matching the FDMIndex row coordinate family while still projecting too high/left. It also emits 7 `fdmTextMaskRightNeighborPromotionReadiness` objects; the `RAID`/`5` case has cohort/component agreement on slot `5`, the FDM glyph bbox inside the pre-fragment corridor, and `preFragmentUnitCount:21`, but still reports `splitFromTextRun:false`, `sameSegmentGroupRunDistinctTextGroupCount:4`, `rowAnchorAmbiguous:true`, and blockers `document-text-slot-not-split-from-text-run`, `line-header-y-run-placement-semantics-unproven`, `document-text-pre-fragment-fdm-mask-role-unproven`, and `fdm-text-mask-to-document-text-baseline-transform-unproven`. This preserves the evidence without moving `5` or broadening mask/text alignment.
- [x] Add diagnostic-only `fdmTextMaskSourceTransformCandidateSummary` for the same `shanai_lan` FDM/text bridge. It derives one current-projection candidate from the `RAID` top text-like component to `/DocumentText` slot `5`: current grid offset range `29.285..38.632`, `sourceUnitsPerTextGridUnitX:38.196`, inferred line-start source x `-12945.565`, text-start source x `-11341.348`, and source gap to text start `128.652`. The candidate is intentionally `referenceBacked:true` because the text grid origin still comes from the reference viewport, and it stays blocked by row-anchor ambiguity, unsplit text slot evidence, pre-fragment role uncertainty, baseline transform uncertainty, and missing cross-sample support.
- [x] Add `gridOriginAuthorityGate` to `shanai_lan` `documentTextLineHeaderProjectionCandidateSummary`. The selected visible line-header set now carries a source-domain row-anchor fit: 7 selected line headers, selected groups `[21,31]`, LineMark records `[22,32]`, complete LineMark coverage, uniform `recordIndexMinusGroupIndex:[1]`, and PageMark entry coverage under entry row `0` (`lineStart:0`, `lineEnd:39`). This proves a useful `/DocumentText group -> /LineMark record` row-domain relation but not page-space origin; the gate remains `referenceBacked:true`, `pageSpaceOriginCandidate:null`, and blocked by `document-text-grid-origin-reference-backed`, `line-header-visible-rule-selector-unproven`, and `page-space-y-origin-unproven`.
- [x] Preserve selected `/LineMark` source-unit intervals inside the same gate. `selectedLineMarkSourceUnitGate` records selected records `[22,32]`, unit starts `[3908,5483]`, unit ends `[4121,5615]`, spans `[213,132]`, record delta `[10]`, and unit-start delta `[1575]` (`sourceUnitDeltaPerRecordEstimate:157.500`). This remains non-promotable because it has only one stride sample and still lacks a source-unit-to-page-y transform.
- [x] Add `sourceOnlyPageMarkYValueProbe` inside the same `gridOriginAuthorityGate`. The selected PageMark entry produces 58 parsed y-value candidates, and the nearest current-origin probe is `parsedEntryU16` word `7` / byte `14`, value `39`, residual `0.300px` from current origin `38.700`. A companion `pageMarkEntryProfileGate` records that this entry is `mixed-payload` rather than an additive layout-origin profile, and `lineBoundaryConflictGate` records that the same value matches the entry `lineEnd=39` (`matchesLineEnd:true`), so this is deliberately not promoted. The probe stays blocked as `page-mark-y-value-field-role-unproven`, `document-text-grid-origin-reference-backed`, and `page-space-y-origin-unproven`.
- [x] Preserve raw `/DocumentText` line-header words for `shanai_lan` text slots and line-rule diagnostics. `getPageLayerTree` now emits `lineHeaderRawWords` and `lineHeaderRawWordsHex`, and `page-svg` exposes `data-line-header-raw-words-hex` on rendered LAN labels. The current sample shows the visible line-header family is consistently `001c 0030 000c 0000 <offset> <extent> 00ff 0000 000c 0000 0030 001f`, so the previously suspected hidden y/dash fields are not present in this 24-byte header itself. Fresh visual probing still rejects direct rendering: all 16 current line-rule projections worsen Poppler whole-page MAE/RMS from `24.536/71.636` to `26.062/74.096`, and FDM connector mask matching continues to rank mostly icon/globe internals rather than the LAN trunk lines. The generated `shanai_lan.pdf` was regenerated back to the diagnostic-only state and verified nonblank through Poppler, PDFKit, and CoreGraphics/sips.
- [x] Aggregate `shanai_lan` line-rule promotion blockers in one `documentTextLineRuleProjectionSummary` op. The current sample has 16 line-rule candidates (7 skipped-inline horizontal, 9 vertical-anchor), all 16 have `/LineMark` interval matches, 11 look orthogonal, but only 2 of 32 endpoints attach to a text slot within one line height and 0 rules attach at both endpoints. A fresh raster overlay sweep also failed to improve the reference diff for any broad source-only subset, so these rules stay diagnostic-only until endpoint ownership and row-boundary semantics are decoded.
- [x] Expose root-level parent compound provenance for `shanai_lan` `fdmConnectorCandidateDiagnostic` ops. Each synthetic nested connector now carries a decoded-false `parentCompoundCommand` object with `parentCommandIndex`, `parentMarkerHex`, `parentRelativeOffset`, `parentCompoundChildOffsets`, `childOffsetInParent`, and `childOffsetTableMatched`, so the `ff000a60 -> ff000660/ff000960` child relationship can be inspected without relying on endpoint-owner summaries or external JSON scripts. This is diagnostic-only and does not promote connector rendering.
- [x] Add parent-normalized owner-order diagnostics for `shanai_lan` connector candidates. `endpointOwnerMatchSummary` now emits connector/nearest-owner parent relative offsets, parent-command-span booleans, `ownerParentCommandRelation`, `ownerParentSourceOrderRelation`, and `parentNormalizedOrderedSameRowSameConnector`; the graph summary also emits `parentNormalizedOrderedSameRowSameConnectorCount` and `parentNormalizedOwnerRowCohortEndpointMatchSummaries`. Current evidence still blocks rendering: the PDF-backed sample has 67 connector candidates, 9 matched connectors, 10 thresholded endpoint matches, 0 dual-endpoint matches, 0 raw ordered same-row/same-connector candidates, and 0 parent-normalized ordered same-row/same-connector candidates. Parent-normalized relation counts are 64 `same-row-after-owner-parent-command-span`, 1 `same-row-before-owner-parent-command-span`, 1 `missing-endpoint-owner-candidate`, and 1 `nearest-owner-row-mismatch`; all 10 thresholded endpoint matches remain in the non-parent-normalized-ordered cohort.
- [x] Add endpoint-side and dominant-row diagnostics to `shanai_lan` `fdmConnectorGraphDiagnosticSummary`. The current source-backed graph still has 0 dual-endpoint line-rule matches: 4 connector candidates match only their start endpoint, 5 match only their end endpoint, and the strongest row concentration is FDM row 33 with 14 connector candidates, 7 matched connectors, 8 thresholded endpoint matches, 0 dual-endpoint matches, 2 start-only matches, and 5 end-only matches. This keeps connector rendering blocked while making the false-positive cluster visible without external JSON scripts.
- [x] Aggregate `shanai_lan` `/DocumentText` line-rule candidates into source-backed graph components. `documentTextLineRuleProjectionSummary` now exposes `lineRuleGraphComponentCount:6`, `largestLineRuleGraphComponentRuleCount:4`, and per-component rule indexes, bbox, horizontal/vertical counts, LineMark coverage, isolated endpoint totals, projected length, and a component-level render blocker. The two largest wiring-like components are `[2,5,13,14]` (`bbox 363.038,433.500,261.430,200.400`, length `765.858px`) and `[3,6,15]` (`bbox 725.679,415.500,357.641,200.400`, length `908.482px`), but temporary raster overlays of line-rule subsets still worsen RMS, so these remain diagnostic until placement/style/endpoint semantics are proven.
- [x] Expose all long `/DocumentText` line-header candidates for `shanai_lan` before broadening wiring rendering. `getPageLayerTree` now emits one `documentTextLineHeaderProjectionCandidateSummary` op with 171 parsed line headers, 103 long line-header candidates, 7 skipped-inline long candidates selected as current horizontal line rules, and 96 unselected long candidates blocked by `line-header-visible-rule-selector-unproven`. This shows that several upper/middle wiring-like spans are already present in `/DocumentText` line headers (for example group 17, units `130..280`, bbox `525.856,361.500,557.464,2.400`) but must not be rendered until the visibility selector is decoded. A companion FDMVector large-span diagnostic found 0 primitives blocked by the previous page-span filter in this sample, so the missing trunk wiring is not explained by that filter.
- [x] Add source-backed context to the `shanai_lan` line-header visibility probe instead of promoting raw headers. Each long `documentTextLineHeaderProjectionCandidateSummary` candidate now includes `lineMarkContext`, `documentTextMapContext`, and `sameSegmentGroupRun`, and the summary now aggregates 41 `sameSegmentGroupRuns` so repeated offset/extent families can be inspected without external scripts. This locks the current split: all 7 selected horizontal rules are inside real `DocumentTextMapKind::SkippedInlineText` entries with selectors `0x008f` or `0x0010`, while representative unselected wiring-like headers such as group 17 `130..280` have no containing map entry and sit between ordinary text/control entries despite sharing the same 24-byte raw header shell. The long `184..280` family spans groups `20..31` with 12 headers, but only 2 are selected/skipped-inline while 10 have no containing map entry, which explains why the current visible rule set shows only fragments of a longer trunk-like run. A fresh reference-backed dark-pixel probe found only 12 of 103 long headers above a `0.10` missing-dark ratio and those still mix text/image residuals with wiring, so no header is promoted to visible PDF output until a source-backed selector is decoded.
- [x] Add non-rendering `fdmOpenStrokeCohortSummary` diagnostics for `shanai_lan` so missing wiring work can separate open FDM stroke families from `/DocumentText` line headers without scraping individual primitives. The current page has 1889 FDM primitive projections, 901 open strokes, 67 connector candidates, 267 horizontal, 145 vertical, 489 diagonal, 588 line-marker, and 313 non-line-marker open strokes across 22 rows. The dominant open-stroke connector row by raw candidate count is row 0 (`24` connector candidates, `155` open strokes), while the existing line-rule endpoint-match summary still concentrates on row 33 (`14` connector candidates, `7` matched connectors, `0` dual-endpoint matches). This mismatch is now exposed in both `getPageLayerTree` and `getDocumentInfo` and keeps connector rendering blocked as `open-stroke-role-and-paint-order-unproven` until source ownership/paint order explains which row families are real LAN wiring.
- [x] Add same-row FDM open-stroke axis-rule evidence to `shanai_lan` connector graph diagnostics without changing visible PDF output. The graph summary now reports `fdmOpenStrokeAxisRuleProjectionCount:224` and a `sameRowFdmOpenStrokeAxisRules` provenance bucket sourced from non-connector open FDM primitives in the same FDM row as each connector. This bucket finds 254 thresholded endpoint matches across 52 of 67 connector candidates, including 21 dual-endpoint candidates (`tight`: 170, `nearby`: 84), but all 21 dual candidates remain blocked as `connector-ownership-and-paint-order-unproven` because row ownership and paint order are not decoded.
- [x] Mirror the same-row FDM open-stroke axis-rule evidence onto each `fdmConnectorCandidateDiagnostic`. Each of the 67 connector candidates now carries `sameRowFdmOpenStrokeAxisRuleEndpointMatches` plus a per-connector summary with source rule command indexes, relative offsets, source segment provenance, projected/source bboxes, tier, grid deltas, and closest points. A neighboring `sameRowFdmOpenStrokeAxisRuleOwnerPromotionGate` mirrors the existing owner/source-order evidence next to this geometry filter. The current dual-endpoint candidates are concentrated in rows `0`, `2`, `3`, `10`, `33`, and `37`; 18 of 21 still fall into `same-row-mixed-command-namespace`, 20 of 21 are `same-row-after-owner-relative-offset-span`, and 0 satisfy `parentNormalizedOrderedSameRowSameConnector`, so this remains comparison evidence only rather than a render gate.
- [x] Aggregate same-row FDM open-stroke axis-rule row cohorts inside `fdmConnectorGraphDiagnosticSummary` instead of relying on external JSON scripts for the wiring probe. The new `sameRowFdmOpenStrokeAxisRuleRowCohorts` field ranks row cohorts by dual-endpoint evidence and exposes row 33 as the leading source-backed cluster: 14 connector candidates, 33 thresholded endpoint matches, 14 matched connectors, 7 dual-endpoint candidates, 5 non-diagonal dual candidates, 6 tight-dual candidates, and 5 non-diagonal tight-dual candidates. A fresh Poppler A/B still rejects broad connector/axis-rule rendering, but the row-33 non-diagonal axis-rule overlay showed only a tiny local improvement (`whole RMS -0.0077`, row33 crop RMS `-0.3148`), so this remains diagnostic-only and blocked as `connector-ownership-and-paint-order-unproven`.
- [x] Refine the `shanai_lan` graph-level connector blocker so axis-rule evidence is not hidden behind the older all-line-rule summary. The layer tree still reports `dualEndpointMatchConnectorCount:0` for document-text line rules, but `sameRowFdmOpenStrokeAxisRules` reports 21 dual-endpoint candidates and `parentNormalizedOrderedSameRowSameConnectorCount:0`, so `renderPromotionBlockedReason` now says `same-row-axis-rule-parent-normalized-order-unproven` instead of `no-dual-endpoint-line-rule-match`. Visible connector rendering remains disabled.
- [x] Add `sameRowFdmOpenStrokeAxisRuleOwnerPromotionGateSummary` to the `shanai_lan` graph summary so the 21 axis-rule dual candidates no longer require scanning 67 per-connector gates. The new aggregate shows all 21 have dual endpoint owner candidates and matching nearest-owner rows, but 0 satisfy the parent-normalized ordered same-row/same-connector gate; parent command/source-order relations are 20 `after` and 1 `before`, with no `between` candidates. It now exposes `parentNormalizedOrderGateBlockedReason:"connector-parent-command-outside-nearest-owner-parent-command-span"` so the graph-level blocker is explicit while rendering stays blocked as `same-row-axis-rule-parent-normalized-order-unproven`.
- [x] Add source-backed bbox and per-candidate tight-match diagnostics to the `shanai_lan` same-row FDM open-stroke axis-rule gate before promoting any connector rendering. `sameRowFdmOpenStrokeAxisRuleRowCohorts` now emits matched and dual-endpoint projected bbox unions; row 33's leading dual cluster covers `x=684.639,y=618.639,w=97.850,h=97.362`, and its tight non-diagonal dual subset narrows to `x=685.802,y=622.380,w=85.255,h=93.621` in generated CSS pixels. The graph summary also exposes a diagnostic-only `renderReadinessPredicate` for `sameRowFdmOpenStrokeAxisRule+tightDualEndpoint+nonDiagonalConnector`, currently `candidateCount:16` across `rowCohortCount:6`, still blocked by `connector-ownership-and-paint-order-unproven`. Each `fdmConnectorCandidateDiagnostic` also exposes `startTightMatchCount`, `endTightMatchCount`, `tightDualEndpointMatch`, `parentNormalizedOrderGateBlockedReason`, connector parent command/relative offsets, nearest-owner parent offsets, and parent command/source-order before/between/after booleans. Visible PDF output is unchanged (`shanai_lan` Poppler 144dpi mean/RMS remains `24.536/71.636`), but the next render predicate is now inspectable without hardcoding row numbers or sample names.
- [x] Add row-level `ownerPromotionGate` summaries to `sameRowFdmOpenStrokeAxisRuleRowCohorts` so the wiring blocker is visible without inspecting each connector. Row 33 now reports 7 dual-endpoint axis-rule candidates, all 7 with endpoint owner rows matching the connector row, but all 7 are still `same-row-after-owner-parent-command-span` / `same-row-after-owner-parent-relative-offset-span`, with 0 parent-normalized ordered candidates. Row 3 reports 5 dual-endpoint candidates, with 1 before and 4 after the owner parent/source span and 0 between candidates. No row currently proves a between/ordered owner relation, so visible connector rendering remains blocked by `connector-parent-command-outside-nearest-owner-parent-command-span`. The regenerated `shanai_lan.pdf` was checked through `sips` and Swift `PDFKit`; both render nonblank, while direct Preview.app UI automation remains unavailable in the sandbox.
- [x] Tighten the source-backed parent-normalized owner/order gate so `parentNormalizedOrderedSameRowSameConnector` requires both the nearest-owner parent command span and the nearest-owner parent relative-offset span. This prevents a command-only match from becoming render authority before source-order evidence agrees. The regression test `fdm_connector_parent_normalized_order_requires_parent_relative_offset_between_nearest_owner_parents` locks the false/true cases, and the current `shanai_lan` axis-rule sweep still reports 21 dual-endpoint candidates, 0 parent-normalized promotions, command/source parent relations `20 after / 1 before / 0 between`, and graph blocker `same-row-axis-rule-parent-normalized-order-unproven`. Regenerated `success_data-test.pdf` and `shanai_lan.pdf` remain nonblank under Apple `sips` and Swift `PDFKit`.
- [x] Couple the `shanai_lan` graph-level connector blocker to the source-backed owner/order gate instead of leaving it at the generic axis-rule transition blocker. When same-row FDM open-stroke axis-rule evidence is present and the aggregate owner gate is populated, `fdmConnectorGraphDiagnosticSummary.renderPromotionBlockedReason` now surfaces the specific owner/order reason, currently `connector-parent-command-outside-nearest-owner-parent-command-span`, and the owner gate itself requires every dual-endpoint candidate to be coherent on both parent-command and parent-relative-offset axes before returning `none`. Rendering remains disabled; this only prevents a partially coherent wiring cluster from becoming render authority.
- [x] Add source-backed marker/style profiles to `sameRowFdmOpenStrokeAxisRuleRowCohorts` to distinguish likely wiring rows from device/internal stroke clusters without rendering them. Each row cohort now reports `matchedConnectorMarkerStyleProfile`, `dualConnectorMarkerStyleProfile`, `tightNonDiagonalDualConnectorMarkerStyleProfile`, and `axisRuleEndpointMatchMarkerStyleProfile`, grouping raw FDM marker families and style words rather than sample coordinates. Current leading row 33 has dual connectors `line-marker=1/path-marker=6` with styles `0x0005=1/0x0080=2/0x00a0=4`, while its matched axis rules have `line-marker=17/path-marker=16` and mixed style words including `other-style=4`; row 3 has all 5 dual connectors as path markers and 57 axis-rule endpoint matches split `line-marker=43/path-marker=14`. This keeps the candidate set diagnostic-only and provides a general source field for the next false-positive filter.
- [x] Mirror the same marker/style profile onto the broader `fdmOpenStrokeCohortSummary` row cohorts so axis-rule rows can be compared against all open-stroke row families. The dominant connector row 0 has 155 open strokes with `line-marker=73/path-marker=82` and style `0x0000=152`, while row 33 has 47 open strokes with `line-marker=21/path-marker=26` and mixed styles `0x0005=21/0x0080=12/0x00a0=8/other=6`. This is still diagnostic-only, but it makes the next source-backed role gate inspectable from `getPageLayerTree` and `getDocumentInfo` without scraping individual primitive ops.
- [x] Add a decoded-false marker/style `roleGate` to every open-stroke marker/style profile. The gate reports marker/style diversity, dominant marker family/style word, homogeneous flags, and an explicit blocked reason. Current shanai_lan evidence shows leading row 33 is blocked as `mixed-marker-family-and-style-word-role-unproven`, row 3 axis-rule matches are similarly mixed, while homogeneous rows such as row 37/row 2 still remain blocked as `homogeneous-marker-style-still-needs-owner-and-paint-order`. This prevents a simple style-homogeneity shortcut from silently becoming render authority before ownership and paint order are decoded.
- [x] Reject visible rendering of the `sameRowFdmOpenStrokeAxisRule+tightDualEndpoint+nonDiagonalConnector` readiness predicate for now. The source-backed, non-hardcoded SVG/PDF probe rendered 16 candidates through existing `fdm_projected_path_data`, but it duplicated device/internal strokes rather than recovering the LAN trunk topology and worsened Poppler 144dpi whole-page diff from approximately `24.536/71.636` to `24.565/71.697`. The probe was removed, `shanai_lan.pdf` was regenerated back to the diagnostic-only state, and rendering remains blocked as `connector-ownership-and-paint-order-unproven`.
- [ ] Decode the remaining `shanai_lan` connector graph semantics: the recovered FDMVector primitives now reconstruct most device silhouettes, some fills/gradients, BGR color families, width states, and source-backed text positions, and the model/export/layer-tree surfaces preserve long open-path `fdmConnectorCandidate` evidence with paint/path segment fields. Directly rendering those connector candidates was measured and rejected because every tested overlay worsened whole-page MAE/RMS and produced false positives; keep them diagnostic-only until grouped connector ownership, exact text style/color controls, multi-label run splitting, exact stroke style semantics, image/object layering, and paint order are decoded. `documentTextLineRuleProjection` now also exposes stable `ruleIndex` values, render-block reasons, endpoint junction degrees, and nearest-text endpoint attachment candidates, while `fdmConnectorCandidateDiagnostic` exposes `projectedTextGrid` coordinates, `endpointOwnerCandidates`, `endpointOwnerMatchSummary`, nearest `lineRuleAttachmentCandidates`, thresholded `lineRuleEndpointMatches`, and `lineRuleEndpointMatchSummary` so promotion attempts can compare FDM endpoints, candidate owner objects, line-rule spans, and text slots in the same document-text grid instead of using raw page-space spans. The endpoint owner surface is source-backed but not proven: all 67 connector diagnostics now have start/end owner candidate arrays and per-connector owner summaries, capped at 3 candidates per endpoint using the text line-height probe radius (`18.000px` in the current sample), with 402 emitted candidates total (`401` FDM primitive, `1` DocumentText slot; 372 within probe radius). 66 connectors have owner candidates within probe radius at both endpoints, but every candidate remains `ownerProven:false` and blocked as either `endpoint-owner-candidate-unproven` or `missing-endpoint-owner-candidate`. Nearest-FDM-owner grouping is also diagnostic only: 65 connectors have nearest owners on the same FDM row as the connector, 1 dual-owner connector has a row mismatch, only 8 place the connector command between the nearest owner commands, 43 occur before the nearest owner command span, 15 occur after it, and 0 satisfy the same-row/same-connector ordered-owner gate once mixed top-level vs synthetic-nested command namespaces are excluded; `ownerGroupingProvenConnectorCount` remains 0. `getPageLayerTree` now also emits one `fdmConnectorGraphDiagnosticSummary` op that aggregates the current blocker state. The latest `shanai_lan` graph-match diagnostic has 67 connector candidates, 134 endpoint probes, and only 10 thresholded endpoint matches across 9 connector candidates (`tight`: 2, `nearby`: 8); all 9 matched candidates are blocked as `single-or-missing-endpoint-line-rule-match`, the remaining 58 are `no-thresholded-line-rule-endpoint-match`, and the aggregate render block is `no-dual-endpoint-line-rule-match`. The graph summary now splits endpoint matches by line-rule provenance, owner-row cohort, owner-command relation, and owner source-order relation: skipped-inline line headers alone account for 9 of the 10 thresholded matches, vertical-anchor line rules add only 1 nearby single-endpoint match, both sources still produce 0 dual-endpoint matches, and the 0 same-row/same-connector ordered-owner candidates have 0 thresholded line-rule endpoint matches while the other 67 connectors hold all 10 matches. After requiring comparable command-index namespaces, the relation cross-tab shows all 10 thresholded endpoint matches fall into the `same-row-mixed-command-namespace` bucket (49 connectors, 9 matched), while `same-row-before-owner-command-span` has only 1 connector and 0 matches, `same-row-between-owner-command-span` has 0 connectors and 0 matches, `same-row-after-owner-command-span` has 15 connectors and 0 matches, and the missing-owner/row-mismatch buckets have 1 connector each and 0 matches. The source-order relation now emits `connectorRelativeOffset` plus nearest owner `relativeOffset` values and shows the row-local byte offsets are comparable diagnostic evidence: `same-row-after-owner-relative-offset-span` has 64 connectors and all 10 thresholded endpoint matches (9 matched connectors), while `same-row-before-owner-relative-offset-span` has 1 connector and 0 matches, `same-row-between-owner-relative-offset-span` has 0 connectors and 0 matches, and missing-owner/row-mismatch remain 1 connector each with 0 matches. This means raw top-level-vs-synthetic-nested command ordering must not be used as promotion evidence, and even source-order evidence remains non-rendering because all thresholded line-rule matches are still single-endpoint only. Temporary raster overlays for both matched FDM connectors and raw line-rule projections worsened MAE/RMS, so connector/line rendering remains blocked until ownership/paint-order semantics are decoded. The compound prefix is now known to carry first fill/stroke dwords plus marker-valid child offsets or child-area end words; do not replace this evidence with broader color heuristics.
- [ ] Decode `shanai_lan` label anchors and general text-style semantics beyond the current group-line/fragment projection. The Microsoft Print To PDF reference confirms that the PDF text layer contains the full LAN labels, and the current `/DocumentText` group/line-header projection places major labels close to reference coordinates. Exact-range property 15 now replaces all label-string color exceptions for this projection, but `/DocumentTextPositionTables` `TCntV.01` is still not direct anchor geometry and cross-sample evidence prevents treating property 15 as a universal foreground field. True FDM text-object anchors, general property roles, and FDM row/object anchors remain to be decoded.
- [ ] Decode `shanai_lan` page/background and object paint-state semantics. Earlier Apple `sips` renders of the reference PDF could show transparent page areas composited as black, but the Poppler comparison and current `sips` generated-PDF smoke check show no source-backed full-page black paint. Do not flip the background by sample name or renderer artifact; find any real source paint/page state in FDM/page/layout streams and expose it before rendering. FDM primitive `paintCoverage` diagnostics now show 1889 primitives, only 3 covering at least 1% of the page, and none over 5%, so the currently decoded FDMVector primitives do not contain a page-fill candidate.
- [ ] Decode semantic object header fields preceding embedded image payload spans and connect payload ownership to `/Figure`, `/Frame`, `/LayoutBox`, and layout mark evidence.
- [x] Preserve direct `/DocumentText` control-run table candidates even when `/DocumentTextPositionTables` is absent: rows are inferred from `0x000e` row delimiters and `0x001c` cell delimiters, kept as `documentTextControlRunTableCandidate`/`decoded:false`, and rendered as reference-backed `tableProjection` grids in SVG/PDF when tsaiten evidence is present. `getPageLayerTree` now exposes per-cell bbox/text metadata with `referenceBacked:true`, while lower-confidence samples remain diagnostic grids. The PDF-reference `tsaiten` sample now exposes the visible scoring tables in `openjtd-samples/pdf-output/ichitaro-20030120132956-0007-sp-dat-tsaiten.pdf`.
- [x] Split table layout evidence from sample-specific reference projections. `getPageLayerTree` now emits a `sourceDerivedLayoutCandidate` for direct `/DocumentText` control-run table grids when line headers prove all cell slots: it is `sourceBacked:true`, `referenceBacked:false`, `decoded:false`, and carries source-derived bbox/column widths, row-height basis, anchor-line evidence, matched span/gap units, and the explicit render blocker `page-space-origin-and-row-baseline-unproven`. SVG/PDF table rendering still prefers reference-backed layouts, but now falls back to a visible `sourceDerivedDiagnosticProjection` only when the source-derived candidate has a fallback text-anchor line. The PDF-backed `tsaiten` sample now renders two non-reference-backed source-derived grid overlays, while the PDF-backed `shanai_lan` false-positive table-like label group remains blocked because it has no anchor line.
- [x] Generalize fallback body-text suppression to every table candidate that the current SVG renderer actually draws, rather than only the `success_data-test` reference table. `tsaiten` source/reference table cells such as `235点以上` and `success_data-test` cells such as `０.８` now appear once in SVG instead of being duplicated by the generic fallback text layer.
- [x] Keep unproven `sourceDerivedDiagnosticProjection` table grids out of visible SVG/PDF when a page already has an observed form/text projection. The source-derived layout evidence remains in `getPageLayerTree`, but the PDF-backed `tsaiten` output no longer draws the two diagnostic source-derived grids over the reference/form projection, removing the visible middle-table text overlap while preserving the generic table evidence for future source-backed promotion.
- [x] Add slot-scoped `/PageMark` u16 subrecord y-sequence diagnostics for `tsaiten` table candidates. `pageMarkScopedYTransformProbe` now emits `slotScopedSubrecordYSequenceComparison`, grouped by `fieldIndex+tailBlock16WordIndex`, with line-mark record indexes, reference row tops/deltas, and page-scale candidates from `/PageMark` word 21 and word 13+14. It also filters bogus subrecord line ranges against the scanned `/PageMark` max line end and splits same-header vs foreign-header slots. This keeps field-family residuals from hiding mixed subrecord slots: both visible table candidates have `sameHeaderSlotCount:0` for matched raw header `0`, while the lower table's direct table-top hit remains only as a foreign-header numeric hint at `fieldIndex:2`, `tailBlock16WordIndex:11`, `byteOffsets:[178,418,1074]`. Rendering remains blocked until same-page/header semantics explain all row y coordinates without reference projection.
- [x] Correct `/PageMark` row-delta diagnostics so row-to-row spacing targets are compared against adjacent ordered candidate value deltas, not absolute candidate y values. `pageMarkScopedYTransformProbe` now exposes `rowDeltaCandidatePolicy:"adjacent-ordered-candidate-value-delta"`, family/slot fits expose `rowDeltaResidualBasis:"adjacent-ordered-member-value-delta"` plus `rowDeltaCandidateDeltasPx`, and the `tsaiten` regression test locks this basis. This keeps `bestRowDeltaFamily`/`bestRowDeltaSlot` from overstating evidence when a field is only an absolute y/top candidate.
- [x] Add ordered one-to-one `/PageMark` slot coverage diagnostics before promoting any table y slot. `slotScopedSubrecordYSequenceComparison` now reports `orderedLineMarkRecordCoveragePolicy:"one-ordered-subrecord-member-per-line-mark-record"`, `bestOrderedLineMarkRecordCoverageSlot`, and per-slot ordered coverage counts/witness offsets. The metric uses a maximum monotone assignment between the ordered table `/LineMark` record set and each slot's source-backed subrecord line ranges, so one wide subrecord can no longer count as proof for multiple compact rows.
- [x] Surface `/PageMark` raw subrecord line-span readiness directly inside `lineMarkStridePromotionReadiness` for `tsaiten` table candidates. The new `subrecordLineSpanReadiness` summary is source-backed and reference-free: the scoring table has 3 of 4 selected post-row-gap span hits, while the lower table has 3 of 3 hits and now records blocker reasons `page-mark-subrecord-spans-fit-selected-post-row-gaps` and `page-mark-subrecord-spans-do-not-decode-page-y-origin`. This makes the promotion gate explicit: span evidence corroborates LineMark row/gap sequencing, but it is not a decoded page-y origin transform.
- [x] Add a source-only `subrecordSpanRoleGate` under the same `tsaiten` `subrecordLineSpanReadiness`. The gate classifies the dominant matched role as `selected-post-row-gap`, not row height: scoring has 3/4 post-row-gap hits and 0 row-span hits, while lower has 3/3 post-row-gap hits and 0 row-span hits. This explicitly blocks treating `/PageMark` raw subrecord spans or `0x0300` absolute-looking fields as visible row-y authority until a separate page-y origin transform is decoded.
- [x] Add a diagnostic-only `sourceDerivedLayoutReadiness` gate to every table-grid candidate so absent or blocked source-derived layouts explain themselves without reference PDF inspection. `tsaiten` scoring now reports `sourcePlacementEvidencePresent:false`, `matchedCellHeaderCount:0/12`, missing row/cell header coverage, and `source-derived-layout-candidate-absent`; the lower table reports `matchedCellHeaderCount:6/6` and an available candidate but remains blocked by `line-mark-record-stride-to-page-y-transform-unproven`.
- [x] Use source-backed `/DocumentText` line-header cell slot widths inside the visible `tsaiten` lower-table reference fallback instead of equal reference columns. The table remains `referenceBacked:true` and its page/y placement is still blocked by `line-mark-record-stride-to-page-y-transform-unproven`, but the rendered lower table now exposes `columnWidthBasis:"documentTextLineHeaderCellSlotUnits"` and `columnWidths:[378.342,175.659]`; `referenceBBoxResidualEvidence` records that the bbox still uses source column widths without source placement. Regenerated `openjtd-samples/pdf-output/ichitaro-20030120132956-0007-sp-dat-tsaiten.pdf` renders under Apple `sips` with `55279` non-white pixels and has zero `/Group`, `/SMask`, `/ExtGState`, `/Pattern`, and `/Shading` safety-token hits.
- [x] Preserve raw `/DocumentText` cell whitespace for rendered direct control-run table cells without changing the normalized model text. `success_data-test` page 1 now keeps `segment.text()` as `ａ`/`１`/`０.８`, while `getPageLayerTree` exposes `renderText`, `renderTextBasis:"documentTextSourceRangePreservedWhitespace"`, and `preservesSourceWhitespace`; SVG/PDF mirrors this with `xml:space="preserve"` and source range attributes. The regenerated `openjtd-samples/pdf-output/ichitaro-20030228030923-success-002-success_data-test.pdf` renders under Poppler and macOS Quartz/PDFKit/CoreGraphics. Poppler table crops were essentially unchanged to slightly worse (`wide-actual-table` RMS `91.864 -> 92.202` on the active 144dpi crop), so the text-loss hypothesis was rejected; subsequent mismatch work moved to line-header font-unit stroke/text-slot candidates.
- [x] Use the `/PageMark` body line-gap pitch for the `success_data-test` abc table y-origin instead of multiplying the page-local line offset by the table row height. The source-derived table bbox is now `x=78.251,y=411.421,width=275.063,height=63.000`; row height still comes from homogeneous `/DocumentText` line-header font units (`12 * 1.75 = 21.000px`), while y-origin uses `/PageMark` line range `0..39` and body height (`linePitchPx:21.214`, `linePitchBasis:"pageMarkBodyLineGap"`). The reference residual moves from `yResidualPx:2.716` to `-0.705`, and A/B Poppler crops improve (`wide-actual-table` RMS `92.202 -> 74.722`, `table-and-prompt` `80.008 -> 68.732`, `table-tight` `105.065 -> 82.445`). The regenerated PDF renders nonblank under Poppler, `sips`, PDFKit, and CoreGraphics.
- [x] Derive the `success_data-test` abc table rule width from the same source line-header font-unit scale instead of fixed SVG stroke constants, and center rendered cell text on the source-derived cell slots. Source-derived table SVG/layer-tree now exposes `strokeWidth:1.750`, `cellStrokeWidth:1.750`, `strokeWidthBasis:"documentTextLineHeaderFontUnitPx"`, `cellTextCentered:true`, and `cellTextAlignmentBasis:"documentTextLineHeaderCellSlotCenterCandidate"`. The change is decoded-false and still needs cross-sample validation, but A/B Poppler crops improve against the Print-to-PDF reference (`wide-actual-table` RMS `73.917 -> 69.118`, `table-and-prompt` `64.824 -> 61.242`, `table-tight` `82.399 -> 75.445`, `rules-tight` `84.834 -> 77.391`, whole-page `45.634 -> 45.241`). The regenerated output remains visible through Poppler, `sips`, PDFKit, and CoreGraphics.
- [x] Derive the `success_data-test` abc table column widths from the source line-header cell slot widths rather than each cell text-span width. The renderer now uses the same `/DocumentText` line-header selected by each cell provenance and measures the slot from that header's offset to the next header offset, producing `columnWidthBasis:"documentTextLineHeaderCellSlotUnits"` and widths `[49.370,56.423,56.423,56.423,56.423]`. This matches the reference grid-line pattern source-backed: at 144dpi the detected vertical rule widths move from generated `[73.5,73.5,88.5,88.5,88.5]` to `[74.0,84.5,84.5,85.0,84.5]` versus reference `[74.0,84.5,84.5,84.5,84.5]`. The active table/problem crop RMS improves `64.786 -> 64.009`, and the regenerated PDF remains PDFKit-safe with `/Group`, `/SMask`, `/ExtGState`, pattern, and shading token counts all zero.
- [x] Add a source-backed `sourceFrameAdmissionGate` to the `success_data-test` abc table horizontal solver so the active render frame and unproven PageMark raw-field agreement are separated. The renderable frame remains `page-body-frame+documentTextLineHeaderUnitTransform` (`x=78.251,width=275.063`), while the strongest PageMark raw agreement selects `x=171,width=548`; the gate records residuals `-92.749px/-272.937px`, `pageMarkRawAgreementConflictsWithRenderFrame:true`, and keeps PageMark raw fields blocked as `page-mark-horizontal-field-semantics-unproven`.
- [ ] Promote preserved `tableCandidates` into decoded table structures only after `/DocumentText` controls, `TCntV.01`, layout marks, and style/layout streams prove row/cell boundaries and geometry; current stream-name inventory finds no named table streams, including in the `hyo` sample.
- [ ] Decode the semantic meaning of `/PaperMark` header count-like values and `0x00010000`/`0x00010010`/`0x00010011` flags; RFC 0007 §PaperMark now documents that `count_value = last_index_value + 1` holds for all 17 tested samples (Ginga x3 + 14 tested samples) and `/PageMark`/`/PaperMark` share the same `count_value` in every sample; `0x00010011` appears only in Ginga vertical samples; cross-referencing PaperMark flag runs with PageMark `lineStart`/`lineEnd` at the same entry index shows that `0x00010000` runs correspond to transitional pages (section separators, spacers, or blank sentinels) while `0x00010010` runs correspond to continuous body sections — gap of ~30 lines between `0x00010010→0x00010000` transitions in `sample-reference` samples and 11–13 lines in `sample-draft` samples — but physical rendering semantics are still not decoded.
- [ ] Explain the three unsupported `/PaperMark` stride values before broadening the parser shape; RFC 0007 §PaperMark documents these as stale/foreign payloads (CFB directory-entry fragments, OLE/ActiveX metadata) — no new evidence available.
- [ ] Decode the `0x1000`, `0x1001`, and `0x1002` tag families inside `/LineMark`; current evidence shows their immediate next word is payload-like rather than a family discriminator and their LineMark offsets are not direct text coordinates.
- [ ] Explain why the MarkV.01 header's final `u16` lands inside `/LineMark` near tag clusters in the three samples that expose both streams, while MarkV.01 entry offsets do not.
- [ ] Replace embedded fragment plausibility filtering with structured object/stream boundary parsing.

Important constraint:

- The Ichitaro filter license restricts decompilation and reverse engineering. Treat the binary as a no-code reference artifact. Do not copy code or derive implementation from disassembly.

## M3: Document Model

Goal: implement `rjtd export <file> --format json` through the document model layer.

Status: minimal model path implemented.

Completed:

- [x] Build a minimal `Document` from extracted `DocumentText`.
- [x] Add a `DocumentParser` entry point similar to rhwp's parser trait.
- [x] Preserve raw `/DocumentText` bytes in the `Document` model.
- [x] Represent non-empty text lines as `Paragraph` blocks with `TextRun` inlines.
- [x] Implement JSON export from `Document`.
- [x] Implement Markdown and plain text export from `Document`.
- [x] Implement native PDF export from `Document` through a rhwp-style SVG-to-PDF path.
- [x] Decode page size from style streams with `/PageLayoutStyle` record `0x4444` taking precedence over `/DocumentViewStyles` `0x1001` page-size fields. `tmogi3_2` shows why: its reference PDF is A4 portrait and `/PageLayoutStyle` carries `21000x29700`, while `/DocumentViewStyles` carries a narrower view-style size. Use `/DocumentViewStyles` as fallback evidence only when the page-layout style record is absent.
- [x] Apply local `a5.jtd`/`a6.jtd`/`b6.jtd` sample hints for portrait vertical writing until the real orientation/writing-mode fields are decoded, and make `0x000c` `/DocumentText` controls force page breaks.
- [x] Wire `rjtd export <file> --format json`.
- [x] Wire `rjtd export <file> --format md`.
- [x] Wire `rjtd export <file> --format pdf -o <output.pdf>`.
- [x] Keep exporters consuming `Document` instead of raw file or stream bytes.
- [x] Add a minimal rhwp-shaped `DocumentCore` facade with `from_bytes`, `page_count`, `get_document_info`, `render_page_svg`, and `render_page_html`.
- [x] Extend the core facade with app-facing `get_page_info`, page/section/page-border settings, `get_page_layer_tree`, `get_page_overlay_images`, `get_canvaskit_replay_plan`, `set_file_name`, DPI, `get_source_format`, and `convert_to_editable` fallbacks.
- [x] Add a `rjtd-wasm` `HwpDocument` wrapper that mirrors the rhwp load/page-info/SVG/layer-tree/source-format method names for browser integration.
- [x] Add text-line range mapping to `DocumentCore` so fallback pages can answer rhwp-shaped cursor, hit-test, line-info, and vertical-navigation APIs.
- [x] Add WASM `renderPageToCanvas`, `renderPageToCanvasFiltered`, and `renderPageToCanvasLegacy` fallback rendering through `web-sys`, following rhwp's browser-canvas dependency direction.
- [x] Add WASM `getCursorRect`, `hitTest`, `getLineInfo`, `moveVertical`, and explicit no-hit header/footer/footnote fallback APIs.
- [x] Add minimal body-text editing APIs to `DocumentCore` and `rjtd-wasm`: `insertText`, `deleteText`, `splitParagraph`, `mergeParagraph`, `getTextRange`, `getParagraphLength`, and `getParagraphCount`.
- [x] Ensure fallback pages are rebuilt after body-text edits so PDF/SVG/canvas/cursor APIs observe the edited model.
- [x] Add a model-owned fallback `WritingMode` path so `DocumentCore`, SVG page rendering, and `getPageLayerTree` can project `horizontal` or `vertical-rl` without exporters reading raw style streams.
- [x] Add rhwp-shaped no-op page/section/page-border setters until JTD page setting streams are decoded.
- [x] Add fallback character/paragraph/style APIs for rhwp Studio formatting panels: default char properties, default paragraph properties, a single `Normal` style, empty numbering/bullet lists, and no-op format/style application.
- [x] Add body selection and plain-text internal clipboard APIs: `getSelectionRects`, `deleteRange`, `copySelection`, `pasteInternal`, `hasInternalClipboard`, `getClipboardText`, and `clipboardHasControl`.
- [x] Add rhwp-shaped undo/search editing APIs for the fallback body model: `saveSnapshot`, `restoreSnapshot`, `discardSnapshot`, `searchText`, `searchAllText`, `replaceText`, `replaceOne`, and `replaceAll`.
- [x] Add low-risk rhwp Studio view/navigation fallback APIs: `setShowParagraphMarks`, `getShowControlCodes`, `setShowControlCodes`, `getShowTransparentBorders`, `setShowTransparentBorders`, `setClipEnabled`, `getPositionOfPage`, `getPageOfPosition`, `findNextEditableControl`, `findNearestControlBackward`, `findNearestControlForward`, `getControlTextPositions`, and `navigateNextEditable`.
- [x] Add no-hit/no-op rhwp Studio field, header/footer, footnote, and endnote fallback APIs so app panels and edit modes fail gracefully until JTD structures are decoded.
- [x] Add no-hit/no-op rhwp Studio table and cell fallback APIs for cell text, table geometry, row/column operations, cell styles, selection, and formula calls until JTD table structures are decoded.
- [x] Add no-hit/no-op rhwp Studio picture, shape, equation, bookmark, form-object, stable-id, control-copy, and image-data fallback APIs until JTD object/control structures are decoded.
- [x] Add explicit fallback behavior for rhwp Studio HWP/HWPX export, HTML paste/export, page/column/new-number commands, header/footer formatting, footnote editing, style mutation, and numbering creation APIs.
- [x] Close direct rhwp Studio WASM API source-surface gaps to the wasm-bindgen-generated `free` method only.
- [x] Preserve observed ruby base plus phonetic annotation pairs as structured `Inline::Ruby` model data while keeping plain text/Markdown/PDF output on the visible base text.
- [x] Preserve observed style/layout streams (`/DocumentEditStyles`, `/DocumentViewStyles`, `/TextLayoutStyle`, `/PageLayoutStyle`, `/PageLayoutStyleHeader`) as named `UnknownStyle` model data before decoding their record semantics.
- [x] Expose preserved JTD style stream sources through app-core document/style JSON (`getDocumentInfo`, `getStyleList`, `getStyleDetail`) without pretending they are decoded paragraph styles.
- [x] Summarize observed style stream families and big-endian header fields (`ssmg` vs table-like prefixes) in JSON so future style record decoding starts from structured evidence instead of raw blobs only.
- [x] Expose observed style stream record boundary candidates in JSON: `ssmg-slots` for `0x5555`/`0x4444` Ssmg slot records and `sequential` for DocumentViewStyles-style `u16 code + u16 payload_len + payload` records.
- [x] Extract conservative UTF-16BE `label` candidates from Ssmg style records, including observed labels such as `脚注(標準)`, `本文(ｵｰﾄｽﾀｲﾙ)`, and page layout labels like `中扉(自動)`.
- [x] Promote labeled `/TextLayoutStyle` records into rhwp-shaped app-core style candidates so `getStyleList` and `getStyleDetail` can expose observed JTD style names while keeping `decoded:false`.
- [x] Make app-core `applyStyle` persist JTD style candidate references in the in-memory `Paragraph` model, and make `getStyleAt` plus split paragraphs observe that fallback style state.
- [x] Verify the WASM `HwpDocument` wrapper exposes and applies JTD style candidates through the rhwp-shaped browser API surface.
- [x] Add `styleCandidateCount` and `styleCandidateNames` to app-core `getDocumentInfo` so apps can detect observed JTD style candidates before opening the style panel.
- [x] Add `rjtd style-records <file>` and `rjtd style-candidates <file>` reverse-engineering diagnostics for preserved style stream summaries, record offsets/codes, payload lengths, and labeled `/TextLayoutStyle` candidates.
- [x] Add `rjtd text-layout-style-records <file>` to inspect all `/TextLayoutStyle` records, including unlabeled records, payload digests, short payload previews, and labeled candidate IDs.
- [x] Add `rjtd document-view-style-groups <file>` to inspect `/DocumentViewStyles` group records with payload lengths, payload digests, and short payload previews before treating group IDs as decoded style references.
- [x] Add `rjtd text-position-style-context <file>` to correlate `TCntV.01` tail fields with observed text/page style candidate IDs and record indexes without treating those hits as decoded style references.
- [x] Add `rjtd text-position-style-summary <file>` to summarize field-level style hit distributions and compare variable fields such as `f1` against text style candidates, page style candidates, and `/DocumentViewStyles` group records while separating near-constant/default-like fields such as `f7`.
- [x] Add `rjtd text-position-count-tail-field-roles <file>` to compare `TCntV.01` tail fields and adjacent field pairs against document-text unit/text hits, showing that `f1/f2` often behaves more like a range/coordinate pair than a pure style reference.
- [x] Preserve valid `TCntV.01` text-count entries as decoded-false `textCountRanges` model/export/app-core metadata instead of dropping the observed range/coordinate evidence after parsing.
- [x] Preserve `/DocumentText` byte/UTF-16 source spans on parsed `TextRun` model data and attach byte/unit `documentTextOverlaps` evidence to decoded-false `textCountRanges`.
- [x] Expose fallback `textRun` ops plus rhwp-shaped `textSources`/`source` spans from app-core `getPageLayerTree`, including JTD byte/unit source ranges where known.
- [x] Add a fallback `pageBackground` paint op to `getPageLayerTree` so layer/replay ordering matches rhwp's background-to-flow model.
- [x] Add rhwp-shaped layer tree envelope metadata: `schema`, `resourceTable`, `outputOptions`, `fontResources`, feature lists, and fallback `textV2` diagnostics.
- [x] Make app-core `getValidationWarnings` report rhwp-shaped JTD fallback/preservation diagnostics instead of always returning an empty report.
- [x] Preserve `/DocumentText` control boundary codes as decoded-false `textControlBoundaries` model/export/app-core metadata with byte/unit source spans where known.
- [x] Project preserved `textControlBoundaries` onto fallback paragraph character offsets for rhwp-shaped `getControlTextPositions` and nearest-control navigation diagnostics.
- [x] Expose projected `textControlBoundaries` through app-core `getPageControlLayout` as `type:"jtdControl"` diagnostics with fallback bounding boxes and `decoded:false`.
- [x] Add `rjtd text-control-ranges <file> [control-code]` to summarize `/DocumentText` intervals split by all controls or a selected control delimiter.
- [x] Make app-core `getCanvasKitReplayPlan` follow rhwp mode policy (`default`/`compat`) and report fallback `pageBackground`/`textRun` ops as direct replay items instead of returning an always-empty plan.
- [x] Verify `rjtd-wasm` against `wasm32-unknown-unknown`.
- [x] Generate PDFs for all 61 current local `.jtd`, `.jtt`, and `.jttc` samples under `openjtd-samples/pdf-output/`.
- [x] Verify all generated PDFs with PDF header/trailer checks, `pypdf`, `pdfinfo`, and representative `pdftoppm` PNG rendering.
- [x] Regenerate all 61 sample PDFs after diagnostic table/image overlay work; final sweep renders 665 pages with Poppler, finds 0 structural failures, 0 first-page render warnings, and 5 PDFs with 25 embedded image XObjects.
- [x] Regenerate all 61 local `.jtd`, `.jtt`, and `.jttc` sample PDFs under `openjtd-samples/pdf-output/` after the Preview/Quartz white-page PDF wrapper change. Current output has 61 PDFs, estimated 1025 pages by `/Type /Page` markers, 0 header/EOF/page-marker failures, and all 61 PDFs contain the explicit white page background op. Representative `sips` renders (`a5`, `a6`, `b6`, `46`, `tsaiten`, `success_data-test`, both `shanai_lan` variants, and `shinsyo`) have 0 failures.
- [x] Add `scripts/regenerate-pdf-output.sh` as the canonical full local-sample PDF artifact refresh command. It builds `rjtd-cli` once, walks all local `.jtd`, `.jtt`, and `.jttc` files, and writes same-stem PDFs to `openjtd-samples/pdf-output/`. The artifact sweep now also compares generated `/MediaBox` page sizes against trusted local reference PDFs, while keeping quarantined references such as `46.pdf` out of reference-backed gates.
- [x] Regenerate `a5.pdf` after page-size/page-break and facing-page decoration projection; it now uses an A5 portrait MediaBox (`419.55x595.275` pt), starts body text after the title-page `0x000c` break, and shows page 6 decorations on the left and page 7 decorations on the right.
- [x] Add a conditional `rjtd-export` local-sample PDF smoke test that parses every available local `.jtd`, `.jtt`, and `.jttc` sample, exports PDF bytes, and checks PDF header, page marker, EOF marker, and minimum size.
- [x] Add a macOS-only `rjtd-export` Quartz smoke test for the current high-risk local samples (`success_data-test`, `shanai_lan`): generated PDF bytes are written to a temp file, rasterized with `sips -s format png`, and required to produce a non-empty PNG.
- [x] Add a stricter macOS CoreGraphics PDF smoke test for the same high-risk samples: generated PDF bytes are loaded through `CGPDFDocument`, drawn with `CGContext.drawPDFPage`, and required to produce non-white pixels. This keeps Chrome/PDFium-only success from hiding Preview/Quartz blank-page regressions.
- [x] Tighten Preview/Quartz PDF regression coverage: `sips` output PNGs now require non-white pixels, PDFKit and CoreGraphics checks fail per checked page rather than by aggregate pixel count, text-only PDF fixtures reject unexpected `/SMask`, and a direct `svgs_to_pdf` contract test locks the white-page wrapper plus normalized `/S1` Form XObject embedding. The macOS PDFKit/CoreGraphics smoke tests now accept sample-specific `page:minNonWhite` checks, so A5 checks page 1 and the body/header-bearing page 6 instead of only the first sequential pages where page 2 is intentionally blank, and selected high-risk pages must exceed conservative minimum ink thresholds rather than merely painting one non-white pixel.
- [x] Extend the Preview/Quartz smoke suite beyond `.jtd`-only samples by allowing explicit sample filenames and adding the PDF-backed `fax02.jtt` case. The `sips`, PDFKit, and CoreGraphics checks now render `fax02.jtt` page 1 with a `1:10000` non-white threshold; the current generated PDF renders `30721` non-white pixels through both PDFKit and CoreGraphics. `raihoumemo01.jtt` remains out of this PDF-backed test list until a local reference PDF is available.
- [x] Route single-page PDF exports through the same `pdf-writer` wrapper as multi-page exports so Preview/PDFKit sees a stable PDF 1.4 document with `Producer: rjtd`; regenerated `shanai_lan` and `success_data-test` PDFs render through both Poppler and Apple `sips`. The wrapper now also paints an explicit white page rectangle before placing the SVG Form XObject, avoiding a transparent-page-only first operation for Preview/Quartz display paths.
- [x] Harden PDF export against Preview/PDFKit transparency regressions with a flexible PDF token scanner. `svgs_to_pdf` now rejects generated PDFs that contain page-level `/Group <<` plus `/S /Transparency` even when PDF whitespace/newlines differ from the exact byte strings previously checked; the scanner still reports `/SMask` for text-only/diagnostic checks, but local sample validation does not globally block soft masks because image-backed samples can contain them legitimately.
- [x] Expose layout-validated but decoded-false `textParagraphBoundaryCandidates` through JSON export and app-core `getDocumentInfo`, and report them through `getValidationWarnings` as diagnostic-only evidence rather than decoded paragraph records.
- [x] Preserve the `success_data-test` abc table as a source-backed 5-column logical topology from `/DocumentText` unit range `519..924`, including 15 candidate cells, 11 non-empty cells, and 4 empty cells (`a: 1,1,7,empty`, `b: 1,empty,24,0.8`, `c: empty,2,empty,1`).
- [x] Decode candidate-specific `/DocumentText` line-header placement evidence for that abc table. Each row exposes 7 monotonic `0x001c/0x0030` headers; the first 5 cell slots per row cover all 15 logical cells, so `getPageLayerTree` now reports `placementAuthority:"documentTextLineHeaders"`, `decodedSourcePlacementEvidence:true`, `decodedSourcePlacementMatchCount:15`, `decodedSourcePlacementRequiredCellCount:15`, and `documentTextLineHeaderEvidence` with 21 raw headers.
- [x] Re-enable the `success_data-test` abc table SVG/PDF `tableProjection` only behind that line-header evidence. The generated `openjtd-samples/pdf-output/ichitaro-20030228030923-success-002-success_data-test.pdf` now shows the 3x5 grid and renders through Poppler and Apple `sips`; the layer tree still marks `referenceBacked:true`, `geometryDecoded:false`, and `placementDerived:false` because page `x/y/width/height` remain reference-calibrated.
- [x] Keep explicit `layoutStreamProbe` evidence for that abc table: `/LineMark` (334 bytes / 167 words), `/PageMark` (2 rows), `/PaperMark` (76 bytes), and 56 `/Frame` records are present, but the table unit range does not directly hit line/page mark indexes and has no object-frame source-unit link.
- [x] Add a source-derived table placement candidate for that abc table using `/DocumentText` line headers plus `/LineMark` and `/PageMark`. The candidate now uses the all-row-agree `matched-cells-plus-first-trailing-header` unit range (`0..88`) for table width, maps row records `16..18` through `/PageMark` entry `0`, derives page y `408.000px` from `pageLineStart:0`, `lineOffsetFromPageStart:16`, and source row height `21.000px`, and exposes the matched PageMark row as `pageMarkU16Fields`/`pageMarkU16FieldsHex`. This is source-backed placement evidence, but still not final decoded geometry until horizontal page-unit scale and exact x-origin semantics are explained across samples.
- [x] Preserve `/PageMark` entry raw bytes plus big-endian `u16Fields` and `u32Fields` through the `Document` model, `getDocumentInfo`, and JSON export. This keeps the source evidence for page-row and table-y/x-origin analysis visible instead of reducing entries to only `flags`, `lineStart`, and `lineEnd`.
- [x] Add decoded-false PageMark u16 geometry hypotheses to that abc table's `lineMarkPageOriginCandidate`. The layer tree now calls out recurring candidate fields (`word10`, `word13`, `word14`, `word17`, `word18`, `word19`, `word20`, `word21`), records the observed `word20 == 0x00ff` gate, and preserves the `word13 + word14 == word21` arithmetic (`370 + 185 = 555` in `success_data-test`) plus layout scale comparisons. Rendering remains blocked as `page-mark-u16-geometry-semantics-unproven`.
- [x] Expose the same decoded-false PageMark u16 geometry hypotheses on every preserved `/PageMark` entry in `getDocumentInfo` and JSON export, with `layoutComparisons:null` unless a downstream probe supplies page/layout context. This keeps the hypothesis global and source-backed instead of tying it only to the current abc table diagnostic.
- [x] Surface the matched page row's `pageMarkU16Fields` and decoded-false `pageMarkU16GeometryHypotheses` through page-level `layoutMarkEvidence` in `getPageInfo`, `rjtd page-info`, and page decoration layer-tree entries. A5 page 6 now reports the source row tuple where `353 + 246 = 599` while keeping `word20Is0x00ff:false` and blocking geometry promotion.
- [x] Add page-level decoded-false `pagePitchEvidence` to `layoutMarkEvidence` for mark-bearing pages. `getPageInfo` and page decoration layer-tree entries now compare `/PageMark` line ranges against the current page/body size without changing rendering; A5 page 6 exposes `lineStart:23`, `lineEnd:40`, `lineGapCount:17`, `pageHeightPxPerLineGap:46.688`, `bodyHeightPxPerLineGap:38.218`, selected PageMark fields, and the explicit blocker `page-mark-line-pitch-semantics-unproven`.
- [x] Expand PageMark u16 hypotheses with additive-unit pattern diagnostics instead of treating `word20 == 0x00ff` as universal. Entries now report `word21MinusWord13`, `word21MinusWord13EqualsWord14`, `word19EqualsWord13`, `selectedFieldsAllZero`, and `nonZeroAdditiveUnitCandidate`; layout-aware callers also report page-height/body-width ratios per `word21` and `word13+word14`. Current PDF-backed samples show the additive relation is useful on normal rows but not sufficient to promote geometry because A5/46, `success_data-test`, `tsaiten`, and `tmogi3_2` disagree on page/body scale.
- [x] Add decoded-false `u16GeometryClass`/`profile` classification for PageMark rows and surface it in model JSON, export JSON, and `rjtd page-marks`. The current classes are `zero-sentinel`, `additive-row`, `additive-boundary`, and `mixed-payload`; they are evidence buckets for future promotion rules, not decoded Ichitaro semantics.
- [x] Add `rjtd page-mark-u16-profile <file>` to summarize PageMark u16 class counts and selected-field tuples (`word10`, `word13`, `word14`, `word17`, `word18`, `word19`, `word20`, `word21`). This makes cross-sample sweeps reproducible without ad hoc scripts and keeps future geometry promotion rules tied to observable profile buckets. Current checks show `success_data-test` has 2 `additive-boundary` rows, while A5 has 68 `additive-row`, 4 `additive-boundary`, 2 `zero-sentinel`, and 1 `mixed-payload` row.
- [x] Add `rjtd page-mark-pitch-profile <file>` to combine PageMark line ranges with the current file-layout hint and report page/body pitch diagnostics per entry. A5 entry 5 now reports `lineStart=23`, `lineEnd=40`, `lineGapCount=17`, `pageHeightPxPerLineGap=46.688`, and `bodyHeightPxPerLineGap=38.218`; `success_data-test` entry 0 reports `lineGapCount=39` and `bodyHeightPxPerLineGap=21.214`. This stays decoded-false and blocks render promotion until PageMark line-pitch semantics are proven.
- [x] Stop promoting square `objectType 0x0001` `/Frame` records as `titleRoundedFrame` page-frame shapes. Real observed title frames are horizontal panels, so `page_frame_title_shape` now requires a title-like aspect ratio; this removes the stray upper-right square from `success_data-test` while preserving the source `/Frame` row as decoded-false model/export evidence.
- [x] Stop replaying `success_data-test` title-art texture paths tagged with EmbeddedPress paint-state word5 `0x2f` over the main title face. The layer tree still reports those shadow-state texture paths as source evidence, but SVG/PDF front-texture rendering now requires main-state word5 `0x10`; this removes an unbacked overpaint source behind title glyph holes and malformed overlaps.
- [x] Remove the hardcoded 24% title-art hole-area gate from long-shadow sidewall generation. The extrusion sweep now follows all non-degenerate source `evenodd` boundary contours, so small counters such as glyph holes are preserved as sidewall boundaries instead of being dropped by an area heuristic.
- [x] Add `shadowTexturePathCount`/`shadowTextureWord5Values` diagnostics for `success_data-test` title art so the 11 shadow-state texture paths are visible without replacing the full 530 preserved texture paths that currently carry the title halftone effect.
- [x] Add EmbeddedPress current-paint-state inheritance diagnostics for `success_data-test` title textures. The 11 explicit shadow-state `0x2f` texture records now explain all 530 texture paths as effective shadow textures, while effective front texture count remains 0; SVG metadata records this state source instead of treating the full texture replay as arbitrary.
- [x] Route `success_data-test` title texture rendering through the source ordering evidence: the 530 texture paths are accepted as the interstitial block between the 11 shadow outlines and 11 main outlines, with a preserved-all-texture fallback only if that ordering proof is absent.
- [x] Expose compact `paintStateSummaries` for `success_data-test` title art groups. The layer tree now reports shadow outlines, the interstitial texture block, and main outlines with path index ranges, explicit vs inherited state counts, and unique payload signatures for EmbeddedPress records `0x40/0x46/0x48/0x60/0x65/0x70/0x82`.
- [x] Historically block `success_data-test` title front-face erase promotion when the only available texture block is the interstitial shadow-state block. This earlier decision kept the 530 `0x2f` texture paths off the main title face and preserved the source outline fill (`#111111`) instead of the precomposited gray `#818181`; it is superseded by the later source-order interstitial front-erase promotion after PDF-safe precomposition and fresh reference comparison.
- [x] Expose the remaining source-backed white front-paint candidate without rendering it. The `success_data-test` title layer tree now reports `frontPaintCandidate.paintColor:"#ffffff"` from `JSFart2Contents.paintColorCandidate`, but also records `mainStateTexturePathCount:0`, `frontEraseTexturePathCount:0`, `renderPathCount:0`, and `renderPromotionBlockedReason:"no-main-state-or-front-owned-texture-paths"`. This keeps the known white paint signal visible while preventing the blocked interstitial shadow texture block from being reused as front-face halftone/erase.
- [x] Apply the source-backed EmbeddedPress `0x70.word0` shadow-effect candidate to `success_data-test` title art. Shadow outlines carry homogeneous `0x70.word0 = 0x2c`, interpreted conservatively as 44% black over white (`#8f8f8f`) and exposed in both SVG metadata and layer-tree diagnostics. The regenerated PDF still renders through Poppler and Apple `sips`; versus the previous front-erase output, whole-page RMS improved slightly from `48.3051` to `48.2703`, and title-crop RMS improved from `69.883` to `69.671` on the current crop. A competing `0x70`-gate experiment that removed the shadow/extrusion texture pass was rejected because it worsened title RMS from `70.50` to `78.03` and whole-page RMS from `45.35` to `46.56` in the SVG A/B.
- [x] Remove the `success_data-test` title-art page routing hardcode for `frameRef 1/16`. The page association now comes from source-order matching of `JSFart.Art.2` embedding frames whose EmbeddedPress snapshot, frame size, and JSFart2 art candidate all agree; layer tree `titleArtProjection.pageAssociation` records `frameRefs:[1,16]`, source-order index, and page number while still marking the association `decoded:false`.
- [x] Add `statePayloadWordColumns` diagnostics to each `success_data-test` title-art paint-state summary so `0x48`, `0x70`, and `0x82` word-position differences are visible without re-parsing raw payload arrays. The new `tmp/pdfs/goal-continue/success-layer-tree-word-columns.json` artifact shows `0x48.word0=0x01` only on shadow outlines, `0x48.word0=0x00` on interstitial/main groups, `0x70.word0=0x2c` and `0x70.word3=0x0a` on shadow outlines, and `0x70.word0=0x1c`/`word3=0x00` on interstitial/main groups. Additional SVG A/B checks rejected source-derived alternatives: `0x82` front erase opacity candidates `0.160`, `0.280`, `0.440`, `0.530`, `1.000`, no front erase, explicit-11 texture routing, no shadow texture, and shadow-face-only `0x70` all worsened title/glyph RMS versus the current baseline, so no render change was promoted this pass.
- [x] Preserve the interstitial title texture block's `0x70.word0=0x1c` as a decoded-false render candidate without consuming it. The layer tree now reports `extrusionTextureEffectCandidateFillColor:"#676767"` and `extrusionTextureEffectRenderPromotionBlockedReason:"interstitial-0x70-texture-effect-semantics-unproven"`, while SVG exposes matching `data-title-texture-effect-candidate-*` attributes. A CoreGraphics A/B rejected rendering this candidate because title RMS worsened from `58.492` to `62.216`, so the visible texture fill remains the previous baseline until the full `0x70`/texture semantics are proven.
- [x] Add a render-preserving `titlePaintRoleSeparationMatrix` diagnostic for `success_data-test` title art and lock it with model regression assertions. The matrix compares every observed EmbeddedPress state record word across shadow outlines, the interstitial texture block, and main outlines: 28 candidate record-word columns, 18 shared by all roles, 4 shadow-only separators, 0 texture-only separators, and 1 main-only separator. The only texture/main disjoint column is `0x82.word5`, where shadow and texture both carry `0x2f` while main carries `0x10`, so it proves main-vs-shadow-state texture separation but not an interstitial-texture-only paint phase. Rendering stays unchanged and blocked with `no-record-word-separates-interstitial-texture-from-both-outline-roles`; regenerated `success_data-test.pdf` remains PDFKit-safe (`/Group`, `/SMask`, `/ExtGState`, `/Pattern`, `/Shading` all 0; `/Subtype /Form` and `/FormType 1` both 2; final `%%EOF` count 1) and nonblank under Poppler (`103585` page-1 non-white pixels) and Apple `sips` (`35353` page-1 non-white pixels).
- [x] Add a render-preserving `horizontalPlacementGate` for `success_data-test` title art so JSFart frame horizontal candidates are visible without silently moving the output. Current rendering remains `frameRecordContentOffsetAnchor` (`frameX/pathX=76.687`) while the rejected source-backed candidate is preserved as `candidateFrameX=72.907`, `candidatePathX=80.995`, `candidateBasis:"jsfartFrameOuterEdgePlusFrameRecordContentOrigin"`, with `strokeWidthCandidateSourceUnits:100` and `strokeOuterAdjustmentCssPx:3.780`. A/B rendering the split candidate aligned the left frame edge better but worsened the reference comparison (`whole RMS 45.069 -> 46.288`, `title_stack 56.475 -> 65.388`, `title_face 79.072 -> 91.836`, `title_lower_shadow 84.976 -> 102.382`), so the candidate is diagnostic-only with `frame-content-split-horizontal-semantics-unproven`.
- [x] Add a render-preserving `frontEraseTextureDirectGrayCandidate` for `success_data-test` title art. The layer tree and SVG now expose interpreting `0x82.word5=0x2f` as a direct gray channel (`#787878`) separately from the active precomposited opacity fill (`#818181`). A fine gray sweep showed this is only a tiny local improvement at best (`gray 0.485` title_stack RMS `56.421` vs current `0.506` RMS `56.475`; `gray 0.470` whole RMS `45.062` vs current `45.069`) and does not solve the over-mid-gray texture distribution, so it remains diagnostic-only with `direct-gray-channel-probe-not-proven-as-paint-semantics`.
- [x] Add render-preserving `frontEraseTextureSourcePaintCandidate` and `frontEraseTextureSpanCoverageProbe` diagnostics for `success_data-test` title art. The layer tree and SVG now expose the source-backed solid white paint candidate (`#ffffff`) beside the active PDFKit-safe precomposited fill (`#818181`), plus the 11 explicit texture state spans that inherit over all 530 interstitial texture paths (`[16,36,91,55,16,55,73,22,64,52,50]`). A solid-white front texture probe was rejected by visual verdict because it removed the mid-gray but over-erased the black title face (`title_crop RMS 58.808 -> 72.734`, `face_core RMS 81.282 -> 109.804`), so rendering stays unchanged and the new span diagnostics block promotion with `span-density-and-clip-semantics-unproven`.
- [x] Expose source-backed `DocumentText` line-header unit geometry for the `success_data-test` abc table instead of leaving the current reference bbox as opaque. `getPageLayerTree` now emits `unitGeometryCandidate` under `documentTextLineHeaderEvidence`, showing 3 homogeneous rows, 5 matched columns, 7 raw headers per row, `minOffsetUnits:0`, `maxExtentUnits:174`, `homogeneousFontSizeUnits:12`, matched cell offsets `[0,14,28,44,60]`, matched extents `[10,24,40,56,72]`, and trailing wide header candidates `[76..88, 92..174]`. Rendering remains reference-backed because page-space origin and unit scale are still unproven, but the column count now comes from the table candidate instead of a success-data-test constant.
- [x] Attach `/DocumentText` source spans to all 21 `success_data-test` top-text projection slots instead of leaving the reference-backed text runs source-less. `getPageLayerTree` now emits `sourceBacked:true`, `sourceStream:"/DocumentText"`, JTD byte/unit ranges in both `source` and `textSources`, and conservative `lineHeaderEvidence` when a nearby `0x001c/0x0030` header is actually parsed. The table `geometryDerivationEvidence` also reports `topTextAnchorEvidence` with `referenceBackedCoordinateCount:21`, `sourceBackedRunCount:21`, `lineHeaderBackedRunCount:2`, and `directTableTransformDerived:false`, explicitly blocking render promotion because those page coordinates are still reference-backed. The rendered PDF is unchanged, but current Poppler output is pixel-identical to the previous generated baseline and the diagnostics now expose source ranges such as `264..275` for `１，次の計算をしなさい`, `426..467` for the table prompt, and `1506..1509` for the combined figure label `（２）`.
- [x] Attach `/LineMark` row evidence to `success_data-test` top-text projection slots and Q4 figure-label spans. Although only two top-text runs have strict `0x001c/0x0030` `lineHeaderEvidence`, every source-backed visible top-text slot now exposes `lineMarkEvidence` with the overlapping `/LineMark` record, source unit range, flag word, containment flag, and blocker `line-mark-row-to-page-y-transform-unproven`. It also exposes a decoded-false `pageGridCandidate` from `/LineMark+/PageMark+documentTextLineHeaders`, including source-backed row top candidates such as record `5 -> rowTopY:177.000`, record `15 -> 387.000`, and record `27 -> 639.000`, plus reference residuals (`topMinusRowTopPx` around `7.5`, `7.3`, and `5.2`). This keeps rendering unchanged while preparing top-text y placement to move away from static reference coordinates.
- [x] Promote the same `/LineMark+/PageMark+documentTextLineHeaders` row-grid evidence into a bounded source-backed y-baseline candidate for `success_data-test` top-text and Q4 figure-label rendering. `getPageLayerTree` now emits `sourceGridPlacementCandidate` per source-backed text run, with `rowTopY`, source row height, `topY`, and `baselineY` (`lineMarkRowTopPlusSourceRowHeight`), and SVG/PDF text records carry `data-y-basis="lineMarkPageGrid"` plus source-grid baseline metadata. Horizontal origins and exact font metrics are still not proven, so these runs remain `placementProven:false`; the regenerated PDF renders through Poppler, Apple `sips`, and PDFKit.
- [x] Add baseline residual and raw line-header diagnostics for the promoted `success_data-test` source-grid text placement. Each `sourceGridPlacementCandidate` now carries `referenceResidualEvidence` comparing source-derived `topY`/`baselineY` to the prior reference-backed coordinates; early rows show near-zero residuals (`0.0..0.1px`), while Q4 figure labels expose the remaining `-1.8px` baseline/top residual explicitly. `lineHeaderEvidence` now preserves the matched `/DocumentText` raw 12-word header (`rawWords`/`rawWordsHex`) so future baseline/top-offset work can compare source fields without re-parsing the stream.
- [x] Add a page-layer `successDataTestTextPlacementResidualSummary` op so baseline residual patterns can be inspected without scraping each text run. The summary reports 21 source-grid candidates, max top/baseline residual `2.300px`, 2 raw line-header profiles, rounded-tenth residual buckets keyed by LineMark flag/font/header presence, and per-entry source ranges/raw headers. The current strongest unresolved bucket is the Q4 figure-label group (`recordIndex 27`, `flagWord 0x8002`, `fontSize 14.0`, residual `-1.8px`, count 3), which remains diagnostic-only with `baseline-residual-source-field-semantics-unproven`.
- [x] Add line-pitch fit evidence to the same `successDataTestTextPlacementResidualSummary` instead of applying an unproven constant baseline correction. The layer tree now reports least-squares reference-baseline fits against `/LineMark` record indexes (`all-visible` pitch `20.926px`, early records pitch `20.976px`, late records pitch `21.006px`), the source row height candidate (`21.000px` from abc-table line-header font units), and the matched `/PageMark` selected fields (`word21=555`, `370 + 185 = 555` additive-boundary row). Rendering remains diagnostic-only because `/PageMark` line-pitch/source-field semantics are still unproven.
- [x] Mirror compact `sourcePitchEvidence` onto each `success_data-test` `sourceGridPlacementCandidate` and `/LineMark` `pageGridCandidate` instead of leaving the row-height source hidden in the summary only. Each candidate now exposes page/body size, `fontSizeUnits:12`, `fontUnitScalePx:1.750`, body-height-per-line-gap `21.214px`, source-minus-body-gap residual `-0.214px`, matched `/PageMark` selected fields, `pageHeightPxPerWord21Unit:1.750`, `pageHeightPxPerWord13Plus14Unit:1.750`, and scale-agreement candidates while still blocking render promotion as `page-mark-line-pitch-semantics-unproven`.
- [x] Sweep `page-mark-u16-profile` across the PDF-backed local samples before promoting PageMark pitch semantics. 52 files expose parsed PageMark profiles: 516 entries total, 20 `zero-sentinel`, 249 `additive-row`, 115 `additive-boundary`, and 132 `mixed-payload`; 46 files contain at least one `mixed-payload` row. This diversity confirms that the `success_data-test` PageMark/font scale agreement is useful evidence but not yet a safe global y-transform rule.
- [x] Regenerate representative PDFs after the pitch-profile diagnostic addition (`openjtd-samples/pdf-output/a5.pdf` and `openjtd-samples/pdf-output/ichitaro-20030228030923-success-002-success_data-test.pdf`) and verify they render nonblank through both Poppler and Apple `sips`. Neither file currently contains `/Group`, `/Transparency`, or `/SMask`, but Preview.app-only visibility differences still need manual app-level confirmation because `qlmanage` is sandbox-blocked in this environment. A follow-up direct PDFKit thumbnail render of `success_data-test` wrote visible page-1/page-2 PNGs, so if Preview.app alone shows blank after Chrome/PDFium renders visible content, treat Preview caching/open-file refresh as the first app-level suspect before changing PDF structure.
- [x] Replace first-overlap `/LineMark` selection with a best-match selector for source-backed text/table row grid evidence. The selector prefers exact unit range matches, then intervals that contain the source span, then larger overlaps and shorter intervals, so ambiguous `/LineMark` joins do not silently depend on stream order. `success_data-test` top text keeps the same visible rows (`recordIndex` 5/8/27 in the checked probes), while table row matching and exact-contiguous checks now use the same selection rule.
- [x] Render the `success_data-test` Q4 figure labels `(1)/(2)/(3)` from the single preserved `/DocumentText` line `1479..1532` instead of three independent reference x coordinates. The renderer now splits label spans from that source line and applies a Japanese fixed-pitch advance model (`advanceModel:"japanese-fixed-pitch-halfwidth-space"`) so ASCII spaces do not collapse in SVG-to-PDF conversion. The regenerated PDF moves the label text bboxes to near-reference positions: `(1)` x0 `44.03` vs `44.16`, `(2)` x0 `185.78` vs `186.76`, and `(3)` x0 `317.03` vs `318.61`; `getPageLayerTree` and SVG both expose `renderSource:"document-text-fixed-pitch-span"` plus the original line evidence.
- [x] Add residual-only physical-geometry diagnostics for the same abc table. `referenceBBoxResidualEvidence` now compares the source line-header spans `[10,10,12,12,12]` and gaps `[4,4,4,4]` against the current equal-width reference columns (`55.198px` each), exposing per-cell ratios `[5.520,5.520,4.600,4.600,4.600]` and `equalReferenceColumnsConflictWithUnitSpans:true`. `topTextAnchorResidualEvidence` records the two shared full-extent/font-size top-text anchors and their reference-assisted offsets, including `tableLeftMinusAnchorLeftPx:13.397` and `tableTopMinusAnchorBaselinePx:2.916` for `空欄を埋めて表を完成させなさい。`. `independentPageTransformEvidence.present` remains `false`, blocked because both top-text page coordinates and table bbox are reference-backed.
- [x] Render the `success_data-test` abc table column widths from the `/DocumentText` line-header span units instead of leaving the visible table as equal-width columns. The first matched line-header row contributes spans `[10,10,12,12,12]`, normalized within the current reference-backed bbox to `[49.284,49.284,59.141,59.141,59.141]` px. `getPageLayerTree` records `columnWidthBasis:"documentTextLineHeaderSpanUnits"` and SVG records `data-column-width-basis="documentTextLineHeaderSpanUnits"`. This is a bounded source-backed improvement to the table internals; the table page-space origin/height/overall width remain reference-backed and still need a decoded transform.
- [x] Promote the `success_data-test` abc table's source-derived `/DocumentText` line-header + `/LineMark` + `/PageMark` layout into the primary SVG/PDF table projection when a `lineMarkPageGrid` origin is present. The rendered `tableGridCandidate` is now `referenceBacked:false` with bbox `x=78.251,y=411.421,width=275.063,height=63.000` and source span column widths `[49.118,49.118,58.942,58.942,58.942]`; fallback-text-anchor-only layouts such as `tsaiten` remain diagnostic-only. The x origin uses the repeated first inter-cell gap as a decoded-false source-backed candidate (`xOriginInsetBasis:"uniform-intercell-gap-half"`, `xOriginInsetUnits:2.000`), reducing the reference residual from `xResidualPx:7.297` to `1.046`, while the y origin now uses `/PageMark` body line-gap pitch (`linePitchPx:21.214`) and reduces the vertical residual from `2.716px` to `-0.705px`. Keep both semantics explicitly unproven until another sample confirms the same transform.
- [x] Add diagnostic-first LineMark record-stride evidence for `tsaiten` table/page-grid candidates instead of treating non-contiguous records as an uninformative failure. `getPageLayerTree` now exposes `lineMarkRecordIndexes`, `uniformLineMarkRecordStride`, `lineMarkRecordStride`, and `interleavedLineMarkRecordCountBetweenRows` across line-header coupling, `/LineMark` row evidence, and `/PageMark+/LineMark` page association evidence. The PDF-backed `tsaiten` scoring table records `[8,10,12,14]` and the lower error table records `[33,35,37]` using the same best-overlap LineMark selector as cell provenance, both with stride `2`; rendering remains reference-backed until mixed-payload PageMark and stride-to-y semantics are decoded.
- [x] Add source-derived `lineMarkPageOriginStrideCandidate` diagnostics for `tsaiten` table layouts so stride-2 row placement evidence is not hidden behind fallback text anchors. The lower error table now preserves stride records `[33,35,37]`, raw record-index row-top hypotheses `[765.000,807.000,849.000]`, stride-collapsed row-top hypotheses `[418.500,439.500,460.500]`, and the matched mixed-payload `/PageMark` `u16` geometry hypotheses. Per-cell layer-tree/SVG provenance also exposes `/DocumentText` line-header candidates, `/LineMark` record indexes, and `/PageMark` entry indexes for cells such as `採点項目` and `１文字につき１点`; rendering is still blocked by `line-mark-record-stride-to-page-y-transform-unproven`.
- [x] Add reference-residual diagnostics for the `tsaiten` lower error table's stride-y hypotheses without making them render authority. `lineMarkStrideYComparison` now compares source-backed `[33,35,37]` row-top hypotheses with the current reference table bbox: raw record-index row tops have residuals `[-3.014, 1.686, 6.385]` px (`maxAbs=6.385`), while stride-collapsed row tops are hundreds of pixels off, so `bestYHypothesisCandidate:"raw-record-index"` is preserved as evidence. Rendering remains blocked by `stride-y-hypothesis-needs-cross-table-validation` because the scoring table still lacks equivalent line-header geometry.
- [x] Add compact-to-sparse sibling topology evidence for `tsaiten` direct `/DocumentText` table candidates. `getPageLayerTree` now emits `sparseTableSiblingEvidence` when a sparse control-run table candidate contains the compact candidate's source range and exact row/cell source spans. The scoring table maps compact source intervals `[1,3,5,7]` to sparse candidate `4`, matches all `12/12` cell spans, and proves compact columns `0..2` correspond to sparse columns `[3,4,5]` with offset `3`; the lower error table similarly maps `[20,22,24]` and `[3,4]`. This is source-backed topology only and remains blocked as `sparse-topology-does-not-decode-page-space-geometry`.
- [x] Add sparse-sibling LineMark y residual diagnostics for `tsaiten` compact tables without promoting rendering. `sparseSiblingLineMarkYComparison` now uses the compact-to-sparse sibling proof, best-overlap `/LineMark` rows, matched `/PageMark` line range, and homogeneous line-header font-size units to compare source-backed row-top hypotheses against the current reference bbox. The scoring table has only partial line-header font evidence (`2/4` rows) and its raw-record-index hypothesis is still far from the reference (`[-61.005,-51.206,-41.406,-31.607]` px, `maxAbs=61.005`), while the lower error table keeps the near-reference raw-record evidence (`maxAbs=6.385`). This proves the sparse topology alone is not enough to replace `tsaiten_table_grid_overlay_layout`.
- [x] Add `postRowGapLineMarkCorrelation` under the same `tsaiten` sparse-sibling y diagnostics. For both the scoring table and lower error table, each compact row's `/LineMark` span length exactly equals the source-unit gap from that row end to the following sparse blank/gap region: scoring rows match `[65,65,65,67]` units against sparse intervals `[2,4,6,8]`, and the lower table matches `[58,52,54]`, with the last row consuming trailing empty sparse intervals `[25,26]`. This is strong evidence that the observed `/LineMark` span is a post-row gap/spacing measure rather than a direct visible row-height or page-y coordinate.
- [x] Add `/PageMark` raw record and numeric value probes for `tsaiten` table geometry. `pageMarkRawRecordScanEvidence` now scans plausible raw PageMark record headers independently of the current `count-plus-one-variable` parse, exposing 15 candidate headers in the 1108-byte stream; both scoring `[8,10,12,14]` and lower `[33,35,37]` LineMark rows still fall under scanned record header `0` (`lineStart=0,lineEnd=42`), so the scan proves page ownership but not y placement. `pageMarkRawReferenceValueProbe` also confirms that the scoring row tops (`301/333/365/398`) have no nearby raw PageMark numeric hits, while the lower table only has two `u16be 0x0300` hits near `768px`; this prevents promoting the lower raw-record-index fit as a general transform.
- [x] Add `lineMarkStridePromotionReadiness` under the same `tsaiten` sparse-sibling y diagnostics. The readiness gate aggregates compact/sparse topology, `/LineMark` stride, line-header coverage, post-row-gap correlation, raw `/PageMark` scan results, and reference residuals, but always reports `promotionReady:false` until a non-reference PageMark/LineMark page-y transform is decoded. The scoring table is blocked by partial line-header evidence, missing cell geometry, post-row-gap-only LineMark spans, and a failed raw-record-index fit (`maxAbs=61.005`), while the lower table is blocked because its near-reference fit (`maxAbs=6.385`) is still reference-backed and not source-decoded.
- [x] Correct the `tsaiten` source-derived table geometry record set to prefer previous compact-row-span `/LineMark` records when sparse-sibling evidence proves the selected records are post-row gaps. The geometry resolver now uses `[7,9,11,13]` for the scoring table and `[32,34,36]` for the lower error table, and `sourceDerivedLayoutCandidate` exposes `lineMarkRowRecordSelection:"previous-compact-row-span-record"` when that role correction applies. This intentionally downgrades the lower table's formerly near-reference raw-record fit (`maxAbs=6.385`) to the source-correct previous-row-span fit (`maxAbs=24.014`) so post-gap records cannot accidentally become render authority. Cell provenance still reports the overlapping selected records (`[8,10,12,14]` / `[33,35,37]`) separately.
- [x] Enrich `tsaiten` `/PageMark` numeric hit probes with raw record/tail/subrecord context. The two lower-table `0x0300` hits are now tied to scanned records `2` and `3`, with tail-block word indexes `11` and `3` respectively. They also resolve to field index `2` of repeated 16-byte u16 subrecord candidates at byte offsets `174` and `334`, with the full eight-word windows preserved in layer-tree JSON. This makes the lower-table hit useful for reverse engineering while explicitly showing that it is not a stable fixed-offset y field.
- [x] Generalize the same raw u16 subrecord candidate scan onto `pageMarks[].entries[]` in model document-info and export JSON. Each entry now carries a decoded-false `u16SubrecordScan` with source/geometry flags, candidate count, entry-relative offsets, stream offsets, raw eight-word windows, and recomposed u32 fields. The `tsaiten` export/model tests lock the two `0x0300` candidates at stream offsets `174` and `334` so future promotion work can compare PageMark variable payloads without relying on table-specific reference probes.
- [x] Attach `/LineMark` row-boundary evidence to the `success_data-test` abc table layout probe. The table unit range `519..924` now matches `/LineMark` records `16..18` exactly (`519..655`, `655..791`, `791..924`) with contiguous record indexes, `exactRowMatchCount:3`, and `lineMarkProfile:"be16-delta-v1"`. This proves source-backed row order/boundaries for the visible 3-row table, while keeping render promotion blocked because LineMark units still need a decoded page-y coordinate transform.
- [x] Attach `/PageMark` page-association evidence for the same abc table by mapping those `/LineMark` records through the page line ranges. `getPageLayerTree` now emits `pageMarkLineMarkRecordEvidence` showing row records `16..18` all fall inside `/PageMark` entry `0` (`pageIndexCandidate:0`, `pageLineStart:0`, `pageLineEnd:39`), so the table's page ownership is source-backed even though rendering remains blocked because `/PageMark` line indexes are not y coordinates.
- [x] Attach structured `/PaperMark` evidence to that abc table layout probe instead of only reporting the raw stream length. `getPageLayerTree` now emits `paperMarkPageAssociationEvidence` showing the same row records `16..18` map from `/PageMark` entry `0` to `/PaperMark` row/index `0` with flags `0x00010010`; it also reports `paperMarkEntryCount:8`, header count `8`, stride `12`, last index `7`, and `pagePaperMarkEntryCountAligned:false`. This keeps render promotion blocked until `/PaperMark` flag semantics are decoded.
- [x] Add source-unit bbox candidates for the same abc table instead of deriving the horizontal span from the reference PDF. `unitGeometryCandidate.tableUnitBBoxCandidates` now reports all-row-agree candidates for matched cells (`0..72`), matched cells plus the first trailing header (`0..88`), and full line header extent (`0..174`), including separate `columnSpanUnits`, `columnSlotWidthUnits`, and `trailingSlotWidthUnits` so future rendering can distinguish text span from grid slot width. `referenceBBoxResidualEvidence.unitBBoxCandidateComparisons` keeps the reference-backed px/unit comparison separate (`3.833`, `3.136`, `1.586` px/unit respectively), and `lineHeaderLineMarkCouplingEvidence` proves the three `/DocumentText` line-header rows are exactly coupled to `/LineMark` records `16..18`. Rendering remains blocked until page-space unit scale and y transform are decoded.
- [x] Add a vertical row-height comparison for that abc table without promoting the y origin. `referenceBBoxResidualEvidence.referenceVerticalComparison` now shows source-derived row height from homogeneous line-header font units (`12 * 1.75 = 21.000px`) versus the reference row height (`21.001px`), a 3-row table height residual of `0.003px`, and `lineMarkRecordSpan:3`; y-origin promotion remains blocked as `page-space-y-origin-unproven`.
- [x] Add prompt-to-table source adjacency evidence for the same abc table. `topTextTableSourceGapEvidence` selects the nearest preceding full-width line-header anchor without text-specific hardcoding, identifies `空欄を埋めて表を完成させなさい。` as source units `501..517` with line-header units `487..499`, and records that the table starts at unit `519` (`sourceGapAfterAnchorTextUnits:2`, `sourceGapAfterAnchorLineHeaderUnits:20`). The first row header starts at `558..570`, and the first row maps to `/LineMark` record `16`; y-origin promotion is still blocked until the page-space transform is decoded.
- [x] Add targeted origin-residual diagnostics for the same abc table instead of leaving `horizontal-x-origin-unproven` as an opaque blocker. `referenceBBoxResidualEvidence.sourceDerivedHorizontalComparison.originResidualEvidence` now records the remaining source-derived-vs-reference deltas (`xResidualPx:1.046`, `yResidualPx:-0.705`, `widthResidualPx:0.927`), converts the x residual into table/full-extent source units (`0.335` / `0.661`), preserves the first matched line-header raw words, records the applied gap-half inset and `/PageMark` body line-gap y pitch, and mirrors the selected `/PageMark` words (`10,13,14,17,18,19,20,21`) that future origin work must explain. Poppler crop RMS against the original `rjtd-testdata/local-samples` Print-to-PDF reference is still dominated by line weight/font styling, so the inset and line-gap pitch are kept as source-backed placement evidence rather than proof of final table styling.
- [x] Preserve raw FDM diagnostics for `success_data-test` in model/export JSON instead of collapsing them into only projected shapes. The surfaces now expose `fdmTextIndexEntries`, `fdmRawVectorSegments`, `fdmRawVectorSegmentCount:5`, `fdmRawVectorCommands`, and `fdmRawVectorCommandCount:37`, giving future work direct source evidence for answer-sheet/title/diagram object decoding.
- [x] Attach source-backed sparse topology and `/LineMark+/PageMark` diagnostics to the `success_data-test` answer-sheet projection instead of leaving the page-2 lower table as opaque reference coordinates. `hatchedAnswerAreaCandidate` now identifies the section `５` to `６` merged empty answer area from the sparse `DocumentText` table, records the neighboring `ＡＢ` answer cell, and exposes source-grid candidates for the relevant section rows. SVG/PDF rendering uses this candidate to draw the full four-edge hatch perimeter instead of only top/bottom hatch lines. The regenerated page-2 Poppler comparison improves whole-page RMS `39.761 -> 39.486`, answer-table RMS `43.439 -> 43.038`, triangle-cell RMS `40.273 -> 38.431`, and hatched-area RMS `42.862 -> 41.317`; Poppler, `sips`, PDFKit, and CoreGraphics renders remain nonblank, and the PDF still contains no `/Group`, `/Transparency`, or `/SMask`.
- [x] Gate the `success_data-test` answer-sheet secondary grid rules at the source-identified merged answer area instead of rendering a page-wide double-rule style. The topology still comes from the sparse `DocumentText` table and answer-sheet reference frame, while `ruleStyleCandidate` now records `renderMode:"primary-plus-source-gated-secondary-line"` and `secondaryLineGate:"before-source-identified-merged-answer-area"` with `styleSourceBacked:false` and blocker `answer-sheet-rule-style-source-field-undecoded`. A/B checks rejected both extremes: global secondary rules keep the top table band but overdraw Q5/right-answer cells, while single-line rules improve the lower sheet but worsen the top table band. The gated renderer keeps 19 primary rules but clips/reduces secondary rules to 13; final page-2 Poppler RMS improves answer-sheet `47.734 -> 45.934`, Q5 hatch `36.880 -> 34.186`, right-answer cells `46.069 -> 37.515`, and keeps the top-table crop unchanged at `56.877`.
- [x] Replace the `success_data-test` answer-sheet hatch renderer's dashed SVG stroke with reference-observed slanted parallelogram dash segments, and separate hatch geometry provenance from hatch style provenance. `hatchedAnswerAreaCandidate.hatchStyleCandidate` now records `sourceBacked:false`, `referenceBacked:true`, `decoded:false`, and blocker `answer-sheet-hatch-style-source-field-undecoded`; SVG carries the same decoded-false style attributes. The renderer suppresses the underlying black table rule on the four hatched edges and paints gray parallelogram dash segments instead. The regenerated page-2 Poppler crops now measure answer-table RMS `40.500` and hatch-perimeter RMS `30.576` on the active crop set; the exact JTD field that encodes the hatch style is still not decoded.
- [x] Replace the `success_data-test` answer-sheet triangle's direct SVG literal path with a decoded-false `trianglePlacementCandidate` derived from the projected A/B/C `FDMText` label slots. The layer tree now exposes the A/B/C label anchors, the `FDMTextIndex` source bbox `-16018..-10611/-15704..-10242`, page-space vertices, and blocker `fdmtext-source-to-sheet-transform-undecoded`; SVG carries `data-source="FDMTextIndex+projectedFdmLabelSlots"` and keeps `data-placement-proven="false"`. This preserves the current pixels while preventing the triangle from looking like fully decoded geometry before the real FDM-source-to-sheet transform is proven.
- [x] Keep the `success_data-test` page-2 title-art front-erase opacity sweep as historical evidence but do not apply it in visible rendering. User visual review showed the gray front-face overpaint is unlike the source PDF, so the interstitial `0x2f` texture block is diagnostics-only until a source field proves front-face ownership.
- [x] Reject two additional `success_data-test` page-2 title-art structural A/B candidates against the active Poppler crop set before touching paint semantics again. Switching all title paths/textures from snapshot `sourceScale` to JSFart content-height `frameScale` worsened the active crops, so `pathScaleDiagnostic` stays diagnostic-only. Earlier pixel-only checks favored interstitial front-erase reuse, but visible review and state ownership evidence now block it from the renderer.
- [x] Expose the `success_data-test` title-art interstitial texture state breakdown without promoting it as a front erase. `frontEraseTextureStateSummary` now stays empty when no main-state/front-state texture paths exist, while `frontEraseTextureRoleGate.groups[].interstitialTextureBlock` preserves the 530 texture paths, first/last indexes `11..540`, explicit/inherited state counts, and payload word columns as blocked evidence.
- [x] Update `success_data-test` title-art `frontEraseTextureRoleGate` so interstitial/current-state inheritance is reported as blocked, not rendered. The gate still records that `0x48.word0` separates shadow outlines (`0x01`) from interstitial/main groups (`0x00`) but cannot separate interstitial textures from main outlines, so visible front texture rendering remains disabled until a source field proves ownership.
- [x] Re-test two more `success_data-test` title-art paint-routing hypotheses before changing visible rendering. Promoting interstitial `0x70.word0=0x1c` as the actual shadow/extrusion texture fill worsened the active title crops, so it remains diagnostic-only. Interstitial front erase is now blocked despite previous RMS experiments because it creates gray front-face overpaint and lacks source ownership proof.
- [x] Re-check the title-art front texture candidate after the dev-branch merge. Rendering all inherited interstitial/front-erase texture paths as white over the front face is visually overexposed; rendering all as source-opacity gray lowers crop RMS but reproduces the rejected gray front-face overpaint. Rendering only explicit state-tagged texture paths is too sparse. Keep the front texture candidate diagnostic-only until a source field separates true front highlights from interstitial/shadow texture inheritance.
- [x] Add a render-preserving `frontErasePaintTransitionGate` for `success_data-test` title art. The layer tree now records the source-order bracket (`shadowLastPathIndex:10`, interstitial texture paths `11..540`, `mainFirstPathIndex:541`) and the relevant state separators: `0x48` separates shadow from texture/main, but neither `0x48` nor `0x70.word0` separates interstitial texture from main; only `0x82.word5` separates texture/main while matching the shadow state. SVG mirrors the same gate as `data-title-front-erase-*` attributes, and promotion remains blocked with `front-erase-transition-boundary-main-state-not-separated` instead of repainting the title face.
- [x] Decode missing main `/FigureData/main_data/FDMText` Shift-JIS labels for `success_data-test` (`９㎝`, `３㎝`, `Ａ`, `Ｂ`, `Ｃ`) and use the recovered Q3 label bboxes when deriving the cone diagram projection. Cone SVG/PDF labels now come from `FDMText` instead of hardcoded page coordinates, and the Q3 source extent is the vector bbox plus nearby text bboxes (`-12004..-10720`, `-12064..-10510`). A Q5 viewport retune experiment was rejected because it worsened the Q5 crop RMS.
- [x] Preserve circular geometry in `success_data-test` Q4 FDM angle diagrams by deriving target height from the source y/x span ratio and the reference-backed target width (`uniform-units-from-horizontal-span`). This keeps the three `r=600` source circles round instead of vertically stretched, moves the Q4 crop RMS from `82.5963` to `74.8490`, and lowers whole-page RMS from `77.1519` to `76.0590` in the Poppler 96dpi comparison.
- [x] Tune `success_data-test` Q4 FDMText label metrics from the preserved text bboxes and reference PDF text layer rather than page-coordinate label nudges. The Q4 labels now use a `0.67` bbox-height font factor and a `0.12` baseline factor, giving `font-size="10.7"` in the SVG. Reference text-bbox checks moved representative label top errors from about `3.6-4.9px` low to about `0.2-1.4px` low (`110°`, `30°`, `160°`, `ｘ`, `Ｏ`), while leaving Q5 unchanged.
- [x] Add the generic, source-backed `fdmTextMirrorAnchorAgreement` diagnostic for FDMText streams. It reports only when an indexed FDMText stream has a different FDMText stream with the same ordered recovered text and record bboxes, and every FDMTextIndex row points to the matching text-record offset and bbox. The current `success_data-test` evidence yields one 15-record agreement between `/FigureData/ExpandData/main_data/Data/FDMText` and `/FigureData/main_data/FDMText`. The gate remains `decoded:false`, `diagnosticOnly:true`, `renderable:false`, and `placementProven:false`; it explicitly blocks promotion with `fdmtext-source-to-page-transform-undecoded` until the source-to-page transform, text role, and paint order are decoded.
- [x] Reclassify Q4's tiny round `ff000460` FDM ellipses (`874`, `1430`) as source control/angle markers instead of visible full circles. The renderer now derives a short angle arc from nearby FDM line geometry (`data-render-source="q4-small-ellipse-angle-arc"`) and renders center dots for the three main `01000460` circles from nearby FDM line endpoints before falling back to the decoded ellipse center (`data-render-source="q4-main-circle-center-nearby-line-endpoint"`). This removes the spurious full inner circles in Q4 while keeping Q5's large `ff000460` cylinder ellipses visible; Q4 crop RMS improves from the text-only `75.0310` to `74.2922`, whole-page RMS improves from `76.0113` to `75.9840` versus the previous center-dot anchor, and Q5 remains unchanged.
- [x] Remove the extra `0.72px` lower bound from `success_data-test` FDM reference primitive strokes and use the existing `fdm_vector_stroke_width` source-style interpretation directly. Line primitives such as Q4 offset `538` and Q5 offset `1992` now render with `data-stroke-width-source:"fdm-vector-style"` and `data-stroke-width:"0.500"` instead of being inflated to `0.72`; ellipse primitives keep their decoded `0.720`. The regenerated 96dpi Poppler comparison improves whole-page RMS `43.8295 -> 43.7239`, Q4 crop RMS `40.2808 -> 39.4841`, Q4 geometry RMS `38.3926 -> 37.3601`, and Q5 RMS `41.5938 -> 41.2462`.
- [x] Split `success_data-test` Q4 FDM angle diagrams into three source-geometry local subdiagram groups anchored by the large `01000460` circle centers (`308`, `470`, `504`) instead of running center-dot and tiny-arc derivation across the full Q4 command set. SVG now emits `rjtd-success-data-test-fdm-subdiagram` groups with `data-grouping-source:"nearest-main-circle-source-center"`, `data-grouping-decoded:"false"`, and `data-paint-order-decoded:"false"`; the local command counts are `7`, `6`, and `7`. This does not promote `FDMIndex` ownership or change paint order semantics, and the regenerated 96dpi Poppler metrics remain stable at whole-page RMS `43.7239`, Q4 crop RMS `39.4841`, Q4 geometry RMS `37.3601`, and Q5 RMS `41.2462`.
- [x] Preserve parser-owned FDMVector command provenance before promoting Q4/Q5 ownership semantics. `ObjectFdmVectorCommandCandidate` now records `sourceVectorRelativeOffset` plus optional `sourceSegment` membership (`relativeOffset`, `localOffset`, `declaredLength`, `commandCount`, `commandIndex`, `commandOffset`) in the model, `getDocumentInfo`, page layer-tree diagnostics, and JSON export. This distinguishes raw primitive spans such as Q3 offset `208` (`sourceSegment:null`) from header-backed commands such as Q5 offset `1992` (`sourceSegment.relativeOffset:1864`, `localOffset:128`, `commandIndex:2`) without changing PDF pixels.
- [x] Expose the same FDMVector command provenance on `success_data-test` SVG primitives as diagnostic `data-*` attributes. Q3/Q4 raw primitives now carry `data-source-vector-relative-offset` plus `data-source-segment-backed="false"`, while Q5 segment-backed primitives expose the full source segment offset-table fields. This keeps SVG inspection aligned with model/export evidence and leaves the regenerated PDF visually unchanged (`sips` page-1 non-white pixels remain `35277`).
- [x] Add source-ownership cohort diagnostics to the `success_data-test` FDM SVG projection and Q4 local subdiagram groups. Projection/subdiagram groups now expose `data-source-ownership-basis="fdmVectorCommandProvenance"`, command offset ranges, command offset lists, source segment cohort counts, and `data-source-ownership-promotion-blocked-reason`. Current Q4/Q5 groups remain `ownership-proven=false`: Q4 has `20` commands with `18` raw spans and `2` segment-backed commands (`690,1246`), Q5 has `7` commands with `1` raw span and `6` segment-backed commands (`1864,2100`), so both stay blocked as `mixed-raw-and-segment-cohorts` instead of promoting FDMIndex ownership prematurely.
- [x] Mirror `success_data-test` Q4/Q5 FDM source-cohort diagnostics into `getPageLayerTree` as `fdmReferenceProjection` ops. The layer tree now exposes projection-level and Q4 subdiagram-level `sourceCohort` JSON with command relative offsets, raw-span vs segment-backed counts, segment cohort offsets, and explicit ownership promotion blockers, so future line/chord/arc ownership work no longer depends on SVG-only `data-*` inspection.
- [x] Mirror the same Q4/Q5 FDM reference projection diagnostics into object-stream JSON for `getDocumentInfo` and `rjtd export --format json` as `successDataTestFdmReferenceProjections`. The aggregate keeps each projection's role, source bbox, reference target bbox, command count, source cohort, render-promotion blocker, and Q4 subdiagram blockers available outside the SVG/layer-tree paths.
- [x] Add decoded-false `primitiveOwnershipComparison` diagnostics for `success_data-test` Q4/Q5 FDM reference projections in `getPageLayerTree`, `getDocumentInfo`, and JSON export. The comparison classifies each preserved FDM command into role candidates such as `line-candidate`, `chord-candidate`, `radial-line-candidate`, `arc-candidate`, `connector-candidate`, and `surface-boundary-candidate` using command provenance plus source-geometry local subdiagram anchors. It keeps `ownershipProven:false` and blocks promotion as `primitive-role-and-paint-order-unproven`, but now exposes stable counts for Q4 (`20` commands, `3` anchors, `11` lines, `6` arcs, `8` connector candidates) and Q5 (`7` commands, `2` lines, `4` arcs, `3` connector candidates) without changing PDF pixels.
- [x] Attach decoded-false FDMIndex row reference candidates to the same Q4/Q5 `primitiveOwnershipComparison` diagnostics. In `success_data-test`, the parsed `vectorOffset` field is invalid for these rows, but the row `bbox.left` field consistently matches either a command relative offset (`matchKind:"command-relative-offset-field"`) or a segment relative offset (`matchKind:"source-segment-relative-offset-field"`). Q4 now has `20` index-row reference candidates and Q5 has `7`, all with `validVectorOffset:false`, so FDMIndex row ownership is preserved as evidence without promoting paint order or vector offset semantics.
- [x] Generalize the same FDMIndex row offset-field evidence outside the Q4/Q5 projection path. Every exported `fdmIndexEntries[]` row now includes decoded-false `offsetFieldReferenceCandidates` when a row field (`vectorOffset`, `bbox.left`, `bbox.top`, `bbox.right`, `bbox.bottom`) matches preserved FDMVector command or source-segment offsets. In `success_data-test`, rows like Q4 command row `bbox.left=308`, Q4 segment row `bbox.left=690`, and Q5 segment row `bbox.left=1864` now expose their matched command offsets directly on the row, so future samples can validate the same hypothesis without relying on sample-specific projection diagnostics.
- [x] Surface the generalized FDMIndex offset-field evidence in CLI diagnostics. `rjtd object-fdm-index` and `rjtd object-fdm-index-rows` now report `offset-field-refs` per row plus summary counts, reusing model `fdm_raw_vector_commands()` rather than adding another vector parser. The PDF-backed `success_data-test` regression locks the observable cases `bbox.left:command:308` and `bbox.left:segment:1864->[1924,1958,1992,2024]`, while synthetic image-only fixtures remain `offset-field-refs=-`.
- [x] Group FDMIndex row references by primitive role candidate inside `primitiveOwnershipComparison`. `indexRowReferenceRoleCandidateGroups` now records each role candidate's referenced command offsets, row indexes, command-relative vs source-segment reference counts, and keeps `ownershipProven:false` with `role-candidate-and-paint-order-unproven`. This exposes, for example, Q4 main-circle rows `[7,12,13]`, Q4 radial-line rows `[8,10]`, and Q5 source-segment line row `[40]` without promoting unproven paint order.
- [x] Add row-command order evidence to those role candidate groups. Each group now exposes `uniqueCommandRelativeOffsetCount`, `uniqueRowIndexCount`, `oneToOneRowCommandReferenceCandidate`, `singleRowBacksMultipleCommandsCandidate`, `rowOrderMatchesCommandOrderCandidate`, and explicit `rowCommandPairs`. This separates Q4's one-row-per-command pattern from Q5's single-row-backed segment commands while keeping paint order decoded-false.
- [x] Add projection-level `indexRowOrderPromotionGate` diagnostics for FDMIndex-backed primitive ownership. The gate summarizes referenced command count, unreferenced command count, unique row count, command-relative vs source-segment reference counts, row-order monotonicity, and one-to-one vs single-row-backed segment patterns. Current `success_data-test` evidence shows Q4 has all 20 commands referenced one-to-one by 20 rows, while Q5 has all 7 commands referenced by 3 rows with `singleRowBacksMultipleCommandsCandidate:true`; both remain blocked by unproven primitive role and paint-order semantics.
- [x] Mirror the same FDMIndex row-order gate onto `success_data-test` SVG projection/subdiagram groups as non-rendering `data-index-row-order-*` attributes, and add `renderPaintOrderBasisCandidate:"fdm-index-row-command-pairs"` to the JSON/layer-tree gate. Browser/SVG inspection now exposes the same decoded-false row-command evidence as JSON and layer-tree diagnostics, while PDF pixels remain unchanged and paint-order promotion remains blocked.
- [x] Consume the FDMIndex row-order gate in the actual Q4 SVG render path when the evidence is one-to-one, monotonic, and references every selected command. `success_data-test` Q4 now renders through `data-render-command-order-basis="fdm-index-row-command-pairs"` with all 20 row-owned commands; Q5 remains on the FDMVector projection-filter order with `fdm-index-row-fanout-primitive-ownership-unproven` because its 7 commands are backed by only 3 rows.
- [x] Gate source-derived table rendering on exact `/LineMark` row boundaries and homogeneous `/DocumentText` line headers instead of treating every `/LineMark+/PageMark` origin as renderable. The `success_data-test` abc table remains renderable with `lineMarkRowsExactAndContiguous:true`, `lineHeaderRowsHomogeneous:true`, and `renderPromotionBlockedReason:"none"`. The PDF-backed `shanai_lan` network diagram keeps its `sourceDerivedLayoutCandidate` as diagnostic evidence, but it is blocked with `line-mark-rows-not-exact-source-boundaries` and no longer emits an SVG/PDF `rjtd-column-grid-candidate` overlay, avoiding the false table rectangle around diagram labels such as `NASサーバ`/`TA`/`8ﾎﾟｰﾄ`. Regenerated `openjtd-samples/pdf-output` outputs render nonblank through Poppler, `sips`, and CoreGraphics; `success_data-test` has one visible table projection, while `shanai_lan` has zero visible table projections.
- [x] Promote `tsaiten` lower-table stride-Y evidence into the representative source-derived layout candidate without enabling visible rendering. The lower error table now reports `source:"documentTextLineHeaders+/LineMark+/PageMark"`, `pageOriginAuthority:"lineMarkPageGridStrideRawRecordIndex"`, and bbox `x=72.000,y=765.000,width=451.181,height=63.000`, selected only when stride records, homogeneous line headers, sparse-sibling topology, and post-row-gap correlation all agree. Rendering remains blocked at candidate readiness as `sparse-sibling-derived-candidate-render-ineligible` and at page-y promotion as `line-mark-record-stride-to-page-y-transform-unproven`, and the regenerated `openjtd-samples/pdf-output/ichitaro-20030120132956-0007-sp-dat-tsaiten.pdf` remains nonblank under Poppler and `sips`.
- [x] Add scoped `/PageMark` Y-transform diagnostics for `tsaiten` table candidates before attempting another render promotion. `recordIndexAffineFit` now shows the scoring table reference slope is `16.100px/record`, while the raw `/LineMark` record-index heuristic advances `21.000px/record` and remains `4.900px/record` too steep. `pageMarkScopedYTransformProbe` compares only the matched parsed `/PageMark` entry/raw header fields instead of scanning the whole stream: scoring rows still have zero row-top hits, with nearest first-row evidence `word25=22366` as centipoints (`298.213px`, residual `-2.792px`), while the lower table keeps mixed evidence (`768` direct px for row 0 and a centipoint near-hit for row 1). This narrows the next proof to finding a reusable `/PageMark` field family, not promoting raw record-index Y.
- [x] Add shared `/PageMark` subrecord field-family residual diagnostics under `pageMarkScopedYTransformProbe`. The scoring table reports no best table-top, row-top, or row-delta family, while the lower table's best table-top family is source-backed `rawRecordHeaderTailU16Subrecord` / `direct-u16-px` / `fieldIndex:2`: table-top hits come from raw records `2` and `3` at byte offsets `178` and `338`, both residual `-0.014px`. This is diagnostic-only and keeps rendering blocked because it explains a lower-table top anchor, not the scoring-table transform or full per-row y decoder.
- [x] Add interval-centric `/LineMark` row/gap sequence diagnostics for `tsaiten` table candidates. `lineMarkRowGapSequenceEvidence` now records that this sample has no `0x1000`/`0x1001`/`0x1002` tag-family payloads (`lineMarkTagCount:0`) and is instead `be16-delta-v1`. For the scoring table, selected records `[8,10,12,14]` all match post-row gaps (`[65,65,65,67]`), while the previous records `[7,9,11,13]` match compact row spans (`[107,113,113,113]`). The lower table repeats the same pattern: selected `[33,35,37]` match gaps (`[58,52,54]`) and previous `[32,34,36]` match row spans (`[111,92,91]`). This proves the currently selected `/LineMark` records are spacing records, not page-y row anchors, so raw record-index Y remains diagnostic-only.
- [x] Add alternate previous-row-span `/PageMark` scoped diagnostics for `tsaiten` so the row-span record family is tested before promotion. `previousRowSpanScopedYTransformProbe` compares previous records `[7,9,11,13]` and `[32,34,36]` with the same matched parsed PageMark entry/raw header. The scoring table still has `targetHitCount:0`, no best same-header field families, and `sameHeaderSlotCount:0`; the lower table still keeps only the foreign-header `fieldIndex:2/tailBlock16WordIndex:11` table-top hint. Header matching is therefore low-signal, and neither selected spacing records nor previous row-span records decode a reusable non-reference page-y transform yet.
- [x] Add source-backed PageMark subrecord line-span correlation diagnostics under `pageMarkScopedYTransformProbe`. `subrecordLineSpanCorrelation` now compares raw u16 subrecord `words[4]..words[6]` spans against the same table's selected post-row gap spans and previous compact-row spans: scoring uses `[65,65,65,67]` vs `[107,113,113,113]`, and lower uses `[58,52,54]` vs `[111,92,91]`. This gives the next reverse-engineering step concrete PageMark span candidates without using reference y coordinates, but it remains blocked because a span correlation alone does not decode page-y origin or row-top placement.
- [x] Add a source-backed `table-page-space-v1` solver gate to table layer-tree diagnostics. `success_data-test` now records `solverStage:"renderable-source-page-space"` and `renderPromotionAuthority:"source-derived-page-space-solver"` for the abc table, while `tsaiten` explicitly separates a scoring-table `candidate-absent` case from the lower table's `blocked-y-origin-transform` / `line-mark-record-stride-to-page-y-transform-unproven` case. `shanai_lan` remains blocked as `blocked-row-height-transform` with `line-mark-rows-not-exact-source-boundaries`, so diagram text-slot false positives cannot silently become visible table overlays.
- [x] Strengthen `table-page-space-v1` with a source-backed `sourcePageYTransformGate` instead of promoting `tsaiten` table geometry from a loose span match. The gate records ordered unique `/PageMark` subrecord coverage for each table row: `success_data-test` remains `promotionReady:true`, `shanai_lan` remains blocked by `line-mark-rows-not-exact-source-boundaries`, the `tsaiten` scoring table stays `source-derived-layout-candidate-absent`, and the `tsaiten` lower table shows selected records `[33,35,37]` hit post-row-gap spans but reuse subrecord candidates `[174,734,174]` (`uniqueCandidateCount:2/3`, duplicate `174`). Rendering therefore stays blocked with `page-mark-subrecord-selected-post-row-gap-candidates-not-row-unique` and `source-page-y-transform-not-decoded`; regenerated `openjtd-samples/pdf-output/ichitaro-20030120132956-0007-sp-dat-tsaiten.pdf` renders under Poppler and Apple `sips`.
- [x] Add source-range and cross-table ordering diagnostics for `tsaiten` `/PageMark` subrecords before any y-transform promotion. `pageMarkRawRecordSourceRangeEvidence` shows the scoring table has raw PageMark header overlap for only `3/4` row source ranges (`matchedScanIndexes:[4,5,6,7,8,9,11,14]`, row 3 uncovered), while the lower table has `0/3` row source coverage. `crossTableSubrecordOrderingProbe` compares all four compact table candidates sharing the same sparse sibling and rejects the span-match family globally: combined byte offsets `[414,414,414,174,414,174,174,734,174]` and raw scan indexes `[3,3,3,2,3,2,2,6,2]` are non-monotonic and reuse family `174` after `734`. The y gate now blocks with `page-mark-cross-table-raw-record-order-regression` and `page-mark-cross-table-subrecord-ordering-unproven`, keeping this evidence diagnostic-only.
- [x] Add `lineMarkStrideToPageYPromotionReadiness` under `sourcePageYTransformGate` so the `tsaiten` lower-table stride hypothesis cannot be mistaken for render authority. The lower table now summarizes the source-only blockers in one place: `strideCandidatePresent:true`, `lineMarkPageOriginPresent:false`, selected post-row-gap spans complete but not ordered-unique (`[174,734,174]`), raw PageMark source-range coverage `0/3`, cross-table subrecord ordering inconsistent, and source-order/subrecord-order contradiction true. The regenerated `openjtd-samples/pdf-output/ichitaro-20030120132956-0007-sp-dat-tsaiten.pdf` is visually unchanged against the previous Poppler render (`RMS 0.000000`, changed ratio `0.000000`) and remains nonblank under Poppler (`186393` non-white pixels) and Apple `sips` (`55283` non-white pixels).
- [x] Add `sparseSiblingColumnPromotionReadiness` beside `tsaiten` table sparse-sibling evidence. The scoring table now explicitly reports that sparse topology is complete (`4/4` rows, `12/12` segments, sparse columns `[3,4,5]`) but column split promotion is not ready because compact line-header geometry is `0/12` and source line-header column widths are missing. The lower table reports `columnSplitReady:true` with `6/6` compact headers and source width fractions `[0.683,0.317]`. Rendering remains unchanged; this gate prevents sparse topology alone from becoming visible column geometry authority.
- [x] Split `tsaiten` lower-table stride-row evidence into source-unit and PageMark-line domains. `lineMarkStrideToPageYPromotionReadiness.sourceOnlyStrideRowCoverage` now compares previous compact row-span `/LineMark` spans with table row source spans in `documentTextSourceUnits` (`rowSpanUnits:[111,92,91]`, `lineMarkSpanUnits:[111,92,91]`, residuals `[0,0,0]`), while `pageMarkEntryLineBoundsCoverage` separately records that record indexes `[32,34,36]` sit inside PageMark entry line bounds `0..42`. The legacy raw `rawRecordSourceRangeCoverage` remains visible but is marked `rawRecordSourceRangeCoverageUsableForPromotion:false` because it mixes document-text unit ranges with PageMark line indexes. Regenerated `openjtd-samples/pdf-output/ichitaro-20030120132956-0007-sp-dat-tsaiten.pdf` renders under Apple `sips` at `595x842` with `55279` non-white pixels and has zero `/Group`, `/SMask`, `/ExtGState`, `/Pattern`, and `/Shading` safety-token hits.
- [x] Add `lineMarkStrideToPageYPromotionReadiness.pageMarkSubrecordLineRangeRecordCoverage` for the same `tsaiten` lower table. The source-backed diagnostic compares selected spacing records `[33,35,37]` and previous row-span records `[32,34,36]` with raw `/PageMark` u16 subrecord line-start/end ranges, and proves neither record family is covered (`selectedCoveredRecordIndexes:[]`, `previousCoveredRecordIndexes:[]`, `selectedCoverageComplete:false`, `previousCoverageComplete:false`). This keeps the nearby `field2Value:768` table-top hits diagnostic-only because the subrecord line ranges do not contain the actual table LineMark records. Regenerated `tsaiten.pdf` remains PDFKit-safe with one final `%%EOF`, three scrubbed internal CMap `%%EOD` comments, zero `/Group`/`/SMask`/`/ExtGState`/`/Pattern`/`/Shading` tokens, and Apple `sips` page 1 `595x842` / `54566` non-white pixels.
- [x] Add source-only `lineMarkRowBoundaryAlignment` beside `tsaiten` lower-table stride-row coverage. The previous row-span records `[32,34,36]` still match row spans (`spanResidualUnits:[0,0,0]`) but do not exactly align to compact row source boundaries: row source starts/ends `[1857,2026,2170]..[1968,2118,2261]` compare to LineMark boundaries `[1775,1944,2088]..[1886,2036,2179]`, yielding stable start/end residuals `[-82,-82,-82]` and a top-level `rowBoundaryOffsetCandidateFamily:"previous-row-span-records"` / `rowBoundaryOffsetCandidateUnits:-82` / `rowBoundaryOffsetCandidateRequiresTransform:true`. Selected spacing records `[33,35,37]` have unstable residuals and negative span residuals. Rendering remains blocked as `line-mark-row-boundaries-require-source-offset-transform`; regenerated `tsaiten.pdf` is PDFKit-safe (`%%EOF` 1, `/Group`/`/SMask`/`/ExtGState`/`/Pattern`/`/Shading` all 0) and Apple `sips` renders `595x842` with `54566` non-white pixels.
- [x] Separate `tsaiten` table horizontal source evidence from render promotion readiness. `sourceFrameCandidateAgreementGate` now reports `sourceOnlyUniqueSelectionCandidatePresent:true` when the best x/width group is uniquely supported by source-only PageMark/line-header field evidence, while keeping `selectionReady:false`, `sourceOnlyUniqueSelectionPromotionReady:false`, and `source-horizontal-field-semantics-unproven`. This locks the scoring table candidate (`x=174,width=421`) and lower table candidate (`x=174,width=554`) as reusable source-backed diagnostics without making either table a render authority.
- [x] Add cross-table row-boundary offset consistency under `sourcePageYTransformGate` before any `tsaiten` table y-origin promotion. All four compact table candidates sharing sparse table candidate `4` now report the same previous-row-span boundary transform: related candidates `[0,1,2,3]`, row-boundary offsets `[-82,-82,-82,-82]`, stable candidate `-82`, and witness record families `[7,9,11,13]`, `[21,23]`, `[25,27]`, `[32,34,36]`. The gate records `cross-table-row-boundary-offset-transform-required` and keeps rendering blocked because the stable source-unit offset still does not decode PageMark page-y origin or solve the existing subrecord ordering blockers.
- [x] Promote the stable `tsaiten` row-boundary offset evidence to offset-normalized exactness diagnostics without changing render admission. Each previous-row-span alignment family now emits `offsetNormalizedStartResidualUnits`, `offsetNormalizedEndResidualUnits`, `offsetNormalizedExactBoundaryMatchCount`, and `offsetNormalizedExactBoundaryAligned`; the lower table records `[0,0,0]`/`[0,0,0]` and `3/3`, while the cross-table sparse-sibling probe records `allOffsetNormalizedBoundariesExact:true` for candidates `[0,1,2,3]`. This proves the stable `-82` transform exactly aligns source row boundaries to previous LineMark boundaries, but rendering remains blocked until PageMark y-origin and subrecord ordering semantics are decoded.
- [x] Add diagnostic-only `lineDomainPostRowGapProjectionProbe` under the `tsaiten` `sourcePageYTransformGate`. The probe records the tempting scoring-table coincidence where line-domain `235.167` plus post-row-gap span `65` lands within `0.967px` of the diagnostic reference top, but it stays blocked as `line-domain-post-row-gap-projection-crosses-source-unit-domain` because the selected records are spacing records, source/reference validation is mixed, and the lower table counterexample (`817.905 + 58`, residual `107.563px`) proves this is not a reusable page-y origin decoder.
- [x] Add source-only transition and absolute-y slot gates for `tsaiten` page-y decoding. `transitionSemanticsReadiness` now keeps table-family gaps source-backed (`sourceRangeGapUnits:[190,72,138]`, row-start gaps `[303,160,230]`, previous/selected LineMark gaps `[8,2,5]`) while explicitly blocking promotion because the source-gap to page-line-gap rule is still undecoded. It also exposes per-boundary `transitionPairs` in the `page-mark-line-index` domain so matching previous/selected transition gaps are visible without reference coordinates, while `pageSpaceTransitionOriginDecoded:false` remains fixed. A direct source-gap ratio check rejects the simple mapping too: `sourceRangeUnitsPerPreviousRecordGap:[23.750,36.000,27.600]` and `rowSourceStartUnitsPerPreviousRecordGap:[37.875,80.000,46.000]` are unstable, even though PageMark y gap per record is stable at `[23.310,23.310,23.310]`. `sourceGapToPageLineGapDirectMapDiagnostic` records the direct-map mismatch explicitly (`pageLineGaps:[8,2,5]`, minus arrays `[182,70,133]` / `[295,158,225]`, equality arrays all false). `sourceGapToPageLineGapSegmentOffsetDiagnostic` then tests the next piecewise offset candidate (`rowSourceStartGapUnits - sourceRangeGapUnits`) and rejects it too: segment offsets `[113,88,92]` are not page-line gaps (`minus [105,86,87]`, ratios `[14.125,44.000,18.400]`, unstable). `sourceGapToPageLineGapTransformReadiness` turns both diagnostics into a source-backed decline path: direct source-range, row-source-start, and segment-offset transforms are rejected, `acceptedTransformKind:null`, and the next required evidence is a decoded source-gap unit domain or segment transition offset rule. `sourceOnlyPageMarkAbsoluteYSlotGate` compares the line-domain plus post-row-gap projection against raw PageMark absolute-y slots without using reference coordinates: the lower table exposes `rawRecordHeaderTailU16Subrecord/direct-u16-px fieldIndex:2 tailBlock16WordIndex:11`, `absoluteYSlotY:768.000`, and rejects the line-domain projection `875.905` as a `107.905px` mismatch. The same slot is mirrored into `sourceOnlyPageYOriginCandidateAgreementGate` as a blocked source-only support, not render authority.

Remaining:

- [ ] Decode real paragraph boundaries instead of deriving them from extracted newline text.
- [x] Render `success_data-test` title-art front-fill paths with the EmbeddedPress path winding preserved as `fill-rule="nonzero"` instead of punching overlap holes with `evenodd`. The SVG records `data-title-fill-rule-source="embedded-press-nonzero-winding"` so this is tracked as source-path fill behavior, not a decoded global paint rule. The change fills overlapping title strokes more like the reference while preserving interior counters such as `〇`; whole-page RMS moved slightly worse (`46.219 -> 46.438`), so exact title paint semantics remain unresolved.
- [x] Apply the same source-backed EmbeddedPress nonzero winding to the `success_data-test` title-art main/shadow face clip paths and shadow face fill instead of mixing nonzero visible fills with evenodd clips. This is not a color or coordinate tune: the layer tree now reports `frontPaintCandidate.renderClipRule:"nonzero"` and `renderClipRuleSource:"embedded-press-nonzero-winding"`, while SVG exposes `data-title-clip-rule-source="embedded-press-nonzero-winding"` on the title face clips. The regenerated `openjtd-samples/pdf-output/ichitaro-20030228030923-success-002-success_data-test.pdf` stays PDF 1.4 with no `/SMask`, `/Group`, transparency, pattern, or shading tokens, and renders through Poppler, `sips`, PDFKit, and CoreGraphics. Against the local reference PDF at 144dpi, page 1 whole/title-wide/title-box/title-glyph RMS improved `44.849/51.380/59.289/62.507 -> 44.605/49.955/57.492/60.297`; page 2 improved `39.293/60.898/64.203/66.788 -> 38.970/59.607/62.670/64.734`.
- [x] Expose the `success_data-test` front-paint candidate directly on title-art SVG elements instead of leaving the hardcoded black front fill unexplained. The SVG records `data-title-front-fill-render-color="#111111"` with `data-title-front-fill-render-color-source-backed="false"`, while separately preserving `data-title-front-paint-candidate-color="#ffffff"` from `JSFart2Contents.paintColorCandidate`. The source paint candidate is retained as decoded-false evidence and is not promoted as either blanket front fill or front-erase texture ownership.
- [x] Replace the temporary title-art front-fill color source marker with a render-preserving mismatch gate. `success_data-test` now records `data-title-front-fill-render-color-source="conservative-front-fill-fallback-source-paint-mismatch"`, `data-title-front-fill-source-paint-color-matches-render-color="false"`, and `source-paint-color-does-not-match-render-fill` because the observed source paint candidate is `#ffffff` while the current conservative render fill is `#111111`. This removes the placeholder without falsely claiming the black fill is source-backed.
- [x] Mirror the same title-art front-fill mismatch gate into `getPageLayerTree`. `frontPaintCandidate` now carries `renderFillColor:"#111111"`, `renderFillColorSource:"conservative-front-fill-fallback-source-paint-mismatch"`, `renderFillColorSourceBacked:false`, `sourcePaintColorMatchesRenderFill:false`, and `renderFillColorPromotionBlockedReason:"source-paint-color-does-not-match-render-fill"`, using the same helper as the SVG attributes so layer-tree consumers do not infer that the source white paint candidate is the black face fill.
- [x] Generalize `success_data-test` title-art front texture ownership to use inherited EmbeddedPress current paint state instead of only explicit per-path `0x82.word5` records. `success_data_test_title_art_front_texture_paths` still reports zero main-state/front-owned texture paths for this sample, while the interstitial texture block is handled by a separate source-order front-erase gate. This keeps future main-state texture promotion dependent on source state spans instead of sample-local explicit tags.
- [x] Promote the `success_data-test` source-order interstitial front-erase texture block as a visible, PDFKit-safe precomposited front-face texture instead of using SVG/PDF transparency. The source gate still records zero main-state/front-owned texture paths, but `frontEraseTexturePathCount:530`, `candidateBasis:"source-order-interstitial-front-erase-texture"`, `visibleRenderPathCount:530`, and `renderPromoted:true`; SVG mirrors this as `data-title-texture-rendering="source-opacity-front-erase-precomposited-filled-source-paths"`, `data-title-texture-opacity="0.470"`, `data-title-texture-render-opacity="1.000"`, and `data-title-texture-render-fill="#818181"`. This is source-backed by the interstitial texture block, `0x82.word5=0x2f`, and JSFart/EmbeddedPress white paint candidates, not a sample-local color tune. A direct opacity probe was rejected by the PDF safety scanner because it introduced `/Group` transparency constructs, and a fill-rule-consistent nonzero side-strip probe was rejected because it slightly worsened the active title RMS (`92.359 -> 92.594`). The landed precomposited probe improves the active 144dpi reference comparison without changing the table crop: page 1 whole RMS `46.356 -> 45.174`, title-wide `69.779 -> 60.428`, title-glyph `92.359 -> 78.328`, table unchanged `70.459`. Regenerated `openjtd-samples/pdf-output/ichitaro-20030228030923-success-002-success_data-test.pdf` remains PDF 1.4 with no `/Group`, `/SMask`, `/ExtGState`, pattern, or shading tokens and renders nonblank through Poppler, `sips`, PDFKit, and CoreGraphics.
- [x] Add render-preserving `success_data-test` title-art `titleTexturePaintPhaseGate` diagnostics for EmbeddedPress `0x46.word0`. The gate records that interstitial texture paths have `textureRecord46Word0Values:["0x00"]`, while both shadow and main outlines carry `["0x00","0x01"]`; `0x01` is therefore an outline-only paint-phase candidate, but `0x00` is shared with the texture and main outline runs. Rendering remains unchanged with `renderPromoted:false`, `visibleRenderPathCount:0`, and `renderPromotionBlockedReason:"record46-paint-phase-semantics-unproven"`.
- [x] Extend the same title-art paint-phase gate to prove EmbeddedPress `0x60.word0` and `0x65.word0` are not useful role separators for the current sample. Shadow outlines, interstitial textures, and main outlines all carry `["0x10","0x11"]` for both record families, so the layer tree now exposes `record60SharedAcrossRoles:true`, `record65SharedAcrossRoles:true`, and explicit `record60/65SeparatesTextureFromOutlines:false` before any future paint promotion attempt.
- [x] Tighten the `success_data-test` title-art `titleTexturePaintPhaseGate` into an explicit promotion-proof gate. It now records record48 value sets, `record46SeparatesTextureFromOutlines:false`, `record48SeparatesTextureFromMain:false`, `record46PromotionProofReady:false`, and `promotionProofBlockedReasons` for the shared record46 zero value plus shared record60/65 state. Rendering is still unchanged; this prevents future paint-phase promotion from treating the outline-only `0x46=1` hint as sufficient without proving texture/main/shadow role separation from source state.
- [x] Add render-preserving `success_data-test` title-art `titleShadowPaintWordGate` diagnostics for EmbeddedPress `0x70` role separation. The gate shows `0x70.word0`, `word3`, and `word7` separate shadow outlines from interstitial texture/main outlines (`0x2c/0x0a/0x1c` vs `0x1c/0x00/0x00`), while `word1` is shared (`0x11`) and none of those fields separates interstitial texture from main outlines. The current shadow effect candidate remains source-backed (`word0=44`, `#8f8f8f`), but the interstitial texture effect candidate (`word0=28`, `#676767`) stays diagnostic-only as `record70-separates-shadow-but-not-interstitial-texture-from-main`. Regenerated `success_data-test.pdf` keeps the same 144dpi Poppler metrics (`whole RMS 45.068641`, `title_wide RMS 68.497339`, `title_bbox RMS 77.257278`) and remains nonblank through `sips`/PDFKit.
- [x] Demote the `success_data-test` source-order interstitial front-erase texture and `0x70.word0=0x1c` texture-effect fill from visible rendering after the latest visual review. The SVG/layer tree now keeps both as diagnostics only: `frontEraseTextureRoleGate.visibleRenderPathCount:0`, `renderPromoted:false`, `renderPromotionBlockedReason:"front-erase-texture-over-main-face-semantics-unproven"`, and `interstitialTextureEffectCandidate.renderPromoted:false` with `record70-separates-shadow-but-not-interstitial-texture-from-main`. The generated title no longer uses the gray `#818181`/`#676767` promoted overpaint; `data-title-texture-render-fill` stays on the conservative front fill `#111111`. Visual verdict is still partial (`0.74`): the gray duplicate-shadow symptom is reduced, but the title is now too black/solid, so the next change must decode source compositing/knockout semantics rather than re-enable front erase or tune arbitrary gray levels. A/B probes using `#8f8f8f`, light gray, and removed texture paths were rejected because they worsened title metrics or visual balance. The regenerated PDF renders through Poppler, Apple `sips`, Quick Look thumbnailing, and the PDFKit/CoreGraphics smoke tests.
- [x] Repair the post-merge title-art texture-effect regression so the demoted interstitial `0x70.word0=0x1c` candidate is no longer reported or rendered as promoted. `interstitialTextureEffectCandidate` and `extrusionTextureEffectRenderPromoted` now both report `false` with blocker `record70-separates-shadow-but-not-interstitial-texture-from-main`, and the SVG extrusion texture keeps the candidate metadata while rendering with conservative `#111111` instead of candidate gray `#676767`.
- [x] Add a render-preserving `success_data-test` title-art `frontPaintArbitrationGate` diagnostic so front-fill selection no longer collapses to a single fallback mismatch reason. The gate keeps the conservative `#111111` front fill, records four competing candidates from JSFart/EmbeddedPress paint, direct-gray, source-paint, and state-span probes, and preserves the source-order transition blocker. SVG mirrors the same `data-title-front-paint-arbitration-*` attributes; rendering remains unchanged with `front-paint-candidate-arbitration-unproven`.
- [x] Add render-preserving `extrusionTextureClipGate` diagnostics for `success_data-test` title art so the visible interstitial texture clip does not remain implicit. Layer tree and SVG now record that the current promoted texture clip is `source-shadow-outline`, that the `long-shadow-side-sweep` alternative was rejected by historical Poppler crop A/B, and that exact front-face knockout/clip semantics remain blocked as `texture-clip-and-knockout-semantics-unproven`. Rendering and PDF output are unchanged.
- [x] Add source-backed `titleTextureGeometryRoleGate` diagnostics for `success_data-test` title art. The layer tree and SVG now compare texture, shadow-outline, main-outline, and long-shadow side-sweep source bboxes without using the reference PDF as authority: texture bbox overlaps both front and shadow bboxes by `0.903`, is not contained by either one alone, and is contained by the side-sweep bbox. Rendering remains unchanged because bbox overlap only proves geometric relation, not the exact front-face knockout or clip semantics (`texture-source-bbox-relation-is-bbox-only-not-knockout-proof`).
- [x] Promote `success_data-test` title front-fill winding from `nonzero` to `evenodd` behind a `frontFillWindingGate`. The gate records source contour evidence (`11` main outline paths, `11` multi-contour paths, `4` paths with opposite signed contours) and the active Poppler title crop A/B (`nonzero RMS 78.059`, `evenodd RMS 76.034`, improvement `2.025`). SVG front-fill paths now use `fill-rule="evenodd"` with `data-title-fill-rule-source="embedded-press-evenodd-boundary-contours"`. The separate source-backed horizontal pathX candidate was rejected for now because it worsened title-tight RMS (`76.034 -> 81.223`) and top crop RMS (`51.191 -> 52.741`).
- [x] Add render-preserving `frontEraseVisibleProbeGate` diagnostics after rechecking front-erase texture under the promoted evenodd front fill. Making all 530 front-erase texture paths visible improves raw RMS (`title-tight 76.034 -> 67.651`, top crop `51.191 -> 48.814`) but is visually rejected as gray overpaint, not distressed knockout. Rendering only the 11 explicit-state texture paths is effectively neutral (`76.034 -> 76.016`). Keep visible front-erase blocked as `front-erase-visible-rms-improvement-is-not-knockout-proof`.
- [x] Add render-preserving `sourcePaintRenderTrace` diagnostics for `success_data-test` title art. The layer tree and SVG now expose the raw JSFart paint words (`styleWord1=0x02141030`, `styleWord2=0x02141018`, `paintColorCandidate=0x00ffffff`, `paintFlagCandidate=0x00000001`, `effectWordCandidate=0x0000000a`) next to the selected conservative render fill `#111111`. The trace conclusion is `source-paint-present-but-render-fill-not-promoted`, because the source paint is white, does not match the active fill, and the only available texture path source remains blocked as `front-erase-texture-over-main-face-semantics-unproven`.
- [x] Add render-preserving `sourceFrameRenderTrace` diagnostics for `success_data-test` title art. The trace proves the JSFart frame candidate and `/Frame` row agree on frame ref/object id `1`, outer width `13260`, and outer height `1327`, while preserving the content dimensions `13031x1054`, selected `jsfartContentLeft` placement, and blocked alternate frame-edge placement (`candidateFrameX=72.907`). Rendering remains unchanged because the frame/content split is still blocked as `frame-content-split-horizontal-semantics-unproven`.
- [x] Sweep local JSFart2Contents candidates before assigning semantics to `styleWord1/styleWord2/effectWordCandidate`. Eight local files contain `/JSFart2Contents` object-path candidates, but only the two `success_data-test` streams currently decode as `jsfartArt` under the `MSTUDIO...` magic gate. Both decoded streams share `paintColorCandidate=0x00ffffff`, `paintFlagCandidate=0x00000001`, and `effectWordCandidate=0x0000000a`, while `styleWord1/styleWord2` differ (`0x02141030/0x02141018` vs `0x02021030/0x02021018`). This is useful evidence but not enough to interpret those words as render authority across JSFart variants.
- [x] Preserve a decoded-false `jsfartStreamProfile` for every `/JSFart2Contents` stream, including non-`MSTUDIO.OCX` variants. The profile records prefix family (`mstudio-ocx-utf16le`, `jsfart-object-utf16le`, `zero-prefix`, etc.), `magicFamilyHex`, UTF-16 preview, header prefix, and `structuredArtCandidatePresent`, while keeping `diagnosticOnly:true`, `sourceBacked:true`, `renderable:false`, and an explicit promotion blocker. Non-MSTUDIO streams therefore remain searchable source evidence without being silently upgraded to `jsfartArt` or rendered title art; `rjtd object-stream-candidates` also reports the same profile family/count for corpus sweeps.
- [x] Lock the `success_data-test` title-art do-not-render chain in regression tests. The model test now requires `sourcePaintRenderTrace`, `frontEraseVisibleProbeGate`, and `titleTextureGeometryRoleGate` to agree that the source white paint, RMS-improving all-visible texture probe, and bbox overlap evidence are all diagnostic-only and still blocked (`source-paint-present-but-render-fill-not-promoted`, `front-erase-visible-rms-improvement-is-not-knockout-proof`, and `texture-source-bbox-relation-is-bbox-only-not-knockout-proof`). This prevents a future patch from silently re-enabling gray overpaint or treating bbox overlap as front-face knockout authority.
- [ ] Decode the remaining exact `success_data-test` title-art paint semantics beyond the blocked source-order front-erase texture candidate, main-vs-shadow EmbeddedPress word5 gate, conservative `0x70.word0` shadow fill, source-path evenodd front fill / nonzero clip rule, and exposed word-column diagnostics. A source-order A/B that moved interstitial texture clipping from the shadow outline to the long-shadow side sweep was rejected because it worsened title-wide RMS `47.215 -> 49.337`, glyph RMS `67.671 -> 71.019`, and whole-page RMS `45.241 -> 45.655`. Promoting interstitial `0x70.word0=0x1c` as actual texture fill was also rejected (`page 1 title RMS 82.344 -> 85.162`, page 2 `84.680 -> 87.768`). Additional A/B probes were rejected on the active 144dpi crop set: skipping duplicate interstitial texture replay on the shadow/extrusion side worsened title RMS by `+5.866/+6.270`; forcing the white `0x82.word3` front paint to full opacity worsened title RMS by `+13.645/+15.460`; changing the hardcoded front fill from `#111111` to `#000000` slightly improved MAE but worsened title RMS by `+0.751/+0.574` and failed visual review; rendering texture paths as strokes instead of filled `TextureBezier` paths worsened title RMS by `+8.924/+7.911`. A render-preserving layer-tree diagnostic, `titleArtPaintStateSequence`, preserves the source-order paint-state runs with raw EmbeddedPress record offsets/payload words and inherited spans: `552` paths, `33` explicit transitions, path-kind runs `outline 0..10`, `texture 11..540`, `outline 541..551`, interstitial texture inherited spans `16,36,91,55,16,55,73,22,64,52,50`, path `11` shadow/front-erase `0x82.word5=0x2f` at offset `18780`, and path `541` main-outline `0x82.word5=0x10` at offset `94960`. The true meaning of full `0x46/0x48/0x60/0x65/0x70` state transitions, exact face/shadow fill colors, texture paint order, and front-face halftone semantics is still unresolved.
- [ ] Infer and attach style IDs from decoded `TextLayoutStyle`, `DocumentEditStyles`, `DocumentViewStyles`, or related streams to paragraphs and text runs during parsing. Initial analysis of `/DocumentEditStyles` in 11 tested samples reveals: (1) the stream header differs between samples (`0x2001` vs `0x2000` word 2); (2) UTF-16BE CJK pairs `0x7b2c/第`, `0x6761/条`, `0x9805/項`, `0x56f3/図` appear early, suggesting auto-numbering style templates; (3) ASCII `{n}` placeholders (`0x007b 0x0034 0x007d` = `{4}`) appear alongside these CJK terms; (4) `標準` (U+6A19 U+6E96) appears repeatedly at offsets 524+ as the only confirmed named style in the dominant run — the earlier CJK section likely encodes auto-number format strings, not style names. Stream structure and record boundaries are not decoded. `/TextLayoutStyle` cross-sample analysis: `sample-reference` samples have a rich 25-record list with law-doc styles (目次1–3, 本文, 本文2, 小見出し1–5, 中見出し, 大見出し, 個条書き, 個条書きの説明, 日付･署名, 文書ﾀｲﾄﾙ, plus generic named styles); `sample-outline`/`sample-draft` samples have 13 records — 2 labeled (脚注(標準)/脚注ｴﾘｱ(標準)), 7 anonymous (payloadPrefix `0003ffff000N` N=1–7), then 3 generic (標準;(Word文書), Default Paragraph Font, Normal Table) and 1 JTD-autostyle (小見出し5(ｵｰﾄｽﾀｲﾙ)); the anonymous records with `0xffff` and sequential N values may correspond to internal JTD style IDs. `sample-table-b` and `sample-table-c` are identical with 10 records including annotation/email styles; `sample-table` has 本文(ｵｰﾄｽﾀｲﾙ) at record 12. The `sample-draft-b` sample has only 3 TextLayoutStyle records (標準/Default/Normal Table) yet w7 takes values 0/2/3/4/6/10 — `w7` cannot be a direct TextLayoutStyle record index since records 3–9 are absent in that sample. The anonymous `0003ffff000N` records in `sample-outline`/`sample-draft` samples (N=1..7) may correspond to w7=1..7 as an internal numbering scheme, but the mapping is not proven, and at least w7=8 (附則) and w7=10 (理由) have no corresponding anonymous record in those samples. The relationship between TextLayoutStyle record index and w7 paragraph-style-ID remains unproven.
- [ ] Persist style edits back to decoded JTD style/body streams once the corresponding stream mutation format is proven.
- [ ] Add fixture-based expected JSON outputs once redistributable samples are available.
- [ ] Convert decompressed `.jttc` template text/control placeholders into meaningful model blocks once the inner `DocumentText` structure is understood.
- [ ] Replace the current text-only PDF/SVG layout with a real Ichitaro layout renderer once page geometry, tables, images, and style streams are decoded.
- [ ] Decode the remaining physical table geometry for the `success_data-test` abc table unit range `519..924`. The active projection now comes from source-derived `lineMarkPageGrid` evidence, visible column widths follow source line-header cell slot widths, and source-derived cell font size is gated on homogeneous line-header `fontSizeUnits`. The remaining proof should focus on cross-sample horizontal origin/total-width semantics and whether the trailing line-header candidates (`76..88`, `92..174`) plus source-backed top-text anchors determine visible table placement without using the reference PDF as render authority.
- [x] Re-check the `success_data-test` PDF when Chrome renders it but Preview.app appears blank. The current on-disk `openjtd-samples/pdf-output/ichitaro-20030228030923-success-002-success_data-test.pdf` keeps 2 pages, has no `com.apple.quarantine` xattr, has exactly one real `%%EOF`, and contains no `/Group`, `/S /Transparency`, `/SMask`, `/ExtGState`, `/Pattern`, or `/Shading` tokens. The same artifact renders nonblank through Poppler (`107617` page-1 non-white pixels at the current debug scale), Apple `sips` (`516x728`, `33056` non-white pixels), Swift PDFKit (`page 1:33056`, `page 2:30672`), CoreGraphics (`page 1:32036`, `page 2:27710`), and QuickLook thumbnailing (`725x1024`, `54897` non-white pixels). Add `local_pdf_output_artifacts_render_visible_content_with_macos_pdfkit_when_available` so the committed `openjtd-samples/pdf-output` artifacts themselves, not only freshly regenerated temp PDFs, are opened through PDFKit. Because PDFKit/CoreGraphics/QuickLook pass, treat direct Preview.app blankness as an app cache/UI-path issue unless it reproduces on a newly named regenerated file; continue using artifact-level PDFKit plus `sips`/CoreGraphics as the local Preview-family gate.
- [x] Scrub embedded CMap `%%EOF` comments during PDF export so Preview-like readers only see one real file EOF marker at the trailer. `svgs_to_pdf` now rewrites internal `%%EndResource\n%%EOF\nendstream` markers to same-length `%%EOD` comments while preserving the final `%%EOF`, and tests assert the exported PDFs contain exactly one `%%EOF`. Regenerated `success_data-test.pdf` keeps 2 pages, has `eof_count:1`, `eod_count:6`, zero `/Group`/`/S /Transparency`/`/SMask`/`/ExtGState`/`/Pattern`/`/Shading` tokens, renders through Apple `sips` at `516x728` with `34017` non-white pixels, and loads through PDFKit with page 1 `33056` non-white pixels.
- [x] Add explicit `/FormType 1` coverage to every SVG Form XObject written by the PDF wrapper and rebuild the local PDF artifacts. The regenerated `openjtd-samples/pdf-output` has same-stem PDFs for all 61 local `.jtd`/`.jtt`/`.jttc` sources, and every generated artifact now has matching `/Subtype /Form` and `/FormType 1` counts with exactly one final `%%EOF`; representative artifacts (`a5`, `a6`, `b6`, `tsaiten`, `success_data-test`, `shanai_lan`, `shinsyo`) have zero `/Group` and `/SMask` token hits. `rjtd-export` now includes a conditional on-disk artifact sweep so this Preview/PDFKit compatibility field cannot silently be missing from committed sample outputs.
- [x] Remove `success_data-test` ABC table reference-layout fallback from the visible render path while keeping the reference bbox diagnostics. The current table still renders because `source_derived_table_grid_overlay_layout` is promoted by the source page-space solver (`data-reference-backed="false"`, `data-source-derived-layout-candidate="true"`), but `success_data_test_table_grid_overlay_layout` is no longer allowed to make the table visible by itself. The same source-grid gate now prevents `success_data-test` top-text static slots without `/DocumentText+/LineMark+/PageMark` placement from rendering as `referenceFallback`; current SVG reports `data-source-grid-render-required="true"`, `data-source-grid-rendered-slot-count="21"`, `data-unbacked-slot-count="0"`, and `data-reference-fallback-rendered-count="0"`. Regenerated output remains visually unchanged on the active 144dpi table crop (`RMS 64.009`, changed ratio `0.159629`) and renders nonblank through Poppler and Apple `sips`.
- [x] Add source-only horizontal unit-transform readiness diagnostics to the `success_data-test` ABC table source-derived layout. `sourceDerivedLayoutCandidate.horizontalUnitTransformReadiness` now records the selected line-header basis (`matched-cells-plus-first-trailing-header`), unit range `0..88`, full extent `174`, width ratio `0.506`, row agreement `3/3`, slot widths `[14,14,16,16,16]`, trailing slot `[12]`, and the `uniform-intercell-gap-half` origin inset. This does not replace the reference bbox residual diagnostics and does not change PDF pixels; it locks the source evidence needed before removing the remaining ABC x/width calibration semantics.
- [x] Add a source-only `totalWidthSemanticsGate` inside the same ABC table `horizontalUnitTransformReadiness`. The gate separates the visible selected range (`88` units) from the full line-header extent (`174` units), records the trailing evidence (`86` trailing units, one included trailing header, trailing slot `[12]`), and keeps width promotion blocked as `source-total-width-semantics-unproven` because selected visible width and full document-row extent are not yet the same semantic. Rendering remains unchanged.
- [x] Add source-only `sourceTablePlacementCoherenceGate` diagnostics to `success_data-test` ABC table `topTextTableSourceGapEvidence`. The gate joins the nearest preceding full-width instruction anchor (`sourceGapAfterAnchorTextUnits:2`, shared extent/font) with the table line-header width candidates, proving that the current source evidence coherently supports the selected visible range `0..88`, first trailing header `76..88`, second trailing header `92..174`, full extent `174`, row agreement `3/3`, and exact contiguous `/LineMark` rows. It remains diagnostic-only and keeps render promotion blocked as `source-total-width-semantics-unproven`, so the renderer does not silently treat the full line-header extent as visible table width.
- [x] Add nested source-only `sourceTopTextPlacementReadinessGate` diagnostics under that same `sourceTablePlacementCoherenceGate`. The gate reuses the source-backed anchor/table evidence (`candidateSourceUnitRange:519..924`, selected range `0..88`, full extent `174`, exact `/LineMark` rows) and now reports `sourceTopTextPlacementReady:true` once the width and y-coupling sub-gates both pass. The older reference-backed residual remains diagnostic evidence, not render authority.
- [x] Add source-only `sourceTopTextPageGridCouplingGate` under the ABC table readiness gate. The gate proves the selected top-text instruction anchor and first table row share the same `/PageMark` entry and adjacent `/LineMark` records (`anchorLineMarkRecordIndex:15`, `firstTableLineMarkRecordIndex:16`, `lineMarkRecordGapAfterAnchor:1`), clearing the previous `page-space-y-origin-unproven` readiness blocker for this source-backed path.
- [x] Add source-only `sourceVisibleWidthVsFullExtentGate` under the same readiness gate. The gate classifies selected visible width `0..88` separately from full line extent `0..174`: first trailing header `76..88` closes the selected visible range, while second trailing header `92..174` is preserved as the full-line remainder. This clears the previous `source-total-width-semantics-unproven` blocker for the ABC top-text/table source-placement readiness without consulting reference/PDF fields.
- [x] Align ABC `horizontalUnitTransformReadiness` with `sourceFrameAdmissionGate`: once the source layout has stable line-header units, source column splits, exact `/LineMark` rows, and a `lineMarkPageGrid` origin, it now reports `pageSpaceUnitScaleDecoded:true` and clears its own `page-space-unit-scale-unproven` blocker. This is render-preserving and keeps the remaining reference bbox residuals as diagnostics only.
- [x] Split the ABC table `totalWidthSemanticsGate` blocker into an explicit next-gate state. When selected visible range evidence is source-ready (`selectedVisibleRangeSourceEvidenceReady:true`) but the full extent is still wider, the gate now records `sourcePlacementCoherenceGateRequired:true` and `renderPromotionNextGate:"source-table-placement-coherence-gate"`. Samples without trailing/header evidence stay on `renderPromotionNextGate:"source-total-width-semantics-decoder"`. Rendering remains unchanged.
- [x] Make that ABC `totalWidthSemanticsGate` handoff visible in the blocker string. When `sourcePlacementCoherenceGateRequired:true`, the gate now reports `renderPromotionBlockedReason:"source-table-placement-coherence-gate-required"` instead of the generic `source-total-width-semantics-unproven`; samples without trailing/header evidence still report the decoder blocker. This keeps the source-ready selected-width path distinct from truly undecoded width semantics.
- [x] Align ABC `sourceOnlyAxisAdmissionGate` with the active source-derived page-space solver. When the table is already renderable through source-backed line-header units, exact `/LineMark` rows, `lineMarkPageGrid` origin, and source y-origin solver, the gate now reports `activeSourceLayoutAdmissionReady:true` and `admissionReady:true` even if the selector-only fallback still lacks a horizontal selector or only has single y support. Those fallback facts are preserved as diagnostics instead of blockers, while `tsaiten` tables without a renderable active source layout remain blocked by fragmented or single-support y selector evidence.
- [x] Align the outer ABC `topTextTableSourceGapEvidence` with its nested source-only readiness gate. The nested `sourceTopTextPlacementReadinessGate` already proves that the instruction anchor, selected table width, trailing header semantics, and adjacent `/LineMark`/`/PageMark` grid coupling are source-ready, so the outer evidence now reports `sourceTopTextPlacementReady:true`, empty `readinessBlockedReasons`, contribution `source-top-text-placement-readiness`, and `renderPromotionBlockedReason:null` instead of the stale `page-space-y-origin-unproven` blocker. This is render-preserving and keeps reference bbox residuals diagnostic-only.
- [x] Mirror the ABC table top-text placement coherence into the horizontal `sourceFrameAdmissionGate`. The source-frame gate now carries `sourceTopTextPlacementCoherenceMirror` with `sourceTopTextPlacementEvidencePresent:true`, `sourceTopTextPlacementReady:true`, empty readiness blockers, and `renderPromotionBlockedReason:null`, tying the visible selected-width source frame (`page-body-frame` plus `documentTextLineHeaderUnitTransform`) directly to the source-only instruction-anchor/trailing-header/page-grid readiness chain instead of leaving that proof only under `topTextTableSourceGapEvidence`.
- [x] Close the ABC table `totalWidthSemanticsGate` handoff when that same source-only placement coherence is present. The width gate now exposes `sourcePlacementCoherenceGateEvidencePresent:true`, `sourcePlacementCoherenceGateResolved:true`, empty coherence blockers, `renderPromotionNextGate:null`, and `renderPromotionBlockedReason:null` for the selected visible range `0..88` while leaving `tsaiten` and other unresolved tables on their existing decoder/coherence blockers. This keeps the selected-width/full-extent split source-backed without pretending the raw PageMark horizontal fields are decoded.
- [x] Add render-preserving ABC table cell whitespace placement diagnostics. The 144dpi reference/generated crop shows the grid is close (generated grid about `2.5px` left, vertical lines within `~0.5px`) but cell glyph ink is typically `6..8.5px` too far right. The active SVG centers raw source text such as `"  １"` with `xml:space="preserve"`, so `whitespacePlacementProbe` now records per-cell leading/trailing whitespace counts, trimmed candidate text, and `centeredWhitespaceMayShiftInk`. SVG mirrors this with `data-whitespace-placement-probe`, `data-cell-text-centered-with-source-whitespace`, `data-render-text-leading-whitespace-chars`, and `data-render-trim-candidate-*`. A centered-cell trim render probe was rejected because it worsened the ABC crop RMS (`71.933 -> 72.565`), so rendering is intentionally unchanged (`abc_crop_rms_final 71.933`) and the trim candidate remains blocked as `table-cell-whitespace-position-semantics-unproven`.
- [x] Improve `success_data-test` ABC table cell text placement through source-derived font-unit diagnostics instead of trimming source whitespace. PDF-level A/B probes showed `font-weight:400` improved the active table crop (`126.606 -> 126.171`), moving the centered-cell baseline from `rowHeight*0.72` to `rowHeight*0.77` improved it further (`125.576`), and applying a left x adjustment equal to the source-derived font-unit stroke width (`-1.750px`, basis `documentTextLineHeaderFontUnitPxStrokeCompensation`) reduced mean glyph-center x residual from `+2.73px` to `+0.18px` with final crop RMS `125.508`. The layer tree and SVG now expose `cellTextXAdjustment`, `cellTextBaselineFactor`, and `cellTextFontWeight` plus their basis strings, while the trim candidate stays unpromoted.
- [x] Narrow that ABC table text metric adjustment to source-derived table layouts with homogeneous line-header font units. A scan of the current 8 PDF-backed local samples found no second visible source-derived centered table on the same path: `tsaiten` emits table group diagnostics but visible table text remains reference-backed, and the other PDF-backed samples do not emit `rjtd-column-grid-candidate` with source-derived visible text in the inspected PDF pages. Future source-derived tables without decoded font-unit metrics now fall back to the older centered baseline/weight instead of inheriting the ABC-specific measured correction.
- [x] Derive source-rendered table cell font size from homogeneous `/DocumentText` line-header `fontSizeUnits` instead of always using the global app font size. For the ABC table this keeps the rendered size at `13.300px` (`12` source units scaled to the app baseline) while exposing `cellTextFontSize`/`cellTextFontSizeBasis:"documentTextLineHeaderFontSizeUnitsScaledToAppFont"` in layer-tree and SVG metadata. Future source-derived tables with different homogeneous source font units no longer silently inherit ABC's visual font size.
- [x] Quarantine the `success_data-test` ABC hardcoded reference table bbox behind `diagnostic_reference_table_grid_overlay_layout` so generic production/reference fallback paths no longer receive the sample-calibrated ABC layout. `reference_table_grid_overlay_layout` now only exposes the non-ABC reference family (`tsaiten`) while ABC reference coordinates remain available to `referenceBBoxResidualEvidence`, top-text residual probes, and source/reference comparison diagnostics. Regression assertions prove `diagnostic_reference_table_grid_overlay_layout(...abc...)` stays present but `reference_table_grid_overlay_layout(...abc...)` is `None`, including a deliberately unproven source-layout variant. Regenerated `success_data-test.pdf` remains PDFKit-safe (`%%EOF` 1, `/Subtype /Form` 2, `/FormType` 2, no `/Group`/`/SMask`/`/ExtGState`/`/Pattern`/`/Shading`) and renders nonblank through Poppler and Apple `sips`.
- [x] Replace `success_data-test` formula frame vertical placement fallback with a source-grid anchor from the matching top text `/DocumentText+/LineMark+/PageMark` placement. The SVG now reports `data-frame-y-basis="topTextSourceGrid"` for all 4 JSEQ formula projections and no `lineAnchorFallback` usage. Also gate the cone diagram visible projection on nearby FDMText bbox corroboration from `/FigureData/main_data/FDMText`: the local sample renders only because 3 text bboxes match the FDMVector cone span, above the minimum 2-source threshold. Regenerated output remains PDFKit-safe (`/Group`, `/SMask`, `/ExtGState`, `/Pattern`, `/Shading` all 0), renders nonblank through Poppler (`103585` non-white pixels) and Apple `sips` (`35353` non-white pixels), and differs from the previous Poppler render by only `RMS 1.233502` / changed ratio `0.000668`.
- [x] Add a diagnostic-only `success_data-test` answer-sheet `sourceFrameCandidate` before replacing the hardcoded `answerSheetReferenceFrame`. It derives a y-scale/top candidate from sparse-table section anchors `５`/`６` and their `/LineMark+/PageMark` row tops: records `59` and `70` map local sheet y `205..377pt` to source y `471..702px`, yielding `sourcePxPerSheetPtY:1.343023`, derived frame top `195.680px`, and height `613.762px` versus the current reference frame top `190.674px` and height `609.358px`. The candidate is mirrored to SVG `data-source-frame-*` attributes, but rendering stays on the reference frame and is blocked as `answer-sheet-x-width-and-local-schema-source-fields-undecoded`.
- [x] Add a source-backed `pageMarkDisambiguationGate` to the same `success_data-test` answer-sheet `sourceFrameCandidate`. The layer tree and SVG now explicitly report that the section `５`/`６` anchors share the same `/PageMark` entry and page-index candidate (`disambiguationReady:true`, `disambiguationClass:"same-page-mark-entry-and-page-index-candidate"`), while keeping rendering on the reference frame and preserving the remaining blocker `answer-sheet-x-width-and-local-schema-source-fields-undecoded`.
- [x] Move the `success_data-test` answer-sheet local rule segment schema out of the SVG loop and expose it as `localRuleSchemaCandidate` in layer-tree/SVG diagnostics. The candidate records the source-backed sparse table facts (`39` rows, max `11` columns, `6` section anchors and their source ranges) separately from the still reference-backed local rule positions (`19` rule segments, `13` horizontal, `6` vertical, x positions `[0,27,168,237,307,445]`, y positions `[0,31,63,79,110,142,173,205,236,268,377,409,457]`). It explicitly keeps `xSchemaSourceBacked:false`, `yScaleSourceBacked:true`, and blocks render promotion as `answer-sheet-local-rule-schema-source-fields-undecoded`.
- [x] Add source-backed PageMark line-domain evidence to `tsaiten` cross-table row-boundary offset diagnostics. The combined previous-row-span `/LineMark` records `[7,9,11,13,21,23,25,27,32,34,36]` all fall inside `/PageMark` entry `0` (`pageLineStart:0`, `pageLineEnd:42`) with monotonic offsets from page start, and each related compact table reports per-table `pageMarkLineOffsetsFromEntryStart`. This proves page-domain ordering for the row-span record family while keeping render promotion blocked because the stable `-82` boundary offset still does not decode a page-y origin.
- [x] Add diagnostic-only PageMark u16 preview and page-line-gap y projection to the same `tsaiten` cross-table row-boundary offset evidence. The shared PageMark entry exposes `pageMarkU16FieldCount:137`, a 24-word preview beginning `[0,0,1,0,0,0,0,42,...]`, and a body-line-gap projection with pitch `23.310px` producing combined y tops `[235.167,281.786,328.405,375.024,561.500,608.119,654.738,701.357,817.905,864.524,911.143]`. This deliberately remains non-rendering because the projection does not decode the table y-origin (`page-line-gap-projection-does-not-decode-table-y-origin`).
- [x] Add a cross-table source-unit-to-PageMark-line affine fit diagnostic for `tsaiten`. Across all four compact table candidates, `rowSourceStartUnits` `[304,476,654,832,1135,1307,1467,1627,1857,2026,2170]` fit previous-row-span `/LineMark` record indexes with slope `0.016` and intercept `1.220`, but the residuals reach `1.706` record indexes, so a single source-unit-to-page-line transform is not exact and remains blocked as `source-unit-to-page-line-affine-fit-not-exact`.
- [x] Add row-level witnesses to that `sourceUnitToPageLineIndexFit` diagnostic so the non-exact residual pattern is visible without re-running external scripts. The largest current misses are table candidate `0` row `3` (`source 832`, actual record `13`, fitted `14.706`, residual `-1.706`) and table candidate `1` row `0` (`source 1135`, actual record `21`, fitted `19.618`, residual `1.382`), while table candidate `3` row `2` is closer (`source 2170`, actual `36`, fitted `36.394`, residual `-0.394`). This points the next y-origin work toward segment/table-family transitions rather than a single global source-unit affine transform.
- [x] Add diagnostic-only `sourceUnitToPageLineIndexPiecewiseFit` evidence beside the global affine fit. It groups the same previous-row-span `/LineMark` records by related compact table and fits `rowSourceStartUnits -> lineMarkRecordIndexes` per table, preserving piece residuals and transitions without changing rendering. The global fit remains non-exact, while per-table residuals shrink to table `0` max `0.027` and table `3` max `0.106`; transitions expose record gaps `8`, `2`, and `5` across the four related tables. This confirms the y-origin decoder needs table/segment transition semantics, not a single global source-unit affine transform, and remains blocked as `piecewise-fit-does-not-decode-page-y-origin`.
- [x] Add source-only `piecewiseRecordFamilyGapYDiagnostic` beside that fit so the selected-spacing and previous-row-span `/LineMark` families can be compared without reference PDF data. Across the `tsaiten` related table family, selected spacing records are consistently one record after the previous row-span records (`stableSelectedMinusPreviousRecordIndexGap:1`) with one PageMark line-pitch y delta (`23.310px`), and table-to-table transition record gaps match between the two families (`8`, `2`, `5`, delta `0`). This confirms the selected records encode post-row gaps while previous records encode compact row spans, but still blocks render promotion as `piecewise-family-gap-y-comparison-blocks-page-y-origin` because the observation does not yet decode an absolute page-y origin.
- [x] Add source-only `sourceOnlyPageMarkSlotScopedSubrecordYSequenceProbe` beside the `tsaiten` cross-table row-boundary diagnostics. It reuses the existing PageMark raw u16 subrecord slot grouping (`fieldIndex+tailBlock16WordIndex`) but compares candidates against source-derived PageMark line-domain y projections instead of reference table bboxes, records `referenceBBoxUsed:false`, `selectionReady:false`, and preserves the combined previous-row-span `/LineMark` sequence `[7,9,11,13,21,23,25,27,32,34,36]`. This narrows the next y-origin decoder to source-backed slot/line-domain evidence while explicitly blocking render promotion as `page-mark-source-y-slot-candidates-do-not-decode-page-y-origin`.
- [x] Add `sourceOnlyPageYOriginHypothesis` inside `sourcePageYTransformGate` so source-only y-origin evidence is summarized before promotion. The `tsaiten` scoring table now reports a `cross-table-page-line-domain` hypothesis from the shared PageMark line-domain/cross-table row-boundary evidence, while the lower error table reports a `line-mark-page-origin-stride` hypothesis from stride row tops. Both keep `referenceBBoxUsed:false`, `selectionReady:false`, and block as `source-page-y-origin-inference-pending`, making the remaining promotion blockers inspectable without hardcoding reference coordinates.
- [x] Add diagnostic-only `sourceOnlyPageYOriginCandidateAgreementGate` beside that hypothesis so y-origin candidates are grouped by source-derived `selectedY` plus row height before any render promotion. The `tsaiten` scoring table now shows a unique source-only agreement at `selectedY:235.167` / `rowHeight:23.310`, supported by the cross-table combined previous-row-span projection and table-0 previous-row-span y top, while the lower table preserves separate stride raw-record-index and stride-collapsed first-row candidates plus the blocked PageMark absolute-y slot support at `selectedY:768.000`. The gate records `referenceBBoxUsed:false`, keeps `selectionReady:false`, and blocks as `source-page-y-origin-agreement-unproven`.
- [x] Split cross-table previous-row-span y support from selector readiness so the agreement cannot be mistaken for an origin decoder. The scoring table now records `crossTablePreviousRowSpanSelectorPresent:true`, support count `5`, but `crossTablePreviousRowSpanSelectionReady:false` because the previous-row-span subrecord coverage is not complete, cross-table ordering regresses, offsets still require a transform, the piecewise fit is not exact, and no decoded page-y origin exists. The agreement gate mirrors this as `crossTablePreviousRowSpanUniqueBestSupported:true` but `crossTablePreviousRowSpanReady:false` with blocker reasons `cross-table-row-boundary-offset-transform-required` and `page-line-gap-projection-does-not-decode-table-y-origin`.
- [x] Add `sourceOnlyPageYOriginDomainGate` so the same `235.167` agreement is explicitly classified as PageMark/LineMark line-domain evidence rather than a page-space table origin. The scoring table now records `crossTableLineDomainRecordCount:11`, `piecewiseTransitionRecordGaps:[8,2,5]`, stable selected-minus-previous gap `1` / `23.310px`, and `pageSpaceOriginDecoded:false`, with blockers `line-domain-to-page-space-origin-transform-required`, `table-family-transition-semantics-undecoded`, and `source-page-y-line-domain-not-page-space-origin`.
- [x] Add a source-only reference-calibration replacement gate to table page-space diagnostics so `tsaiten` now reports exactly which axes can replace the reference table bbox. The scoring table reports all replacement gates false because the source-derived layout candidate is absent; the lower error table reports internal column/row evidence as ready (`sourceColumnSplitReady:true`, `rowHeightSolverReady:true`) but keeps full page-space replacement blocked (`pageSpaceHorizontalTransformReady:false`, `yOriginSolverReady:false`) with `table-horizontal-page-space-transform-incomplete` and `source-page-y-transform-not-decoded`. Regression coverage also locks the lower table's source-only horizontal unit transform (`matched-cells`, `0..100` units, slot widths `[20,80]`) while keeping `tsaitenReferenceProjection` rendering unchanged.
- [x] Add a dedicated source-only `pageSpaceHorizontalTransformGate` under `table-page-space-v1` so internal column readiness cannot be confused with decoded page-space x/width placement. The `tsaiten` scoring table now reports the source layout candidate absent, while the lower table reports `sourceColumnSplitReady:true`, `xUnitAllRowsAgree:true`, `fullExtentUnitsPresent:true`, `xUnitRange:0..100`, and `fullExtentUnits:144`, but still blocks with `sourceFrameDecoded:false`, `page-space-horizontal-frame-not-decoded`, and `line-mark-record-stride-to-page-y-transform-unproven`. This preserves the lower table's useful line-header unit evidence without promoting the hardcoded/reference bbox.
- [x] Add diagnostic-only `sourceFrameHypotheses` under the same horizontal gate so candidate page frames can be compared without using the reference PDF as an admission rule. For the `tsaiten` lower table, the page-body frame hypothesis yields `selectedX:72.000` / `selectedWidth:451.181`, and the media-box hypothesis yields `selectedX:0.000` / `selectedWidth:551.181`; neither explains the current reference bbox `x=174,width=554`, so both remain blocked as `page-body-frame-not-proven-for-table` and `page-media-box-not-proven-for-table`. This makes the next task concrete: find the missing source-backed horizontal frame/inset before removing `tsaiten_table_grid_overlay_layout`.
- [x] Carry `PageMark` selected u16 fields from stride-only source layouts into `originResidualEvidence` instead of reporting `pageMarkSelectedFields:null`. The `tsaiten` lower table now exposes `word14=194` and `word21=564` beside the reference bbox, plus diagnostic-only `pageMarkRawFieldReferenceComparison`: `word14-referenceX=20px` equals one first column slot (`20` units), and `word21-referenceWidth=9.999px` equals half of that slot. This still remains reference-backed and blocked as `page-mark-raw-horizontal-field-semantics-unproven`, but it narrows the next source-backed x/width decoder to the `PageMark` raw horizontal fields plus line-header slot semantics rather than opaque sample constants.
- [x] Add source-only `PageMark` raw-field hypotheses to `pageSpaceHorizontalTransformGate.sourceFrameHypotheses`. For the `tsaiten` lower table, `page-mark-word14-word21-direct` yields `selectedX:194.000` / `selectedWidth:564.000`, while the line-header first-slot adjusted candidate yields `selectedX:174.000` / `selectedWidth:554.000` from `word14 - firstColumnSlotUnits` and `word21 - firstColumnSlotUnits/2`. This candidate is not promoted and remains blocked as `page-mark-raw-horizontal-field-semantics-unproven`; the next proof must validate the same formula against the scoring table or another sample before it can replace reference calibration.
- [x] Generalize the `tsaiten` horizontal PageMark field probe across the visible table family without promoting it to rendering. `sourceDerivedHorizontalFieldAdjustmentProbe` now scans selected PageMark fields `10/13/14/15/16/17/18/19/20/21` against each table reference bbox x/width/right. The scoring table exposes `word15=423` as the best direct width field for reference width `421` (`2px` residual) even though its source layout candidate is absent, while the lower table keeps the line-header slot-adjusted `word14 -> x` and `word21 -> width` matches (`0.000px` and `0.001px` residuals). This confirms the horizontal decoder likely needs multiple PageMark fields plus line-header slot semantics, not a fixed `word14/word21` rule.
- [x] Add a source-only cross-table horizontal field consensus diagnostic for the same `tsaiten` table family. `sourceOnlyHorizontalFieldConsensus` now uses sparse sibling source-derived layouts `[1,2,3]` to prove a stable unit frame (`firstColumnSlotUnits:20`, `firstMatchedCellSpanUnits:16`, `firstIntercellGapUnits:4`, `xUnitRange:0..100`, `fullExtentUnits:144`) and applies it to the shared PageMark fields without consulting the reference bbox. For the scoring table this yields source-only frame hypotheses `word14 - firstSlot => x=174`, `word15 - halfGap => width=421`, and `word21 - halfSlot => width=554`; for the lower table the same consensus preserves the `word21 - halfSlot` width candidate. Rendering still stays blocked as `cross-table-horizontal-field-semantics-unproven`, but the remaining decoder work is now to choose width-field semantics per table family rather than recover opaque page coordinates.
- [x] Add a source-only `sourceWidthFieldRoleGate` to the `tsaiten` horizontal selector so the current width-field split is explicit instead of hidden inside the chosen frame basis. The scoring table now reports the 3-column selector as `word15 - halfGap` (`selectedWidthWordIndex:15`, role `compact-three-column-visible-width`), while the lower 2-column table reports `word21 - halfSlot` (`selectedWidthWordIndex:21`, role `compact-two-column-visible-width`). The gate remains diagnostic-only and blocked as `width-field-role-semantics-needs-cross-sample-validation`, preserving the reference fallback until the column-count/field-role rule is validated beyond this table family.
- [x] Mirror the same source-only cross-table horizontal consensus into `pageSpaceHorizontalTransformGate.sourceFrameHypotheses` so the scoring table exposes `x=174,width=421` candidates inside the actual horizontal promotion gate even when its own source-derived layout is absent. The lower table now keeps the existing body/media/PageMark raw hypotheses and appends the same consensus candidates, all still blocked as `cross-table-horizontal-field-semantics-unproven`.
- [x] Add `sourceFrameCandidateAgreementGate` under the `tsaiten` horizontal transform gate to separate candidate agreement from render admission. The scoring table now explicitly reports no unique source-only x/width agreement (`3` candidates, `3` groups, best support `1`), while the lower table reports a unique `x=174,width=554` agreement supported by both `page-mark-word14-word21-first-slot-adjusted` and `page-mark-word14-first-slot-word21-half-slot`. The gate still keeps `selectionReady:false` and blocks as `source-horizontal-field-semantics-unproven`, so this narrows the decoder without silently replacing the reference layout.
- [x] Add a diagnostic-only `sourceOnlyHorizontalFieldSelector` beside the same consensus. It uses only PageMark fields, sparse-sibling source unit frames, and compact table topology: the scoring table selects `page-mark-word14-first-slot-word15-half-gap` (`x=174,width=421`) for the 3-column family, while the lower table selects `page-mark-word14-first-slot-word21-half-slot` (`x=174,width=554`) for the 2-column family. The selector records `referenceBBoxUsedForSelection:false`, keeps `selectionReady:false`, and remains blocked as `cross-table-horizontal-field-semantics-unproven`.
- [x] Feed the topology selector back into `sourceFrameCandidateAgreementGate` as source-only support instead of leaving it as isolated JSON. The scoring table now has a unique best horizontal agreement for `x=174,width=421` supported by the consensus candidate plus the topology selector, and the lower table has `x=174,width=554` supported by the raw PageMark hypothesis, consensus candidate, and topology selector. `selectionReady` remains false and rendering still blocks on unresolved horizontal semantics plus the separate page-y transform gate.
- [x] Add source-only `sourceGapToPageLineGapReadinessHints` to the `tsaiten` page-y gates before any transition-based y-origin promotion. The hints summarize the same `/PageMark` transition evidence in both `sourceOnlyPageYOriginHypothesis` and `transitionSemanticsReadiness`: all 3 transitions stay within the same PageMark entry, but direct source-range gaps miss page-line gaps by up to `182` units, row-source-start gaps by `295`, and the best segment-offset candidate still misses by `105` with ratio spread `29.875`. The scoring-table hypothesis now carries `source-gap-to-page-line-gap-transform-not-stable` as an explicit blocker while rendering remains reference-calibrated.
- [x] Add `absoluteVsSpanLineageGate` under `pageMarkScopedYTransformProbe` so reference-backed absolute table-top probes and source-backed subrecord span probes are correlated in one place without promoting either as a decoded page-y origin. The `tsaiten` scoring table now records reference top `301.005` with selected post-row-gap spans `[65,65,65,67]` and previous/compact spans `[107,113,113,113]`; the lower table records reference top `768.014` with selected spans `[58,52,54]` and previous/compact spans `[111,92,91]`. Both keep `sourceOnlyAbsoluteTopDecoded:false` and block promotion as `absolute-top-evidence-reference-backed-span-evidence-non-positional`.
- [x] Add `yOriginReadinessClass` to `sourceOnlyPageYOriginHypothesis` so source-only y candidates are separated before any page-space promotion. The `tsaiten` scoring table is now classified as `cross-table-line-domain-only` with blockers for line-domain-to-page-space transform, non-exact piecewise fit, source/subrecord order regression, and missing decoded page-y origin. The lower table is classified as `stride-only` with explicit blockers `line-mark-page-origin-stride-present`, `stride-origin-needs-direct-line-origin-rule`, `direct-line-mark-page-origin-absent`, and `decoded-page-y-origin-missing`. `originDecisionReady` remains false in both cases.
- [x] Add table-family coverage diagnostics to `sourceOnlyPageYOriginCandidateAgreementGate` so source-only y-origin agreement cannot look stronger than it is. The `tsaiten` scoring table's current best group is unique, but it covers only table candidate `[0]` out of the four related table candidates (`bestSupportedTableCandidateCount:1`, `bestSupportedCoversMultipleTableCandidates:false`, `crossTablePreviousRowSpanBestGroupCoversMultipleTables:false`, `crossTablePreviousRowSpanBestGroupTableCoverageRatio:0.250`). The gate now exposes `crossTablePreviousRowSpanTableCandidateIndexes:[0,1,2,3]` and `crossTablePreviousRowSpanSupportFragmentedByTable:true`, then blocks with `source-page-y-origin-best-support-not-cross-table` and `cross-table-previous-row-span-support-fragmented-by-table` before any reference-calibration replacement.
- [x] Add a diagnostic-only `sourceOnlyPageYOriginSelector` to the same `tsaiten` y agreement gate. It deterministically selects the best-supported source-only y group using `tableCandidateIndex` plus origin-basis evidence, not reference bbox coordinates: scoring table candidate `0` selects `cross-table-combined-previous-row-span-first-record` at `selectedY:235.087`, `selectedRowHeight:23.298`, with support from table `[0]` only. The selector keeps `selectionReady:false` and blocks as `cross-table-previous-row-span-support-fragmented-by-table`, making the eventual renderer admission rule explicit without promoting the near-reference coincidence.
- [x] Extend that y selector with a single-support diagnostic fallback for `tsaiten` lower-table candidates. When no multi-support agreement exists, the selector still records the strongest source-only hypothesis instead of returning `null`: lower table candidate `3` now selects the PageMark absolute slot candidate `page-mark-absolute-y-slot-field2-tail-block16-word11` at `selectedY:768.000`. It remains non-rendering with `singleSupportFallback:true`, `selectionReady:false`, and blockers `page-mark-absolute-y-slot-semantics-unproven` plus `single-source-y-origin-support-unproven`.
- [x] Keep the PageMark absolute-slot y fallback candidate-local instead of leaking it onto `fallbackTextAnchors` diagnostic tables. The `tsaiten` middle two-row candidates now fall back to their own raw `/LineMark` stride y (`534.000` and `618.000`) while only the lower table with `lineMarkPageGridStrideRawRecordIndex` authority selects the PageMark absolute slot at `768.000`.
- [x] Prefer source-derived table fallback geometry before `tsaiten` reference constants in `table_grid_overlay_layout`. This does not promote unproven rendering, but it removes the remaining reference-first fallback path for diagnostic bbox generation: when a source-derived layout exists, fallback geometry now uses that source bbox/row height/column width and only falls back to `tsaiten_table_grid_overlay_layout` when source evidence is absent. The `success_data-test` unproven-source regression fixes this precedence without changing visible reference projection gates.
- [x] Add `sourceOnlyAxisAdmissionGate` to the `tsaiten` table page-space solver so source-only x/width agreement and source-only y-origin selection are evaluated together before any reference calibration removal. The scoring table now preserves the unique horizontal agreement at `x=174,width=421` while recording that the y selector is fragmented across only table candidate `[0]`; the lower table preserves the unique horizontal agreement at `x=174,width=554` while recording that the y selector is a single-support PageMark absolute-slot fallback at `selectedY=768`. Both axes remain non-rendering and blocked as `source-page-space-axis-selector-coupling-unproven`.
- [x] Harden `tsaiten` `sourceOnlyAxisAdmissionGate` so a selected source-only y candidate is not confused with render admissibility. The gate now emits `ySelectorSupportCount`, `ySelectorCrossTableSupportPresent`, `ySelectorAgreementAdmissible`, and `ySelectorAdmissionBlockedReason`: the scoring table is explicitly blocked as `source-y-origin-selector-fragmented-by-table-not-render-admissible`, while the lower table's PageMark absolute-slot fallback is blocked as `source-y-origin-selector-single-support-fallback-not-render-admissible`.
- [x] Add `sourceOnlyProjectionDomainGate` inside the `tsaiten` line-domain plus post-row-gap projection probe. The parent probe may still compare against the reference PDF for diagnostics, but the nested gate is source-backed only (`referenceBacked:false`) and records the actual blockers: mixed line-domain/subrecord-gap units, selected records being post-row-gap records, incomplete or non-ordered-unique span coverage, and missing page-y transform. This keeps the near-reference scoring-table projection (`235.087 + 65 -> 300.087`) from looking like render authority.
- [x] Make `sourceOnlyPageMarkAbsoluteYSlotGate` report source disagreement instead of a generic hardcoded blocker. The gate now computes the best PageMark absolute-y slot against the line-domain plus post-row-gap projection and only clears `page-mark-absolute-y-slot-semantics-unproven` when those source candidates agree. Current `tsaiten` candidates disagree (`875.539` projected vs `768.000` absolute slot on the lower table), so rendering remains blocked with the more specific `line-domain-projection-disagrees-with-page-mark-absolute-y-slot`.
- [x] Carry that PageMark absolute-slot disagreement into the source-only y selector supports as well as the dedicated gate. The lower `tsaiten` table still records `selectedY:768.000` as the strongest single-support diagnostic, but its `sourceOnlyPageYOriginSelector` and agreement group now include `line-domain-projection-disagrees-with-page-mark-absolute-y-slot` next to `page-mark-absolute-y-slot-semantics-unproven`. This keeps the raw `768` source value from being promoted later without also satisfying the line-domain agreement path.
- [x] Mirror source-gap-to-page-line transform readiness into both `sourceOnlyPageYOriginDomainGate` and `sourceOnlyPageYRenderAdmissionGate`. The new `sourceGapToPageLineGapTransformAdmissionGate` is source-backed only (`referenceBacked:false`) and reports `transformDomain:"source-unit-gap-to-page-mark-line-index-gap"`, `canDecodeSourceTransform:false`, `tableFamilyTransformStable:false`, best candidate `segment-offset-gap`, max delta `105`, and blockers `source-gap-to-page-line-gap-transform-not-stable`, `source-gap-to-page-line-gap-transform-unstable-across-table-family`, and `source-gap-to-page-line-gap-transform-undecoded`. This keeps the render admission path tied to the same transition evidence instead of relying on the reference table bbox.
- [x] Mirror the same y-support blockers and source-gap transform gate into `sourceOnlyAxisAdmissionGate` so the combined x/y selector cannot look cleaner than the y path it depends on. The `tsaiten` scoring table axis gate now carries the fragmented cross-table blockers plus the transform gate, while the lower table carries the PageMark absolute-slot disagreement, residual `107.539`, selected raw slot `768.000`, and source-gap blockers. Axis coupling remains blocked as `source-page-space-axis-selector-coupling-unproven`.
- [x] Enrich `sourceGapToPageLineGapReadinessHints` and `sourceGapToPageLineGapTransformAdmissionGate` with candidate-level transform taxonomy. The gates now expose candidate count, exact-match count, best-candidate transition coverage, best-candidate spread, lowest-spread candidate, full candidate summaries, and declined transform candidates. Current `tsaiten` evidence shows best max-delta candidate `segment-offset-gap` (`105`) but lowest-spread candidate `direct-source-range-gap` (`12.250`), so no single metric is enough to promote the transform.
- [x] Add diagnostic-only `affineRowSourceStartGapFit` to the same source-gap transform gates. The fit is exact rational source evidence scoped to one PageMark/table family: `tsaiten` emits `slope=143/6`, `intercept=671/6`, `maxAbsResidual:1.000`, `sampleCount:3`, `familyScoped:true`, and `fitStable:true`, while the transform candidate is always declined with `affine-row-source-start-gap-family-transform-authority-unproven`. It raises the diagnostic candidate count but leaves `canDecodeSourceTransform:false`, `admissionReady:false`, and all render blockers unchanged.
- [x] Add diagnostic-only `sourceOnlyAxisCandidateBBox` under `sourceOnlyAxisAdmissionGate`. This combines the best source-only horizontal selector with the selected source-only y candidate without using reference bboxes: scoring table emits `x=174,y=235.087,width=421,height=93.192`, lower table emits `x=174,y=768,width=554,height=63`. Both remain `referenceBacked:false`, `selectionReady:false`, and blocked as `source-page-space-axis-selector-coupling-unproven`, providing a concrete comparison target before replacing `tsaiten` reference calibration.
- [x] Add `sourceOnlyPageYRenderAdmissionGate` under `sourcePageYTransformGate` so reference-fallback removal has a single source-only y admission contract. The gate emits `admissionReady:true` for the already source-rendered ABC table (`lineMarkPageGrid` origin, exact `/LineMark` rows, decoded y solver) even when selector fallback diagnostics remain non-authoritative. For `tsaiten`, it keeps rendering blocked without consulting reference coordinates: the scoring table is blocked by missing direct origin, cross-table line-domain evidence that is not page-space origin, fragmented y selector support, source/subrecord ordering contradiction, and PageMark absolute-slot disagreement; the lower table is blocked by stride-only origin, non-exact line rows, single-support y fallback, non-unique selected post-row-gap coverage, and the same PageMark disagreement.
- [x] Lock the local PAGE01 right/down probe family as diagnostic-only admission evidence. The right-tick files show linear `/DocumentText` line-header X deltas (`firstCellOffsetDelta=2/4/6/8`) but still keep `sourceOnlyAxisAdmissionGate.admissionReady:false` because the horizontal selector and x/y coupling are not render-admissible. The down files expose `/LineMark` stride correlation, but the editor cannot isolate table-only Y movement without pushing following text, so the layer tree keeps `sourceOnlyPageYAdmissionClass:"flow-y-stride-only-diagnostic"`, `pageOriginAuthority:"fallbackTextAnchors"`, and `sourceOnlyPageYRenderAdmissionGate.admissionReady:false`. These samples therefore lock the blocker shape and do not justify source-only render promotion.
- [x] Expose a `referenceFallbackAdmissionGate` on table layer JSON and SVG diagnostics so the legacy reference-table fallback can be audited from the same source-only admission contract. The gate reuses the existing visible-render boolean: `success_data-test` ABC tables report `referenceFallbackAllowed:false`, `referenceFallbackUsed:false`, `sourceOnlyPageYAdmissionReady:true`, and `blockedReason:"active-source-layout-admission-suppresses-reference-fallback"` while still rendering from the source-derived layout. Current `tsaiten` visible tables continue to use reference fallback (`referenceFallbackAllowed:true`, two `referenceFallbackUsed:true` SVG groups), but now carry `sourceReplacementBlockedReason` values such as `source-derived-layout-not-renderable` and `source-page-y-render-admission-not-ready` so the remaining removal work is explicit without changing output.
- [x] Remove `tsaiten_table_grid_overlay_layout` from the generic `table_grid_overlay_layout` fallback path while keeping explicit `reference_table_grid_overlay_layout` / diagnostic reference probes intact. This makes the visible `tsaiten` calibration a legacy reference-only path instead of a silent default bbox source for unrelated generic table diagnostics. The `tsaiten` regression now proves `render_page_svg` still exposes `tsaitenReferenceProjection`, while direct `table_grid_overlay_layout` for the scoring table falls back to the generic page body frame instead of `(174,301,421,32.2)` reference constants.
- [x] Mark `pageMarkScopedYTransformProbe` as a reference-target diagnostic at its own top level. The probe now emits `referenceBBoxUsed:true`, `referenceTargetBasis:"referenceTableBBox.rowTopTargets"`, and `sourceOnlyReplacementBlockedReason:"page-mark-scoped-y-transform-targets-reference-backed"` immediately beside its source-backed PageMark/LineMark record matches. `tsaiten` assertions lock this split for candidate `0` (`lineMarkRecordIndexes:[8,10,12,14]`) and candidate `3` (`[33,35,37]`): the scoped probe may still compare against reference row tops, but source-only render admission must continue through `sourceOnlyPageYOriginSelector`, `sourceOnlyPageMarkAbsoluteYSlotGate`, and `lineHeaderLineMarkCouplingEvidence` instead.
- [ ] Generalize table/page-grid geometry on `tsaiten` before removing its reference table calibration. Its lower error table now carries the best raw-record-index stride-Y candidate as the representative source-derived layout, but the sparse-sibling-derived candidate must stay render-ineligible as `sparse-sibling-derived-candidate-render-ineligible`; X/width semantics and the non-reference page-Y transform are still unproven. The scoring table has compact-to-sparse source topology evidence, but record-index affine diagnostics show the raw `/LineMark` stride slope is too steep for the reference row tops, and `lineMarkRowGapSequenceEvidence` shows selected records are post-row spacing records while immediately previous records are compact row spans. The new per-family row-source-start affine diagnostic records the real `tsaiten` transform candidate (`x = 143/6*y + 671/6`, residual `<= 1`) and is independently consistent with the decoded `success_data-test` ABC table (`1/3` residual), but `hyo` contradicts the `tsaiten` formula by `51.667` and the probe corpus fits a different family (`1103/17*y + 276/17`), so `affine-row-source-start-gap-family-transform-authority-unproven` keeps it non-rendering. Worker-10's PageMark slot sweep also blocks broad raw-slot promotion: the lower `tsaiten` table's `768.000` slot matches reference top `768.014`, but the scoring table exposes raw slot `1024.000` against reference top `301.005`, and the ABC table has no same slot. The alternate previous-row-span PageMark probe also fails to produce same-header y fields; `sourcePageYTransformGate` now proves the lower table's PageMark span hits are not row-unique (`[174,734,174]`), source-range coverage is incomplete (`3/4` scoring rows, `0/3` lower rows), and cross-table ordering under the shared sparse sibling regresses (`rawScanIndexes:[3,3,3,2,3,2,2,6,2]`). Therefore span correlation still cannot serve as a page-y origin decoder. Derive the missing scoring-table page-space transform from additional source fields, validate any y transform across both visible table families, and keep `/PaperMark` semantics diagnostic-only until the evidence is stable enough to replace `tsaiten_table_grid_overlay_layout`.
- [ ] Promote Q4/Q5 FDM primitive ownership only after FDMIndex row ownership and paint-order semantics explain the new `primitiveOwnershipComparison` role candidates without contradicting source cohorts or reference PDFs.
- [x] Add source-only `paintOrderContinuityProfile` diagnostics to Q4/Q5 FDM primitive role groups without changing rendering. The Q4 `main-circle-anchor` role spans command offsets `308..504` but has `4` interleaved non-role commands (`commandCountInSpan:7`, continuity score `0.429`), while the Q5 `line-candidate` role spans `1992..2024` with no interleaved commands (`commandCountInSpan:2`, score `1.000`). Both remain `ownershipProven:false`; interleaved spans block as `role-span-interleaved-non-role-commands`, while contiguous spans stay non-rendering as `role-paint-order-authority-unproven` until FDMIndex row order is proven as paint-order authority.
- [x] Add an explicit source-backed `ownershipGate` inside Q4/Q5 `primitiveOwnershipComparison` diagnostics. The gate records row-command direction, one-to-one row mapping, multi-command single-row evidence, raw/segment cohort mixing, p95 command-offset gaps, and active row/index ownership blockers without changing rendering. Q4 remains blocked by `mixed-raw-and-segment-cohorts` (`rowCommandGapP95:240.000`), while Q5 records `multi-command-single-index-row`, `mixed-raw-and-segment-cohorts`, and `row-command-reference-not-one-to-one` (`rowCommandGapP95:132.000`); role and paint-order uncertainty stays in the role/profile/admission gates instead of this ownership blocker list.
- [x] Add a source-backed `offsetFieldAuthorityGate` inside the same Q4/Q5 `primitiveOwnershipComparison` diagnostics. The gate separates FDMIndex `bbox.left` references that match command-relative offsets from those that match source-segment-relative offsets, records raw/segment command cohorts, and keeps render promotion blocked as `fdm-index-offset-field-authority-mixed-command-and-segment-fields`. Q4 currently has `18` command-relative and `2` source-segment references; Q5 has `1` command-relative and `6` source-segment references, so neither can be promoted until the authoritative offset namespace is decoded.
- [x] Add a source-backed `rowFanoutSegmentOwnerGate` for Q4/Q5 FDM primitive ownership. Q4 is now explicitly classified as `20` row references over `20` unique rows with no fanout (`maxRowFanout:1`) but mixed offset namespaces, while Q5 records fanout rows `[40,41]`, `fanoutRowCount:2`, `fanoutReferenceCount:6`, `maxRowFanout:4`, and `fanoutRowsUseSourceSegmentOffsetFields:true`. Rendering remains blocked as `fdm-index-row-fanout-segment-owner-multi-command-single-row` for Q5 and `fdm-index-row-fanout-segment-owner-offset-namespace-mixed` for Q4.
- [x] Add source-backed role-level `roleFanoutSegmentOwnerGate` diagnostics inside each Q4/Q5 `indexRowReferenceRoleCandidateGroups` entry. The Q5 `line-candidate` role now exposes its own fanout blocker: references `[1992,2024]` both map to FDMIndex row `40`, `fanoutRowCount:1`, `maxRowFanout:2`, `fanoutRowsUseSourceSegmentOffsetFields:true`, and `renderPromotionBlockedReason:"fdm-index-role-row-fanout-multi-command-single-row"`. This narrows the remaining ownership work from projection-level fanout to the specific role candidates that would otherwise be tempting to render.
- [x] Add a source-backed `primitiveOwnershipAdmissionGate` inside Q4/Q5 `primitiveOwnershipComparison` diagnostics. The gate aggregates `ownershipGate`, `offsetFieldAuthorityGate`, `rowFanoutSegmentOwnerGate`, role-level `roleFanoutSegmentOwnerGate`, and unresolved paint-order continuity into one render-admission blocker list before any primitive ownership promotion. Q4 remains blocked by mixed raw/segment cohorts, mixed offset namespaces, missing valid vector-offset role references, and paint-order continuity (`roleGroupCount:6`); Q5 remains blocked by multi-command single-row ownership, row fanout, role fanout, missing valid vector-offset role references, and paint-order continuity (`roleGroupCount:4`, `roleFanoutBlockedGroupCount:3`). The admission list now carries paint-order uncertainty through the specific `role-paint-order-continuity-unproven` blocker rather than duplicating a generic primitive-role blocker from `ownershipGate`.
- [x] Split Q4/Q5 role paint-order diagnostics into source-span continuity blockers and authority-pending candidates. `paintOrderContinuityProfile` now reports `spanContiguousCandidate`, `continuityBlocked`, and `paintOrderAuthorityPending`; interleaved roles block as `role-span-interleaved-non-role-commands`, while contiguous roles stay non-rendering as `role-paint-order-authority-unproven`. Q4 still has 6 continuity-blocked roles, but Q5 solid-diagram now separates 2 continuity-blocked roles (`arc-candidate`, `connector-candidate`) from 2 authority-pending roles (`line-candidate`, `surface-boundary-candidate`) instead of treating all 4 as continuity failures.
- [x] Replace the generic `indexRowOrderPromotionGate` primitive-role blocker with source-backed row-order blocker lists. Q4 now records `fdm-index-row-order-valid-vector-offset-missing`, mixed offset namespace, and role paint-order continuity blockers while preserving its monotonic one-to-one row-command evidence; Q5 records the stricter row-order failures first (`fdm-index-row-order-reference-not-one-to-one`, `fdm-index-row-order-single-row-backs-multiple-commands`) before valid-vector, namespace, continuity, and authority blockers. SVG diagnostics mirror the same `data-index-row-order-render-promotion-blocked-reasons` without changing visible rendering.
- [x] Add role-level `roleVectorOffsetAuthorityGate` diagnostics before the Q4/Q5 role fanout gates. The gate records that the current role references are source-backed through FDMIndex offset fields, but all matching role references still have invalid `FDMIndex.vectorOffset` values (`validVectorOffsetReferenceCount:0`, `allReferencesHaveInvalidVectorOffset:true`). The top-level admission gate now carries the more specific blocker `fdm-index-role-vector-offset-authority-valid-vector-offset-missing` before the legacy `fdm-index-role-valid-vector-offset-missing`, so future work must prove which FDMIndex field is authoritative before using role references as render ownership.
- [x] Add source-backed FDMIndex segment membership gates to `shanai_lan` same-row axis-rule connector diagnostics. Each `fdmConnectorCandidateDiagnostic` now preserves `sourceVectorRelativeOffset` and `sourceSegment`, and each row cohort exposes `fdmIndexSegmentGate` with FDMIndex vector offset/length, source-segment match counts, image signature counts, and dual-endpoint image-bearing counts. The current leading row 33 maps to vector offset `142284`, length `7222`, `3` image signatures, `7` dual-endpoint candidates, and `7` dual image-bearing connectors, so rendering remains blocked as `fdm-index-image-bearing-segment-role-unproven`; non-image-bearing rows `3/0/37/2/10` remain blocked as `fdm-index-segment-ownership-and-paint-order-unproven`.
- [x] Add a source-backed FDMIndex bbox relation gate to the same `shanai_lan` row cohorts. `fdmIndexBboxRelationGate` compares each connector command source bbox with the owning FDMIndex bbox and separates contained/internal strokes from disjoint external-connector candidates without drawing anything. Current evidence: row 37 has all `3` dual-endpoint connectors contained inside its FDMIndex bbox and is blocked as `fdm-index-bbox-contained-internal-stroke-role-unproven`; rows 3, 0, 2, and 10 have all dual-endpoint connectors disjoint from the FDMIndex bbox and stay blocked as `fdm-index-bbox-disjoint-connector-ownership-unproven`; row 33 remains blocked earlier as image-bearing despite disjoint connector bboxes.
- [x] Add a diagnostic-only FDMIndex connector-composition gate for the same `shanai_lan` row cohorts before attempting any visible wiring promotion. `fdmIndexConnectorCompositionGate` now aggregates parsed `vectorCommandCount`, `connectorCandidateCount`, non-connector command count, connector density, row-cohort matched coverage, and row-cohort dual-endpoint coverage from `ObjectFdmIndexEntryCandidate` plus the current row-cohort counts. Current evidence keeps rendering unpromoted: row 33 is still image-bearing (`85` commands, `14` connector candidates, density `164` permille, dual coverage `500`), rows 3/0/2/10 stay disjoint ownership/paint-order candidates, and row 37 remains contained/internal. Row 0 explicitly proves coverage must use the row-cohort connector denominator (`24`) rather than the first FDMIndex entry connector count (`11`).
- [x] Add a source-backed axis-rule source-order gate for `shanai_lan` row cohorts before any paint-order promotion. `axisRuleSourceOrderGate` compares each dual-endpoint connector's normalized parent relative offset with the normalized parent offset span of its same-row axis-rule matches. Current evidence: row 0 has all `4` dual connectors between the axis-rule parent span (`7644..9734` inside `3460..12022`), row 10 has its single dual connector before the axis-rule parent span (`1298` before `1376..2056`), rows 3/33/37 are mixed, and all inspected rows have source-order evidence backed with `0` unclassified connectors. This narrows paint-order work without drawing any connector lines.
- [x] Reject the straight-line render probe for the source-order `shanai_lan` connector classes. The baseline 144dpi Poppler comparison is `mean=24.536415`, `RMS=71.637773`, `changed>32=273818`; drawing row 0 between-span connectors worsens to `24.553023/71.672634/273874`, row 2 between-span to `24.539779/71.647012/273813`, row 10 before-span to `24.547586/71.658740/273897`, row 3 mixed to `24.562030/71.689283/273965`, and rows 0+2 together to `24.556386/71.681868/273869` at width 2. Even the one changed-pixel decrease for row 2 increases mean/RMS, so endpoint straight-line drawing remains diagnostic-only; the next wiring work must decode FDMIndex ownership/paint order or compound path semantics instead of promoting projected connector endpoints.
- [x] Add per-connector `sameRowFdmOpenStrokeAxisRuleConnectorOrderTrace` layer diagnostics for the tight, non-diagonal, dual-endpoint `shanai_lan` same-row FDM axis-rule candidates. The trace is selected by the same source-backed readiness predicate, not by row numbers, and emits each connector's FDMIndex row facts (`indexOffset`, `vectorOffset`, `vectorLength`, `kindHex`, normalized bbox, image-bearing flags, entry connector count), connector provenance (`sourceSegment`, parent compound, source bbox, entry-connector membership), endpoint nearest owners, same-row axis-rule matches with per-rule normalized parent relative offsets, derived owner/axis/bbox relations, and sorted `sourceOrderNodes`. This does not render wiring; it makes the current promotion blocker inspectable at connector granularity after straight-line endpoint rendering was rejected.
- [x] Add a source-backed relation-count `summary` to `sameRowFdmOpenStrokeAxisRuleConnectorOrderTrace` so future render promotion cannot cherry-pick individual connector traces. The summary records trace count, source-segment-to-FDMIndex matches, entry connector membership, image-bearing segment count, parent-normalized ordered owner count, bbox relation counts, connector-vs-axis-rule parent-span counts, and owner-vs-axis-rule parent-span counts. It also splits image-bearing traces by FDMIndex bbox relation, connector-vs-axis-rule parent span, complete payload extraction, and connector offset relation to the segment image-signature range. Each trace exposes `imageBearingSegmentGate` with image signature offsets, payload extraction status, nearest signature distance, per-signature containing FDMVector command contexts, and bbox/source-order blockers. The current local regression pins `promotionReady:false`, `readinessBlockedReason:"image-signature-fragment-role-unproven"`, `imageBearingSegmentCount:5`, `imageBearingCompletePayloadSegmentCount:0`, `imageBearingSignatureWithoutPayloadSegmentCount:5`, `imageBearingBboxRelationCounts:{disjoint:5}`, `imageBearingConnectorVsAxisRuleParentSpanCounts:{before:4,after:1}`, `imageBearingConnectorVsSegmentImageSignatureRangeCounts:{inside:4,after:1}`, and `parentNormalizedOrderedSameRowSameConnectorCount:0`. The row-33 segment signatures are now proven to sit inside FDMVector command records rather than complete image payloads: local offset `2453` is inside both command 10 and synthetic nested command 10001, `4389` is inside command 25, and `5957` is inside command 45. Wiring promotion stays blocked until signature-fragment command roles and connector/owner/axis ordering are explained globally rather than by a local visual overlay.
- [x] Add a source-backed `compoundChildLayoutGate` to every compound FDMVector command diagnostic before further connector promotion. The gate validates that the prefix child-offset table starts exactly at the first child, is strictly increasing, and that every marker-valid child record's declared length fits the parent without overlapping the next child. Current `shanai_lan` compound commands expose these boundary facts through layer-tree and object-stream JSON, but stay diagnostic-only with `compound-child-boundaries-do-not-prove-connector-ownership-or-paint-order`: structural child boundaries alone do not assign a connector role, endpoint owner, page placement, or paint order. This preserves the unchanged A4-landscape PDF while removing marker-scan ambiguity from the next source-order investigation.
- [x] Remove the last row-index based styling from `shanai_lan` fallback `fdmCommandBBoxReferenceProjection` SVG diagnostics. Image-bearing command boxes are now highlighted from source evidence (`FDMIndex.imageSignatures` / `segmentImageSignatures`) rather than hardcoded rows `23` and `33`, and the SVG records `data-diagnostic-style-basis`, signature counts, and `data-valid-vector-offset` for auditability. This does not promote the diagnostic overlay when primitive rendering is already available; it only makes the fallback path reusable for other FDMVector documents.
- [x] Promote the strict `sameRowFdmOpenStrokeAxisRuleConnectorOrderTrace` readiness summary into the top-level `fdmConnectorGraphDiagnosticSummary` without drawing any wiring. The graph summary now carries `sameRowFdmOpenStrokeAxisRuleConnectorOrderTraceSummary`, using the same non-hardcoded predicate (`tightDualEndpoint`, non-diagonal, same-row FDM axis-rule matches), and preserves the more specific blocker `image-signature-fragment-role-unproven` at graph level. Row cohorts also expose a source-backed `markerStyleAgreementGate` comparing tight non-diagonal connector marker/style dominance with same-row axis-rule marker/style dominance; current evidence remains non-rendering because connector and axis-rule marker/style dominance still diverges.
- [x] Remove the exact filename gate from the `shanai_lan` single-page diagram projection. `project_fdm_single_page_diagram` now uses `document_has_shanai_lan_fdm_command_evidence` only, so a renamed LAN diagram with the same source-backed `/DocumentText` and FDMVector command-density evidence still truncates fallback text pagination to one diagram page. The local regression constructs the same parsed sample under `renamed-lan-diagram.jtd` and verifies page count plus FDM primitive layer output.
- [x] Remove exact filename-based layout/rendering hints from the runtime path. `DocumentCore::from_document` and `set_file_name` now route through `source_document_layout_hint(document, decoded_layout)`, which reuses parsed document evidence for Ginga vertical/front-matter documents, `shanai_lan` FDM command/frame evidence, `success_data-test` projection evidence, `tsaiten` table evidence, and `fax02` VisualList evidence. Filename-only synthetic inputs now stay on default/source-decoded layout, while renamed local samples for A5/A6/B6, `shanai_lan`, `success_data-test`, and `tsaiten` keep the same writing mode, page size, page count, and representative layer/SVG projections.
- [x] Add a non-hardcoded FDM page-paint coverage gate before investigating `shanai_lan` background/page-fill shapes. Each `fdmVectorPrimitiveProjection` now computes `paintCoverage` from the projected bbox, closed/fill evidence, and the existing large-span filter (`FDM_VECTOR_PATH_DIAGNOSTIC_MAX_SPAN_RATIO = 0.28`) instead of pinning `pageFillCandidate:false`. `fdmConnectorGraphDiagnosticSummary` also aggregates `pagePaintCoverageSummary`, while large-span filtered primitives keep `renderable:false` and would block as `fdm-page-fill-source-evidence-unproven` until source-role, page extent, and paint order are decoded. The current PDF-backed `shanai_lan` regression proves no `pageFillCandidate:true` is emitted, so visible LAN primitives and SVG/PDF pixels are not changed by this diagnostic gate.
- [x] Reject full-span rendering for selected `shanai_lan` `/DocumentText` line-header candidates and preserve them as diagnostic-only. A 144dpi Poppler probe worsened the reference comparison from `mean=24.536415`, `RMS=71.637773`, `changed>32=273818` to `25.839653/73.920877/284015` when the 7 currently selected skipped-inline horizontal line headers were drawn as full-width rules. `documentTextLineHeaderProjectionCandidateSummary` now exposes `fullSpanRenderPromotionGate` with `fullSpanRenderableCandidateCount:0`, and every candidate is blocked as `line-header-segment-clipping-and-endpoint-ownership-unproven` until segment clipping, endpoint ownership, and paint order are decoded from source evidence.
- [x] Keep the `/DocumentViewStyles` first sequential record `0x1001` signal as diagnostic writing-mode evidence without promoting it to render authority. `0x1001` appears in vertical Ginga samples, but reference PDFs show it also appears in horizontal `tsaiten`, `tmogi3_2`, `success_data-test`, and `shanai_lan` samples; therefore `DocumentCore::from_document` selects writing mode from source document layout hints or the horizontal default while `getDocumentInfo` exposes `writingModeDecision`, `writingModeCandidateFromDocumentViewStyles`, `writingModeCandidateFromDocumentViewStylesSourceBacked`, and the first record code/hex for audit. The same document-info JSON is available from `rjtd document-info <file.jtd>` so local sample comparisons can audit the selected writing-mode source without app/wasm glue. `writingModeDecision` also records whether source document layout hints, DocumentViewStyles, or PaperMark candidates disagree with the selected writing mode.
- [x] Strengthen image-payload render admission without promoting visible rendering. `imagePayloadDiagnostic` layer/overlay JSON and SVG data attributes now separate source ownership, `/Frame` reference-row geometry candidates, `EmbeddingInfo -> /Frame` source-frame traces, source frame-record geometry, candidate frame bbox (`candidateFrameBBox`), and payload-to-frame aspect-fit diagnostics from the still-blocking page-assignment/paint-order proof. Synthetic coverage proves that even with ownership, frame-row evidence, `EmbeddingInfo`, nonzero `/Frame` geometry, a source-derived candidate bbox, and a best payload aspect candidate present, `renderable:false` remains enforced with `image-payload-frame-geometry-present-but-page-assignment-and-paint-order-unproven`; local PDF-backed samples also prove at least one real payload reaches that final gate.
- [x] Add per-rule and per-component render admission gates for `shanai_lan` raw line-rule projections without drawing wiring. Each `documentTextLineRuleProjection` now carries `renderAdmissionGate` with component membership, `/LineMark` coverage, orthogonal/topology readiness, endpoint text-attachment candidates, and blockers such as `document-text-grid-origin-reference-backed`, `line-rule-topology-partial-orthogonal-coverage`, `line-rule-component-topology-unproven`, `line-rule-text-attachment-pair-absent`, `line-rule-endpoint-ownership-unproven`, `line-rule-component-endpoint-ownership-unproven`, `line-rule-component-line-mark-coverage-incomplete`, `line-rule-style-role-unproven`, `line-rule-component-style-role-unproven`, `line-rule-paint-order-unproven`, `line-rule-component-render-admission-not-ready`, and `line-rule-text-attachment-pair-unproven`. The projection summary also aggregates `lineRuleRenderAdmissionGate` (`ruleCount:16`, `componentCount:6`, `orthogonalComponentCandidateCount:3`, `bothEndpointAttachmentWithinLineHeightRuleCount:0`) so future wiring work cannot cherry-pick the one raster-improving rule; rendering remains blocked as `line-rule-render-admission-not-ready`.
- [x] Add a table-family stability blocker to the `tsaiten` source-gap-to-page-line transform gates. The transform admission gate now distinguishes a merely best current candidate from a family-stable transform: all transitions must stay in one PageMark entry and the selected source-gap transform must have zero max delta before page-y/axis admission can treat it as decoded. Current `tsaiten` scoring and lower tables therefore expose `source-gap-to-page-line-gap-transform-unstable-across-table-family` in both nested transform gates and final source-only admission blockers, while visible rendering remains reference-fallback only.
- [ ] Decode the remaining Ichitaro vertical-writing flags beyond `/DocumentViewStyles`, especially `/PaperMark` flag `0x00010011`. `getDocumentInfo` now exposes bit-level PaperMark evidence: `paperMarkFlagBit0VerticalCandidate:true` for bit-0 rows, `paperMarkFlagBit17IndexStepCandidate:true` for bit-17 rows, `paperMarkWritingModeCandidateEvidence:["paper-mark-flag-bit0-vertical-corpus-consistent"]` on the Ginga vertical samples, and the blocker `paper-mark-writing-mode-flag-semantics-unproven` while `writingModeCandidateDecoded:false` stays unchanged. Current corpus evidence says bit 0 appears only in parsed vertical-rl Ginga samples (`46.jtd`, `a5.jtd`, `b6.jtd`), while bit 17 is not a landscape bit: portrait `dousoukai.jtd` is a counterexample and landscape `shanai_lan` lacks it. Bit 17 instead tracks PaperMark index step `2` versus bit-16-only step `1`. The item remains unchecked because three vertical samples cannot prove cross-version semantics; prove whether bit 0 is a stable writing-mode flag, section transition marker, or page-layout state before using it as render authority.
- [ ] Decode the newly revealed local reference PDF pagination/orientation gaps now locked as known divergences in the `rjtd-export` local PDF artifact regression: 7 fallback-pagination divergences (`iwata_file`, `resume`, `fujimoto_file`, `christmas_2001`, `syokuhin`, `natsu`, `asobinin_24`) and 4 `seminar2004` landscape orientation divergences (`shortcutkey1`, `shortcutkey2`, `toolbox`, `shortcutkey3`). Keep the lock as drift evidence until source-backed pagination and paper-orientation semantics replace fallback output.
- [ ] Implement real canvas/layer paint operations in the app compatibility layer once the renderer can emit non-text visual primitives.
- [ ] Replace fallback cursor/hit-test geometry with real JTD layout geometry after `/LineMark`, `/PageMark`, `/PaperMark`, style, and object semantics are proven.
- [ ] Replace text-flattening body edit fallback with structure-preserving edits once true JTD paragraph records, inline controls, and styles are decoded.
- [ ] Implement real page/section/page-border mutation once the corresponding JTD streams are decoded.
- [ ] Implement real character, paragraph, style, numbering, and bullet streams instead of default formatting fallback JSON.
- [ ] Replace table/cell no-hit fallback APIs with decoded JTD table structures, nested cell paths, geometry, style, and formula semantics.
- [ ] Replace picture/shape/equation/bookmark/form no-hit fallback APIs with decoded JTD object/control structures and real binary/image/form data.
- [ ] Implement true HTML paste/export and HWP/HWPX/common-format export once the common document model and JTD object semantics are mature enough.
- [ ] Extend selection/clipboard APIs beyond body plain text to preserve inline controls, tables, cells, pictures, shapes, fields, footnotes, and unknown objects.
- [ ] Implement table, cell, header/footer, footnote, picture, shape, field, style, undo/redo, clipboard, and search APIs needed for full rhwp Studio feature parity.
- [ ] Implement save/export back to JTD or a common document format; current `HwpDocument` compatibility is read/render oriented.

## M4: Markdown Export

Goal: implement `rjtd export <file> --format md` through the document model layer.

Status: minimal model-based Markdown export implemented.

Completed:

- [x] Implement Markdown exporter that consumes `Document`.
- [x] Wire `rjtd export <file> --format md`.
- [x] Verify local `a5.jtd` produces recovered section titles such as `一、午后の授業`.

Remaining:

- [ ] Preserve headings, lists, tables, ruby, and layout semantics once the record parser exposes them.
- [x] Add HTML export after the model has stable block/inline semantics.

## M5: WASM Viewer on Cloudflare Pages

Goal: build `rjtd-wasm` with `wasm-pack` and deploy a static web viewer to Cloudflare Pages where users can drag-and-drop JTD files for in-browser viewing.

Status: not started.

Background:
- `rjtd-wasm` is already configured as `cdylib` + `wasm-bindgen` and exposes `renderPageSvg()`, `renderPageHtml()`, `renderPageToCanvas()`, `plainText()`, `pageCount()`, and other browser-facing APIs.
- Rendering accuracy is actively improving in M2/M3; the viewer implementation must not interfere with ongoing parser work.

Prerequisites:
- `wasm-pack --target web` build succeeds (verify WASM compatibility of `cfb` crate and other I/O dependencies).
- `rjtd-core` reads CFB via a byte-slice interface without calling `std::fs` directly.

Immediate tasks:

- [x] Verify `wasm-pack build --target web rjtd/crates/rjtd-wasm` compiles and identify any WASM-incompatible dependencies (`std::fs`, desktop-only `image` crate features, etc.).
- [x] Add `cfg(target_arch = "wasm32")` guards as needed to make the WASM build pass (I/O stubs, `image` crate feature gating, etc.).
- [x] Create a minimal HTML frontend in `openjtd.github.io/` (file drop → `HwpDocument.new(bytes)` → `renderPageSvg()` injected into a `<div>`, MVP only).
- [x] Add page navigation UI (previous/next buttons, page number display).
- [x] Add a `plainText()` output tab as a baseline for text search.
- [x] Package `wasm-pack` artifacts (`.wasm` + JS glue) alongside the HTML for deployment to Cloudflare Pages (static files only, no Workers required).
- [x] Set up a Cloudflare Pages GitHub Actions pipeline for automatic build and deploy when `rjtd-wasm` source is updated.

Constraints:
- Files must be processed entirely in-browser; nothing is uploaded to a server (privacy requirement).
- Rendering quality depends on M2/M3 progress; the MVP may ship with `decoded:false` output.
- Do not modify `rjtd-model` or `rjtd-core` parsing code for the viewer; the viewer adapts to the WASM API.
