# TODO

## M1: Container Explorer

Goal: 最初の実行可能マイルストーンとして `rjtd streams <file.jtd>` を実装する。

Status: CFB entry inventory は実装済み。壊れた FAT ファイルに対する rhwp 風の lenient fallback も含む。

これを最初に行う理由:

- rjtd は rhwp の階層的な解析順序に従う必要がある。
- rhwp は header、record、body text、model、renderer、exporter より前に CFB container 層から binary parsing を始める。
- text extraction を試す前に、JTD 構造を観察し文書化する必要がある。
- exporter は raw file data を直接読んではならず、以降の作業は安定した下位層に依存する。

Immediate tasks:

- [x] ローカル `.jtd`、`.jtt`、`.jttc` サンプルを `rjtd-testdata/local-samples/` に置く。
- [x] 実装前にテストを書く。
- [x] 最小限の CFB container reader を `rjtd-core` に追加する。
- [x] 正規化された path、byte size、entry kind を持つ stream/storage entry を公開する。
- [x] malformed FAT samples のために rhwp 風の lenient CFB fallback を追加する。
- [x] `rjtd-cli` に `rjtd streams <file.jtd>` を実装する。
- [x] rhwp 風の軽量 inventory command として `rjtd info <file>` を実装する。
- [x] 初期観察結果を `openjtd-spec` に記録する。

Acceptance criteria:

- [x] Unit tests が小さな synthetic CFB file を作り、stream listing が動くことを証明する。
- [x] CLI integration tests が synthetic CFB file に対して `rjtd streams` を実行する。
- [x] ローカル sample files を `cargo run -p rjtd-cli -- streams ../rjtd-testdata/local-samples/<name>.jtd` で手動確認できる。
- [x] `rjtd/` から `cargo fmt --all --check`、`cargo check --workspace`、`cargo test --workspace`、`cargo clippy --workspace --all-targets -- -D warnings` が通る。

## M2: Text Extraction

Goal: M1 が関連 stream layout を明らかにした後、`rjtd cat <file.jtd>` を実装する。

M1 が local samples の stream observations を文書化するまでは M2 を開始しない。

Status: 観察済み text run と display inline segment のための structured `ParsedDocumentText` token layer を実装済み。観察済み `.jttc` samples は `/JSCompDocument` `JustCompressedDocument` wrapper 経由で開く。現在 `/DocumentText` を持たない samples については embedded `SsmgV.01` fragments を復元する。

Priority prework:

- [x] historical Ichitaro Document Filter `.oxt` をダウンロードする。
- [x] `.oxt` を展開し file tree を記録する。
- [x] Java、native code、OpenOffice registry files の有無を確認する。
- [x] filter/type registration metadata を記録する。
- [x] DLL を逆コンパイルせず、stream と conversion string を検索する。
- [x] filter で確認された stream names に照らして local samples の `/DocumentText` bytes を調べる。
- [x] 必要なら read-only `rjtd dump-stream <file.jtd> <path>` helper または test utility を追加する。
- [x] `DocumentText` text-run marker を使った初期 `rjtd cat <file.jtd>` を実装する。
- [x] ruby base text と template placeholder を含む、`0x001D ... 0x001E` で囲まれた common display inline segments を復元する。
- [x] plain `cat` output では既知の phonetic annotation と template instruction inline segments をスキップする。
- [x] `/DocumentText` がない `.jttc` samples が `/JSCompDocument` を `JustCompressedDocument` として保存していることを検出する。
- [x] LHA dependency を追加せず、観察済み `.jttc` `/JSCompDocument` `-lh5-` payloads を decode する。
- [x] invalid または unsupported compressed payloads を明確に報告する。
- [x] malformed FAT fallback 後に local sample inventory が開けることを確認する。
- [x] `/DocumentText` がない場合に embedded `SsmgV.01`/`TextV.01` fragments を復元する。
- [x] `SsmgV.01` w[9]=`0x0001` 生テキストセグメント形式をパースする：`TextV.01` セグメント名（8 バイト ASCII）＋2 ワード長＋純粋 UTF-16BE（`0x001f` 段落マーカーなし）；`te.jtd` のテキスト抽出を可能にする。
- [x] 現在の local samples 61 件すべてで `rjtd cat` が成功することを確認する。
- [x] structured `ParsedDocumentText` output を調べる `rjtd text-tokens <file>` を追加する。
- [x] `/DocumentText` control boundaries を neighboring map entries と nearest control distances 付きで調べる `rjtd text-control-context <file> [control-code]` を追加する。
- [x] plain string extractor path を structured `ParsedDocumentText` parser layer に置き換える。
- [x] スキップした inline text segments を structured tokens と document-model unknown objects として保存する。
- [x] token/stream inspection pipelines のために CLI stdout writes が broken pipes に耐えるようにする。
- [x] 初期 `/DocumentTextPositionTables` `MarkV.01` diagnostics のために `rjtd text-positions <file>` を追加する。
- [x] `/DocumentText` tokens を byte range と UTF-16 unit range に対応付ける `rjtd text-map <file>` を追加する。
- [x] `MarkV.01` offsets を byte、unit、provisional `unit + 29` token contexts と比較する `rjtd text-position-context <file>` を追加する。
- [x] MarkV.01 UTF-16 unit delta candidates 0 through 64 を採点する `rjtd text-position-delta-scan <file>` を追加する。
- [x] `MarkV.01` と first entry の間の raw six bytes と raw `(id, offset)` entries を表示する `rjtd text-position-mark-header <file>` を追加する。
- [x] MarkV.01 header を `/DocumentText`、`/LineMark`、`/PageMark`、`/PaperMark` metrics と相関させる `rjtd text-position-mark-summary <file>` を追加する。
- [x] MarkV.01 header と entry offsets を `/LineMark` word contexts と nearest tag rows と比較する `rjtd text-position-line-context <file>` を追加する。
- [x] `/DocumentTextPositionTables` observations を `openjtd-spec` に記録する。
- [x] `MarkV.01` offsets は raw byte や extracted-character positions より UTF-16 unit/internal coordinates に近いという現在の仮説を記録する。
- [x] parsed `MarkV.01` entries を持つ現在の 5 samples で `text-position-context` output を sweep し、初期 id classification を記録する。
- [x] 現在の MarkV.01 samples 5 件で `text-position-delta-scan` を sweep する。40 entries total; `delta 9`、`delta 29`、`delta 30` は visible text hits 31 で同率、`delta 9` は `delta 29` より unit hits が多い。
- [x] 現在の MarkV.01 samples 5 件で `text-position-mark-header` を sweep する。marker offset は常に 30、header prefix は `00000000`、最後の big-endian `u16` は `0x0603`、`0x0610`、`0x061c` のいずれかなので、この header は constant 29-unit adjustment を直接 encode していない。
- [x] 現在の MarkV.01 samples 5 件で `text-position-mark-summary` を sweep する。`0x0603` は異なる page/paper counts で現れ、`0x061c` は `/LineMark`/`/PageMark`/`/PaperMark` がない samples で現れるため、直接的な document-length または page-count の意味は未証明。
- [x] 現在の local samples 61 件で `text-position-line-context` を sweep する。`46.jtd`、`a5.jtd`、`b6.jtd` だけが readable `/LineMark` と parsed `MarkV.01` の両方を持つ。MarkV.01 entry offsets 24 件はすべて `/LineMark` word range 外だが、MarkV.01 header の final `u16` は `/LineMark` 内に入る。
- [x] 代表的な MarkV samples で four-byte MarkV.01 header values `00000603`、`00000610`、`0000061c` を検索し、観察済み layout/style streams ではなく `/DocumentTextPositionTables` にだけ現れることを確認する。
- [x] 観察済み non-Mark `TCntV.01` numeric tables のために `rjtd text-position-counts <file>` を追加する。
- [x] 11 件の non-Mark position-table samples を 10 件の `TCntV.01` tables と、inventory size は non-zero だが empty/unreadable position-table payload 1 件に分類する。
- [x] `TCntV.01` の first two fields を `/DocumentText` byte と UTF-16 unit token ranges に対して比較する `rjtd text-position-count-context <file>` を追加する。
- [x] 現在の `TCntV.01` samples 10 件で `text-position-count-context` を sweep し、coordinate behavior が mixed であることを記録する。
- [x] tail `t1/t2` fields を `/DocumentText` byte と UTF-16 unit token ranges に対して比較する `rjtd text-position-count-tail-context <file>` を追加する。
- [x] 現在の local samples で `text-position-count-tail-context` を sweep する。readable `TCntV.01` files 10 件、89 rows、any byte hit 21、both byte hit 5、any unit hit 49、both unit hit 28、both unit text hit 26。`t1/t2` には UTF-16 unit coordinates がより強いが未完成な signal として見える。
- [x] tail `t1/t2` endpoints に positive `0..64` UTF-16 unit deltas を scan する `rjtd text-position-count-tail-delta-scan <file>` を追加する。
- [x] 現在の local samples で `text-position-count-tail-delta-scan` を sweep する。delta 29 と 30 が unit endpoint hits 124 で同率首位、delta 29 は text endpoint hits と both-unit rows で 30 より強い。一方 text endpoint hits は delta 53 で peak するため、single offset は未証明。
- [x] `(family,t0,t3,t4,t7)` pattern ごとに tail delta scores をまとめる `rjtd text-position-count-tail-delta-groups <file>` を追加する。
- [x] 現在の local samples で `text-position-count-tail-delta-groups` を sweep する。28-row `be0/0x0101/0x0100/0x0001/0x0001` group は unit/text とも delta 29 を好む。16-row shifted group は unit delta 31、text delta 30 を好む。28-row `be0/0x0202/0x0100/0x0000/0x0001` group は多くの best deltas に分散し、single global adjustment は考えにくい。
- [x] per-row best unit/text deltas、chosen spans、tail spans、document byte/unit lengths を出す `rjtd text-position-count-tail-row-deltas <file>` を追加する。
- [x] 現在の local samples で `text-position-count-tail-row-deltas` を sweep する。主要 `be0/0x0202/0x0100/0x0000/0x0001` group は 8 files 28 rows、chosen spans 398..1212、tail spans 46..72、document unit lengths 65889..146657、row-level best unit deltas 17 種を持つ。これは file-level/global correction ではなく row-local structure に見える。
- [x] chosen start/end byte/unit contexts と best-delta tail contexts を同じ row に置く `rjtd text-position-count-tail-row-context <file>` を追加する。
- [x] 現在の local samples で `text-position-count-tail-row-context` を sweep する。major 28-row `be0/0x0202/0x0100/0x0000/0x0001` group では chosen start/end が byte-context body hits または boundaries に出ることが多く、unit-context は mostly `between`、best-delta tail `t2` は 27/28 rows で text に着地する。chosen range と tail fields は duplicate text coordinates ではなく異なる role と見るべきである。
- [x] chosen `TCntV.01` range が overlap する `/DocumentText` entries を byte/UTF-16 unit intervals として要約する `rjtd text-position-count-range-preview <file>` を追加する。
- [x] 現在の local samples で `text-position-count-range-preview` を sweep する。61 checked、10 readable `TCntV.01` files、89 rows。`t0=0x0202` rows 36 件のうち 31/36 が chosen byte ranges で text と overlap する。major group は byte 25/28、unit 21/28 が text と overlap する。
- [x] chosen range edge alignment、first/last/previous/next `/DocumentText` entries、overlapped control-code counts を出す `rjtd text-position-count-range-boundaries <file>` を追加する。
- [x] 現在の local samples で `text-position-count-range-boundaries` を sweep する。major group の chosen byte ranges は map entries 535 件と overlap し、513 件が fully contained、22 件が partial。25/28 rows が controls を含み、`0x001c` が 281 回、`0x000e` が 38 回で優勢。`/DocumentText` control-delimited range structure が次の対象。
- [x] 現在の local samples で `text-control-context` を sweep する。61 checked、0 errors、60 files with controls。`0x001c` は 60 files で 51,971 回、`0x000e` は 41 files で 6,621 回。`0x001c` は text/text、text/control、control/text、control/control をよく分ける。`0x000e` は control/control、text/control、text/skipped-inline に多い。
- [x] duplicate `TCntV.01` ranges を group し raw-tail variants を表示する `rjtd text-position-count-clusters <file>` を追加する。
- [x] `be32@0/4` と shifted `be32@1/5` offset candidates を比較する `rjtd text-position-count-candidates <file>` を追加する。
- [x] 現在の `TCntV.01` samples のうち 9 件は `be32@0/4` に合い、`ichitaro-20030316043238-success-001-success_data-iwata_file.jtd` は 32 件の `be32@0/4` plausible entries と 18 件の shifted `be32@1/5` plausible entries に分かれることを確認する。
- [x] `TCntV.01` entries を `be0` または `be1-shifted` に分類し、candidate offsets と raw tail を保存する `rjtd text-position-count-family <file>` を追加する。
- [x] 現在の `TCntV.01` samples で `text-position-count-family` を sweep する。10 files、89 records total、71 `be0`、18 `be1-shifted`。shifted entries はすべて `iwata_file` entries 32-49。
- [x] 各 `TCntV.01` record を chosen range と tail `u16be` fields に展開する `rjtd text-position-count-fields <file>` を追加する。
- [x] 現在の `TCntV.01` samples で `text-position-count-fields` を sweep する。`be0` records は 10 tail `u16be` fields plus extra byte `00`、`be1-shifted` records は exactly 10 tail `u16be` fields and no extra byte。
- [x] current tail-field invariants を記録する。shifted records は fixed `t3=0x0100`、`t4=0x0001`、`t5=0x0000`、`t6=0x0000`、`t7=0x0001`、`t8=0x0000`、`t9=0x0000`。`be0` records は複数の mostly fixed fields を共有するが variation が多い。
- [x] chosen `TCntV.01` range span と tail `t1..t2` span を signed deltas 付きで比較する `rjtd text-position-count-field-deltas <file>` を追加する。
- [x] 現在の local samples で `text-position-count-field-deltas` を sweep する。10 readable `TCntV.01` files、89 rows、すべて `t2 >= t1` だが、`t2 - t1` が chosen range span と等しい row はない。relation counts は `be0` `gt` 27 / `lt` 44、`be1-shifted` `gt` 15 / `lt` 3。
- [x] chosen `TCntV.01` ranges を `/LineMark` word/byte offsets、parsed `/PageMark`、`/PaperMark` row/byte offsets と比較する `rjtd text-position-count-layout-context <file>` を追加する。
- [x] 現在の local samples で `text-position-count-layout-context` を sweep する。10 readable `TCntV.01` files で 89 rows、chosen start/end values は word/row/byte interpretation のどれでも direct `/LineMark`、`/PageMark`、`/PaperMark` ranges 外。
- [x] CFB regular/mini stream placement を調べる `rjtd stream-meta <file> <stream-path>` を追加する。
- [x] empty `/DocumentTextPositionTables` payload 1 件は mini-sector start `224` で、observed 7680-byte mini stream の範囲外を指すことを確認する。
- [x] 初期 `/LineMark`、`/PageMark`、`/PaperMark` observations を `openjtd-spec` に記録する。
- [x] generic `rjtd stream-words <file> <stream-path>` と `rjtd stream-word-frequencies <file> <stream-path>` diagnostics を追加する。
- [x] `/LineMark` raw words を `/DocumentText` raw words と比較し、`0x1000`、`0x1001`、`0x1002` は LineMark-specific tags らしいと記録する。
- [x] `/LineMark` `0x1000`/`0x1001`/`0x1002` tag positions を surrounding word context 付きで出す `rjtd line-mark-tags <file>` を追加する。
- [x] 61 current local samples で `line-mark-tags` を sweep する。5 files が tags を含み、6 files は `/LineMark` なし、50 readable `/LineMark` streams は tags なし。tag totals は `0x1000` 915、`0x1001` 67、`0x1002` 554。
- [x] 各 `/LineMark` tag の直後の word は unique tag-family discriminator ではないことを確認する。high-frequency next words は `0x1000`、`0x1001`、`0x1002` 間で強く重なる。
- [x] `/LineMark` tag offsets と immediate next words を `/DocumentText` token map と比較する `rjtd line-mark-text-context <file>` を追加する。
- [x] 61 current local samples で `line-mark-text-context` を sweep する。55 files successful、6 lack `/LineMark`、known tag rows 1536 件を報告。direct LineMark byte/unit offsets は 587 rows で mapped `DocumentText` entries に hit し、tag-next words は 1511 rows で raw `/DocumentText` に現れる。
- [x] direct `/LineMark` word/byte offsets は proven `DocumentText` coordinates ではないが、tag-next words は通常どこかの raw `/DocumentText` に出ると記録する。
- [x] u32-oriented streams 用に generic `rjtd stream-dwords <file> <stream-path>` と `rjtd stream-dword-frequencies <file> <stream-path>` diagnostics を追加する。
- [x] 観察済み `/PaperMark` row shape が 12-byte header plus 8-byte `(index, flags)` rows であることを current large layout samples で証明する。
- [x] row shape が安定するまでは `/PaperMark` を document model に入れず、parser-backed `rjtd paper-marks <file>` diagnostics を追加する。
- [x] local samples で `paper-marks` を sweep し、55 件の `/PaperMark` streams のうち 52 件が observed row shape で parse することを記録する。
- [x] observed 84-byte `/PageMark` row family の parser-backed `rjtd page-marks <file>` diagnostics を追加し、各 row を raw bytes として保存する。
- [x] local samples で `page-marks` を sweep し、55 件の `/PageMark` streams のうち 20 件が observed 84-byte row family に合うことを記録する。
- [x] `/PageMark` stream length、declared CFB size、header values、row-shape candidates を分類する `rjtd page-mark-shape <file>` を追加する。
- [x] 初期 `/PageMark` reject families を特定する: count-plus-one variable rows、count-plus-one with 2-byte tail/trimming、declared-size mismatch regular streams、non-PageMark-looking payloads。
- [x] count-plus-one variable と count-plus-one-trim2 `/PageMark` families を raw-preserved row families として parser に昇格する。
- [x] count-plus-one family promotion 後、parser-backed `page-marks` が current `/PageMark` streams 55 件のうち 43 件を開くことを確認する。
- [x] count-variable `/PageMark` family を raw-preserved row family として parser に昇格する。
- [x] parser-backed `page-marks` が current `/PageMark` streams 55 件のうち 45 件を開くことを確認する。
- [x] fixed84-tail `/PageMark` family を trailing bytes preserved で parser に昇格する。
- [x] parser-backed `page-marks` が current `/PageMark` streams 55 件のうち 52 件を開くことを確認する。
- [x] suspicious raw streams で ASCII/UTF-16 string candidates を検出する `rjtd stream-text-probe <file> <stream-path>` を追加する。
- [x] 残り 3 件の unsupported `/PageMark` payloads は numeric PageMark rows ではなく stream/object names または legacy class/control metadata を含むことを確認する。
- [x] `/PaperMark` stream length、declared CFB size、header values、fixed-row candidates を分類する `rjtd paper-mark-shape <file>` を追加する。
- [x] `paper-mark-shape` が current `/PaperMark` streams 55 件すべてを開き、3 件の unsupported payloads を `non-paper-header` として分離することを確認する。
- [x] suspicious stream entries の FAT/miniFAT sector chains を調べる `rjtd stream-chain <file> <stream-path>` を追加する。
- [x] 残り 3 件の unsupported `/PageMark` と `/PaperMark` entries は complete miniFAT chains を持つが、payload bytes は layout rows ではなく CFB directory-entry fragments、OLE/ActiveX control metadata、または unrelated stream names と decode されることを確認する。
- [x] FAT、directory、mini FAT、root mini-stream sector chains を調べる `rjtd cfb-map <file>` を追加する。
- [x] `kaisya_annai` と `shanai_lan` の CFB directory-entry fragments を説明する。root mini-stream chains が CFB directory chains と重なるため、`/PageMark` と `/PaperMark` が structurally complete miniFAT chains 経由で directory-entry bytes を読む。
- [x] 同じ CFB file 内で exact duplicate stream payloads を探す `rjtd stream-find <file> <stream-path>` を追加する。
- [x] `kazoku_ryoko` `/PageMark` を `/EmbedItems/Embedding 1/JSFart2Contents` offset 1664 の exact slice まで追跡する。
- [x] raw CFB directory ids、sibling links、start sectors、resolved paths を調べる `rjtd cfb-dir <file>` を追加する。
- [x] `kazoku_ryoko` `/PaperMark` を root-level object/control stream sequence around `/EmbedFrame` and `/Figure` に追跡する。`SO` marker は `/Figure` と `\x01CompObj` にも現れ、最初の nonzero fields は `/EmbedItems/Embedding 2/JSFart2Contents` offset 192 と一致する。
- [x] arbitrary byte markers を all readable streams across CFB で検索する `rjtd stream-find-bytes <file> <hex>` を追加する。
- [x] `kazoku_ryoko` `/PaperMark` object/control evidence を `stream-find-bytes` で再現する。`534f0000` は `/PaperMark`、`/Figure`、`\x01CompObj` に現れ、20-byte coordinate-like suffix は `/PaperMark` と `/EmbedItems/Embedding 2/JSFart2Contents` に現れる。
- [x] all readable streams で `SO\0\0` object/control marker を scan し preserved little-endian fields と raw bytes を出す `rjtd so-records <file>` を追加する。
- [x] local samples で `so-records` を sweep する。4/61 samples が SO records を持ち、24 records total、`kazoku_ryoko` だけが `/PaperMark` 経由で SO record を expose する。
- [x] preserved raw bytes で SO records を group し repeated locations を報告する `rjtd so-record-clusters <file>` を追加する。
- [x] `JSFart2Contents` SO records が singleton geometry-like records と repeated default/control clusters に分かれること、`kazoku_ryoko` `/PaperMark` が older `kazoku_ryoko` `JSFart2Contents` sample の geometry-like cluster と一致することを確認する。
- [x] SO records を little-endian 32-bit fields plus signed and low/high 16-bit views に展開する `rjtd so-record-fields <file>` を追加する。
- [x] 現在の SO records は 9 little-endian dwords として扱うのがよいと確認する。field 0 は marker `0x00004f53`、repeated default/control records は `0x00000100` や `0x00000064` のような小さな constants、singleton geometry-like records は fields 1-4 に coordinate-like values を持つ。
- [x] SO records を `geometry-like`、`default-control`、packed subfamilies、`truncated`、`unknown` に分類する `rjtd so-record-geometry <file>` を追加する。
- [x] SO payload dwords を low/high 16-bit unsigned and signed halves として出す `rjtd so-record-halves <file>` を追加する。
- [x] local samples で `so-record-geometry` を sweep する。61 checked、0 failures、4 files with SO records、24 records total (`geometry-like` 9、`default-control` 8、`packed-jseq3-like` 4、`packed-ffff-preamble` 2、`truncated` 1)。
- [x] 以前の generic `packed` SO bucket を分割する。`packed-jseq3-like` は `JSEQ3Contents` records だけに現れ、`packed-ffff-preamble` は repeated geometry-like records 前の `JSFart2Contents` offset-324 preamble だけに現れる。
- [x] readable CFB streams の object/image/shape/table path evidence、`SO\0\0` marker、binary image signature、SVG text signature、payload prefix、`decoded=false` を classify する `rjtd object-stream-candidates <file>` を追加する。
- [x] current local samples で `object-stream-candidates` を sweep する。61 checked、candidates を持つ files 43、2,253 readable streams 中 candidate rows 933、object-path evidence files 27、shape-path evidence files 42、image-signature evidence files 17、SO-marker evidence files 4、SVG signature 0、table-path evidence 0、unreadable stream 0。
- [x] object/image stream inventory を `openjtd-spec` RFC 0008 に記録する。Embedded JPEG recovery は `/EmbedItems/Embedding */Contents` から始められ、`hyo` table sample は named table stream ではなく `/DocumentText` control/layout decoding が必要である可能性が高い。
- [x] non-text PDF rendering へ接続する前に、object/image/shape stream candidate evidence を model/export/app-core decoded-false `objectStreamCandidates` に promote する。
- [x] model-preserved `objectStreamCandidates` JSON export を sweep する。61 checked、0 failures、positive files 43、candidates total 933、image-signature files 17、SO-marker files 4、table-path files 0、SVG-signature files 0。
- [x] image-signature object candidates から complete embedded image payload spans を extract し、object geometry はまだ decoded せず decoded-false `objectStreamCandidates.imagePayloads` に bytes を保存する。
- [x] model-preserved `imagePayloads` JSON export を sweep する。61 checked、0 failures、payload files 12、strict complete payloads 67、dimensioned payloads 35、629,024 bytes、JPEG 35 rows/5 files、GIF89a 31 rows/9 files、GIF87a 1 row/1 file。
- [x] fully decodable JPEG/PNG image payloads を PNG data URI に再 encode し、fallback SVG/PDF pages に decoded-false diagnostic thumbnails として project する。`placementProven=false` のまま維持し、original page geometry はまだ主張しない。
- [x] app-core overlay/layer/SVG diagnostics に image payload render-promotion gates を expose する。object envelopes、source-path candidates、ownership candidate/reference counts、`ownershipProven=false`、`pageGeometryProven=false`、`paintOrderDecoded=false`、`diagnosticRenderable=true`、ownership/geometry/paint order decode 前の `renderable=false` を含む。
- [x] `fax02.jtt` のような `/VisualList` streams を decoded-false `objectStreamCandidates` として保存し、`visual-list-path` evidence を付ける。form/rule/vector decoding は exporter が raw stream を読むのではなく model から始める。
- [x] observed `fax02.jtt` `/VisualList` の BMDV header を model metadata (`declaredSize=2296`, `120x169`, `8bpp`, RLE length `2216`) として decode し、long horizontal raster runs を decoded-false `visualListRasterDiagnostic` form-rule geometry として SVG/PDF に project する。
- [x] decoded `/VisualList` BMDV pixel buffer を SVG/PDF の PNG image layer に promote する。renderer は dominant pixel value を transparent background として扱い、DocumentText-backed form-text projection もある場合は darkest raster foreground value を suppress して低解像度 text の double-paint を避ける。一方で source-backed rule/hatch pixels は保持し、bitmap encoding が使えない時だけ old horizontal-run/title-band projection を fallback として使う。
- [x] `fax02.jtt` の VisualList title-band hatch、static `FAX:`/`TEL:` form labels、DocumentText-derived form text slots を SVG/PDF と `getPageLayerTree` に decoded-false model-owned diagnostics として project する。gate は `/VisualList` と document text evidence の両方に置く。
- [x] decoded-false image payload object envelopes を保存する。header/trailer slices と conservative `le32` declared payload-length candidates を含む。
- [x] image payload envelopes を sweep する。61 checked、0 failures、envelopes 67、`le32` declared length matches 20、現在はすべて `Embedding */Contents` rows に限られる。
- [x] decoded-false image envelope header field candidates を保存する。先頭 prefix `u16/u32` values と `Embedding */Contents` headers の source path candidates を含む。
- [x] image envelope header fields を sweep する。61 checked、0 failures、source path candidates 66、すべて `Contents` rows (`jpg` 34、`gif` 32)。first prefix word `9` は payload rows 59、`4` は 6 に現れる。
- [x] object stream candidates に decoded-false path-derived `ownershipCandidate` evidence を保存する。`Embedding N`、stream role、figure/frame/layout stream families を含む。
- [x] ownership candidates を sweep する。61 checked、0 failures、candidates 933、ownership 付き 474、strict image payload rows 67 はすべて ownership candidate に covered (`contents` 67)。
- [x] embedded image candidates の `Embedding N` byte pattern を `FigureData`、`/Figure`、`/Frame`、`/LayoutBox`、`/PageMark`、`/PaperMark` streams で検索し、decoded-false `ownershipReferences` evidence として保存する。
- [x] ownership references を sweep する。61 checked、0 failures、references 付き files 12、references 付き embedded image candidates 52、reference rows 604、total byte matches 9,949、strict image payload rows 67 のうち 67 が cross-stream reference candidates に covered。
- [x] `rjtd object-ownership-references <file>` を追加する。model-owned ownership reference match の source stream、target stream、encoding、offset、total match count、alignment、local context hex、match offset の le/be 16/32-bit values を出力する。
- [x] `object-ownership-references` を sweep する。61 checked、0 failures、rows 付き files 12、reported preview offsets 3,167。target families は `figure-data` 1,010、`frame` 897、`layout-box` 528、`page-mark` 498、`paper-mark` 151、`figure` 83。
- [x] reference-context alignment evidence を記録する。reported preview offsets は mod2 `0` 1,401 / `1` 1,872、mod4 `0` 728 / `1` 855 / `2` 673 / `3` 1,017 に分かれる。`u16-be`/`u16-le` offsets は embedding index を match offset の be/le16 value として直接 expose し、`u32-le` は low 16 bits に expose する。
- [x] `rjtd object-ownership-reference-fields <file>` を追加する。ownership reference offsets を candidate record strides に投影し、target、encoding、stride、field offset、row indexes、source count、embedding indexes、cross-row matches を summarize する。
- [x] `object-ownership-reference-fields` を sweep する。61 checked、0 failures、field groups 付き files 12、projected field groups 33,492。すべての reported offset を 20 個の candidate strides に投影する diagnostic surface であり、decoded geometry ではない。
- [x] cross-row-free かつ stride >= 12 の強い候補を記録する。`frame/u16-le/12/5` は weighted matches 102、`frame/u16-be/12/7` は 89、`frame/u16-be/20/15` は 70。
- [x] `rjtd object-frame-reference-records <file>` を追加する。最も強い `/Frame` reference projections を candidate row bytes として展開し、row hex、BE/LE 16-bit fields、BE/LE 32-bit fields を出力する。
- [x] `object-frame-reference-records` を sweep する。61 checked、0 failures、candidate records 付き files 12、expanded rows 261。Candidate counts は `u16-le/12/5` 102、`u16-be/12/7` 89、`u16-be/20/15` 70。
- [x] dominant expanded `/Frame` row families を記録する。`00010000000N000000020001` style の 12-byte rows と `00000000010200380000000N` rows が繰り返されるが、まだ decoded placement geometry ではなく row-family evidence である。
- [x] `rjtd object-frame-record-families <file>` を追加する。expanded `/Frame` candidate rows を decoded-false diagnostic family として group する。
- [x] `object-frame-record-families` を sweep する。61 checked、0 failures、family 付き files 12、records 261。Family counts は `frame-index-tail-coordinate-row12` 65、`frame-index-tail-window20` 65、`frame-index-mixed-row12` 61、`frame-index-flag-row12` 41、`frame-index-tail-zero-row12` 22、`frame-index-mixed-window20` 5、`frame-index-tail-mixed-row12` 2。
- [x] `rjtd object-frame-row-links <file>` を追加する。20-byte `/Frame` window が matching 12-byte frame row を suffix として含むか検証する。
- [x] `object-frame-row-links` を sweep する。61 checked、0 failures、20-byte rows 付き files 9、row20 windows 70、same-source suffix row に link されたもの 65、unlinked 5。Linked rows はすべて `frame-index-tail-window20 -> frame-index-tail-coordinate-row12` であり、20-byte context window より `u16-be/12/7` がより強い authoritative-row candidate である。
- [x] decoded-false `/Frame` reference rows と suffix links を model/export `objectStreamCandidates[].frameReferenceRows` に promote する。future image placement implementation は model-first のまま維持する。
- [x] model-preserved `frameReferenceRows` JSON export を sweep する。61 checked、0 failures、positive files 12、rows 261、suffix links 65。Family counts は CLI sweep と完全に一致する。
- [x] `rjtd object-image-frame-candidates <file>` を追加する。各 image payload source を model-owned `/Frame` row evidence、payload kind/dimensions、row family、row20 suffix link、coordinate-looking row12 pair、best coordinate/payload aspect delta で summarize する。
- [x] `object-image-frame-candidates` を sweep する。61 checked、0 failures、image payload source 付き files 12、image sources 52、frame-linked sources 52、missing-frame sources 0、frame rows 261、dimensioned payloads 35、aspect candidates 付き sources 13。Preferred diagnostic buckets は `row12-tail-coordinate` 25、`row12-tail-zero` 7、`u16-le-row12` 19、`none` 1。
- [x] `row12-tail-coordinate` は強い placement-analysis candidate だが、まだ rendering promotion には不十分であると記録する。best coordinate/payload aspect delta <= 250 permille の sources は `natsu.jtd` の 2 件のみであり、aspect evidence はまだ sparse である。
- [x] `rjtd object-fdm-index <file>` を追加し、`/FigureData/*/FDMIndex` rows を sibling `FDMVector` segments と照合する。vector offset、kind field、bbox-like fields、segment prefix、image signature hits を decoded-false evidence として保存する。
- [x] `object-fdm-index` を sweep する。61 checked、0 failures、indexes 付き files 31、index streams 39、parsed rows 417、image 付き rows 6、image hits 13、missing sibling vectors 2。これは FDMIndex/FDMVector が `Embedding N` `/Frame` rows とは別の image-placement evidence path であることを示す。
- [x] `rjtd object-fdm-index-shape <file>` を追加し、exact 22-byte FDMIndex tables、declared-count prefix tables と trailing auxiliary payload、mixed declared rows、unknown-header streams、missing vectors を分離する。
- [x] `object-fdm-index-shape` を sweep する。61 checked、0 failures、indexes 39、`fdm-index-v1` headers 35、unknown headers 4、plausible declared counts 34、raw stream rows 417、raw invalid offsets 252、declared-prefix rows 147、declared-prefix invalid offsets 43、declared-prefix image hits 13。Shape counts は `row22-count-prefix` 17、`row22-exact` 14、`row22-mixed-declared` 3、`unknown-header` 3、`missing-vector` 2。
- [x] `rjtd object-fdm-index-rows <file>` を追加し、FDMIndex analysis 用に row scope (`declared`, `post-declared`, `raw`)、row role (`vector-segment`, `coordinate-like-invalid`, `invalid-vector-offset`)、BE16/i16 field views、row bytes、segment image hits を出力する。
- [x] `object-fdm-index-rows` を sweep する。61 checked、0 failures、indexes 付き files 31、indexes 39、rows 417、declared rows 147、post-declared rows 253、raw rows 17、valid vector rows 165、invalid rows 252、image hits 13、missing vectors 2。Role counts は `vector-segment` 165、`coordinate-like-invalid` 231、`invalid-vector-offset` 21 で、declared invalid rows 43 はすべて `coordinate-like-invalid` である。
- [x] decoded-false `FDMIndex` row evidence を、対応する `FDMVector` candidate の model/export/app-core JSON `objectStreamCandidates[].fdmIndexEntries` に promote する。ただし auxiliary payload bytes を false rows として expose しないよう、有効な `fdm-index-v1` declared-count prefix rows に制限する。
- [x] model-preserved `fdmIndexEntries` JSON export を sweep する。61 checked、0 failures、entries 付き files 24、entries 付き candidates 30、rows 147、image-linked rows 付き files 3、image-linked rows 6、image hits 13、valid vector offsets 104、invalid/out-of-range vector offsets 43。
- [x] `fdmIndexEntries` は現在観測されている image-bearing FDMVector segments をすべて識別しつつ、false auxiliary rows を減らす。invalid declared-prefix rows 43 件は、ちょうど 3 files の coordinate-like diagnostic rows として分類でき、image-bearing vector segments ではないため、decoded-false のまま保持し、renderable page geometry や paint resources へ promote してはならない。
- [x] `rjtd object-fdm-image-candidates <file>` を追加し、model-owned `fdmIndexEntries` から image-bearing FDMVector rows を summarize する。normalized bbox diagnostics、complete payload coverage、page placement が証明されるまでの明示的な `renderable=false` を含む。
- [x] `object-fdm-image-candidates` を sweep する。61 checked、0 failures、candidates 付き files 3、FDM sources 3、image-bearing rows 6、image hits 13、strict complete payloads 0、plausible bbox rows 5、renderable rows 0。FDMVector segments 内で観測される `FFD8FF` hits は SOF/SOS structure を持つ valid JPEG payload ではなく、vector data 内の JPEG-like byte patterns であるため、signature-only decoded-false evidence のまま残す。
- [x] `object-fdm-image-candidates` の render blocker を分離し、complete image payload rows は `page-placement-unproven` のまま維持しつつ、signature-only FDM rows は `image-signature-without-complete-payload-role-unproven` を報告するようにする。これにより FDM image row を promote する前に、payload extraction failure と page geometry failure を混同しない。
- [x] 同じ FDM image rows を app-core `getPageOverlayImages` の `unplacedDiagnostics` として expose する。`imageCount:0`、`placementProven:false`、`renderable:false` を維持し、rhwp-shaped overlay API は callable にしつつ page placement が decoded されたようには見せない。
- [x] `/Frame` fixed 60-byte records を decoded-false model/export/app-core `objectFrameRecords` として保存する。observed object id、record kind/type、geometry-like fields を含む。
- [x] `rjtd object-fdm-frame-links <file>` を追加し、image-bearing FDMIndex rows と `/Frame` records を `fdm row index == frame object id` で相関させる。
- [x] strict image payload dimension diagnostics を追加する。model/export JSON は optional `imagePayloads[].dimensions` を保持し、JPEG payload spans は SOF/SOS structure validation を通過した場合だけ promote され、`object-fdm-frame-links` は frame size、payload dimensions、dimensioned payload counts、best frame/payload aspect delta を報告する。
- [x] `object-fdm-frame-links` を sweep する。61 checked、0 failures、positive files 3、FDM image rows 6、image hits 13、frame-linked rows 6、missing-frame rows 0、strict complete payloads 0、dimensioned payloads 0、renderable rows 0。
- [x] `/EmbedItems/EmbeddingInfo` rows を decoded-false model/export/app-core `objectEmbeddingFrames` として保存する。embedding index、class name (`JSFart.Art.2`, `JSEQ.Document.3`)、primary size、frame reference、frame size を含む。
- [x] PDF reference を持つ `success_data-test` sample について、`objectEmbeddingFrames` を `/Frame` row geometry 経由で first-page SVG/PDF と `getPageLayerTree` に decoded-false `embeddingFrameDiagnostic` boxes として project する。この projection は `placementProven:false`、`renderable:false` のまま維持する。
- [x] `/EmbedItems/Embedding */\x03EmbeddedPress` streams の `JSSnapShot32` headers を decoded-false `embeddedPressSnapshot` metadata として `objectStreamCandidates` に保存する。width、height、body-length candidate、object-count candidate、payload-length candidate、`renderable:false` を含む。
- [x] UTF-16LE `MATH.VAF` magic を持つ `/EmbedItems/Embedding */JSEQ3Contents` streams を decoded-false `jseq3Formula` metadata として `objectStreamCandidates` に保存する。SO trailer fields と `Times New Roman`、`JustUnitMark`、`JustOubunMark` などの stable UTF-16LE text markers を含む。
- [x] `JSEQ.Document.3` embedding-frame diagnostics を matching `jseq3Formula` evidence に接続し、missing formula boxes が file data に追跡できるよう decoded-false `MATH.VAF` labels を SVG/PDF output に render する。
- [ ] FDM または `Embedding N` image ownership candidates を page geometry や paint resources へ promote する前に、`/Frame` geometry units、page association、paint order、payload-to-image selection を decode する。
- [ ] `embeddingFrameDiagnostic` boxes を real title art、formulas、table borders、shape paint operations に置き換える前に、`/EmbedItems/Embedding */EmbeddedPress` の `JSSnapShot32` payloads と `JSEQ.Document.3` embedded formula/document payloads を decode する。
- [x] unknown inline formatting/control records の背後に隠れた残り text を復元する。`sample-academic.jtd` のプレーンテキスト抽出は完了済み。以前「隠れたコンテンツ」として疑われた `0xff1c`/`0xff1e` は制御コードではなく U+FF1C/U+FF1E の全角不等号文字であることを確認。RFC 0009 で、現在のパーサーが `0x001c` で停止し `0x001f` で再開することにより自己記述型ヘッダーを正しくスキップしていることを文書化。
- [ ] current token layer を超えて true `DocumentText` record boundaries と control semantics を decode する。RFC 0009 でレコードファミリーの部分解読が完了：`0x0030` b0/b1=セル境界座標、`0x0010` w4 サブタイプ、`0x0000` インラインコンテキスト/セルマーカー、`0x0020` 表→段落遷移。`0x0010 w4=0x0026 w5=0x0005` len=17 は `w6/w7/w8/w10` の組み合わせで 9 種のペイロードパターンを持つ（`w6=1` + `w7=0x1ec/w8=0x1cc` がハンギングインデントの主要グループで 93/142 件；`w4=0x002e` len=13 は単列サンプルで完全固定）。新サンプル 11 件の `w4=0x0026 len=17` 計 246 件のクロスサンプル分析で `(w8, w10)` の大きさによる構造的分割が判明：`(w8=w10=0x1cc)` は `sample-reference` 専属（常に `w6=1`）、`(w8=w10=1/2)` は `sample-outline`/`sample-draft` 専属であり、(w8,w10) が文書種別識別子か短テキストではスタイル ID・長文参照条文では物理座標をエンコードしている可能性を示す。全 14 件のテストサンプルの `paragraph-style-records` スイープで `w7` 値セット確定：0=本文/見出し、1=本文継続、2=号リスト、3=附則小見出し、4=別表/深インデント、6=タイトル/前文（全 sample-outline/sample-draft タイトル行で出現）、8=附則見出し、10=理由見出し、492=ハンギングインデント本文（sample-reference 専属）。全 sample-outline/sample-draft サンプルで開頭タイトル行への w7=6 の一貫した出現が段落スタイル ID 仮説を強化する（RFC 0009 §w7 クロスサンプル表を 14 サンプルスイープで更新済み）。残課題：単位スケール確定、len=20 の `w10`、`0x0000 len=21` の w8/w9/w13。
- [ ] `MarkV.01` delta candidates 9、29、30 が強く score する理由を説明する。現在の evidence は `unit + 29` を unique stable adjustment と扱うことを支持しない。
- [ ] varying MarkV.01 header value `0x0603`/`0x0610`/`0x061c` の意味を decode する。current summary、LineMark-context、exact-byte-search evidence は direct page-count、document-length、global page-style-code、direct LineMark-entry-offset interpretation を弱める。
- [ ] 29-byte `TCntV.01` records 内の remaining fields の semantic meaning を decode する。current `text-position-count-field-deltas` evidence は `t1/t2` が ordered range-like pair であるが chosen `start/end` range と同じ span ではないことを示す。
- [ ] `TCntV.01` が byte coordinates、UTF-16 unit coordinates、layout/object-local coordinates を混在させるのか、または current token map が intervening record structure を欠いているのかを判断する。tail `t1/t2` は現時点で UTF-16 unit coordinates に寄り、delta 29/30 で unit hits は改善する。一方 `0x0202` chosen ranges は strong byte-range text/control overlap を示すが同じ tail coordinate behavior とは一致しない。
- [ ] `/DocumentText` の `0x001c`/`0x000e` 制御境界の解読。RFC 0009 でレコードファミリー全体を文書化：`0x0030` b0/b1=セル座標；`0x0010` w4 サブタイプ（`0x002e`=段落、`0x008f`=表行、`0x002a`=複合遷移、`0xffff`=ヌル）；`0x0010 w4=0x008f` ペイロード = 4 ワードサブエントリ `[tag, v1, v2, v3]`（`0xffff` 終端）；主要ケースで `n_sub_entries = n_cells − 1` 成立；`0x23`/`0x2b`/`0x1b` タグ観測済みだが意味未解読；`0x0010 w4=0x008f` の w8 は `0x0001`/`0x0003` の 2 値のみ（n_cells=12 の全行が w8=0x0001；n_cells=4 行は大多数 w8=0x0003；行種別フラグの可能性）；`0x0000 len=12`=インラインコンテキストマーカー（w4=inline-len、w6=525 定数、w5=セル位置依存スタイルセレクタ候補）；`0x0000 len=21`=表セルマーカー（定数ブロック+可変 w8/w9/w13）—`w8=0` は小値フラグ（0/2/4/5）、`w8=1` は大値でセル固有（`w9−w13=400=(b1−b0)×3.125` 不変量；ルビ/インライン直前に出現）；`0x0020 len=12`=表→段落遷移（w4=0x0010、w7=1）；`0x000a`（`sample-table` に 210 件、全現行サンプルに存在）は `0x001f` または CJK 文字の後に現れ、169/210 件で `0x001c/0x0030`、24/210 件で `0x001c/0x0010` に続く — `0x000e`（`0x0030` レコード間に限定）とは別のセル内または段落内行区切りとして機能；残課題：座標の物理単位、サブエントリ v3 計算式とタグ意味論、`0x0010 w4=0x008f` w8 の正確な規則、`0x0000 len=21` の w9/w13、`0x000a` の意味論。
- [ ] `text-boundary-layout-map` で見えた file-specific shifts を説明できる row-local、section-local、または record-local base offset を探す。
- [ ] `iwata_file` strict boundary candidates のうち 10 件だけが line-word/page-field exact endpoint evidence を両方持ち、残りの strict candidates と selected finance spans が持たない理由を説明する。view-style group hits は strict non-paragraph rows にも現れるため、反証されるまでは default/flag-like evidence として扱い、real paragraph construction には使わない。
- [ ] shifted leading byte が flag、prefix、version、または preceding record boundary として説明できたら、`TCntV.01` `be0` と `be1-shifted` diagnostics を explicit raw-preserving record family types に昇格する。
- [ ] `TCntV.01` の actual coordinate target を特定する。current evidence は direct `/LineMark`、`/PageMark`、`/PaperMark` word/row/byte coordinates を rejected。
- [ ] empty `/DocumentTextPositionTables` sample の out-of-range mini-sector が stale directory entry、malformed ministream chain、または別の storage/object boundary から復元可能なものか判断する。
- [x] `page-marks` がまだ support していない `/PageMark` variants の record layout を完全に証明する。RFC 0007 §PageMark Observation で 3 件の unsupported エントリ（`kaisya_annai`、`kazoku_ryoko`、`shanai_lan`）が layout rows ではなく stale/foreign payloads（CFB ディレクトリ断片、OLE/ActiveX メタデータ）であることを記録済み。miniFAT chain は完全だがバイト内容は PageMark row 構造と無関係。追加の証明は不要。
- [x] reference `a5.pdf` で、visible page number は 6 ページから始まり、偶数ページは page number と running chapter title が左側、奇数ページは page number と document title が右側に出ること、さらに running document title が `/AutoTextInfo` text candidates と一致することを確認する。
- [x] `PageMark` の first-field diagnostics を raw-preserving `flags`、`lineStart`、`lineEnd` values として expose し、reference PDF pagination と比較できるようにする。ただし final semantics はまだ主張しない。
- [x] `/PageLayoutStyle` nested `31xx..39xx` slot groups を decoded-false raw evidence として調べる `rjtd page-layout-style-slots <file>` を追加する。active `paired-slot-pairs` と `facing-pages-candidate` summary fields を含め、facing-page/header/footer/page-number analysis のための観測面にする。
- [x] local A/B/Ginga-style samples の facing-page evidence を確認する。`a5.jtd`、`a6.jtd`、`b6.jtd`、`46.jtd`、`shinsyo.jtd` はいずれも `0x32/0x33`、`0x34/0x35`、`0x36/0x37`、`0x38/0x39` などの active paired `/PageLayoutStyle` slot groups を持つ。page decoration layer output は `facingPagesCandidate:true` と `sidePolicyDecoded:false` を明示する。
- [x] `/AutoTextInfo` UTF-16BE text candidates を document model/export JSON に保存し、running document title を hardcoded sample name ではなく file data から参照できるようにする。
- [x] `/AutoTextInfo`、active paired `/PageLayoutStyle` slots、document-text chapter-title evidence がすべて存在する vertical Ginga-style documents に限り、decoded-false running header/page-number projection を render する。layer-tree output は `sidePolicy:"facing-pages-odd-right-even-left"` と source slot pairs を保持し、running header は Ichitaro reference と同じ横書きで render する。page-number x placement は page margin line に合わせ、A5 reference/generated の page 6 x は約 57.4/54.0 pt、page 7 x は約 358.8/360.7 pt になった。
- [x] 同じ Ginga vertical projection の a5-only front-matter gate を外す。reference PDFs がある場合、`a6.jtd` と `b6.jtd` でも page 6 は even-left で `一、午后の授業`、page 7 は odd-right で `銀河鉄道の夜` として出力され、`getPageLayerTree` は `sidePolicyDecoded:false`、`facingPagesCandidate:true`、`0x32/0x33` から `0x38/0x39` までの paired slot evidence を保持する。
- [x] decoded-false page decorations の根拠になる raw `/PageLayoutStyle` slot payload evidence を `getPageLayerTree` に保存する。各 `pageDecoration` は `slotEvidence` として record index/offset/label、slot id、`part05First`/`part05NonZero`、opaque `part04..part07` hex payloads を持ち、future header/footer/page-number decoding が hardcoded geometry なしで追跡できるようにする。
- [x] `rjtd page-layer-tree <file> <zero-based-page-index>` を追加し、facing-page page-number/header projection を model layer tree 経由で確認できるようにする。`side`、`sidePolicyDecoded:false`、`facingPagesCandidate:true`、`pairedSlotPairs`、`slotEvidence` を含む。
- [x] `getPageLayerTree` の JSON framing を修正し、page-decoration evidence を machine-parseable にする。tests は balanced JSON delimiters と `root.ops` から `textSources` への boundary を確認する。
- [x] local reference-backed export sweep で PDF text-layer preservation を保護する。generated PDFs は svg2pdf Form XObject path 内に `/ToUnicode` maps と CID font resources を保持し、path-only output へ silent regression しないことを確認する。
- [ ] `/PageLayoutStyle` nested `31xx..39xx` slot groups の exact semantics を decode する。どの active slot pairs が facing pages、header/footer areas、page-number fields に対応するかを含む。10 サンプル 5 ファミリーの part06 末尾 u16 クロスサンプル分析：`sample-outline`/`sample-draft`→0x32/0x33=1000/0x34=453/0x36=600/0x38=407；`sample-table`→0x32/0x33=800/0x34=453/0x36=407+773/0x38=411；`sample-draft-b`→0x32/0x33=2000/他は全て0；`sample-table-b` と `sample-table-c` は完全一致→0x32/0x33=1405/0x36=0+1397/他は0。part06 バイト 1 は `sample-outline`/`sample-draft` のみ 0x01；他全サンプルは 0x00。`sample-reference` 系と `sample-academic` には `/PageLayoutStyle` がない。参照 PDF との物理ページ寸法・ヘッダーフッター位置の照合はブロック中（a5.jtd 不在）。
- [ ] current decoded-false running header/page-number projection は、source slots、page parity rules、chapter-title binding、document-title binding が current a5-style evidence を超えて証明された時点で decoded layout records に置き換える。
- [x] suspicious `46.pdf` は full-document regression として扱う前に replace/quarantine する。local reference は `pypdf` で text extraction が空で、144dpi first-page render もほぼ blank/title-like だが、`46.jtd`/generated output は Ginga body text を持つ。updated `a6.pdf` reference は `114` pages の Ginga text として usable で、generated `openjtd-samples/pdf-output/a6.pdf` も A6 portrait MediaBox で `114` pages になった。確認済み：`46.pdf` は現行テストで参照 PDF として使われていない。`46.jtd` はファイル名ベースのページサイズ unit test にのみ使用。regression リスクなし。
- [x] `openjtd-samples/pdf-output` artifact regression coverage を Preview/PDFKit compatibility 向けに強化する。output-artifact test は PDFKit-risky な `/Group`、`/S /Transparency`、`/SMask` constructs を reject し、trusted local reference PDFs と generated PDF page-object counts を比較する。quarantined `46.pdf` は明示的に除外するため、Chrome/PDFium で見えるだけの状態に依存せず、usable reference-backed samples の page count を固定できる。
- [ ] `SO` object/control record family field semantics を decode する。current evidence は singleton records の fields 1-4 が geometry-like tuples、repeated records が default/control constants を持つことを示すが、`packed-jseq3-like` 16-bit halves の exact meaning は未証明。
- [x] 新しい PDF-reference `shanai_lan` sample を current fidelity sweep に追加し、A4 landscape sample hint を適用する。generated PDF は reference と同じ single A4 landscape page (`841.875x595.275` pt generated vs `841.920x595.320` pt reference) になり、未 decode の `/FigureData/main_data/FDMVector` content は fallback text を multiple pages に流すのではなく first page diagnostics として保持する。
- [x] `shanai_lan` を最初の FDMVector/Frame rendering target として使う。`/FigureData/main_data/FDMIndex` は 1 件、parsed entries 39、image-bearing entries 2、JPEG signatures 4、frame-linked image candidates 2 件 (`row=23`, `row=33`) を持つ。`getPageLayerTree` と SVG/PDF output は reference-backed `/Frame` projection (`x/width/height ÷ 24`, `y` direct) による decoded-false `fdmFrameDiagnostic` boxes を linked rows へ expose し、`placementProven:false`、`geometryDecoded:false`、`renderable:false` のまま保持する。
- [x] `shanai_lan` `fdmFrameDiagnostic` rows に FDM frame image-payload blocker を直接 expose する。frame rows `23` と `33` は `imagePayloadExtractionStatus:"signature-without-complete-payload"` と `renderPromotionBlockedReason:"image-signature-without-complete-payload-role-unproven"` を報告し、観測された `FFD8FF` hits が complete image payload ではなく JPEG-like vector byte patterns であるという raw FDMVector probe と一致する。
- [x] fallback frame boxes が emit される場合、同じ FDM frame payload blocker を SVG frame-diagnostic `data-*` attributes に mirror する。complete JPEG payload を持つ synthetic frame fixture は `data-image-payload-extraction-status="complete-payload-in-fdm-index-segment"` を expose しつつ、`fdm-frame-linked-image-payload-placement-and-paint-order-unproven` で block されたままにする。real `shanai_lan` layer-tree rows は引き続き signature-only evidence として block される。CLI `object-fdm-frame-links` も frame-linked complete payloads について generic `page-placement-unproven` ではなく同じ source-backed blocker を報告する。
- [x] FDMVector/FDMIndex pairing を exact sibling paths 以外にも generalize する。valid な exact `/FigureData/.../FDMVector` -> `/FigureData/.../FDMIndex` は引き続き優先するが、sibling index が存在しない、または invalid な場合は `/FDMIndex` streams を scan し、declared row count と valid vector offsets に基づく unique content-backed match を選ぶ。synthetic coverage では root `/FDMIndex` が nested `/FigureData/main_data/FDMVector` と pair でき、invalid decoy `/FigureData/other/FDMIndex` は reject される。
- [x] `shanai_lan` FDMVector segment command tables を parse する。各 segment は `0x01000b60` で始まり、segment length と command count、16-bit offset table を持つ。model は command records を保存し、bbox を持つ 334 件の `0xff000a60` commands を decoded-false `fdmVectorCommandDiagnostic` layer/SVG/PDF outlines として `fdmCommandBBoxReferenceProjection` で project する。
- [x] `shanai_lan` の source-backed FDMVector drawing primitives を promote する。model は two-point `0xff000160` line records、top-level `0xff000660`/`0xff000960` path records、`0xff000460`/`00000460` ellipse records、`0xff000a60` compound records 内の nested `00000660`/`00000960` と nested `ff000660`/`ff000960` primitives を保存する。`0xff000960`/`00000960` tails は cubic control-handle deltas として保持されるため、globe/orbit curves は angular polyline ではなく cubic Bezier として render される。`getPageLayerTree` は 1889 件の `fdmVectorPrimitiveProjection` ops を `geometryDecoded:true`、`primitiveKind`、`curveSegmentCount`、`styleWord`、BGR interpreted `fillColor`/`strokeColor`、inferred `renderStrokeColor`、`renderFillKind`/`renderGradient`、`strokeWidth`、`pathClosed`、source ellipse geometry、reference-backed `projectionViewport` evidence 付きで expose する。SVG/PDF は old blue bbox/path debug overlays ではなく source-backed strokes、conservative closed-path fills、source-prefix-backed linear gradients、ellipse dots/circles、black device fills 内の uncolored closed `ff000660` port-box paths への source-bbox-gated white stroke を render し、latest `shanai_lan` 144dpi raster diff mean は約 `24.35`、right globe crop RMS は cubic-handle projection 後 `103.85` から `96.07` へ改善した。
- [x] source-backed `shanai_lan` `DocumentText` labels を同じ reference-backed diagram viewport に合わせ、`/DocumentText` の `0x001c/0x0010` text-group records と nearest `0x001c/0x0030` line headers から project する。layer tree は source byte/unit spans、`groupIndex`、`lineOffsetUnits`、`leadingUnits`、`fragmentStartUnits`、`splitFromTextRun`、`lineHeaderHex`、`baselineFactor:0.800` を持つ `documentTextGroupLineProjection` text runs を expose する。large whitespace で区切られた multi-label runs は source-spanned fragments に分割し、bottom captions を independent x coordinates に配置する。color は label string exception を使わず、trailing `TextV.01` style event section の property 15 が exact source range 全体で uniform な 16 slots にだけ適用し、残り 22 slots は default black のままにする。
- [x] rendered `shanai_lan` label slot ごとに decoded-false `/DocumentTextPositionTables` `TCntV.01` overlap evidence を付与し、この range を anchor や color selector としては使わない。`getPageLayerTree` は slot ごとに `textCountRangeEvidence` と `textCountRangeEvidenceCount` を記録し、SVG も同じ evidence を `data-*` attributes として出す。`fillColorBasis` は `document-text-style-property-15-text-run-candidate` と `default-text-fill` を区別し、`TCntV.01` は比較 evidence のままにする。
- [x] `shanai_lan` label style-link evidence を property-15 color candidate と分離して expose する。`getPageLayerTree` は label slot ごとに `textStyleLinkEvidence` を emit し、SVG も `data-style-link-*` attributes として mirror する。`TextLayoutStyle` record count `0`、`/DocumentViewStyles` group count `9`、`TCntV.01` tail-field-f7 group candidate、`styleLinkProven:false`、`document-view-style-group-link-unproven` はそのまま保持する。property-15 color は別の `documentTextProperty15ColorCandidate` evidence に `propertyRoleDecoded:false` と `shanai-lan-text-projection` context gate を持たせる。
- [x] 同じ `shanai_lan` label style-link diagnostics に decoded-false `/DocumentText` inline style-candidate evidence を追加する。title label は直前の raw inline block (`001c 0001 0007 0000 0000 0001 001d 0002 001e 0005 0000 0001 001f`) を selector `0x0001`、source span `60..86`、payload word `0x0002`、post-inline words `0005 0000 0001 001f`、blocker `document-text-inline-control-semantics-unproven` として expose する。この block は comparison evidence であり、title color authority は exact-range property 15 candidate から得る。
- [x] `shanai_lan` label style diagnostics に decoded-false `/DocumentText` group-header evidence を追加する。`getPageLayerTree` は visible label slots 35 件に `documentTextGroupHeaderCandidate` を記録し、SVG も `data-document-text-group-header-*` attributes として mirror する。blue `ファイルサーバ` label は source span `286..324` と raw words `001c 0010 0013 0000 008f 0007 0118 0000 0052 0008 005b 0000 0068 ffff 0000 0013 0000 0010 001f` を持ち、`ｶﾗｰｼﾞｪｯﾄﾌﾟﾘﾝﾀｰ` は近接する `0017` group header を持つ。これらの fields は比較用に保存するが、同じ header family は non-blue labels の周辺にも出るため `document-text-group-header-semantics-unproven` で rendering promotion を block する。
- [x] `shanai_lan` label styling 用に decoded-false aggregate `shanaiLanTextStyleEvidenceSummary` layer op を追加する。summary は text slots 38 件、`/DocumentViewStyles` group-candidate slots 8 件、`/DocumentText` group-header candidates 35 件、inline style candidate 1 件、source-property-colored slots 16 件、default-black slots 22 件を記録する。source-property distribution は green 1 (`#008000`)、blue 14 (`#000080`)、dark blue 1 (`#000066`) である。same `0x0017` header family は default-black と property-15-blue の両方を含むため、group headers は comparison evidence のままにする。
- [x] split された `shanai_lan` labels の parent text-run fragment context を保存する。`getPageLayerTree` は label ごとに `textRunFragmentContext` を emit し、SVG も `data-parent-text-run-*` / `data-fragment-*` attributes として mirror する。current sample は split slots 14 件、maximum fragment count 5 件を持つ。bottom parent run `byte 12060..12384` / `unit 6030..6192` の 5 labels は exact property-15 state がすべて同じため blue で render するが、general fragment style semantics は undecoded のままにする。
- [x] `shanai_lan` color/style evidence を再確認し、label-string color exceptions を trailing `TextV.01` style event stream に置き換える。persistent-state resolver は property changes と run lengths を exact `/DocumentText` source units に map する。この projection 内でのみ uniform property 15 values を packed BGR として `0x00008000 -> #008000`、`0x00800000 -> #000080`、`0x00660000 -> #000066` に map し、`0xffffffff` は automatic/default のままにする。state boundary をまたぐ fragment は promote しない。
- [x] fragment-level style hypothesis を parent-run level で検査できるよう、`shanai_lan` label style evidence に `multiFragmentParentRunFillMixes` を追加する。current sample は multi-fragment parent text runs 5 件、split slots 14 件、`mixedFillMultiFragmentParentRunCount:2` を持つ。runs `6010..6096` と `9494..9628` は default-black 1 fragment と property-15-blue 1 fragment をそれぞれ含み、bottom run `12060..12384` は 5 fragments すべて blue である。cross-sample `hyo` では property 15 が non-text/control state にも現れるため universal role は undecoded のままとし、`shanai_lan` projection の uniform exact text ranges だけに gate する。
- [ ] `shanai_lan` の single-page LAN diagram を diagnostic Frame boxes ではなく actual diagram objects として render できるよう、`/FigureData/main_data/FDMVector` drawing/image payload semantics を decode する。
- [x] rendering を変えずに object geometry 用の source-backed `fdmIndexSegmentBboxAxisPairGate` を追加する。valid FDMIndex row は vector offset が sibling FDMVector segment の header bbox に到達する場合だけ対象とし、raw index fields が `left`、`right`、`top`、`bottom` の順で segment と完全一致するときだけ `normalizationInputSourceBacked:true` を報告する。`shanai_lan` は valid linked rows 39/39 が一致し、local corpus でも observed FDMIndex/FDMVector candidates の linked rows 77 件中 70 件が一致する。unmatched 7 rows は gate の外に残す。evidence は `decoded:false`、`diagnosticOnly:true`、`renderable:false` のままとし、page placement と object-role promotion は `fdm-index-axis-pair-does-not-decode-page-transform-or-object-role` で block する。
- [x] `shanai_lan` FDMVector compound prefix trailer の最も common な形を style trailer ではなく nested child offset table として decode する。model は parent `fdmVectorCommandDiagnostic` ops に marker-valid `compoundChildOffsets` を保存し、non-marker child-area end words はこの list から除外する。nested primitive parser は marker scan に fallback する前にこの offsets を使うため、row/parent paint order が source-backed になり false marker risk が下がる。
- [x] `shanai_lan` compound fill semantics を source-backed に保存する。parent style `0x0001` で single child を持つ `0xff000a60` compound は、prefix 先頭 2 dwords を BGR gradient stops (`strokeColor -> fillColor`) として扱い、SVG/PDF linear gradients として render する。これにより laptop/CRT/monitor screen fills と clip-art shaded surfaces は `#003366 -> #7a7acc` の hardcode ではなく source prefix から復元される。
- [x] `shanai_lan` text/icon mask の filled path counters を保存する。same-compound holes と black/white text-mask pairs は explicit counter overlays として replay されるため、Router `R`/`o`/`e` interiors は source child order によって白く repaint されず black のまま残る。
- [x] `shanai_lan` text slots の source-backed line-header y-placement candidates を、rendering は変更せず保存する。`getPageLayerTree` は `lineHeaderSameSegmentGroupRun` と `lineHeaderYPlacementCandidate` を emit し、SVG も同じ run facts を `data-line-header-same-segment-run-*` attributes として mirror する。分離された `RAID`/`5` label (`offsetUnits=84`, `extentUnits=174`, groups `1..6`, `positionInRun=1`) や `CD-RW` (`offsetUnits=0`, `extentUnits=84`, groups `27..31`, `positionInRun=2`) を捕捉しつつ、その run をまだ row anchor に使えない理由も peer counts として保存する: `RAID`/`5` run は visible text slots 5 件 / distinct groups 4 件にまたがり、`CD-RW` run も slots 4 件 / groups 2 件にまたがる。render promotion は `line-header-y-run-placement-semantics-unproven`、detail `same-segment-run-spans-multiple-visible-text-rows` のまま block する。
- [x] label placement を変更する前に、`shanai_lan` FDMVector closed-fill text/icon masks 用の source-backed `fdmTextMaskCohortSummary` diagnostic を追加する。この summary は black/white closed-fill primitives を FDM row ごとに group し、right-neighbor の `/DocumentText` slots を探すが、alignment は promote しない。`RAID` の FDM path cohort は row `5` (`22` primitives, projected bbox `465.107,84.575,34.683,76.196`) として捕捉され、近接する `/DocumentText` `5` slot は diagnostic right-neighbor candidate (`gap=12.464px`, vertical overlap `19.950px`, center delta `-19.998px`) として記録される。同じ row は source bbox adjacency によって components にも分割され (`topTextLikeComponentCandidateCount:3`, `componentRightNeighborCandidateCount:1`)、server-rack rectangles から top glyph-like `RAID` component (`7` primitives, bbox `465.204,84.575,34.586,10.435`) を分離する。この component-level right-neighbor relation は `/DocumentText` `5` に対して vertical overlap `2.310px` と center delta `12.882px` を記録する。さらに component relation は `leadingWhitespaceBridgeCandidate` も expose する: `/DocumentText` parent run は unit `578..613`、pre-fragment span は `578..599` (`21` units)、pre-fragment projection は `42` grid units / `155.418px` で、FDM glyph component はその projected pre-fragment corridor 内にある (`sourceBboxWithinPreFragmentProjection:true`, offset `108.368..142.954px`, `5` の手前 `12.464px`)。real な FDM-mask-to-DocumentText baseline transform が decode されるまで rendering は `fdm-text-mask-document-text-alignment-unproven` / `fdm-text-mask-component-to-document-text-alignment-unproven` / `document-text-pre-fragment-fdm-mask-role-unproven` / `fdm-text-mask-to-document-text-baseline-transform-unproven` で block する。
- [x] `shanai_lan` に non-rendering FDM text-mask promotion readiness と projection extent diagnostics を追加する。`getPageLayerTree` は `fdmProjectionExtentSummary` を 1 件 emit する。FDMIndex entry の bbox-like fields は entry 専用の axis pair (`x0,x1,y0,y1`) として normalize し、FDMVector command bbox は通常の left/top/right/bottom semantics のまま扱う。この normalize 後、active command extent (`-16154,-16224,-5612,-9344`)、rendered primitive source extent (`-16154,-16224,-5453,-9344`)、FDMIndex entry extent (`-16154,-16224,-5576,-9336`) は近くなるがまだ一致しない (`commandVsFdmIndexEntries.maxAbsDelta:36`, `renderedPrimitivesVsFdmIndexEntries.maxAbsDelta:123`)。さらに `RAID` 周辺の reference crop では generated glyph が約 `10.7px` 左、`12.1px` 上に残るため、page-placement transform authority は `fdm-vector-page-placement-transform-source-fields-unproven` のまま block される。text-mask cohorts/components も normalized `sourceBbox` を expose するようになり、`RAID` row 5 cohort source bbox は `-11828,-15758,-11470,-14984`、top glyph component source bbox は `-11827,-15758,-11470,-15652` で、FDMIndex row coordinate family とは一致するが projection はまだ上/左にずれている。さらに 7 件の `fdmTextMaskRightNeighborPromotionReadiness` を emit する。`RAID`/`5` case は slot `5` で cohort/component agreement を持ち、FDM glyph bbox は pre-fragment corridor 内にあり、`preFragmentUnitCount:21` だが、`splitFromTextRun:false`、`sameSegmentGroupRunDistinctTextGroupCount:4`、`rowAnchorAmbiguous:true` と、`document-text-slot-not-split-from-text-run` / `line-header-y-run-placement-semantics-unproven` / `document-text-pre-fragment-fdm-mask-role-unproven` / `fdm-text-mask-to-document-text-baseline-transform-unproven` blockers を報告する。これにより `5` を動かしたり mask/text alignment を広げたりせず evidence だけを保存する。
- [x] 同じ `shanai_lan` FDM/text bridge に diagnostic-only `fdmTextMaskSourceTransformCandidateSummary` を追加する。`RAID` の top text-like component から `/DocumentText` slot `5` への current-projection candidate を 1 件導出し、current grid offset range `29.285..38.632`、`sourceUnitsPerTextGridUnitX:38.196`、推定 line-start source x `-12945.565`、text-start source x `-11341.348`、source gap to text start `128.652` を記録する。この candidate は text grid origin がまだ reference viewport 由来なので意図的に `referenceBacked:true` のままにし、row-anchor ambiguity、unsplit text slot evidence、pre-fragment role uncertainty、baseline transform uncertainty、cross-sample support missing により render promotion を block する。
- [x] `shanai_lan` の `documentTextLineHeaderProjectionCandidateSummary` に `gridOriginAuthorityGate` を追加する。選択済み visible line-header set は source-domain row-anchor fit を持つ: selected line headers 7 件、selected groups `[21,31]`、LineMark records `[22,32]`、LineMark coverage complete、uniform `recordIndexMinusGroupIndex:[1]`、PageMark entry row `0` (`lineStart:0`, `lineEnd:39`) の coverage である。これは `/DocumentText group -> /LineMark record` の row-domain relation として有用だが、page-space origin はまだ証明しない。gate は `referenceBacked:true`、`pageSpaceOriginCandidate:null` のまま、`document-text-grid-origin-reference-backed` / `line-header-visible-rule-selector-unproven` / `page-space-y-origin-unproven` で block される。
- [x] 同じ gate に selected `/LineMark` source-unit intervals を保存する。`selectedLineMarkSourceUnitGate` は selected records `[22,32]`、unit starts `[3908,5483]`、unit ends `[4121,5615]`、spans `[213,132]`、record delta `[10]`、unit-start delta `[1575]` (`sourceUnitDeltaPerRecordEstimate:157.500`) を記録する。stride sample が 1 件だけで、source-unit-to-page-y transform もまだないため、これは non-promotable のままにする。
- [x] 同じ `gridOriginAuthorityGate` に `sourceOnlyPageMarkYValueProbe` を追加する。selected PageMark entry は parsed y-value candidates 58 件を出し、current origin probe に最も近いものは `parsedEntryU16` word `7` / byte `14`、value `39`、current origin `38.700` から residual `0.300px` である。companion `pageMarkEntryProfileGate` はこの entry が additive layout-origin profile ではなく `mixed-payload` であることを記録し、`lineBoundaryConflictGate` は同じ値が entry の `lineEnd=39` に一致することを記録する (`matchesLineEnd:true`) ため、これは意図的に promote しない。probe は `page-mark-y-value-field-role-unproven`、`document-text-grid-origin-reference-backed`、`page-space-y-origin-unproven` で block する。
- [x] `shanai_lan` text slots と line-rule diagnostics の raw `/DocumentText` line-header words を保存する。`getPageLayerTree` は `lineHeaderRawWords` と `lineHeaderRawWordsHex` を emit し、`page-svg` は rendered LAN labels に `data-line-header-raw-words-hex` を expose する。current sample の visible line-header family は一貫して `001c 0030 000c 0000 <offset> <extent> 00ff 0000 000c 0000 0030 001f` なので、疑っていた hidden y/dash fields はこの 24-byte header 自体には見つからない。fresh visual probing でも direct rendering は rejected のまま: current 16 line-rule projections は Poppler whole-page MAE/RMS を `24.536/71.636` から `26.062/74.096` に悪化させ、FDM connector mask matching も LAN trunk lines ではなく icon/globe internals を上位に出し続ける。generated `shanai_lan.pdf` は diagnostic-only state に戻して再生成し、Poppler、PDFKit、CoreGraphics/sips で nonblank を確認済み。
- [x] `shanai_lan` line-rule promotion blockers を `documentTextLineRuleProjectionSummary` op 1 件に aggregate する。current sample は line-rule candidates 16 件 (skipped-inline horizontal 7 件、vertical-anchor 9 件) を持ち、16 件すべてが `/LineMark` interval と match し、11 件は orthogonal に見える。しかし 32 endpoints 中 text slot へ 1 line-height 以内で attach するものは 2 件だけで、both endpoints が attach する rule は 0 件である。fresh raster overlay sweep でも broad source-only subset は reference diff を改善しなかったため、endpoint ownership と row-boundary semantics が decode されるまで diagnostic-only のままにする。
- [x] `shanai_lan` `fdmConnectorCandidateDiagnostic` ops に root-level parent compound provenance を expose する。synthetic nested connector は decoded-false `parentCompoundCommand` object を持ち、`parentCommandIndex`、`parentMarkerHex`、`parentRelativeOffset`、`parentCompoundChildOffsets`、`childOffsetInParent`、`childOffsetTableMatched` を出す。これにより `ff000a60 -> ff000660/ff000960` child relationship を endpoint-owner summaries や external JSON scripts に依存せず確認できる。これは diagnostic-only であり connector rendering は promote しない。
- [x] `shanai_lan` connector candidates に parent-normalized owner-order diagnostics を追加する。`endpointOwnerMatchSummary` は connector/nearest-owner parent relative offsets、parent-command-span booleans、`ownerParentCommandRelation`、`ownerParentSourceOrderRelation`、`parentNormalizedOrderedSameRowSameConnector` を emit し、graph summary も `parentNormalizedOrderedSameRowSameConnectorCount` と `parentNormalizedOwnerRowCohortEndpointMatchSummaries` を出す。current evidence はまだ rendering を block する: PDF-backed sample は connector candidates 67、matched connectors 9、thresholded endpoint matches 10、dual-endpoint matches 0、raw ordered same-row/same-connector candidates 0、parent-normalized ordered same-row/same-connector candidates 0 である。parent-normalized relation counts は 64 `same-row-after-owner-parent-command-span`、1 `same-row-before-owner-parent-command-span`、1 `missing-endpoint-owner-candidate`、1 `nearest-owner-row-mismatch` で、10 件すべての thresholded endpoint matches は non-parent-normalized-ordered cohort に残る。
- [x] `shanai_lan` `fdmConnectorGraphDiagnosticSummary` に endpoint-side と dominant-row diagnostics を追加する。current source-backed graph は dual-endpoint line-rule match がまだ 0 件で、4 connector candidates は start endpoint だけ、5 candidates は end endpoint だけ match する。最も match が集中する FDM row 33 も connector candidates 14 件、matched connectors 7 件、thresholded endpoint matches 8 件、dual-endpoint matches 0 件、start-only 2 件、end-only 5 件にとどまる。この false-positive cluster を external JSON scripts なしで確認できるようにしつつ、connector rendering は blocked のまま維持する。
- [x] `shanai_lan` fallback `fdmCommandBBoxReferenceProjection` SVG diagnostics から最後の row-index based styling を除去する。image-bearing command boxes は hardcoded rows `23` / `33` ではなく、source evidence (`FDMIndex.imageSignatures` / `segmentImageSignatures`) に基づいて highlight されるようになり、SVG は `data-diagnostic-style-basis`、signature counts、`data-valid-vector-offset` を監査用に記録する。primitive rendering が可能な場合は diagnostic overlay を promote せず、他の FDMVector documents でも再利用できる fallback path にする。
- [x] `shanai_lan` `/DocumentText` line-rule candidates を source-backed graph components に aggregate する。`documentTextLineRuleProjectionSummary` は `lineRuleGraphComponentCount:6`、`largestLineRuleGraphComponentRuleCount:4` と、component ごとの rule indexes、bbox、horizontal/vertical counts、LineMark coverage、isolated endpoint totals、projected length、component-level render blocker を expose する。最も wiring-like な large components は `[2,5,13,14]` (`bbox 363.038,433.500,261.430,200.400`, length `765.858px`) と `[3,6,15]` (`bbox 725.679,415.500,357.641,200.400`, length `908.482px`) だが、temporary raster overlays of line-rule subsets はまだ RMS を悪化させるため、placement/style/endpoint semantics が証明されるまで diagnostic-only のままにする。
- [x] `shanai_lan` wiring rendering を広げる前に、長い `/DocumentText` line-header candidates をすべて expose する。`getPageLayerTree` は `documentTextLineHeaderProjectionCandidateSummary` op を 1 件 emit し、parsed line headers 171 件、long line-header candidates 103 件、current horizontal line rules として選ばれた skipped-inline long candidates 7 件、`line-header-visible-rule-selector-unproven` で block された unselected long candidates 96 件を報告する。これにより upper/middle の wiring-like spans は `/DocumentText` line headers に既に存在することが分かる (例: group 17, units `130..280`, bbox `525.856,361.500,557.464,2.400`) が、visibility selector が decode されるまでは render しない。併せて FDMVector large-span diagnostic を追加して確認したところ、この sample では previous page-span filter に block された primitive は 0 件だったため、missing trunk wiring はその filter では説明できない。
- [x] raw headers を promote せず、`shanai_lan` line-header visibility probe に source-backed context を追加する。長い `documentTextLineHeaderProjectionCandidateSummary` candidate は `lineMarkContext`、`documentTextMapContext`、`sameSegmentGroupRun` を持ち、summary は 41 件の `sameSegmentGroupRuns` も aggregate するようになったため、repeated offset/extent families を external scripts なしで確認できる。これで current split が固定される: selected horizontal rules 7 件はすべて real `DocumentTextMapKind::SkippedInlineText` entries 内にあり、selectors は `0x008f` または `0x0010` である。一方、group 17 `130..280` のような unselected wiring-like headers は同じ 24-byte raw header shell を持つものの containing map entry を持たず、ordinary text/control entries の間にある structural header として現れる。長い `184..280` family は groups `20..31` に 12 headers あるが、selected/skipped-inline は 2 件だけで、10 件は containing map entry を持たない。これにより current visible rule set が longer trunk-like run の fragments だけを描く理由を source-backed に説明できる。fresh な reference-backed dark-pixel probe では 103 long headers のうち missing-dark ratio `0.10` 超えは 12 件だけで、そこにも text/image residuals と wiring が混在するため、source-backed selector が decode されるまで visible PDF output には promote しない。
- [x] `shanai_lan` の missing wiring 作業で individual primitive scraping に頼らず open FDM stroke families と `/DocumentText` line headers を分離できるよう、non-rendering `fdmOpenStrokeCohortSummary` diagnostics を追加する。current page は FDM primitive projections 1889 件、open strokes 901 件、connector candidates 67 件、horizontal 267、vertical 145、diagonal 489、line-marker open strokes 588、non-line-marker open strokes 313、row count 22 を持つ。raw connector candidate count で dominant な open-stroke row は row 0 (`24` connector candidates, `155` open strokes) だが、既存の line-rule endpoint-match summary は row 33 (`14` connector candidates, `7` matched connectors, `0` dual-endpoint matches) に集中する。この不一致を `getPageLayerTree` と `getDocumentInfo` の両方で expose し、source ownership/paint order がどの row family を real LAN wiring として扱うべきか説明するまで、connector rendering は `open-stroke-role-and-paint-order-unproven` のまま block する。
- [x] visible PDF output を変えずに、same-row FDM open-stroke axis-rule evidence を `shanai_lan` connector graph diagnostics に追加する。graph summary は `fdmOpenStrokeAxisRuleProjectionCount:224` と、各 connector と同じ FDM row にある non-connector open FDM primitives 由来の `sameRowFdmOpenStrokeAxisRules` provenance bucket を報告する。この bucket は 67 connector candidates のうち 52 件で 254 thresholded endpoint matches を見つけ、21 件は dual-endpoint candidates になる (`tight`: 170、`nearby`: 84) が、row ownership と paint order がまだ decode されていないため、21 件すべてを `connector-ownership-and-paint-order-unproven` として blocked のままにする。
- [x] same-row FDM open-stroke axis-rule evidence を各 `fdmConnectorCandidateDiagnostic` に mirror する。67 connector candidates すべてが `sameRowFdmOpenStrokeAxisRuleEndpointMatches` と per-connector summary を持ち、source rule command indexes、relative offsets、source segment provenance、projected/source bboxes、tier、grid deltas、closest points を expose する。隣接する `sameRowFdmOpenStrokeAxisRuleOwnerPromotionGate` は既存の owner/source-order evidence をこの geometry filter の横に mirror する。current dual-endpoint candidates は rows `0`、`2`、`3`、`10`、`33`、`37` に集中するが、21 件中 18 件はまだ `same-row-mixed-command-namespace`、20 件は `same-row-after-owner-relative-offset-span`、`parentNormalizedOrderedSameRowSameConnector` は 0 件なので、これは comparison evidence であり render gate ではない。
- [x] wiring probe を external JSON scripts に頼らず確認できるよう、same-row FDM open-stroke axis-rule row cohorts を `fdmConnectorGraphDiagnosticSummary` に aggregate する。新しい `sameRowFdmOpenStrokeAxisRuleRowCohorts` field は dual-endpoint evidence で row cohorts を rank し、row 33 を leading source-backed cluster として expose する: connector candidates 14、thresholded endpoint matches 33、matched connectors 14、dual-endpoint candidates 7、non-diagonal dual candidates 5、tight-dual candidates 6、non-diagonal tight-dual candidates 5。fresh Poppler A/B では broad connector/axis-rule rendering はまだ rejected だが、row-33 non-diagonal axis-rule overlay だけは very small local improvement (`whole RMS -0.0077`, row33 crop RMS `-0.3148`) を示した。そのためこれは diagnostic-only のまま維持し、`connector-ownership-and-paint-order-unproven` で block する。
- [x] `shanai_lan` graph-level connector blocker を refine し、axis-rule evidence が older all-line-rule summary に隠れないようにする。layer tree は document-text line rules については `dualEndpointMatchConnectorCount:0` のままだが、`sameRowFdmOpenStrokeAxisRules` は 21 dual-endpoint candidates と `parentNormalizedOrderedSameRowSameConnectorCount:0` を報告するため、`renderPromotionBlockedReason` は `no-dual-endpoint-line-rule-match` ではなく `same-row-axis-rule-parent-normalized-order-unproven` になった。visible connector rendering は引き続き disabled。
- [x] `shanai_lan` graph summary に `sameRowFdmOpenStrokeAxisRuleOwnerPromotionGateSummary` を追加し、21 件の axis-rule dual candidates の owner/order gate を 67 件の per-connector diagnostics を読まずに確認できるようにする。new aggregate は 21 件すべてが dual endpoint owner candidates と matching nearest-owner rows を持つ一方で、parent-normalized ordered same-row/same-connector gate は 0 件であることを示す。parent command/source-order relations は 20 件が `after`、1 件が `before`、`between` は 0 件で、`parentNormalizedOrderGateBlockedReason:"connector-parent-command-outside-nearest-owner-parent-command-span"` も expose するため、rendering は `same-row-axis-rule-parent-normalized-order-unproven` として block のまま。
- [x] connector rendering promotion の前段として、`shanai_lan` same-row FDM open-stroke axis-rule gate に source-backed bbox と per-candidate tight-match diagnostics を追加する。`sameRowFdmOpenStrokeAxisRuleRowCohorts` は matched / dual-endpoint projected bbox union を emit し、leading row 33 dual cluster は generated CSS pixels で `x=684.639,y=618.639,w=97.850,h=97.362`、tight non-diagonal dual subset は `x=685.802,y=622.380,w=85.255,h=93.621` を覆う。graph summary は diagnostic-only `renderReadinessPredicate` も expose し、`sameRowFdmOpenStrokeAxisRule+tightDualEndpoint+nonDiagonalConnector` は current `candidateCount:16` / `rowCohortCount:6` だが、まだ `connector-ownership-and-paint-order-unproven` で blocked である。各 `fdmConnectorCandidateDiagnostic` も `startTightMatchCount`、`endTightMatchCount`、`tightDualEndpointMatch`、`parentNormalizedOrderGateBlockedReason`、connector parent command/relative offsets、nearest-owner parent offsets、parent command/source-order before/between/after booleans を expose する。visible PDF output は変えず (`shanai_lan` Poppler 144dpi mean/RMS は `24.536/71.636` のまま)、次の render predicate を row number や sample name hardcode なしで検査できるようにする。
- [x] wiring blocker を connector ごとに見なくても確認できるよう、`sameRowFdmOpenStrokeAxisRuleRowCohorts` に row-level `ownerPromotionGate` summaries を追加する。row 33 は dual-endpoint axis-rule candidates 7 件を報告し、7 件すべてで endpoint owner row は connector row と一致するが、7 件すべて `same-row-after-owner-parent-command-span` / `same-row-after-owner-parent-relative-offset-span` であり、parent-normalized ordered candidates は 0 件である。row 3 は dual-endpoint candidates 5 件で、owner parent/source span の before 1 件、after 4 件、between 0 件となる。どの row も between/ordered owner relation を証明していないため、visible connector rendering は `connector-parent-command-outside-nearest-owner-parent-command-span` で blocked のままにする。regenerated `shanai_lan.pdf` は `sips` と Swift `PDFKit` で nonblank rendering を確認したが、Preview.app UI の直接 automation は sandbox 内ではできない。
- [x] source-backed parent-normalized owner/order gate を強化し、`parentNormalizedOrderedSameRowSameConnector` が nearest-owner parent command span と nearest-owner parent relative-offset span の両方を要求するようにする。これにより command-only match が source-order evidence と一致する前に render authority になることを防ぐ。regression test `fdm_connector_parent_normalized_order_requires_parent_relative_offset_between_nearest_owner_parents` は false/true cases を固定し、current `shanai_lan` axis-rule sweep は dual-endpoint candidates 21、parent-normalized promotions 0、command/source parent relations `20 after / 1 before / 0 between`、graph blocker `same-row-axis-rule-parent-normalized-order-unproven` のままである。regenerated `success_data-test.pdf` と `shanai_lan.pdf` は Apple `sips` と Swift `PDFKit` で nonblank。
- [x] `shanai_lan` graph-level connector blocker を source-backed owner/order gate に結合し、generic な axis-rule transition blocker のままにしないようにする。same-row FDM open-stroke axis-rule evidence があり aggregate owner gate が存在する場合、`fdmConnectorGraphDiagnosticSummary.renderPromotionBlockedReason` は具体的な owner/order reason、現在は `connector-parent-command-outside-nearest-owner-parent-command-span`、を surface する。owner gate 自体もすべての dual-endpoint candidates が parent-command axis と parent-relative-offset axis の両方で coherent になるまで `none` を返さない。rendering は引き続き disabled であり、これは partially coherent wiring cluster が render authority になることを防ぐための diagnostic hardening である。
- [x] likely wiring rows と device/internal stroke clusters を render なしで分離できるよう、`sameRowFdmOpenStrokeAxisRuleRowCohorts` に source-backed marker/style profiles を追加する。各 row cohort は `matchedConnectorMarkerStyleProfile`、`dualConnectorMarkerStyleProfile`、`tightNonDiagonalDualConnectorMarkerStyleProfile`、`axisRuleEndpointMatchMarkerStyleProfile` を持ち、sample coordinates ではなく raw FDM marker family と style word を aggregate する。current leading row 33 の dual connectors は `line-marker=1/path-marker=6`、style は `0x0005=1/0x0080=2/0x00a0=4` で、matched axis rules は `line-marker=17/path-marker=16` かつ `other-style=4` を含む mixed style words である。row 3 は dual connectors 5 件すべてが path markers で、axis-rule endpoint matches 57 件は `line-marker=43/path-marker=14` に分かれる。これにより candidate set は diagnostic-only のまま維持しつつ、次の false-positive filter 用の general source field を得る。
- [x] axis-rule rows を all open-stroke row families と比較できるよう、同じ marker/style profile を broader `fdmOpenStrokeCohortSummary` row cohorts にも mirror する。dominant connector row 0 は open strokes 155 件、`line-marker=73/path-marker=82`、style `0x0000=152` である一方、row 33 は open strokes 47 件、`line-marker=21/path-marker=26`、mixed styles `0x0005=21/0x0080=12/0x00a0=8/other=6` を持つ。これは引き続き diagnostic-only だが、individual primitive ops を scrape せず `getPageLayerTree` と `getDocumentInfo` から次の source-backed role gate を検査できる。
- [x] すべての open-stroke marker/style profile に decoded-false marker/style `roleGate` を追加する。gate は marker/style diversity、dominant marker family/style word、homogeneous flags、explicit blocked reason を報告する。current shanai_lan evidence では leading row 33 は `mixed-marker-family-and-style-word-role-unproven`、row 3 axis-rule matches も同様に mixed として blocked される。一方 row 37/row 2 のような homogeneous rows でも `homogeneous-marker-style-still-needs-owner-and-paint-order` として blocked のままなので、style homogeneity shortcut が ownership と paint order の decode 前に render authority へ昇格することを防ぐ。
- [x] `sameRowFdmOpenStrokeAxisRule+tightDualEndpoint+nonDiagonalConnector` readiness predicate の visible rendering は現時点では rejected とする。source-backed かつ non-hardcoded な SVG/PDF probe は既存の `fdm_projected_path_data` で 16 candidates を描画したが、LAN trunk topology ではなく device/internal strokes の重複に近く、Poppler 144dpi whole-page diff をおよそ `24.536/71.636` から `24.565/71.697` に悪化させた。この probe は削除し、`shanai_lan.pdf` は diagnostic-only state に戻して再生成した。rendering は引き続き `connector-ownership-and-paint-order-unproven` で block する。
- [ ] 残る `shanai_lan` connector graph semantics を decode する。recovered FDMVector primitives は device silhouettes、一部の fills/gradients、BGR color families、width states、source-backed text positions を再構成し、model/export/layer-tree surfaces は paint/path segment fields 付きの long open-path `fdmConnectorCandidate` evidence を保存する。これらの connector candidates を直接 render する実験は、すべて whole-page MAE/RMS を悪化させ false positives も出たため rejected とする。grouped connector ownership、exact text style/color controls、multi-label run splitting、exact stroke style semantics、image/object layering、paint order が decode されるまでは diagnostic-only のままにする。`documentTextLineRuleProjection` は stable `ruleIndex`、render-block reasons、endpoint junction degrees、nearest-text endpoint attachment candidates を expose し、`fdmConnectorCandidateDiagnostic` は `projectedTextGrid` coordinates、`endpointOwnerCandidates`、`endpointOwnerMatchSummary`、nearest `lineRuleAttachmentCandidates`、thresholded `lineRuleEndpointMatches`、`lineRuleEndpointMatchSummary` を expose するため、次の promotion attempt は raw page-space spans ではなく同じ document-text grid 上で FDM endpoints、candidate owner objects、line-rule spans、text slots を比較できる。endpoint owner surface は source-backed だが proven ではない: 67 件すべての connector diagnostics が start/end owner candidate arrays と per-connector owner summaries を持ち、text line-height probe radius (`18.000px`) で endpoint あたり 3 candidates に cap される。current sample は owner candidates 402 件 (`401` FDM primitive、`1` DocumentText slot; 372 within probe radius) を emit し、66 connectors は both endpoints に within-probe owner candidates を持つが、すべて `ownerProven:false` で `endpoint-owner-candidate-unproven` または `missing-endpoint-owner-candidate` として blocked のままにする。Nearest-FDM-owner grouping も diagnostic-only で、65 connectors は nearest owners が connector と同じ FDM row にあり、1 dual-owner connector は row mismatch、connector command が nearest owner commands の間に入るものは 8 件だけで、43 件は nearest owner command span より前、15 件は後にあり、mixed top-level vs synthetic-nested command namespace を除外すると same-row/same-connector ordered-owner gate を満たすものは 0 件である。`ownerGroupingProvenConnectorCount` は 0 のままにする。`getPageLayerTree` は current blocker state を aggregate する `fdmConnectorGraphDiagnosticSummary` op も 1 件 emit する。latest `shanai_lan` graph-match diagnostic は connector candidates 67 件、endpoint probes 134 件を持つが、thresholded endpoint matches は 9 connector candidates の 10 件だけ (`tight`: 2、`nearby`: 8) で、matched candidates 9 件はすべて `single-or-missing-endpoint-line-rule-match`、残り 58 件は `no-thresholded-line-rule-endpoint-match` として blocked され、aggregate render block は `no-dual-endpoint-line-rule-match` になる。graph summary は line-rule provenance、owner-row cohort、owner-command relation と owner source-order relation 別にも endpoint matches を分けて expose するようになった。skipped-inline line headers だけで thresholded matches 10 件中 9 件を説明し、vertical-anchor line rules は nearby single-endpoint match を 1 件だけ追加し、どちらも dual-endpoint match は 0 件のままである。さらに same-row/same-connector ordered-owner candidates 0 件は thresholded line-rule endpoint match が 0 件で、残り 67 connectors が 10 件すべての matches を持つ。comparable command-index namespace を要求した後の relation cross-tab では thresholded endpoint matches 10 件すべてが `same-row-mixed-command-namespace` bucket (49 connectors、9 matched) に入り、`same-row-before-owner-command-span` は 1 connector で 0 matches、`same-row-between-owner-command-span` は 0 connectors で 0 matches、`same-row-after-owner-command-span` は 15 connectors で 0 matches、missing-owner/row-mismatch buckets は各 1 connector で 0 matches である。source-order relation は `connectorRelativeOffset` と nearest owner `relativeOffset` も emit し、row-local byte offsets が比較可能な diagnostic evidence であることを示す: `same-row-after-owner-relative-offset-span` は 64 connectors と thresholded endpoint matches 10 件すべて (9 matched connectors) を持ち、`same-row-before-owner-relative-offset-span` は 1 connector で 0 matches、`same-row-between-owner-relative-offset-span` は 0 connectors で 0 matches、missing-owner/row-mismatch は各 1 connector で 0 matches のままである。したがって raw top-level-vs-synthetic-nested command ordering は promotion evidence として使ってはならず、source-order evidence も thresholded line-rule matches がすべて single-endpoint のため rendering promotion には使わない。matched FDM connectors と raw line-rule projections の temporary raster overlays はどちらも MAE/RMS を悪化させたため、ownership/paint-order semantics が decode されるまで connector/line rendering は blocked のままにする。compound prefix は first fill/stroke dwords と marker-valid child offsets または child-area end words を持つことが分かったため、より広い color heuristic に置き換えない。
- [ ] `shanai_lan` label anchors と general text-style semantics を current group-line/fragment projection より先へ decode する。Microsoft Print To PDF reference は PDF text layer に full LAN labels が入っていることを確認し、現在の `/DocumentText` group/line-header projection は major labels を reference coordinates 近くに配置できる。exact-range property 15 がこの projection の label-string color exceptions をすべて置き換えたが、`/DocumentTextPositionTables` `TCntV.01` は direct anchor geometry ではなく、cross-sample evidence により property 15 を universal foreground field とみなせない。true FDM text-object anchors、general property roles、FDM row/object anchors は引き続き未解読である。
- [ ] `shanai_lan` の page/background と object paint-state semantics を decode する。以前の Apple `sips` reference render では transparent page areas が black に合成されることがあったが、Poppler comparison と current `sips` generated-PDF smoke check では source-backed full-page black paint の根拠はない。sample name や renderer artifact で background を切り替えず、FDM/page/layout streams 内の real source paint/page state を見つけて expose してから render する。FDM primitive `paintCoverage` diagnostics は 1889 primitives を示し、page coverage 1%以上は 3 件、5%以上は 0 件なので、現在 decode 済みの FDMVector primitives には page-fill candidate はない。
- [ ] embedded image payload spans の前にある semantic object header fields を decode し、payload ownership を `/Figure`、`/Frame`、`/LayoutBox`、layout mark evidence に接続する。
- [x] `/DocumentTextPositionTables` がない場合でも direct `/DocumentText` control-run table candidates を保存する。`0x000e` row delimiters と `0x001c` cell delimiters から rows/cells を推定し、`documentTextControlRunTableCandidate`/`decoded:false` として保持する。tsaiten evidence がある場合は SVG/PDF に reference-backed `tableProjection` grids として render し、`getPageLayerTree` には `referenceBacked:true` と per-cell bbox/text metadata を出す。confidence が低い samples は diagnostic grids のまま維持する。PDF reference を持つ `tsaiten` sample は `openjtd-samples/pdf-output/ichitaro-20030120132956-0007-sp-dat-tsaiten.pdf` で visible scoring tables を出せるようになった。
- [x] table layout evidence を sample-specific reference projections から分離する。direct `/DocumentText` control-run table grids で line headers が全 cell slots を証明できる場合、`getPageLayerTree` は `sourceDerivedLayoutCandidate` を emit する。これは `sourceBacked:true`、`referenceBacked:false`、`decoded:false` のまま、source-derived bbox/column widths、row-height basis、anchor-line evidence、matched span/gap units、explicit render blocker `page-space-origin-and-row-baseline-unproven` を持つ。SVG/PDF table rendering は引き続き reference-backed layouts を優先するが、source-derived candidate が fallback text-anchor line を持つ場合だけ visible `sourceDerivedDiagnosticProjection` に fallback する。PDF-backed `tsaiten` sample は non-reference-backed source-derived grid overlays を 2 件 render し、PDF-backed `shanai_lan` の false-positive table-like label group は anchor line がないため blocked のままにする。
- [x] fallback body-text suppression を `success_data-test` reference table 専用ではなく、current SVG renderer が実際に描画する table candidate 全体へ generalize する。`tsaiten` の `235点以上` と `success_data-test` の `０.８` のような cells は generic fallback text layer と重複せず SVG 内で 1 回だけ現れる。
- [x] page が observed form/text projection を持つ場合、未証明の `sourceDerivedDiagnosticProjection` table grids は visible SVG/PDF には描画しない。source-derived layout evidence は `getPageLayerTree` に保持するが、PDF-backed `tsaiten` output では 2 件の diagnostic source-derived grids が reference/form projection に重ならなくなり、中段 table text overlap を除去しつつ future source-backed promotion 用の generic table evidence は残す。
- [ ] table semantics は named stream matching ではなく `/DocumentText` control ranges と layout/style streams から decode する。current inventory は `hyo` sample を含め named table stream を見つけていない。
- [ ] `/PaperMark` header count-like values と `0x00010000`/`0x00010010`/`0x00010011` flags の semantic meaning を decode する。RFC 0007 §PaperMark に `count_value = last_index_value + 1` が全 17 サンプル（Ginga x3 + テストサンプル 14 件）で成立すること、`/PageMark`/`/PaperMark` が同じサンプル内で `count_value` を共有すること、`0x00010011` は Ginga 縦書きサンプルにのみ出現することを記録済み。PaperMark フラグ群と同エントリインデックスの PageMark `lineStart`/`lineEnd` 照合により `0x00010000` 群が遷移ページ（セクション区切り・スペーサー）に、`0x00010010` 群が連続本文セクションに対応することを確認（`sample-reference` ではギャップ約 30 行、`sample-draft` では 11〜13 行）。物理的なレンダリング意味論は未解読。
- [ ] parser shape を広げる前に、3 件の unsupported `/PaperMark` stride values を説明する。RFC 0007 §PaperMark に stale/foreign payloads（CFB ディレクトリ断片、OLE/ActiveX メタデータ）として記録済み。新しい証拠なし。
- [ ] `/LineMark` 内の `0x1000`、`0x1001`、`0x1002` tag families を decode する。current evidence では immediate next word は family discriminator ではなく payload-like で、LineMark offsets は direct text coordinates ではない。
- [ ] MarkV.01 header の final `u16` が両 stream を持つ 3 samples で `/LineMark` tag clusters 付近に入る一方、MarkV.01 entry offsets は入らない理由を説明する。
- [ ] embedded fragment plausibility filtering を structured object/stream boundary parsing に置き換える。

Important constraint:

- Ichitaro filter license は decompilation と reverse engineering を制限している。binary は no-code reference artifact として扱う。disassembly から code を copy したり implementation を derive したりしない。

## M3: Document Model

Goal: document model layer 経由で `rjtd export <file> --format json` を実装する。

Status: minimal model path implemented.

Completed:

- [x] extracted `DocumentText` から minimal `Document` を構築する。
- [x] rhwp の parser trait に似た `DocumentParser` entry point を追加する。
- [x] raw `/DocumentText` bytes を `Document` model に保存する。
- [x] non-empty text lines を `TextRun` inlines を持つ `Paragraph` blocks として表現する。
- [x] `Document` から JSON export を実装する。
- [x] `Document` から Markdown と plain text export を実装する。
- [x] rhwp-style SVG-to-PDF path で native PDF export を実装する。
- [x] style streams から page size を decode するときは `/PageLayoutStyle` record `0x4444` を `/DocumentViewStyles` `0x1001` page-size fields より優先する。`tmogi3_2` は reference PDF が A4 portrait で `/PageLayoutStyle` が `21000x29700` を持つ一方、`/DocumentViewStyles` はより狭い view-style size を持つため、`/DocumentViewStyles` は page-layout style record がない場合の fallback evidence として扱う。
- [x] real orientation/writing-mode fields が decode されるまで、local `a5.jtd`/`a6.jtd`/`b6.jtd` sample hints で portrait vertical writing を適用し、`0x000c` `/DocumentText` controls を forced page break として扱う。
- [x] `rjtd export <file> --format json` を wire する。
- [x] `rjtd export <file> --format md` を wire する。
- [x] `rjtd export <file> --format pdf -o <output.pdf>` を wire する。
- [x] exporter が raw file または stream bytes ではなく `Document` を consume するように保つ。
- [x] observed ruby base plus phonetic annotation pair を structured `Inline::Ruby` model data として保存し、plain text/Markdown/PDF output は visible base text を使い続ける。
- [x] observed style/layout streams (`/DocumentEditStyles`, `/DocumentViewStyles`, `/TextLayoutStyle`, `/PageLayoutStyle`, `/PageLayoutStyleHeader`) を record semantics decode 前に named `UnknownStyle` model data として保存する。
- [x] decoded paragraph styles と偽らず、app-core document/style JSON (`getDocumentInfo`, `getStyleList`, `getStyleDetail`) から preserved JTD style stream sources を expose する。
- [x] future style record decoding が raw blobs だけでなく structured evidence から始められるよう、observed style stream families と big-endian header fields (`ssmg` vs table-like prefixes) を JSON に summarize する。
- [x] observed style stream record boundary candidates を JSON に expose する。`0x5555`/`0x4444` Ssmg slot records は `ssmg-slots`、DocumentViewStyles-style `u16 code + u16 payload_len + payload` records は `sequential` として報告する。
- [x] Ssmg style records から conservative UTF-16BE `label` candidates を抽出する。observed labels には `脚注(標準)`、`本文(ｵｰﾄｽﾀｲﾙ)`、page layout labels の `中扉(自動)` などが含まれる。
- [x] labeled `/TextLayoutStyle` records を rhwp-shaped app-core style candidates として promote し、`getStyleList` と `getStyleDetail` が observed JTD style names を `decoded:false` のまま expose できるようにする。
- [x] app-core `applyStyle` が JTD style candidate references を in-memory `Paragraph` model に保存し、`getStyleAt` と split paragraphs がその fallback style state を反映するようにする。
- [x] WASM `HwpDocument` wrapper が rhwp-shaped browser API surface 経由で JTD style candidates を expose/apply することを検証する。
- [x] apps が style panel を開く前に observed JTD style candidates を検出できるよう、app-core `getDocumentInfo` に `styleCandidateCount` と `styleCandidateNames` を追加する。
- [x] preserved style stream summaries、record offsets/codes、payload lengths、labeled `/TextLayoutStyle` candidates を観測する reverse-engineering diagnostics として `rjtd style-records <file>` と `rjtd style-candidates <file>` を追加する。
- [x] unlabeled records、payload digests、short payload previews、labeled candidate IDs を含む all `/TextLayoutStyle` records を inspect する `rjtd text-layout-style-records <file>` を追加する。
- [x] `/DocumentViewStyles` group records を decoded style references として扱う前に payload lengths、payload digests、short payload previews を inspect する `rjtd document-view-style-groups <file>` を追加する。
- [x] `TCntV.01` tail fields と observed text/page style candidate IDs・record indexes を相関させる `rjtd text-position-style-context <file>` を追加する。ただし、これらの hits を decoded style references として扱わない。
- [x] field-level style hit distributions を summarize し、`f1` のような variable fields を text style candidates、page style candidates、`/DocumentViewStyles` group records と比較しつつ、`f7` のような near-constant/default-like fields を分離する `rjtd text-position-style-summary <file>` を追加する。
- [x] `TCntV.01` tail fields と adjacent field pairs を document-text unit/text hits と比較し、`f1/f2` が pure style reference より range/coordinate pair に近く振る舞うことを示す `rjtd text-position-count-tail-field-roles <file>` を追加する。
- [x] valid `TCntV.01` text-count entries を decoded-false `textCountRanges` model/export/app-core metadata として保存し、parse 後に observed range/coordinate evidence を捨てないようにする。
- [x] parsed `TextRun` model data に `/DocumentText` byte/UTF-16 source span を保存し、decoded-false `textCountRanges` に byte/unit `documentTextOverlaps` evidence を attach する。
- [x] app-core `getPageLayerTree` から fallback `textRun` ops と rhwp-shaped `textSources`/`source` span を expose し、可能な場合は JTD byte/unit source range も含める。
- [x] `getPageLayerTree` に fallback `pageBackground` paint op を追加し、layer/replay order を rhwp の background-to-flow model に合わせる。
- [x] `schema`、`resourceTable`、`outputOptions`、`fontResources`、feature lists、fallback `textV2` diagnostics など rhwp-shaped layer tree envelope metadata を追加する。
- [x] model-owned fallback `WritingMode` path を追加し、`DocumentCore`、SVG page rendering、`getPageLayerTree` が exporter から raw style streams を読まずに `horizontal` と `vertical-rl` を project できるようにする。
- [x] conditional `rjtd-export` local-sample PDF smoke test を追加し、available local `.jtd`、`.jtt`、`.jttc` samples をすべて parse/export して PDF header、page marker、EOF marker、minimum size を確認する。
- [x] current high-risk local samples (`success_data-test`, `shanai_lan`) 向けに macOS-only `rjtd-export` Quartz smoke test を追加する。generated PDF bytes を temp file に書き出し、`sips -s format png` で rasterize し、non-empty PNG が生成されることを確認する。
- [x] 同じ high-risk samples 向けに、より直接的な macOS CoreGraphics PDF smoke test を追加する。generated PDF bytes を `CGPDFDocument` で load し、`CGContext.drawPDFPage` で描画して non-white pixels が出ることを要求する。これにより Chrome/PDFium だけで見える状態が Preview/Quartz blank-page regression を隠さないようにする。
- [x] Preview/Quartz PDF regression coverage をさらに厳しくする。`sips` output PNG は non-white pixels を要求し、PDFKit と CoreGraphics checks は aggregate pixel count ではなく checked page ごとに fail する。text-only PDF fixtures は unexpected `/SMask` を拒否し、direct `svgs_to_pdf` contract test で white-page wrapper と normalized `/S1` Form XObject embedding を固定する。macOS PDFKit/CoreGraphics smoke tests は sample-specific `page:minNonWhite` checks を受け取るようになり、A5 は page 2 が意図的に blank でも page 1 と body/header-bearing page 6 を検査できる。selected high-risk pages は 1 pixel だけでなく conservative minimum ink thresholds も超える必要がある。
- [x] Preview/Quartz smoke suite を `.jtd` 専用の sample list から explicit filename list に広げ、PDF-backed な `fax02.jtt` も追加する。`sips`、PDFKit、CoreGraphics checks は `fax02.jtt` page 1 を `1:10000` non-white threshold で render し、current generated PDF は PDFKit と CoreGraphics の両方で `30721` non-white pixels を出す。`raihoumemo01.jtt` は local reference PDF が追加されるまで、この PDF-backed test list には入れない。
- [x] single-page PDF export も multi-page と同じ `pdf-writer` wrapper 経路に通し、Preview/PDFKit が安定した PDF 1.4 document (`Producer: rjtd`) として読めるようにする。`shanai_lan` と `success_data-test` PDFs は Poppler と Apple `sips` の両方で render 済み。wrapper は SVG Form XObject を置く前に explicit white page rectangle も paint するため、Preview/Quartz display path で page content の先頭が transparent-only operation になることを避ける。
- [x] flexible PDF token scanner で Preview/PDFKit transparency regressions に対して PDF export を harden する。`svgs_to_pdf` は page-level `/Group <<` plus `/S /Transparency` を、PDF whitespace/newline が以前の exact byte strings と違う場合でも検出して reject する。scanner は text-only/diagnostic checks 向けに `/SMask` も報告するが、image-backed samples が legitimate soft masks を含む場合があるため local sample validation では soft masks を globally block しない。
- [x] Preview/Quartz white-page PDF wrapper 変更後に、61 件の local `.jtd`、`.jtt`、`.jttc` sample PDFs を `openjtd-samples/pdf-output/` に再生成する。current output は 61 PDFs、`/Type /Page` markers から estimated 1025 pages、header/EOF/page-marker failures 0、61 PDFs すべてが explicit white page background op を含む。representative `sips` renders (`a5`、`a6`、`b6`、`46`、`tsaiten`、`success_data-test`、両方の `shanai_lan` variants、`shinsyo`) は failures 0。
- [x] full local-sample PDF artifact refresh の canonical command として `scripts/regenerate-pdf-output.sh` を追加する。この script は `rjtd-cli` を一度 build し、local `.jtd`、`.jtt`、`.jttc` files をすべて巡回して same-stem PDFs を `openjtd-samples/pdf-output/` に書き出す。artifact sweep は trusted local reference PDFs と generated `/MediaBox` page size も比較し、`46.pdf` のような quarantined references は reference-backed gates から引き続き除外する。
- [x] page-size/page-break と facing-page decoration projection 後に `a5.pdf` を再生成する。`a5.pdf` は A5 portrait MediaBox (`419.55x595.275` pt) を使い、title-page `0x000c` break 後に body text が始まり、6 ページ decorations は左側、7 ページ decorations は右側に出る。
- [x] skipped-inline selector `0x0101` から見える `DocumentText` table-of-contents page-label candidates を、source spans と `decoded:false` を持つ model/export/app-core `tocEntries` として保存する。A5 sample は `6,10,12,16,21,23,28,35,42` の labels を expose するが、selector semantics はまだ完全 decode と扱わない。
- [x] A5 table-of-contents page を hardcoded labels ではなく model-owned `tocEntries` から render する。leader dots、page labels、title text の source-span-backed ruby annotations を含み、`openjtd-samples/pdf-output/a5.pdf` page 3 を regenerate して reference PDF と比較した。
- [x] `/PageMark` rows を decoded-false model/export/app-core `pageMarks` として保存する。header count/stride/last-index、family、entry count、row index、flags、`lineStart`/`lineEnd` を含める。A5 sample は `count=74`、`stride=16`、`last=73`、`75` parsed rows、row 5 `lineStart=23,lineEnd=40` を expose し、reference body start page 6 と対応する。
- [x] `/PaperMark` rows を decoded-false model/export/app-core `paperMarks` として保存する。header count/stride/last-index、entry count、row index、flags、flag hex、raw row length を含める。A5 sample は `count=74`、`stride=12`、`last=73`、`75` parsed rows、`0x00010010`/`0x00010011` のような flags を expose するが、semantic page/paper meaning はまだ主張しない。
- [x] page decoration layer-tree entries に decoded-false `/PageMark` + `/PaperMark` row-alignment evidence を付与する。A5 と B6 の page 6 は matching `pageMarkEntryIndex=5` と `paperMarkEntryIndex=5`、aligned mark indexes、matching entry counts、source-backed `/PaperMark` flag (`0x00010010`) を expose するが、flag semantics が decode されるまで render promotion は block する。
- [x] 同じ decoded-false page mark context を app-core `get_page_info` と CLI `rjtd page-info <file> <page>` でも expose する。mark-bearing pages は source-backed `/PageMark+/PaperMark` row context を報告し、現在の `a6.jtd` のような markless fallback pages は `layoutMarkEvidence:null` を報告する。
- [x] page count を直接 clamp せず、vertical fallback display-unit advance を詰めることで A5 generated PDF の page count を Ichitaro reference と一致させる。`openjtd-samples/pdf-output/a5.pdf` は reference と同じ A5 portrait MediaBox で 72 pages になった。
- [x] A5/Ginga の first body chapter title を normal body column ではなく standalone vertical heading block として project する。Page 6 は Ichitaro reference と同じく `大きな望遠鏡...何でしょう。` で終わり、`やっぱり星だ...` は page 7 から始まり、72-page count は維持される。
- [x] `/Font` table names を model/export JSON に保存し、PDF generation では Mincho-compatible aliases を優先する。macOS では exporter font discovery が Apple MobileAsset font directories も読むため、A5 は `ArialUnicodeMS` fallback ではなく `YuMin-Medium` を embed する。
- [ ] `0x0101` page-label candidate の背後にある exact table-of-contents layout semantics を decode する。leader length、title/ruby binding、page-label alignment は current A5 projection では reference-backed だが `decoded:false` のまま。
- [ ] vertical fallback display-unit heuristic を、original layout rows と `Document` text lines を結ぶ proven `/PageMark`/`/LineMark` mapping に置き換える。rows 73 と 74 は `lineStart=lineEnd` で zero-geometry sentinel-like に見えるため、visible pages として盲目的に扱わない。14 件のテストサンプルから `additive-boundary` PageMark エントリのスパン `lineEnd−lineStart = ページあたり行数−1` が一定（例：30 行/ページサンプルでスパン=29）であることが判明し、`lineStart`/`lineEnd` は 0 始まりのレイアウト行番号をエンコードしているが、レイアウト行から DocumentText unit 座標へのマッピングは未解読、また PageMark は実コンテンツ行数より多くのスロットを事前確保している。
- [x] A5 colophon/end-matter geometry を decoded-false projection で reference PDF に合わせる。Ginga colophon text と preserved layout evidence で gate し、page 72 は running headers/page numbers を suppress しつつ lower-half に colophon block を置き、title/date/copyright spacing、note の 3 vertical columns、trailing control-noise line removal を反映する。
- [ ] suspicious reference PDFs は full-document regressions として扱う前に検証または差し替える。local `46.pdf` は document body ではなく SummaryInformation-like metadata を render しているように見える。local `a6.pdf` は現在 usable reference として扱い、generated `openjtd-samples/pdf-output/a6.pdf` も `114` pages/A6 portrait MediaBox に揃っているため、page-count regression は model/export tests で守る。
- [x] app-core `getValidationWarnings` が empty report 固定ではなく、rhwp-shaped JTD fallback/preservation diagnostics を report するようにする。
- [x] `/DocumentText` control boundary codes を decoded-false `textControlBoundaries` model/export/app-core metadata として保存し、可能な場合は byte/unit source span を attach する。
- [x] preserved `textControlBoundaries` を fallback paragraph character offsets に project し、rhwp-shaped `getControlTextPositions` と nearest-control navigation diagnostics で使えるようにする。
- [x] projected `textControlBoundaries` を app-core `getPageControlLayout` でも `type:"jtdControl"`、fallback bounding box、`decoded:false` diagnostics として expose する。
- [x] `/DocumentText` intervals を all controls または selected control delimiter で summarize する `rjtd text-control-ranges <file> [control-code]` を追加する。
- [x] chosen `TCntV.01` ranges と control-delimited `/DocumentText` intervals を比較する `rjtd text-position-count-control-ranges <file> [control-code]` を追加する。
- [x] candidate control-delimited overlap summaries を decoded-false `textCountRanges` model/export/app-core JSON の `controlRangeOverlaps` として expose する。
- [x] `controlRangeOverlaps` から derived decoded-false `textBoundaryCandidates` model/export/app-core JSON を expose し、parsed paragraph semantics を変えずに paragraph-boundary candidate evidence を保存する。
- [x] multi-interval `textBoundaryCandidates` を decoded-false `tableCandidates` として model/export/app-core JSON と validation warnings に保存し、interval ごとの source range、text preview、line-break count も保持する。ただし parsed blocks や table/cell fallback APIs は変更しない。
- [x] model-derived decoded-false table candidates と interval source evidence を出力する `rjtd table-candidates <file>` を追加する。
- [x] `table-candidates` を current local samples 全体に sweep する。61 checked、0 failures、candidates を持つ files 10、candidates total 134、byte-basis rows 65、unit-basis rows 69、max 44 intervals、interval line breaks を持つ candidates 0。row/cell geometry が証明されるまでは diagnostic-only に保つ。
- [x] preserved table candidate ごとの interval text shape、empty intervals、line breaks、edge context、strict diagnostic `cell-like` flag を summarize する `rjtd table-candidate-context <file>` を追加する。
- [x] `table-candidate-context` を current local samples 全体に sweep する。61 checked、0 failures、positive files 10、candidates 134、strict cell-like candidates 10、non-cell-like candidates 124、empty intervals を持つ candidates 54、line breaks を持つ candidates 112、max 44 intervals、max interval text length 7,443 characters。multi-interval candidates 全体を table として promote してはいけない。
- [x] row/cell geometry analysis の compact input として strict cell-like decoded-false table candidates だけを出力する `rjtd table-cell-like-candidates <file>` を追加する。
- [x] `table-cell-like-candidates` を current local samples 全体に sweep する。61 checked、0 failures、positive files 5、candidates 10、すべて `delimiter=0x000e`、unit-basis rows 6、byte-basis rows 4、max 6 intervals、max cell text length 65 characters。
- [x] strict row-like table candidates を existing rhwp-shaped read-only app-core table APIs (`get_table_dimensions`, `get_cell_info`, `get_text_in_cell`, `get_table_signature`) から observed N-row/1-column decoded-false tables として expose する。ただし edit APIs と non-row-like candidates は compatibility fallback のままにする。
- [x] downstream tools が compact row-like evidence と diagnostic-only multi-interval spans を区別できるように、model/export/app-core `tableCandidates` JSON に `cellLike`、`rowLike`、`observedTable` shape metadata を追加する。
- [x] JSON export `tableCandidates[].observedTable` を current local samples 全体に sweep する。61 checked、0 failures、positive files 5、total candidates 134、`cellLike=true`/`rowLike=true` 10、`observedTable` objects 10、bad N-row/1-column shapes 0 で、strict CLI subset と一致する。
- [x] row-like `tableCandidates[].intervals[]` に decoded-false `columnSegments` diagnostics を追加する。conservative finance-style label/value runs だけを split し、geometry が証明されるまでは `observedTable.colCount=1` を維持する。
- [x] JSON export `columnSegments` を current local samples 全体に sweep する。61 checked、0 failures、positive files 5、observed tables 10、split rows 38、segments 181、max 8 segments per row、non-observed candidates 下の split rows 0。
- [x] `rjtd table-cell-like-candidates <file>` に compact な `columnSegments` diagnostics を追加し、current local samples 全体に sweep する。61 checked、0 failures、positive files 5、candidates 10、split rows 38、segments 181、max 8 segments per row。
- [x] observed row-like table metadata と CLI output に decoded-false column segment pattern consistency diagnostics (`columnSegmentPatternConsistent` と `columnSegmentPatternMismatchRows`) を追加する。ただし `observedTable.colCount=1` は変更しない。
- [x] column segment pattern consistency を current local samples 全体に sweep する。61 checked、0 failures、positive files 5、observed/strict candidates 10、pattern-consistent candidates 5、pattern-inconsistent candidates 5、mismatch rows 5、split rows 38。JSON export と CLI distribution は一致する。
- [x] すべての row が column segments を持ち、lexical segment pattern が一致するさらに strict な subset に decoded-false `columnGridCandidate` diagnostics を追加する。ただし `observedTable.colCount=1` は維持する。
- [x] `columnGridCandidate` を current local samples 全体に sweep する。61 checked、0 failures、positive files 5、observed/strict candidates 10、grid candidates 3、non-grid candidates 7、shapes は `4x4` が 1 件、`5x4` が 2 件。JSON export と CLI distribution は一致し、missing fields は 0。
- [x] `columnGridCandidate` を fallback SVG pages と `getPageLayerTree` に decoded-false diagnostic table-grid overlay として project する。local-sample regression coverage で SVG と layer-tree の件数が model-owned candidates と一致することを確認した。
- [x] model-derived decoded-false boundary candidate の basis、delimiter、interval count、single/multi classification、source span、`decoded=false` を出力する `rjtd text-boundary-candidates <file>` を追加する。
- [x] `text-boundary-candidates` を current local samples 全体に sweep する。61 checked、candidates を持つ files 10、candidate rows 356、overlapped intervals 1,586、single-interval candidates 222、multi-interval candidates 134。最大 single candidate は `justsystems-20120223023609-jp-just-finance-j200403sc.jtd` の `0x001c`/unit 44 intervals であり、paragraph promotion にはより strict な rule が必要である。
- [x] decoded-false boundary candidate を `/DocumentText` visible text、line breaks、edge alignment と比較する `rjtd text-boundary-candidate-context <file>` を追加する。
- [x] `text-boundary-candidate-context` を current local samples 全体に sweep する。candidate rows 356、line break を 1 つ以上持つ rows 276、total line breaks 3,458、control gap 直後に始まり aligned text boundary で終わる rows 210。`0x001c` single-interval edge-good rows のうち byte basis は one-line-break 17 と zero-line-break 16、unit basis は one-line-break 22 と zero-line-break 13 である。`0x000e` は many line breaks を含む coarse cases が多く、direct paragraph promotion にはまだ粗すぎる。
- [x] byte/unit decoded-false boundary candidate を text-count range と delimiter で pair し、edge-good flags、line-break counts、visible text previews、match flags を報告する `rjtd text-boundary-candidate-agreement <file>` を追加する。
- [x] `text-boundary-candidate-agreement` を current local samples 全体に sweep する。10 files で byte/unit pairs 178。exact visible-text match は 1 row だけで、その row も empty であるため text equality は promotion rule として使えない。より strict な `0x001c` single/single set は 43 pairs で、unit-basis edge-good/non-empty/line-break<=1 は 33 rows、byte-basis は 28 rows を残す。
- [x] unit-basis `0x001c` single candidate を `/LineMark`、`/PageMark`、`/PaperMark` の direct index/byte context と比較する `rjtd text-boundary-candidate-layout-context <file>` を追加し、rule は diagnostic-only に保つ。
- [x] `text-boundary-candidate-layout-context` を current local samples 全体に sweep する。8 files で unit `0x001c` single candidates 52、strict rule-selected rows 35。ただし selected rows のうち `/LineMark`、`/PageMark`、`/PaperMark` start/end direct hits は 0 であり、candidate source units と layout mark rows は同じ coordinate space ではない。
- [x] unit-basis `0x001c` candidates を sparse `/LineMark` tag positions、`/PageMark` entry indexes/raw fields/byte boundaries、`/PaperMark` entry indexes/byte boundaries と複数の global unit transform hypotheses で score する `rjtd text-boundary-layout-map <file>` を追加する。
- [x] `text-boundary-layout-map` を current local samples 全体に sweep する。61 checked、0 failures、8 files に unit `0x001c` single candidates 52、4 files に strict selected candidates 35。non-boundary exact hits は存在するが、winning target/base/delta combinations は file-specific である。`iwata_file` は line-word-value/page-be32-field の unit-div2 shifts around -1140..-1192 を好み、finance samples は別々の page-be32-field shifts を好むため、paragraph promotion 用の single global layout-map transform はまだ reject する。
- [x] unit `0x001c` single candidate ごとに linked `TCntV.01` row、local delta、nearest start/end layout points、exact endpoint count を出力する `rjtd text-boundary-layout-map-rows <file>` を追加する。
- [x] `text-boundary-layout-map-rows` を current local samples 全体に sweep する。61 checked、0 failures、同じ unit `0x001c` single candidates 52 と strict selected candidates 35 が見つかる。`iwata_file` の strict selected candidates 32 のうち 10 件が `line-word-value` と `page-be32-field` の両方で row-local `exact=2` evidence を持ち、selected finance candidates 3 は row-local `exact=2` evidence を持たない。したがって strict candidates は引き続き diagnostic-only であり、次の rule は paragraph-like rows と non-paragraph large spans を分離する必要がある。
- [x] strict unit `0x001c` single candidate に加えて `line-word-value` と `page-be32-field` の両方に row-local exact endpoint evidence がある場合だけ `paragraph-like=true` を報告する diagnostic-only classifier `rjtd text-boundary-paragraph-like <file>` を追加する。
- [x] `text-boundary-paragraph-like` を current local samples 全体に sweep する。61 checked、0 failures、unit `0x001c` single candidates 52、strict selected candidates 35、paragraph-like candidates 10、strict selected but non-paragraph-like candidates 25。この rule では `iwata_file` だけが paragraph-like candidates を作る。
- [x] paragraph-like classifier と linked `TCntV.01` tail fields、text/page style hits、view-style group hits、byte/unit range preview を同時に出力する `rjtd text-boundary-paragraph-like-style-context <file>` を追加する。
- [x] `text-boundary-paragraph-like-style-context` を current local samples 全体に sweep する。61 checked、0 failures、同じ unit candidates 52、strict selected candidates 35、paragraph-like candidates 10、selected non-paragraph-like candidates 25。paragraph-like rows 10 件は text/page style candidate hits を持たないが、すべて `iwata_file` の `/DocumentViewStyles` group evidence を持つ。strict non-paragraph rows も 25/25 で view-group hit を持つため、これは paragraph discriminator ではなく、paragraph style attachment を証明しない。
- [x] paragraph-like、strict-non-paragraph、non-strict bucket を exact layout evidence、`TCntV.01` family/span、tail field counts、style/view-style hits で要約する `rjtd text-boundary-paragraph-like-discriminators <file>` を追加する。
- [x] `text-boundary-paragraph-like-discriminators` を current local samples 全体に sweep する。61 checked、0 failures。dual exact layout evidence は paragraph-like rows だけに現れる (10/10)。strict-non-paragraph と non-strict はそれぞれ 0/25、0/17。`iwata_file` では paragraph-like rows は `be0` で `range-spans=2..8` だが、strict-non-paragraph と non-strict rows は `range-spans=0..0` である。したがって nonzero chosen `TCntV.01` span と dual row-local layout exactness を次の discriminator としてテストしつつ、まだ decoded-false に保つ。
- [x] この stricter discriminator を real paragraph として再構築せず、decoded-false `textParagraphBoundaryCandidates` として model/export/app-core JSON に保存する。61-sample JSON export sweep は 0 failures で、合計 10 candidates を保存し、すべて `ichitaro-20030316043238-success-001-success_data-iwata_file.jtd` 由来である。
- [x] `rjtd text-paragraph-boundary-targets <file>` を追加し、preserved `textParagraphBoundaryCandidates` を concrete `/LineMark` word indexes と `/PageMark` raw row fields へ trace する。
- [x] `text-paragraph-boundary-targets` を current local samples 全体に sweep する。61 checked、0 failures、candidates を持つ file は 1、total candidates は 10。現在の hit formatter では 20 endpoints のうち line endpoints 6 件と page endpoints 4 件が non-unique または missing であるため、exactness だけで real paragraph construction を開始してはならない。
- [x] app-core `getCanvasKitReplayPlan` が rhwp mode policy (`default`/`compat`) に従い、fallback `pageBackground`/`textRun` ops を empty plan ではなく direct replay items として report するようにする。
- [x] `success_data-test` の abc table を `/DocumentText` unit range `519..924` 由来の source-backed 5-column logical topology として保存する。15 candidate cells、11 non-empty cells、4 empty cells (`a: 1,1,7,empty`, `b: 1,empty,24,0.8`, `c: empty,2,empty,1`) を保持する。
- [x] 同じ abc table について candidate-specific `/DocumentText` line-header placement evidence を decode する。各 row は monotonic な `0x001c/0x0030` headers を 7 件持ち、先頭 5 cell slots が 15 logical cells すべてを cover するため、`getPageLayerTree` は `placementAuthority:"documentTextLineHeaders"`、`decodedSourcePlacementEvidence:true`、`decodedSourcePlacementMatchCount:15`、`decodedSourcePlacementRequiredCellCount:15`、21 raw headers 付きの `documentTextLineHeaderEvidence` を出す。
- [x] この line-header evidence がある場合に限り、`success_data-test` abc table の SVG/PDF `tableProjection` を再有効化する。generated `openjtd-samples/pdf-output/ichitaro-20030228030923-success-002-success_data-test.pdf` は 3x5 grid を表示し、Poppler と Apple `sips` で render 済み。ただし page `x/y/width/height` はまだ reference-calibrated なので layer tree は `referenceBacked:true`、`geometryDecoded:false`、`placementDerived:false` のままにする。
- [x] 同じ abc table に explicit `layoutStreamProbe` evidence を維持する。`/LineMark` (334 bytes / 167 words)、`/PageMark` (2 rows)、`/PaperMark` (76 bytes)、56 件の `/Frame` records は存在するが、table unit range は line/page mark indexes に直接 hit せず、object-frame source-unit link もない。
- [x] square な `objectType 0x0001` `/Frame` records を `titleRoundedFrame` page-frame shapes として promote しないようにする。observed real title frames は horizontal panels なので、`page_frame_title_shape` は title-like aspect ratio を要求する。これにより `success_data-test` の upper-right stray square は消え、source `/Frame` row は decoded-false model/export evidence として保持される。
- [x] EmbeddedPress paint-state word5 `0x2f` が付いた `success_data-test` title-art texture paths を main title face に replay しないようにする。layer tree は shadow-state texture paths を source evidence として保持するが、SVG/PDF の front-texture rendering は main-state word5 `0x10` を要求する。これにより title glyph holes と overlap の malformed overpaint source を除去する。
- [x] long-shadow sidewall generation から hardcoded な 24% title-art hole-area gate を取り除く。extrusion sweep は source `evenodd` の non-degenerate boundary contours すべてを辿るため、glyph holes のような小さな counters も area heuristic で落とさず sidewall boundary として保持する。
- [x] `success_data-test` title-art について `shadowTexturePathCount`/`shadowTextureWord5Values` diagnostics を追加し、11 件の shadow-state texture paths を full preserved 530 texture paths と分けて見えるようにする。ただし title halftone effect を担っている可能性があるため、rendering は full texture paths を維持する。
- [x] `success_data-test` title textures について EmbeddedPress current-paint-state inheritance diagnostics を追加する。explicit な shadow-state `0x2f` texture records 11 件が effective shadow textures 530 件全体を説明し、effective front texture count は 0 のままであることを layer tree と SVG metadata に保存する。これにより full texture replay を arbitrary fallback ではなく source state inheritance evidence として扱える。
- [x] `success_data-test` title texture rendering を source ordering evidence 経由にする。530 件の texture paths は 11 件の shadow outlines と 11 件の main outlines の間にある interstitial block として採用し、その ordering proof がない場合だけ preserved-all-texture fallback に戻す。
- [x] `success_data-test` title art groups の compact な `paintStateSummaries` を expose する。layer tree は shadow outlines、interstitial texture block、main outlines について path index ranges、explicit/inherited state counts、EmbeddedPress records `0x40/0x46/0x48/0x60/0x65/0x70/0x82` の unique payload signatures を report する。
- [x] Historically block `success_data-test` title front-face erase promotion when only the interstitial shadow-state texture block was available. This earlier decision kept the 530 `0x2f` texture paths off the main title face and preserved source outline fill (`#111111`) instead of precomposed gray `#818181`; it is superseded by the later PDF-safe source-order interstitial front-erase promotion.
- [x] 残っている source-backed white front-paint candidate を expose する。`success_data-test` title layer tree は `JSFart2Contents.paintColorCandidate` 由来の `frontPaintCandidate.paintColor:"#ffffff"` を報告する。initially this stayed non-rendering with `renderPathCount:0`, but the later source-order interstitial front-erase promotion now records `renderTexturePathSource:"source-order-interstitial-front-erase-texture"` and `renderPathCount:530`.
- [x] `success_data-test` title art に source-backed EmbeddedPress `0x70.word0` shadow-effect candidate を適用する。shadow outlines は homogeneous な `0x70.word0 = 0x2c` を持つため、conservative に 44% black over white (`#8f8f8f`) と解釈し、SVG metadata と layer-tree diagnostics の両方に expose する。regenerated PDF は Poppler と Apple `sips` の両方で render 済み。previous front-erase output と比べ、current crop では whole-page RMS が `48.3051` から `48.2703`、title-crop RMS が `69.883` から `69.671` に小さく改善した。`0x70` で shadow/extrusion texture pass を削除する competing experiment は title RMS を `70.50` から `78.03`、whole-page RMS を `45.35` から `46.56` に悪化させたため reject した。
- [x] `success_data-test` title-art page routing の `frameRef 1/16` hardcode を取り除く。page association は EmbeddedPress snapshot、frame size、JSFart2 art candidate が一致する `JSFart.Art.2` embedding frames の source order から導き、layer tree の `titleArtProjection.pageAssociation` に `frameRefs:[1,16]`、source-order index、page number を記録する。ただし association 自体はまだ `decoded:false` として保持する。
- [x] `success_data-test` title-art paint-state summary ごとに `statePayloadWordColumns` diagnostics を追加し、raw payload arrays を再解析しなくても `0x48`、`0x70`、`0x82` の word-position differences を見えるようにする。new artifact `tmp/pdfs/goal-continue/success-layer-tree-word-columns.json` は `0x48.word0=0x01` が shadow outlines のみに現れ、interstitial/main groups は `0x48.word0=0x00` であること、shadow outlines は `0x70.word0=0x2c` と `0x70.word3=0x0a`、interstitial/main groups は `0x70.word0=0x1c` と `word3=0x00` であることを示す。追加の SVG A/B では source-derived alternatives (`0x82` front erase opacity `0.160`、`0.280`、`0.440`、`0.530`、`1.000`、front erase なし、explicit-11 texture routing、shadow texture なし、shadow-face-only `0x70`) が current baseline より title/glyph RMS を悪化させたため、この pass では render change を promote しない。
- [x] interstitial title texture block の `0x70.word0=0x1c` を decoded-false render candidate として保持するが、rendering には使わない。layer tree は `extrusionTextureEffectCandidateFillColor:"#676767"` と `extrusionTextureEffectRenderPromotionBlockedReason:"interstitial-0x70-texture-effect-semantics-unproven"` を report し、SVG も対応する `data-title-texture-effect-candidate-*` attributes を expose する。CoreGraphics A/B ではこの candidate を実際に render すると title RMS が `58.492` から `62.216` に悪化したため、full `0x70`/texture semantics が証明されるまで visible texture fill は previous baseline のままにする。
- [x] `success_data-test` abc table について source-backed `DocumentText` line-header unit geometry を expose し、current reference bbox を opaque なままにしない。`getPageLayerTree` は `documentTextLineHeaderEvidence` の下に `unitGeometryCandidate` を出し、3 homogeneous rows、5 matched columns、row あたり raw headers 7 件、`minOffsetUnits:0`、`maxExtentUnits:174`、`homogeneousFontSizeUnits:12`、matched cell offsets `[0,14,28,44,60]`、matched extents `[10,24,40,56,72]`、trailing wide header candidates `[76..88, 92..174]` を示す。page-space origin と unit scale はまだ未証明なので rendering は reference-backed のままだが、column count は success-data-test constant ではなく table candidate から取るようにした。
- [x] `success_data-test` top-text projection 21 slots すべてに `/DocumentText` source spans を付与し、reference-backed text runs が source-less のままにならないようにする。`getPageLayerTree` は `sourceBacked:true`、`sourceStream:"/DocumentText"`、`source` と `textSources` の JTD byte/unit ranges、実際に近傍 `0x001c/0x0030` header を parse できた場合だけ conservative な `lineHeaderEvidence` を出す。table `geometryDerivationEvidence` も `topTextAnchorEvidence` として `referenceBackedCoordinateCount:21`、`sourceBackedRunCount:21`、`lineHeaderBackedRunCount:2`、`directTableTransformDerived:false` を report し、page coordinates がまだ reference-backed のため render promotion を明示的に block する。rendered PDF は変えないが、current Poppler output は previous generated baseline と pixel-identical で、diagnostics は `１，次の計算をしなさい` の `264..275`、table prompt の `426..467`、combined figure label `（２）` の `1506..1509` などの source ranges を expose する。
- [x] `success_data-test` top-text projection slots と Q4 figure-label spans に `/LineMark` row evidence を付与する。strict な `0x001c/0x0030` `lineHeaderEvidence` は 2 件だけだが、source-backed visible top-text slots は overlapping `/LineMark` record、source unit range、flag word、containment flag、`line-mark-row-to-page-y-transform-unproven` blocker を持つ `lineMarkEvidence` を expose する。さらに `/LineMark+/PageMark+documentTextLineHeaders` 由来の decoded-false `pageGridCandidate` も expose し、record `5 -> rowTopY:177.000`、record `15 -> 387.000`、record `27 -> 639.000` のような source-backed row top candidates と reference residuals (`topMinusRowTopPx` は約 `7.5`、`7.3`、`5.2`) を保持する。rendering は変えず、top-text y placement を static reference coordinates から移す準備にする。
- [x] 同じ `/LineMark+/PageMark+documentTextLineHeaders` row-grid evidence を、`success_data-test` top-text と Q4 figure-label rendering の bounded source-backed y-baseline candidate として promote する。`getPageLayerTree` は source-backed text run ごとに `sourceGridPlacementCandidate` を emit し、`rowTopY`、source row height、`topY`、`baselineY` (`lineMarkRowTopPlusSourceRowHeight`) を保持する。SVG/PDF text records も `data-y-basis="lineMarkPageGrid"` と source-grid baseline metadata を持つ。horizontal origin と exact font metrics はまだ未証明なので `placementProven:false` のままにし、regenerated PDF は Poppler、Apple `sips`、PDFKit で render 確認済み。
- [x] promoted `success_data-test` source-grid text placement に baseline residual と raw line-header diagnostics を追加する。各 `sourceGridPlacementCandidate` は source-derived `topY`/`baselineY` と以前の reference-backed coordinates を比較する `referenceResidualEvidence` を持つ。early rows は residual がほぼ 0 (`0.0..0.1px`) で、Q4 figure labels は残る `-1.8px` baseline/top residual を明示的に expose する。`lineHeaderEvidence` も matched `/DocumentText` raw 12-word header (`rawWords`/`rawWordsHex`) を保存するため、future baseline/top-offset work は stream を再解析せず source fields を比較できる。
- [x] text run ごとの scraping なしで baseline residual pattern を inspect できるよう、page-layer に `successDataTestTextPlacementResidualSummary` op を追加する。summary は 21 source-grid candidates、max top/baseline residual `2.300px`、raw line-header profiles 2 種、LineMark flag/font/header presence で key した rounded-tenth residual buckets、per-entry source ranges/raw headers を report する。current strongest unresolved bucket は Q4 figure-label group (`recordIndex 27`、`flagWord 0x8002`、`fontSize 14.0`、residual `-1.8px`、count 3) であり、`baseline-residual-source-field-semantics-unproven` の diagnostic-only のままにする。
- [x] 同じ `successDataTestTextPlacementResidualSummary` に、未証明の constant baseline correction を入れる代わりに line-pitch fit evidence を追加する。layer tree は `/LineMark` record indexes に対する reference-baseline least-squares fits (`all-visible` pitch `20.926px`、early records pitch `20.976px`、late records pitch `21.006px`)、source row height candidate (`abc-table` line-header font units 由来 `21.000px`)、matched `/PageMark` selected fields (`word21=555`、`370 + 185 = 555` additive-boundary row) を報告する。`/PageMark` line-pitch/source-field semantics は未証明なので rendering は diagnostic-only のままにする。
- [x] row-height source を summary だけに隠さず、各 `success_data-test` `sourceGridPlacementCandidate` と `/LineMark` `pageGridCandidate` に compact `sourcePitchEvidence` を mirror する。各 candidate は page/body size、`fontSizeUnits:12`、`fontUnitScalePx:1.750`、body-height-per-line-gap `21.214px`、source-minus-body-gap residual `-0.214px`、matched `/PageMark` selected fields、`pageHeightPxPerWord21Unit:1.750`、`pageHeightPxPerWord13Plus14Unit:1.750`、scale-agreement candidates を expose しつつ、render promotion は `page-mark-line-pitch-semantics-unproven` として block したままにする。
- [x] PageMark pitch semantics を promote する前に PDF-backed local samples で `page-mark-u16-profile` を sweep する。parsed PageMark profiles を持つ 52 files は 516 entries total、20 `zero-sentinel`、249 `additive-row`、115 `additive-boundary`、132 `mixed-payload` を expose し、46 files は少なくとも 1 件の `mixed-payload` row を持つ。この diversity により、`success_data-test` の PageMark/font scale agreement は有用な evidence だが global y-transform rule としてはまだ安全でないことを確認する。
- [x] pitch-profile diagnostic 追加後に representative PDFs (`openjtd-samples/pdf-output/a5.pdf` と `openjtd-samples/pdf-output/ichitaro-20030228030923-success-002-success_data-test.pdf`) を再生成し、Poppler と Apple `sips` の両方で nonblank rendering を確認する。現在どちらの file にも `/Group`、`/Transparency`、`/SMask` は含まれない。ただしこの環境では `qlmanage` が sandbox-blocked のため、Preview.app-only visibility difference は manual app-level confirmation がまだ必要。follow-up で `success_data-test` を direct PDFKit thumbnail render したところ page 1/page 2 とも visible PNG が生成されたため、Chrome/PDFium で見えて Preview.app だけ blank の場合は、PDF 構造変更より先に Preview の cache/open-file refresh を app-level suspect として扱う。
- [x] source-backed text/table row grid evidence の `/LineMark` 選択を first-overlap から best-match selector に置き換える。selector は exact unit range match、source span containment、larger overlap、shorter interval の順に優先するため、ambiguous な `/LineMark` join が stream order に暗黙依存しない。`success_data-test` top text は checked probes で同じ visible rows (`recordIndex` 5/8/27) を維持し、table row matching と exact-contiguous checks も同じ selection rule を使う。
- [x] `success_data-test` Q4 figure labels `(1)/(2)/(3)` を、3 個の独立した reference x coordinate ではなく、単一の preserved `/DocumentText` line `1479..1532` から render する。renderer は source line から label spans を split し、SVG-to-PDF conversion で ASCII spaces が collapse しないよう Japanese fixed-pitch advance model (`advanceModel:"japanese-fixed-pitch-halfwidth-space"`) を適用する。regenerated PDF の label text bboxes は reference 近傍まで移動した: `(1)` x0 `44.03` vs `44.16`、`(2)` x0 `185.78` vs `186.76`、`(3)` x0 `317.03` vs `318.61`。`getPageLayerTree` と SVG は `renderSource:"document-text-fixed-pitch-span"` と original line evidence の両方を expose する。
- [x] 同じ abc table に residual-only physical-geometry diagnostics を追加する。`referenceBBoxResidualEvidence` は source line-header spans `[10,10,12,12,12]` と gaps `[4,4,4,4]` を current equal-width reference columns (`55.198px` each) と比較し、per-cell ratios `[5.520,5.520,4.600,4.600,4.600]` と `equalReferenceColumnsConflictWithUnitSpans:true` を expose する。`topTextAnchorResidualEvidence` は shared full-extent/font-size top-text anchors 2 件と reference-assisted offsets を保存し、`空欄を埋めて表を完成させなさい。` について `tableLeftMinusAnchorLeftPx:13.397`、`tableTopMinusAnchorBaselinePx:2.916` を示す。`independentPageTransformEvidence.present` は、top-text page coordinates と table bbox がどちらも reference-backed のため `false` のままにする。
- [x] `success_data-test` abc table の visible column widths を equal-width のままにせず、`/DocumentText` line-header span units から render する。最初に match した line-header row は spans `[10,10,12,12,12]` を与え、current reference-backed bbox 内で `[49.284,49.284,59.141,59.141,59.141]` px に正規化される。`getPageLayerTree` は `columnWidthBasis:"documentTextLineHeaderSpanUnits"`、SVG は `data-column-width-basis="documentTextLineHeaderSpanUnits"` を記録する。これは table internals に対する bounded source-backed improvement であり、次の source-derived projection promotion の根拠になった。
- [x] `success_data-test` abc table の source-derived `/DocumentText` line-header + `/LineMark` + `/PageMark` layout を、`lineMarkPageGrid` origin がある場合の primary SVG/PDF table projection に promote する。rendered `tableGridCandidate` は `referenceBacked:false` となり、bbox `x=78.251,y=411.421,width=275.063,height=63.000` と source span column widths `[49.118,49.118,58.942,58.942,58.942]` を使う。x origin は repeated first inter-cell gap を decoded-false source-backed candidate (`xOriginInsetBasis:"uniform-intercell-gap-half"`, `xOriginInsetUnits:2.000`) として使い、reference bbox との差分を `xResidualPx:7.297` から `1.046` に縮める。y origin は `/PageMark` line range `0..39` と body height 由来の line pitch (`linePitchPx:21.214`, `linePitchBasis:"pageMarkBodyLineGap"`) を使い、vertical residual を `2.716px` から `-0.705px` に縮める。ただしどちらの semantics もまだ未証明として保持する。`tsaiten` のような fallback-text-anchor-only layout は diagnostic-only のままにする。
- [x] `success_data-test` abc table の rule width を fixed SVG stroke constants ではなく、同じ source line-header font-unit scale から導く。source-derived table は layer-tree/SVG に `strokeWidth:1.750`、`cellStrokeWidth:1.750`、`strokeWidthBasis:"documentTextLineHeaderFontUnitPx"` を expose し、cell text は source-derived cell slot center candidate (`cellTextCentered:true`, `cellTextAlignmentBasis:"documentTextLineHeaderCellSlotCenterCandidate"`) で anchor する。これは decoded-false で cross-sample validation が必要だが、A/B Poppler crops は Print-to-PDF reference に対して改善した (`wide-actual-table` RMS `73.917 -> 69.118`、`table-and-prompt` `64.824 -> 61.242`、`table-tight` `82.399 -> 75.445`、`rules-tight` `84.834 -> 77.391`、whole-page `45.634 -> 45.241`)。regenerated output は Poppler、Apple `sips`、PDFKit、CoreGraphics で visible rendering を確認した。
- [x] `tsaiten` table/page-grid candidates に diagnostic-first の LineMark record-stride evidence を追加し、non-contiguous records を単なる failure として捨てないようにする。`getPageLayerTree` は line-header coupling、`/LineMark` row evidence、`/PageMark+/LineMark` page association evidence に `lineMarkRecordIndexes`、`uniformLineMarkRecordStride`、`lineMarkRecordStride`、`interleavedLineMarkRecordCountBetweenRows` を expose する。PDF-backed `tsaiten` の scoring table は `[8,10,12,14]`、lower error table は `[33,35,37]` を、cell provenance と同じ best-overlap LineMark selector で記録し、どちらも stride `2` である。mixed-payload PageMark と stride-to-y semantics が decode されるまで rendering は reference-backed のままにする。
- [x] `tsaiten` table layouts に source-derived `lineMarkPageOriginStrideCandidate` diagnostics を追加し、stride-2 row placement evidence が fallback text anchors の背後に隠れないようにする。lower error table は stride records `[33,35,37]`、raw record-index row-top hypotheses `[765.000,807.000,849.000]`、stride-collapsed row-top hypotheses `[418.500,439.500,460.500]`、matched mixed-payload `/PageMark` `u16` geometry hypotheses を保持する。cell-level layer-tree/SVG provenance も `採点項目` や `１文字につき１点` の `/DocumentText` line-header candidates、`/LineMark` record indexes、`/PageMark` entry indexes を expose するが、rendering は `line-mark-record-stride-to-page-y-transform-unproven` として block のままにする。
- [x] `tsaiten` lower error table の stride-y hypotheses に、render authority にせず reference-residual diagnostics を追加する。`lineMarkStrideYComparison` は source-backed `[33,35,37]` row-top hypotheses を current reference table bbox と比較し、raw record-index row tops の residuals `[-3.014, 1.686, 6.385]` px (`maxAbs=6.385`) と、数百 px 外れる stride-collapsed row tops を保存する。そのため `bestYHypothesisCandidate:"raw-record-index"` を evidence として保持するが、scoring table に同等の line-header geometry がまだないため rendering は `stride-y-hypothesis-needs-cross-table-validation` として block のままにする。
- [x] `tsaiten` direct `/DocumentText` table candidates に compact-to-sparse sibling topology evidence を追加する。`getPageLayerTree` は sparse control-run table candidate が compact candidate の source range と exact row/cell source spans を含む場合に `sparseTableSiblingEvidence` を emit する。scoring table は compact source intervals `[1,3,5,7]` を sparse candidate `4` に map し、cell spans `12/12` をすべて match し、compact columns `0..2` が sparse columns `[3,4,5]` に offset `3` で対応することを示す。lower error table も `[20,22,24]` と `[3,4]` を同様に map する。これは source-backed topology evidence のみで、`sparse-topology-does-not-decode-page-space-geometry` として render promotion は block のままにする。
- [x] `tsaiten` compact tables に sparse-sibling LineMark y residual diagnostics を追加し、rendering には promote しない。`sparseSiblingLineMarkYComparison` は compact-to-sparse sibling proof、best-overlap `/LineMark` rows、matched `/PageMark` line range、homogeneous line-header font-size units を使って、source-backed row-top hypotheses を current reference bbox と比較する。scoring table は partial line-header font evidence (`2/4` rows) しかなく、raw-record-index hypothesis も reference から大きく外れる (`[-61.005,-51.206,-41.406,-31.607]` px、`maxAbs=61.005`)。一方 lower error table は near-reference raw-record evidence (`maxAbs=6.385`) を維持する。これにより sparse topology だけでは `tsaiten_table_grid_overlay_layout` を置き換えられないことを明示する。
- [x] 同じ `tsaiten` sparse-sibling y diagnostics の下に `postRowGapLineMarkCorrelation` を追加する。scoring table と lower error table の両方で、各 compact row の `/LineMark` span length は、その row end から後続 sparse blank/gap region までの source-unit gap と正確に一致する。scoring rows は sparse intervals `[2,4,6,8]` に対して `[65,65,65,67]` units、lower table は `[58,52,54]` units で一致し、last row は trailing empty sparse intervals `[25,26]` を consume する。これは observed `/LineMark` span が direct visible row-height/page-y coordinate ではなく post-row gap/spacing measure であることを示す強い evidence である。
- [x] `tsaiten` table geometry に `/PageMark` raw record と numeric value probes を追加する。`pageMarkRawRecordScanEvidence` は current `count-plus-one-variable` parse とは独立して plausible raw PageMark record headers を scan し、1108-byte stream 内の 15 件の candidate headers を expose する。scoring `[8,10,12,14]` と lower `[33,35,37]` の LineMark rows はどちらも scanned record header `0` (`lineStart=0,lineEnd=42`) に入るため、これは page ownership を示すが y placement は説明しない。`pageMarkRawReferenceValueProbe` は scoring row tops (`301/333/365/398`) 近傍に raw PageMark numeric hits がなく、lower table も `768px` 近傍の `u16be 0x0300` hits 2 件だけであることを示すため、lower の raw-record-index fit を general transform として promote しない根拠になる。
- [x] 同じ `tsaiten` sparse-sibling y diagnostics に `lineMarkStridePromotionReadiness` を追加する。この gate は compact/sparse topology、`/LineMark` stride、line-header coverage、post-row-gap correlation、raw `/PageMark` scan results、reference residuals を集約するが、non-reference の PageMark/LineMark page-y transform が decode されるまでは常に `promotionReady:false` を返す。scoring table は partial line-header evidence、missing cell geometry、post-row-gap-only LineMark spans、failed raw-record-index fit (`maxAbs=61.005`) で block され、lower table は near-reference fit (`maxAbs=6.385`) があっても reference-backed で source-decoded ではないため block のままにする。
- [x] `tsaiten` `/PageMark` numeric hit probes に raw record/tail/subrecord context を追加する。lower table の 2 件の `0x0300` hits は scanned records `2` と `3` に属し、tail-block word indexes もそれぞれ `11` と `3` に分かれる。さらに byte offsets `174` と `334` にある repeated 16-byte u16 subrecord candidates の field index `2` として解決し、8-word windows 全体を layer-tree JSON に保存する。これにより lower-table hit は reverse engineering の手掛かりとして残しつつ、stable fixed-offset y field としては promote しない。
- [x] `tsaiten` table candidates に slot-scoped `/PageMark` u16 subrecord y-sequence diagnostics を追加する。`pageMarkScopedYTransformProbe` は `slotScopedSubrecordYSequenceComparison` を emit し、`fieldIndex+tailBlock16WordIndex` で grouped slots、LineMark record indexes、reference row tops/deltas、`/PageMark` word 21 と word 13+14 由来の page-scale candidates を保存する。さらに bogus な subrecord line ranges を scanned `/PageMark` max line end で filter し、same-header と foreign-header slots を分離する。これにより field-family residuals が異なる subrecord slots を混ぜる問題を避ける。visible table candidates はどちらも matched raw header `0` では `sameHeaderSlotCount:0` で、lower table の direct table-top hit は `fieldIndex:2`、`tailBlock16WordIndex:11`、`byteOffsets:[178,418,1074]` の foreign-header numeric hint としてだけ残る。全 row y を reference projection なしで説明する same-page/header semantics が未証明なので rendering は block のまま。
- [x] `/PageMark` row-delta diagnostics を修正し、row-to-row spacing targets を absolute candidate y values ではなく adjacent ordered candidate value deltas と比較するようにする。`pageMarkScopedYTransformProbe` は `rowDeltaCandidatePolicy:"adjacent-ordered-candidate-value-delta"` を expose し、family/slot fits は `rowDeltaResidualBasis:"adjacent-ordered-member-value-delta"` と `rowDeltaCandidateDeltasPx` を expose する。`tsaiten` regression test はこの basis を固定し、absolute y/top candidate を row-delta evidence として過大評価しないようにする。
- [x] table y slot を promote する前に、ordered one-to-one `/PageMark` slot coverage diagnostics を追加する。`slotScopedSubrecordYSequenceComparison` は `orderedLineMarkRecordCoveragePolicy:"one-ordered-subrecord-member-per-line-mark-record"`、`bestOrderedLineMarkRecordCoverageSlot`、per-slot ordered coverage counts/witness offsets を report する。この metric は ordered table `/LineMark` record set と各 slot の source-backed subrecord line ranges の最大 monotone assignment を使うため、1 つの wide subrecord が複数 compact rows の proof として過大評価されない。
- [x] `tsaiten` table candidates の `lineMarkStridePromotionReadiness` に、reference-free の `/PageMark` raw subrecord line-span readiness を直接 expose する。新しい `subrecordLineSpanReadiness` は scoring table で selected post-row-gap span hits が 3/4、lower table で 3/3 であることを示し、lower table には `page-mark-subrecord-spans-fit-selected-post-row-gaps` と `page-mark-subrecord-spans-do-not-decode-page-y-origin` blocker を追加する。これは LineMark row/gap sequence の corroboration であり、page-y origin transform の decode ではないため rendering promotion は引き続き禁止する。
- [x] 同じ `tsaiten` `subrecordLineSpanReadiness` の下に source-only `subrecordSpanRoleGate` を追加する。この gate は dominant matched role を row height ではなく `selected-post-row-gap` と分類する。scoring は post-row-gap hit 3/4・row-span hit 0、lower は post-row-gap hit 3/3・row-span hit 0 であるため、別の page-y origin transform が decode されるまでは `/PageMark` raw subrecord spans や `0x0300` absolute-looking fields を visible row-y authority として扱わない。
- [x] すべての table-grid candidates に diagnostic-only `sourceDerivedLayoutReadiness` gate を追加し、source-derived layout が無い/blocked である理由を reference PDF inspection なしで説明できるようにする。`tsaiten` scoring table は `sourcePlacementEvidencePresent:false`、`matchedCellHeaderCount:0/12`、missing row/cell header coverage、`source-derived-layout-candidate-absent` を報告する。lower table は `matchedCellHeaderCount:6/6` と available candidate を報告するが、`line-mark-record-stride-to-page-y-transform-unproven` により rendering は block のまま。
- [x] `tsaiten` table candidates に interval-centric `/LineMark` row/gap sequence diagnostics を追加する。`lineMarkRowGapSequenceEvidence` はこの sample が `0x1000`/`0x1001`/`0x1002` tag-family payload を持たず (`lineMarkTagCount:0`)、実体は `be16-delta-v1` interval family であることを記録する。scoring table は selected records `[8,10,12,14]` がすべて post-row gaps (`[65,65,65,67]`) と一致し、直前 records `[7,9,11,13]` が compact row spans (`[107,113,113,113]`) と一致する。lower table も selected `[33,35,37]` が gaps (`[58,52,54]`)、直前 `[32,34,36]` が row spans (`[111,92,91]`) と一致する。同時に現在選ばれている `/LineMark` records は spacing records であって page-y row anchors ではないことを示すため、raw record-index Y は diagnostic-only のままにする。
- [x] `tsaiten` lower table の stride-Y evidence を、visible rendering には使わず representative source-derived layout candidate へ反映する。lower error table は `source:"documentTextLineHeaders+/LineMark+/PageMark"`、`pageOriginAuthority:"lineMarkPageGridStrideRawRecordIndex"`、bbox `x=72.000,y=765.000,width=451.181,height=63.000` を expose する。これは stride records、homogeneous line headers、sparse-sibling topology、post-row-gap correlation がすべて揃う場合だけ選択し、rendering は candidate readiness では `sparse-sibling-derived-candidate-render-ineligible`、page-y promotion では `line-mark-record-stride-to-page-y-transform-unproven` のまま block する。regenerated `openjtd-samples/pdf-output/ichitaro-20030120132956-0007-sp-dat-tsaiten.pdf` は Poppler と `sips` で nonblank。
- [x] `tsaiten` table candidates に scoped `/PageMark` Y-transform diagnostics を追加し、次の render promotion の前に source field family の不足を明示する。`recordIndexAffineFit` は scoring table の reference slope が `16.100px/record` である一方、raw `/LineMark` record-index heuristic は `21.000px/record` 進み、`4.900px/record` 速すぎることを示す。`pageMarkScopedYTransformProbe` は stream 全体ではなく matched parsed `/PageMark` entry/raw header fields だけを比較し、scoring rows では row-top hits が 0 件、first row nearest は `word25=22366` の centipoint 解釈 (`298.213px`, residual `-2.792px`) に留まる。一方 lower table は row 0 の `768` direct px hit と row 1 の centipoint near-hit を持つため、raw record-index Y ではなく reusable `/PageMark` field family を探す必要がある。
- [x] `pageMarkScopedYTransformProbe` に shared `/PageMark` subrecord field-family residual diagnostics を追加する。scoring table は best table-top/row-top/row-delta family を持たない一方、lower table の best table-top family は source-backed `rawRecordHeaderTailU16Subrecord` / `direct-u16-px` / `fieldIndex:2` であり、raw records `2` と `3` の byte offsets `178`、`338` がどちらも residual `-0.014px` で table top に一致する。これは lower-table top anchor の証拠であって scoring-table transform や per-row y decoder ではないため、rendering はまだ block する。
- [x] `tsaiten` の row-span record family を promotion 前に検証するため、alternate previous-row-span `/PageMark` scoped diagnostics を追加する。`previousRowSpanScopedYTransformProbe` は previous records `[7,9,11,13]` と `[32,34,36]` を同じ matched parsed PageMark entry/raw header に対して比較する。scoring table は依然として `targetHitCount:0`、best same-header field family なし、`sameHeaderSlotCount:0` である。lower table も foreign-header の `fieldIndex:2/tailBlock16WordIndex:11` table-top hint だけを保持する。したがって header matching は low-signal で、selected spacing records と previous row-span records のどちらも reusable な non-reference page-y transform をまだ decode していない。
- [x] `tsaiten` の source-derived table geometry record set を、sparse-sibling evidence が selected records を post-row gaps と証明する場合は previous compact-row-span `/LineMark` records を優先するように修正する。geometry resolver は scoring table で `[7,9,11,13]`、lower error table で `[32,34,36]` を使い、role correction が適用された `sourceDerivedLayoutCandidate` は `lineMarkRowRecordSelection:"previous-compact-row-span-record"` を expose する。これにより lower table の near-reference だった post-gap raw-record fit (`maxAbs=6.385`) は source-correct previous-row-span fit (`maxAbs=24.014`) に下がり、post-gap records が偶然 render authority にならない。cell provenance は別経路として overlapping selected records (`[8,10,12,14]` / `[33,35,37]`) を保持する。
- [x] `pageMarkScopedYTransformProbe` に source-backed PageMark subrecord line-span correlation diagnostics を追加する。`subrecordLineSpanCorrelation` は raw u16 subrecord の `words[4]..words[6]` span を、同じ table の selected post-row gap spans と previous compact-row spans に対して比較する。scoring table は `[65,65,65,67]` と `[107,113,113,113]`、lower table は `[58,52,54]` と `[111,92,91]` を target として expose する。reference y coordinates を使わずに次の PageMark span candidate を見えるようにしたが、span correlation だけでは page-y origin や row-top placement を decode しないため rendering は block のままである。
- [x] 同じ raw u16 subrecord candidate scan を model document-info と export JSON の `pageMarks[].entries[]` に一般化する。各 entry は decoded-false の `u16SubrecordScan` を持ち、source/geometry flags、candidate count、entry-relative offsets、stream offsets、raw 8-word windows、recomposed u32 fields を expose する。`tsaiten` の export/model tests は stream offsets `174` と `334` の 2 件の `0x0300` candidates を固定し、table-specific reference probes に頼らず PageMark variable payloads を比較できるようにする。
- [x] `success_data-test` abc table の layout probe に `/LineMark` row-boundary evidence を付与する。table unit range `519..924` は `/LineMark` records `16..18` と正確に一致し (`519..655`、`655..791`、`791..924`)、contiguous record indexes、`exactRowMatchCount:3`、`lineMarkProfile:"be16-delta-v1"` を expose する。visible 3-row table の source-backed row order/boundaries は証明されたが、LineMark units から page-y coordinate への transform は未 decode なので render promotion はまだ block する。
- [x] 同じ abc table について、`/LineMark` records を `/PageMark` page line ranges に写した page-association evidence を付与する。`getPageLayerTree` は `pageMarkLineMarkRecordEvidence` を emit し、row records `16..18` がすべて `/PageMark` entry `0` (`pageIndexCandidate:0`, `pageLineStart:0`, `pageLineEnd:39`) 内に入ることを示す。table の page ownership は source-backed になったが、`/PageMark` line indexes は y coordinates ではないため rendering promotion はまだ block する。
- [x] 同じ abc table layout probe に raw stream length だけではなく structured `/PaperMark` evidence を付与する。`getPageLayerTree` は `paperMarkPageAssociationEvidence` を emit し、同じ row records `16..18` が `/PageMark` entry `0` から `/PaperMark` row/index `0`、flags `0x00010010` へ map されることを示す。さらに `paperMarkEntryCount:8`、header count `8`、stride `12`、last index `7`、`pagePaperMarkEntryCountAligned:false` を報告するが、`/PaperMark` flag semantics が decode されるまで render promotion は block する。
- [x] 同じ abc table について、reference PDF 由来の horizontal span に頼らず source-unit bbox candidates を追加する。`unitGeometryCandidate.tableUnitBBoxCandidates` は all-row-agree candidates として matched cells (`0..72`)、matched cells plus first trailing header (`0..88`)、full line header extent (`0..174`) を報告し、future rendering が text span と grid slot width を区別できるよう `columnSpanUnits`、`columnSlotWidthUnits`、`trailingSlotWidthUnits` も分けて expose する。`referenceBBoxResidualEvidence.unitBBoxCandidateComparisons` は reference-backed px/unit comparison を別枠で保持し、それぞれ `3.833`、`3.136`、`1.586` px/unit を報告する。`lineHeaderLineMarkCouplingEvidence` は 3 件の `/DocumentText` line-header rows が `/LineMark` records `16..18` と正確に coupling することを示す。page-space unit scale と y transform が decode されるまで rendering は block のままにする。
- [x] 同じ abc table に y origin を promote せず vertical row-height comparison を追加する。`referenceBBoxResidualEvidence.referenceVerticalComparison` は homogeneous line-header font units 由来の source row height (`12 * 1.75 = 21.000px`) と reference row height (`21.001px`) を比較し、3-row table height residual `0.003px` と `lineMarkRecordSpan:3` を報告する。y-origin promotion は `page-space-y-origin-unproven` として block のままにする。
- [x] 同じ abc table に prompt-to-table source adjacency evidence を追加する。`topTextTableSourceGapEvidence` は text-specific hardcoding なしで nearest preceding full-width line-header anchor を選び、`空欄を埋めて表を完成させなさい。` を source units `501..517`、line-header units `487..499` として特定し、table が unit `519` から始まること (`sourceGapAfterAnchorTextUnits:2`、`sourceGapAfterAnchorLineHeaderUnits:20`) を記録する。first row header は `558..570` から始まり、first row は `/LineMark` record `16` に map される。page-space transform が decode されるまで y-origin promotion は block のままである。
- [x] 同じ abc table に targeted origin-residual diagnostics を追加し、`horizontal-x-origin-unproven` を opaque な blocker のままにしない。`referenceBBoxResidualEvidence.sourceDerivedHorizontalComparison.originResidualEvidence` は source-derived candidate と current reference bbox の残差 (`xResidualPx:1.046`、`yResidualPx:-0.705`、`widthResidualPx:0.927`) を記録し、x residual を table/full-extent source units (`0.335` / `0.661`) に換算し、applied gap-half inset、`/PageMark` body line-gap y pitch、first matched line-header raw words、selected `/PageMark` words (`10,13,14,17,18,19,20,21`) を保持する。original `rjtd-testdata/local-samples` Print-to-PDF reference に対する Poppler crop RMS はまだ line weight/font styling が支配的なので、この inset と line-gap pitch は final table styling の証明ではなく source-backed placement evidence として扱う。
- [x] `success_data-test` abc table の y-origin について、table row height (`21.000px`) を line offset に掛ける代わりに `/PageMark` body line-gap pitch (`21.214px`) を使う A/B を採用する。A/B Poppler crops は `wide-actual-table` RMS `92.202 -> 74.722`、`table-and-prompt` `80.008 -> 68.732`、`table-tight` `105.065 -> 82.445` に改善し、regenerated PDF は Poppler、Apple `sips`、PDFKit、CoreGraphics で nonblank render を確認した。
- [x] 同じ abc table に `/DocumentText` line headers、`/LineMark`、`/PageMark` から source-derived table placement candidate を追加する。candidate は all-row-agree の `matched-cells-plus-first-trailing-header` unit range (`0..88`) を table width に使い、row records `16..18` を `/PageMark` entry `0` に map し、`pageLineStart:0`、`lineOffsetFromPageStart:16`、source row height `21.000px` から page y `408.000px` を導出し、matched PageMark row を `pageMarkU16Fields`/`pageMarkU16FieldsHex` として expose する。これは source-backed placement evidence だが、horizontal page-unit scale と exact x-origin semantics が cross-sample で説明されるまでは final decoded geometry ではない。
- [x] `/PageMark` entry raw bytes と big-endian `u16Fields`、`u32Fields` を `Document` model、`getDocumentInfo`、JSON export まで保存する。これにより page-row/table-y/x-origin analysis の source evidence が `flags`、`lineStart`、`lineEnd` だけに潰れない。
- [x] 同じ abc table の `lineMarkPageOriginCandidate` に decoded-false PageMark u16 geometry hypotheses を追加する。layer tree は recurring candidate fields (`word10`、`word13`、`word14`、`word17`、`word18`、`word19`、`word20`、`word21`)、observed `word20 == 0x00ff` gate、`word13 + word14 == word21` arithmetic (`success_data-test` では `370 + 185 = 555`) と layout scale comparisons を保存する。ただし rendering は `page-mark-u16-geometry-semantics-unproven` として block する。
- [x] 同じ decoded-false PageMark u16 geometry hypotheses を、`getDocumentInfo` と JSON export の preserved `/PageMark` entries すべてにも expose する。downstream probe が page/layout context を渡さない場合は `layoutComparisons:null` とし、この hypothesis を current abc table diagnostic だけに閉じず global な source-backed evidence として保持する。
- [x] matched page row の `pageMarkU16Fields` と decoded-false `pageMarkU16GeometryHypotheses` を、`getPageInfo`、`rjtd page-info`、page decoration layer-tree entries の page-level `layoutMarkEvidence` にも expose する。A5 page 6 は `353 + 246 = 599` の source row tuple を報告しつつ、`word20Is0x00ff:false` として geometry promotion を block したままにする。
- [x] mark-bearing pages の `layoutMarkEvidence` に page-level decoded-false `pagePitchEvidence` を追加する。`getPageInfo` と page decoration layer-tree entries は rendering を変えずに `/PageMark` line ranges と current page/body size を比較し、A5 page 6 では `lineStart:23`、`lineEnd:40`、`lineGapCount:17`、`pageHeightPxPerLineGap:46.688`、`bodyHeightPxPerLineGap:38.218`、selected PageMark fields、明示的 blocker `page-mark-line-pitch-semantics-unproven` を expose する。
- [x] `word20 == 0x00ff` を universal rule として扱わず、PageMark u16 hypotheses に additive-unit pattern diagnostics を追加する。entries は `word21MinusWord13`、`word21MinusWord13EqualsWord14`、`word19EqualsWord13`、`selectedFieldsAllZero`、`nonZeroAdditiveUnitCandidate` を report し、layout context がある場合は `word21` と `word13+word14` に対する page-height/body-width ratios も report する。current PDF-backed samples では additive relation は normal rows で有用だが、A5/46、`success_data-test`、`tsaiten`、`tmogi3_2` の page/body scale が一致しないため、geometry promotion にはまだ不足している。
- [x] PageMark rows の decoded-false `u16GeometryClass`/`profile` classification を追加し、model JSON、export JSON、`rjtd page-marks` に expose する。current classes は `zero-sentinel`、`additive-row`、`additive-boundary`、`mixed-payload` であり、future promotion rules 用の evidence buckets であって decoded Ichitaro semantics ではない。
- [x] `rjtd page-mark-u16-profile <file>` を追加し、PageMark u16 class counts と selected-field tuples (`word10`、`word13`、`word14`、`word17`、`word18`、`word19`、`word20`、`word21`) を summarize する。これにより cross-sample sweep を ad hoc scripts なしで再現でき、future geometry promotion rules を observable profile buckets に結び付けられる。current checks では `success_data-test` は 2 件の `additive-boundary` rows を持ち、A5 は 68 `additive-row`、4 `additive-boundary`、2 `zero-sentinel`、1 `mixed-payload` rows を持つ。
- [x] `rjtd page-mark-pitch-profile <file>` を追加し、PageMark line ranges と current file-layout hint を組み合わせて entry ごとの page/body pitch diagnostics を出す。A5 entry 5 は `lineStart=23`、`lineEnd=40`、`lineGapCount=17`、`pageHeightPxPerLineGap=46.688`、`bodyHeightPxPerLineGap=38.218` を報告し、`success_data-test` entry 0 は `lineGapCount=39` と `bodyHeightPxPerLineGap=21.214` を報告する。PageMark line-pitch semantics が証明されるまで decoded-false のまま render promotion を block する。
- [x] `success_data-test` の raw FDM diagnostics を projected shapes だけに潰さず model/export JSON に保持する。surface は `fdmTextIndexEntries`、`fdmRawVectorSegments`、`fdmRawVectorSegmentCount:5`、`fdmRawVectorCommands`、`fdmRawVectorCommandCount:37` を expose し、answer-sheet/title/diagram object decoding のための direct source evidence として使えるようにする。
- [x] `success_data-test` answer-sheet projection に source-backed sparse topology と `/LineMark+/PageMark` diagnostics を付与し、page 2 lower table を opaque な reference coordinates のままにしない。`hatchedAnswerAreaCandidate` は sparse `DocumentText` table から section `５` から `６` までの merged empty answer area と隣接する `ＡＢ` answer cell を特定し、関連 section rows の source-grid candidates を expose する。SVG/PDF はこの candidate がある場合に top/bottom だけでなく four-edge hatch perimeter を描く。regenerated page-2 Poppler comparison は whole-page RMS `39.761 -> 39.486`、answer-table RMS `43.439 -> 43.038`、triangle-cell RMS `40.273 -> 38.431`、hatched-area RMS `42.862 -> 41.317` に改善した。Poppler、`sips`、PDFKit、CoreGraphics render は nonblank のままで、PDF に `/Group`、`/Transparency`、`/SMask` は含まれない。
- [x] `success_data-test` answer-sheet secondary grid rules を page-wide double-rule として描かず、source-identified merged answer area の手前で gate する。topology は sparse `DocumentText` table と answer-sheet reference frame 由来のまま、`ruleStyleCandidate` は `renderMode:"primary-plus-source-gated-secondary-line"` と `secondaryLineGate:"before-source-identified-merged-answer-area"`、`styleSourceBacked:false`、blocker `answer-sheet-rule-style-source-field-undecoded` を記録する。A/B checks では global secondary rules は top table band を維持する一方で Q5/right-answer cells を overdraw し、single-line rules は lower sheet を改善するが top table band を悪化させた。gated renderer は primary rules 19 件を保ちつつ secondary rules を 13 件へ clip/reduce し、final page-2 Poppler RMS は answer-sheet `47.734 -> 45.934`、Q5 hatch `36.880 -> 34.186`、right-answer cells `46.069 -> 37.515` に改善し、top-table crop は `56.877` のまま維持する。
- [x] `success_data-test` answer-sheet hatch renderer の dashed SVG stroke を、reference-observed の slanted parallelogram dash segments に置き換え、hatch geometry provenance と hatch style provenance を分離する。`hatchedAnswerAreaCandidate.hatchStyleCandidate` は `sourceBacked:false`、`referenceBacked:true`、`decoded:false`、blocker `answer-sheet-hatch-style-source-field-undecoded` を記録し、SVG も同じ decoded-false style attributes を持つ。renderer は hatched four edges の underlying black table rule を suppress し、gray parallelogram dash segments を描く。regenerated page-2 Poppler crops は active crop set で answer-table RMS `40.500`、hatch-perimeter RMS `30.576` になった。hatch style を encode する exact JTD field はまだ decoded ではない。
- [x] `success_data-test` answer-sheet triangle の direct SVG literal path を、projected A/B/C `FDMText` label slots から作る decoded-false `trianglePlacementCandidate` に置き換える。layer tree は A/B/C label anchors、`FDMTextIndex` source bbox `-16018..-10611/-15704..-10242`、page-space vertices、blocker `fdmtext-source-to-sheet-transform-undecoded` を expose し、SVG は `data-source="FDMTextIndex+projectedFdmLabelSlots"` と `data-placement-proven="false"` を持つ。これは current pixels を維持しつつ、real FDM-source-to-sheet transform が証明される前に triangle geometry を fully decoded と見せないための source/projection evidence である。
- [x] `success_data-test` page-2 title-art front-erase opacity sweep は historical evidence として残しつつ、source-order gate を追加して再評価する。この時点では `outline(texture)outline` source order、shadow/main `0x82.word5` split、`0x82.word3=#ffffff`、interstitial opacity source を source-backed candidate として保持し、gray front-face overpaint と ownership ambiguity のため visible rendering には promote しなかった。この判断は later PDF-safe precomposited front-erase promotion によって superseded された。
- [x] paint semantics をさらに触る前に、`success_data-test` page-2 title-art の structural A/B candidates を active Poppler crop set で追加検証して reject する。title paths/textures 全体を snapshot `sourceScale` から JSFart content-height `frameScale` に切り替える候補は悪化したため、`pathScaleDiagnostic` は diagnostic-only のまま維持する。この時点では interstitial front-erase reuse を source-order/paint-state candidate として保存し visible rendering を blocked にしたが、later PDF-safe precomposited front-erase promotion によって superseded された。
- [x] `success_data-test` title-art interstitial texture state breakdown を front-erase candidate evidence として expose する。`frontEraseTextureStateSummary` は 530 件の texture paths、first/last `11..540`、explicit/inherited state counts、payload word columns を保持し、`frontEraseTexturePathSource:"interstitial-between-shadow-and-main-outlines"` として source evidence を残す。当初は `visibleRenderPathCount:0`、`renderPromoted:false` だったが、later PDF-safe precomposited front-erase promotion によって superseded された。
- [x] `success_data-test` title-art の `frontEraseTextureRoleGate` diagnostics を更新し、source-gated interstitial/current-state inheritance を candidate と render promotion に分離する。当初 gate は `candidateBasis:"source-order-interstitial-front-erase-texture"`、`visibleRenderPathCount:0`、`renderPromoted:false`、`renderPromotionBlockedReason:"front-erase-texture-role-not-promoted"` を報告したが、later PDF-safe precomposited front-erase promotion で `visibleRenderPathCount:530`、`renderPromoted:true` に更新された。
- [x] visible rendering 変更前後で paint-routing hypotheses を再検証する。interstitial `0x70.word0=0x1c` を actual shadow/extrusion texture fill として promote する候補は page 1 title RMS `82.344 -> 85.162`、page 2 `84.680 -> 87.768` に悪化したため reject する。source-gated interstitial front erase は一度 gray front-face overpaint/ownership ambiguity を理由に blocked candidate として扱ったが、later PDF-safe precomposited front-erase promotion と fresh reference comparison によって superseded された。
- [x] `success_data-test` の main `/FigureData/main_data/FDMText` に残っていた Shift-JIS labels (`９㎝`, `３㎝`, `Ａ`, `Ｂ`, `Ｃ`) を decode し、Q3 cone diagram projection の source extent に復元した label bboxes を使うようにする。cone SVG/PDF labels は hardcoded page coordinates ではなく `FDMText` 由来になり、Q3 source extent は vector bbox と近接 text bbox の union (`-12004..-10720`, `-12064..-10510`) になった。Q5 viewport retune は Q5 crop RMS を悪化させたため採用しない。
- [x] `success_data-test` Q4 FDM angle diagrams は source y/x span ratio と reference-backed target width から target height を導く `uniform-units-from-horizontal-span` projection に変更し、`r=600` source circles が縦長にならず円として残るようにした。Poppler 96dpi 比較では Q4 crop RMS が `82.5963` から `74.8490`、whole-page RMS が `77.1519` から `76.0590` に改善した。
- [x] `success_data-test` Q4 FDMText labels は preserved text bboxes と reference PDF text layer を根拠に metrics を調整し、page-coordinate label nudges にはしない。Q4 labels は bbox-height font factor `0.67` と baseline factor `0.12` を使い、SVG では `font-size="10.7"` になる。reference text-bbox check では代表 labels の top error が約 `3.6-4.9px` low から約 `0.2-1.4px` low (`110°`, `30°`, `160°`, `ｘ`, `Ｏ`) に改善し、Q5 は変わらない。
- [x] FDMText streams 用の generic で source-backed な `fdmTextMirrorAnchorAgreement` diagnostic を追加する。indexed FDMText stream に、復元した text と record bbox の順序が同じ別の FDMText stream があり、すべての FDMTextIndex row が対応する text-record offset と bbox を指す場合だけ報告する。current `success_data-test` evidence では `/FigureData/ExpandData/main_data/Data/FDMText` と `/FigureData/main_data/FDMText` の間で 15-record agreement が 1 件得られる。gate は `decoded:false`、`diagnosticOnly:true`、`renderable:false`、`placementProven:false` のままとし、source-to-page transform、text role、paint order が decode されるまで `fdmtext-source-to-page-transform-undecoded` で promotion を block する。
- [x] Q4 の tiny round `ff000460` FDM ellipses (`874`, `1430`) は visible full circles ではなく source control/angle markers として扱う。renderer は近接 FDM line geometry から短い angle arc を導き (`data-render-source="q4-small-ellipse-angle-arc"`)、3 つの main `01000460` circles は decoded ellipse center へ fallback する前に近接 FDM line endpoints から center dots を render する (`data-render-source="q4-main-circle-center-nearby-line-endpoint"`)。これにより Q4 の spurious full inner circles を消しつつ Q5 の large `ff000460` cylinder ellipses は表示したままにでき、Q4 crop RMS は text-only `75.0310` から `74.2922` に改善し、previous center-dot anchor に対する whole-page RMS も `76.0113` から `75.9840` に改善した。Q5 は変わらない。
- [x] `success_data-test` FDM reference primitive strokes から余分な `0.72px` lower bound を外し、既存の `fdm_vector_stroke_width` source-style interpretation を直接使う。Q4 offset `538` や Q5 offset `1992` のような line primitives は `0.72` に inflate されず、`data-stroke-width-source:"fdm-vector-style"` と `data-stroke-width:"0.500"` で render される。ellipse primitives は decoded `0.720` のまま維持する。regenerated 96dpi Poppler comparison では whole-page RMS `43.8295 -> 43.7239`、Q4 crop RMS `40.2808 -> 39.4841`、Q4 geometry RMS `38.3926 -> 37.3601`、Q5 RMS `41.5938 -> 41.2462` に改善した。
- [x] `success_data-test` Q4 FDM angle diagrams を、full Q4 command set 全体で center-dot と tiny-arc derivation を走らせるのではなく、large `01000460` circle centers (`308`, `470`, `504`) を anchor にした 3 個の source-geometry local subdiagram groups に分割する。SVG は `rjtd-success-data-test-fdm-subdiagram` groups を emit し、`data-grouping-source:"nearest-main-circle-source-center"`、`data-grouping-decoded:"false"`、`data-paint-order-decoded:"false"` を記録する。local command counts は `7`、`6`、`7` である。これは `FDMIndex` ownership や paint order semantics を promote せず、regenerated 96dpi Poppler metrics は whole-page RMS `43.7239`、Q4 crop RMS `39.4841`、Q4 geometry RMS `37.3601`、Q5 RMS `41.2462` のまま安定している。
- [x] Q4/Q5 ownership semantics を promote する前に、parser-owned FDMVector command provenance を保存する。`ObjectFdmVectorCommandCandidate` は `sourceVectorRelativeOffset` と optional な `sourceSegment` membership (`relativeOffset`, `localOffset`, `declaredLength`, `commandCount`, `commandIndex`, `commandOffset`) を model、`getDocumentInfo`、page layer-tree diagnostics、JSON export に expose する。これにより Q3 offset `208` のような raw primitive span (`sourceSegment:null`) と Q5 offset `1992` のような header-backed command (`sourceSegment.relativeOffset:1864`, `localOffset:128`, `commandIndex:2`) を、PDF pixels を変えずに区別できる。
- [x] 同じ FDMVector command provenance を `success_data-test` SVG primitives に diagnostic `data-*` attributes として expose する。Q3/Q4 raw primitives は `data-source-vector-relative-offset` と `data-source-segment-backed="false"` を持ち、Q5 の segment-backed primitives は full source segment offset-table fields を持つ。これにより SVG inspection と model/export evidence が揃い、regenerated PDF の見た目は変わらない (`sips` page-1 non-white pixels は `35277` のまま)。
- [x] `success_data-test` FDM SVG projection と Q4 local subdiagram groups に source-ownership cohort diagnostics を追加する。projection/subdiagram groups は `data-source-ownership-basis="fdmVectorCommandProvenance"`、command offset ranges、command offset lists、source segment cohort counts、`data-source-ownership-promotion-blocked-reason` を expose する。current Q4/Q5 groups はまだ `ownership-proven=false` のままである。Q4 は `20` commands のうち raw spans `18`、segment-backed commands `2` (`690,1246`)、Q5 は `7` commands のうち raw span `1`、segment-backed commands `6` (`1864,2100`) なので、どちらも `mixed-raw-and-segment-cohorts` として block し、FDMIndex ownership を premature に promote しない。
- [x] `success_data-test` Q4/Q5 FDM source-cohort diagnostics を `getPageLayerTree` の `fdmReferenceProjection` ops に mirror する。layer tree は projection-level と Q4 subdiagram-level の `sourceCohort` JSON を expose し、command relative offsets、raw-span vs segment-backed counts、segment cohort offsets、ownership promotion blockers を保持するため、次の line/chord/arc ownership work は SVG-only `data-*` inspection に依存しない。
- [x] 同じ Q4/Q5 FDM reference projection diagnostics を `getDocumentInfo` と `rjtd export --format json` の object-stream JSON に `successDataTestFdmReferenceProjections` として mirror する。aggregate は各 projection の role、source bbox、reference target bbox、command count、source cohort、render-promotion blocker、Q4 subdiagram blockers を SVG/layer-tree 経路の外でも保持する。
- [x] `success_data-test` Q4/Q5 FDM reference projections に decoded-false `primitiveOwnershipComparison` diagnostics を `getPageLayerTree`、`getDocumentInfo`、JSON export で追加する。この comparison は command provenance と source-geometry local subdiagram anchors を使い、各 preserved FDM command を `line-candidate`、`chord-candidate`、`radial-line-candidate`、`arc-candidate`、`connector-candidate`、`surface-boundary-candidate` などの role candidates に分類する。`ownershipProven:false` のまま `primitive-role-and-paint-order-unproven` で promotion を block するが、PDF pixels を変えずに Q4 (`20` commands、`3` anchors、`11` lines、`6` arcs、`8` connector candidates) と Q5 (`7` commands、`2` lines、`4` arcs、`3` connector candidates) の stable counts を expose する。
- [x] 同じ Q4/Q5 `primitiveOwnershipComparison` diagnostics に decoded-false FDMIndex row reference candidates を追加する。`success_data-test` では parsed `vectorOffset` field はこれらの rows で invalid だが、row の `bbox.left` field は command relative offset (`matchKind:"command-relative-offset-field"`) または segment relative offset (`matchKind:"source-segment-relative-offset-field"`) に一貫して一致する。Q4 は `20` index-row reference candidates、Q5 は `7` を持ち、すべて `validVectorOffset:false` なので、paint order や vector offset semantics を promote せずに FDMIndex row ownership evidence を保持する。
- [x] 同じ FDMIndex row offset-field evidence を Q4/Q5 projection path の外にも generalize する。export される各 `fdmIndexEntries[]` row は、row field (`vectorOffset`, `bbox.left`, `bbox.top`, `bbox.right`, `bbox.bottom`) が preserved FDMVector command または source-segment offsets に一致する場合、decoded-false `offsetFieldReferenceCandidates` を持つ。`success_data-test` では Q4 command row `bbox.left=308`、Q4 segment row `bbox.left=690`、Q5 segment row `bbox.left=1864` などが matched command offsets を row 自体に expose するため、future samples は sample-specific projection diagnostics に頼らず同じ hypothesis を検証できる。
- [x] generalized FDMIndex offset-field evidence を CLI diagnostics にも expose する。`rjtd object-fdm-index` と `rjtd object-fdm-index-rows` は row ごとの `offset-field-refs` と summary counts を report し、別の vector parser を増やさず model の `fdm_raw_vector_commands()` を再利用する。PDF-backed `success_data-test` regression は `bbox.left:command:308` と `bbox.left:segment:1864->[1924,1958,1992,2024]` を observable case として固定し、synthetic image-only fixtures は `offset-field-refs=-` のままにする。
- [x] `primitiveOwnershipComparison` の中で FDMIndex row references を primitive role candidate ごとに group 化する。`indexRowReferenceRoleCandidateGroups` は role candidate ごとの referenced command offsets、row indexes、command-relative/source-segment reference counts を記録し、`ownershipProven:false` と `role-candidate-and-paint-order-unproven` を維持する。これにより Q4 main-circle rows `[7,12,13]`、Q4 radial-line rows `[8,10]`、Q5 source-segment line row `[40]` のような ownership evidence が、unproven paint order を promote せずに見える。
- [x] それらの role candidate groups に row-command order evidence を追加する。各 group は `uniqueCommandRelativeOffsetCount`、`uniqueRowIndexCount`、`oneToOneRowCommandReferenceCandidate`、`singleRowBacksMultipleCommandsCandidate`、`rowOrderMatchesCommandOrderCandidate`、明示的な `rowCommandPairs` を expose する。これにより Q4 の one-row-per-command pattern と Q5 の single-row-backed segment commands を分けて観察できるが、paint order は decoded-false のまま維持する。
- [x] FDMIndex-backed primitive ownership の projection-level `indexRowOrderPromotionGate` diagnostics を追加する。gate は referenced/unreferenced command count、unique row count、command-relative/source-segment reference counts、row-order monotonicity、one-to-one vs single-row-backed segment patterns を summarize する。current `success_data-test` evidence では Q4 は 20 commands すべてが 20 rows に one-to-one で参照され、Q5 は 7 commands すべてが 3 rows に参照され `singleRowBacksMultipleCommandsCandidate:true` だが、どちらも primitive role と paint-order semantics が未証明なので block のままにする。
- [x] 同じ FDMIndex row-order gate を `success_data-test` SVG projection/subdiagram groups に non-rendering `data-index-row-order-*` attributes として mirror し、JSON/layer-tree gate にも `renderPaintOrderBasisCandidate:"fdm-index-row-command-pairs"` を追加する。Browser/SVG inspection は JSON と layer-tree diagnostics と同じ decoded-false row-command evidence を expose し、PDF pixels は変えず paint-order promotion も block のままにする。
- [x] evidence が one-to-one、monotonic、かつ selected commands すべてを参照する場合に限り、FDMIndex row-order gate を実際の Q4 SVG render path で consume する。`success_data-test` Q4 は 20 個の row-owned commands を `data-render-command-order-basis="fdm-index-row-command-pairs"` で render し、Q5 は 7 commands が 3 rows に fan-out しているため `fdm-index-row-fanout-primitive-ownership-unproven` として FDMVector projection-filter order のままにする。
- [x] `/LineMark+/PageMark` origin があるだけで source-derived table を render せず、全行の `/LineMark` row boundary が exact かつ `/DocumentText` line headers が homogeneous な場合だけ render promotion する gate を追加する。`success_data-test` abc table は `lineMarkRowsExactAndContiguous:true`、`lineHeaderRowsHomogeneous:true`、`renderPromotionBlockedReason:"none"` のまま render される一方、PDF-backed `shanai_lan` の network diagram text-slot 候補は diagnostic evidence として残しつつ `line-mark-rows-not-exact-source-boundaries` で block し、`NASサーバ`/`TA`/`8ﾎﾟｰﾄ` 周辺の false table rectangle が SVG/PDF に出ないようにした。regenerated `openjtd-samples/pdf-output` outputs は Poppler、`sips`、CoreGraphics で nonblank render を確認し、`success_data-test` は visible table projection 1 件、`shanai_lan` は visible table projection 0 件になった。
- [x] table layer-tree diagnostics に source-backed `table-page-space-v1` solver gate を追加する。`success_data-test` abc table は `solverStage:"renderable-source-page-space"` と `renderPromotionAuthority:"source-derived-page-space-solver"` を報告し、`tsaiten` は scoring table の `candidate-absent` と lower table の `blocked-y-origin-transform` / `line-mark-record-stride-to-page-y-transform-unproven` を分離する。`shanai_lan` は `blocked-row-height-transform` と `line-mark-rows-not-exact-source-boundaries` のまま block されるため、diagram text-slot false positives が visible table overlay に昇格しない。
- [x] `success_data-test` ABC table の source-derived layout に source-only horizontal unit-transform readiness diagnostics を追加する。`sourceDerivedLayoutCandidate.horizontalUnitTransformReadiness` は selected line-header basis (`matched-cells-plus-first-trailing-header`)、unit range `0..88`、full extent `174`、width ratio `0.506`、row agreement `3/3`、slot widths `[14,14,16,16,16]`、trailing slot `[12]`、`uniform-intercell-gap-half` origin inset を記録する。reference bbox residual diagnostics は残し、PDF pixels は変えないまま、残る ABC x/width calibration semantics を取り除く前の source evidence を固定した。
- [x] 同じ ABC table `horizontalUnitTransformReadiness` に source-only の `totalWidthSemanticsGate` を追加する。gate は visible selected range (`88` units) と full line-header extent (`174` units) を分離し、trailing evidence (`86` trailing units、included trailing header 1 件、trailing slot `[12]`) を記録する。selected visible width と full document-row extent が同じ semantic だとまだ証明されていないため、width promotion は `source-total-width-semantics-unproven` のまま block し、rendering は変更しない。
- [x] `success_data-test` ABC table の `topTextTableSourceGapEvidence` に source-only `sourceTablePlacementCoherenceGate` diagnostics を追加する。gate は nearest preceding full-width instruction anchor (`sourceGapAfterAnchorTextUnits:2`、shared extent/font) と table line-header width candidates を結合し、current source evidence が selected visible range `0..88`、first trailing header `76..88`、second trailing header `92..174`、full extent `174`、row agreement `3/3`、exact contiguous `/LineMark` rows と coherent であることを保存する。これは diagnostic-only のままで、render promotion は `source-total-width-semantics-unproven` として block し、full line-header extent を visible table width と誤って扱わないようにする。
- [x] 同じ `sourceTablePlacementCoherenceGate` の下に nested source-only `sourceTopTextPlacementReadinessGate` diagnostics を追加する。この gate は source-backed anchor/table evidence (`candidateSourceUnitRange:519..924`、selected range `0..88`、full extent `174`、exact `/LineMark` rows) を再利用し、width と y-coupling の sub-gates がどちらも pass した場合に `sourceTopTextPlacementReady:true` を報告する。古い reference-backed residual は diagnostic evidence として残すが、render authority にはしない。
- [x] ABC table readiness gate の下に source-only `sourceTopTextPageGridCouplingGate` を追加する。この gate は selected top-text instruction anchor と first table row が同じ `/PageMark` entry と隣接する `/LineMark` records (`anchorLineMarkRecordIndex:15`、`firstTableLineMarkRecordIndex:16`、`lineMarkRecordGapAfterAnchor:1`) を共有することを証明し、この source-backed path の previous `page-space-y-origin-unproven` readiness blocker を解除する。
- [x] 同じ readiness gate の下に source-only `sourceVisibleWidthVsFullExtentGate` を追加する。この gate は selected visible width `0..88` と full line extent `0..174` を分離し、first trailing header `76..88` が selected visible range を閉じ、second trailing header `92..174` が full-line remainder として保存されることを分類する。reference/PDF fields を使わずに ABC top-text/table source-placement readiness の previous `source-total-width-semantics-unproven` blocker を解除する。
- [x] ABC `horizontalUnitTransformReadiness` を `sourceFrameAdmissionGate` と揃える。source layout が stable line-header units、source column splits、exact `/LineMark` rows、`lineMarkPageGrid` origin を持つ場合、`pageSpaceUnitScaleDecoded:true` を報告し、自身の `page-space-unit-scale-unproven` blocker を解除する。Rendering は変えず、remaining reference bbox residuals は diagnostics-only として残す。
- [x] ABC table の `totalWidthSemanticsGate` blocker を explicit next-gate state に分離する。selected visible range の evidence が source-ready (`selectedVisibleRangeSourceEvidenceReady:true`) だが full extent がまだ広い場合、gate は `sourcePlacementCoherenceGateRequired:true` と `renderPromotionNextGate:"source-table-placement-coherence-gate"` を記録する。trailing/header evidence がない sample は `renderPromotionNextGate:"source-total-width-semantics-decoder"` に留める。rendering は変更しない。
- [x] その ABC `totalWidthSemanticsGate` handoff を blocker string にも反映する。`sourcePlacementCoherenceGateRequired:true` の場合は generic `source-total-width-semantics-unproven` ではなく `renderPromotionBlockedReason:"source-table-placement-coherence-gate-required"` を報告し、trailing/header evidence がない samples は従来どおり decoder blocker を報告する。これにより source-ready selected-width path と本当に undecoded な width semantics を区別する。
- [x] outer ABC `topTextTableSourceGapEvidence` を nested source-only readiness gate と整合させる。nested `sourceTopTextPlacementReadinessGate` は instruction anchor、selected table width、trailing header semantics、adjacent `/LineMark`/`/PageMark` grid coupling が source-ready であることをすでに証明しているため、outer evidence も `sourceTopTextPlacementReady:true`、empty `readinessBlockedReasons`、contribution `source-top-text-placement-readiness`、`renderPromotionBlockedReason:null` を報告する。古い `page-space-y-origin-unproven` blocker は解除し、reference bbox residuals は diagnostic-only のまま保持する。
- [x] `success_data-test` ABC の hardcoded reference table bbox を `diagnostic_reference_table_grid_overlay_layout` の内側に隔離し、generic production/reference fallback path が sample-calibrated ABC layout を受け取らないようにする。`reference_table_grid_overlay_layout` は non-ABC reference family (`tsaiten`) だけを expose し、ABC reference coordinates は `referenceBBoxResidualEvidence`、top-text residual probes、source/reference comparison diagnostics だけで使う。regression assertions は `diagnostic_reference_table_grid_overlay_layout(...abc...)` が present のまま、`reference_table_grid_overlay_layout(...abc...)` が normal case と deliberately unproven source-layout variant の両方で `None` になることを固定する。regenerated `success_data-test.pdf` は PDFKit-safe (`%%EOF` 1、`/Subtype /Form` 2、`/FormType` 2、`/Group`/`/SMask`/`/ExtGState`/`/Pattern`/`/Shading` 0) のまま、Poppler と Apple `sips` で nonblank render できる。
- [x] loose な span match から `tsaiten` table geometry を promote しないよう、`table-page-space-v1` に source-backed `sourcePageYTransformGate` を追加する。gate は table row ごとの ordered unique `/PageMark` subrecord coverage を記録する。`success_data-test` は `promotionReady:true` のまま、`shanai_lan` は `line-mark-rows-not-exact-source-boundaries` で block のまま、`tsaiten` scoring table は `source-derived-layout-candidate-absent` のまま、`tsaiten` lower table は selected records `[33,35,37]` が post-row-gap spans に hit するが subrecord candidates `[174,734,174]` を再利用している (`uniqueCandidateCount:2/3`, duplicate `174`) ことを示す。そのため rendering は `page-mark-subrecord-selected-post-row-gap-candidates-not-row-unique` と `source-page-y-transform-not-decoded` で block のままにし、regenerated `openjtd-samples/pdf-output/ichitaro-20030120132956-0007-sp-dat-tsaiten.pdf` は Poppler と Apple `sips` で render 確認済み。
- [x] `tsaiten` の y-transform promotion 前に `/PageMark` subrecord の source-range と cross-table ordering diagnostics を追加する。`pageMarkRawRecordSourceRangeEvidence` は scoring table が raw PageMark header overlap を `3/4` row source ranges でしか持たず (`matchedScanIndexes:[4,5,6,7,8,9,11,14]`, row 3 は uncovered)、lower table は `0/3` row source coverage であることを示す。`crossTableSubrecordOrderingProbe` は同じ sparse sibling を共有する 4 個の compact table candidates をまとめて比較し、combined byte offsets `[414,414,414,174,414,174,174,734,174]` と raw scan indexes `[3,3,3,2,3,2,2,6,2]` が non-monotonic かつ `174` family を `734` 後に再利用することを示す。y gate は `page-mark-cross-table-raw-record-order-regression` と `page-mark-cross-table-subrecord-ordering-unproven` で block し、この evidence は diagnostic-only のままにする。
- [x] `sourcePageYTransformGate` の下に `lineMarkStrideToPageYPromotionReadiness` を追加し、`tsaiten` lower table の stride hypothesis が render authority と誤解されないようにする。lower table は source-only blockers を 1 か所に summarize する: `strideCandidatePresent:true`、`lineMarkPageOriginPresent:false`、selected post-row-gap spans は complete だが ordered-unique ではない (`[174,734,174]`)、raw PageMark source-range coverage は `0/3`、cross-table subrecord ordering は inconsistent、source-order/subrecord-order contradiction は true。regenerated `openjtd-samples/pdf-output/ichitaro-20030120132956-0007-sp-dat-tsaiten.pdf` は previous Poppler render から視覚変化なし (`RMS 0.000000`, changed ratio `0.000000`) で、Poppler `186393` non-white pixels、Apple `sips` `55283` non-white pixels で nonblank render できる。
- [x] visible な `tsaiten` lower-table reference fallback の内部 column split を equal reference columns ではなく source-backed `/DocumentText` line-header cell slot widths から出す。table 自体は `referenceBacked:true` のままで、page/y placement はまだ `line-mark-record-stride-to-page-y-transform-unproven` により blocked だが、rendered lower table は `columnWidthBasis:"documentTextLineHeaderCellSlotUnits"` と `columnWidths:[378.342,175.659]` を expose する。`referenceBBoxResidualEvidence` は bbox が source column widths を使うものの source placement ではないことを記録する。再生成した `openjtd-samples/pdf-output/ichitaro-20030120132956-0007-sp-dat-tsaiten.pdf` は Apple `sips` で `55279` non-white pixels を出し、`/Group`、`/SMask`、`/ExtGState`、`/Pattern`、`/Shading` の safety-token hits は 0。
- [x] `tsaiten` table sparse-sibling evidence の横に `sparseSiblingColumnPromotionReadiness` を追加する。scoring table は sparse topology が complete (`4/4` rows、`12/12` segments、sparse columns `[3,4,5]`) でも compact line-header geometry が `0/12` で source line-header column widths も missing のため column split promotion 不可と明示する。lower table は compact headers `6/6` と source width fractions `[0.683,0.317]` により `columnSplitReady:true` を報告する。rendering は変えず、sparse topology だけが visible column geometry authority にならないように gate する。
- [x] `tsaiten` lower-table stride-row evidence を source-unit domain と PageMark-line domain に分離する。`lineMarkStrideToPageYPromotionReadiness.sourceOnlyStrideRowCoverage` は previous compact row-span `/LineMark` spans と table row source spans を `documentTextSourceUnits` で比較し (`rowSpanUnits:[111,92,91]`, `lineMarkSpanUnits:[111,92,91]`, residuals `[0,0,0]`)、`pageMarkEntryLineBoundsCoverage` は record indexes `[32,34,36]` が PageMark entry line bounds `0..42` 内にあることだけを別に記録する。legacy raw `rawRecordSourceRangeCoverage` は残すが、document-text unit ranges と PageMark line indexes を混ぜるため `rawRecordSourceRangeCoverageUsableForPromotion:false` と明示する。regenerated `openjtd-samples/pdf-output/ichitaro-20030120132956-0007-sp-dat-tsaiten.pdf` は Apple `sips` で `595x842`、`55279` non-white pixels、`/Group`、`/SMask`、`/ExtGState`、`/Pattern`、`/Shading` safety-token hits は 0。
- [x] `tsaiten` table horizontal source evidence を render promotion readiness から分離する。`sourceFrameCandidateAgreementGate` は best x/width group が source-only の PageMark/line-header field evidence で一意に支持される場合に `sourceOnlyUniqueSelectionCandidatePresent:true` を報告する一方、`selectionReady:false`、`sourceOnlyUniqueSelectionPromotionReady:false`、`source-horizontal-field-semantics-unproven` は維持する。これにより scoring table (`x=174,width=421`) と lower table (`x=174,width=554`) の候補を reusable source-backed diagnostics として固定しつつ、render authority にはしない。
- [x] 同じ `tsaiten` lower table に `lineMarkStrideToPageYPromotionReadiness.pageMarkSubrecordLineRangeRecordCoverage` を追加する。source-backed diagnostic は selected spacing records `[33,35,37]` と previous row-span records `[32,34,36]` を raw `/PageMark` u16 subrecord の line-start/end ranges と比較し、どちらの record family も covered されないことを示す (`selectedCoveredRecordIndexes:[]`, `previousCoveredRecordIndexes:[]`, `selectedCoverageComplete:false`, `previousCoverageComplete:false`)。これにより近傍の `field2Value:768` table-top hit は actual table LineMark records を含まない subrecord line ranges に属するため diagnostic-only のまま保持する。regenerated `tsaiten.pdf` は final `%%EOF` 1 件、scrub 済み internal CMap `%%EOD` 3 件、`/Group`/`/SMask`/`/ExtGState`/`/Pattern`/`/Shading` tokens 0、Apple `sips` page 1 `595x842` / `54566` non-white pixels で PDFKit-safe のまま。
- [x] `tsaiten` lower-table stride-row coverage の横に source-only `lineMarkRowBoundaryAlignment` を追加する。previous row-span records `[32,34,36]` は row span には一致する (`spanResidualUnits:[0,0,0]`) が、compact row source boundaries とは exact alignment しない。row source boundaries `[1857,2026,2170]..[1968,2118,2261]` と LineMark boundaries `[1775,1944,2088]..[1886,2036,2179]` を比較すると stable start/end residuals `[-82,-82,-82]` と top-level `rowBoundaryOffsetCandidateFamily:"previous-row-span-records"` / `rowBoundaryOffsetCandidateUnits:-82` / `rowBoundaryOffsetCandidateRequiresTransform:true` が得られる。一方 selected spacing records `[33,35,37]` は residual が unstable で span residuals も negative である。rendering は `line-mark-row-boundaries-require-source-offset-transform` で block のまま。regenerated `tsaiten.pdf` は PDFKit-safe (`%%EOF` 1、`/Group`/`/SMask`/`/ExtGState`/`/Pattern`/`/Shading` 0) で、Apple `sips` は `595x842` / `54566` non-white pixels。
- [x] `tsaiten` table y-origin promotion の前に `sourcePageYTransformGate` へ cross-table row-boundary offset consistency を追加する。同じ sparse table candidate `4` を共有する compact table candidates `[0,1,2,3]` はすべて previous-row-span boundary transform として `[-82,-82,-82,-82]` を報告し、stable candidate `-82` と witness record families `[7,9,11,13]`, `[21,23]`, `[25,27]`, `[32,34,36]` を expose する。gate は `cross-table-row-boundary-offset-transform-required` を記録するが、この source-unit offset は PageMark page-y origin や既存の subrecord ordering blockers を decode しないため rendering は block のままにする。
- [x] stable な `tsaiten` row-boundary offset evidence を render admission には使わず、offset-normalized exactness diagnostics として昇格する。各 previous-row-span alignment family は `offsetNormalizedStartResidualUnits`、`offsetNormalizedEndResidualUnits`、`offsetNormalizedExactBoundaryMatchCount`、`offsetNormalizedExactBoundaryAligned` を emit し、lower table は `[0,0,0]` / `[0,0,0]` と `3/3` を記録する。cross-table sparse-sibling probe も candidates `[0,1,2,3]` について `allOffsetNormalizedBoundariesExact:true` を報告する。これにより stable `-82` transform が source row boundaries と previous LineMark boundaries を exact に合わせることは証明するが、PageMark y-origin と subrecord ordering semantics が decode されるまで rendering は block のまま。
- [x] `tsaiten` の `sourcePageYTransformGate` に diagnostic-only な `lineDomainPostRowGapProjectionProbe` を追加する。scoring table では line-domain `235.167` に post-row-gap span `65` を足すと diagnostic reference top から `0.967px` 以内に入るが、selected records は spacing records であり、source/reference validation も混在し、lower table counterexample (`817.905 + 58`, residual `107.563px`) が大きく外れるため、`line-domain-post-row-gap-projection-crosses-source-unit-domain` として render authority にはしない。
- [x] `tsaiten` page-y decoding に source-only の transition gate と absolute-y slot gate を追加する。`transitionSemanticsReadiness` は table-family gaps を source-backed に保持し (`sourceRangeGapUnits:[190,72,138]`, row-start gaps `[303,160,230]`, previous/selected LineMark gaps `[8,2,5]`)、source-gap から page-line-gap への rule が未 decode であることを明示して promotion を block する。さらに `page-mark-line-index` domain の per-boundary `transitionPairs` を expose し、previous/selected transition gaps の一致を reference coordinates なしで確認できるようにしつつ、`pageSpaceTransitionOriginDecoded:false` は固定する。direct source-gap ratio check も simple mapping を reject する: `sourceRangeUnitsPerPreviousRecordGap:[23.750,36.000,27.600]` と `rowSourceStartUnitsPerPreviousRecordGap:[37.875,80.000,46.000]` は stable ではなく、PageMark y gap per record だけが `[23.310,23.310,23.310]` と stable である。`sourceGapToPageLineGapDirectMapDiagnostic` は direct-map mismatch を明示的に記録し (`pageLineGaps:[8,2,5]`, minus arrays `[182,70,133]` / `[295,158,225]`, equality arrays はすべて false)。さらに `sourceGapToPageLineGapSegmentOffsetDiagnostic` は次の piecewise offset candidate (`rowSourceStartGapUnits - sourceRangeGapUnits`) を検査し、segment offsets `[113,88,92]` も page-line gaps ではない (`minus [105,86,87]`, ratios `[14.125,44.000,18.400]`, unstable) として reject する。`sourceGapToPageLineGapTransformReadiness` は両 diagnostics を source-backed decline path に変換する。direct source-range、row-source-start、segment-offset transforms は reject され、`acceptedTransformKind:null` のまま、次に必要な evidence は decoded source-gap unit domain または segment transition offset rule である。`sourceOnlyPageMarkAbsoluteYSlotGate` は reference coordinates を使わず、line-domain + post-row-gap projection と raw PageMark absolute-y slot を比較する。lower table は `rawRecordHeaderTailU16Subrecord/direct-u16-px fieldIndex:2 tailBlock16WordIndex:11`、`absoluteYSlotY:768.000` を expose し、line-domain projection `875.905` は `107.905px` mismatch として reject する。同じ slot は `sourceOnlyPageYOriginCandidateAgreementGate` に blocked source-only support として mirror し、render authority にはしない。

Remaining:

- [ ] extracted newline text から導出するのではなく、real paragraph boundaries を decode する。
- [x] `success_data-test` title-art front-fill paths を overlap holes が抜ける `evenodd` ではなく、EmbeddedPress path winding を保持する `fill-rule="nonzero"` で render する。SVG は `data-title-fill-rule-source="embedded-press-nonzero-winding"` を記録するため、これは decoded global paint rule ではなく source-path fill behavior として追跡される。overlapping title strokes は reference に近く塗られ、`〇` の internal counters は維持されるが、whole-page RMS はわずかに悪化した (`46.219 -> 46.438`) ため exact title paint semantics は未解決のままにする。
- [x] 同じ source-backed EmbeddedPress nonzero winding を `success_data-test` title-art の main/shadow face clip paths と shadow face fill にも適用し、nonzero visible fills と evenodd clips の混在を解消する。これは color/coordinate tuning ではない。layer tree は `frontPaintCandidate.renderClipRule:"nonzero"` と `renderClipRuleSource:"embedded-press-nonzero-winding"` を報告し、SVG は title face clips に `data-title-clip-rule-source="embedded-press-nonzero-winding"` を expose する。regenerated `openjtd-samples/pdf-output/ichitaro-20030228030923-success-002-success_data-test.pdf` は PDF 1.4 のまま `/SMask`、`/Group`、transparency、pattern、shading tokens を含まず、Poppler、`sips`、PDFKit、CoreGraphics で render できる。local reference PDF との 144dpi comparison では page 1 whole/title-wide/title-box/title-glyph RMS が `44.849/51.380/59.289/62.507 -> 44.605/49.955/57.492/60.297`、page 2 が `39.293/60.898/64.203/66.788 -> 38.970/59.607/62.670/64.734` に改善した。
- [x] hardcoded black front fill が説明なしに見えないよう、`success_data-test` front-paint candidate を title-art SVG elements に直接 expose する。SVG は `data-title-front-fill-render-color="#111111"` と `data-title-front-fill-render-color-source-backed="false"` を記録し、別途 `JSFart2Contents.paintColorCandidate` 由来の `data-title-front-paint-candidate-color="#ffffff"` を保持する。source paint candidate は whole front face replacement としても front-erase texture ownership としても promote せず、decoded-false evidence として保持する。
- [x] temporary な title-art front-fill color source marker を render-preserving mismatch gate に置き換える。`success_data-test` は `data-title-front-fill-render-color-source="conservative-front-fill-fallback-source-paint-mismatch"`、`data-title-front-fill-source-paint-color-matches-render-color="false"`、`source-paint-color-does-not-match-render-fill` を記録する。observed source paint candidate は `#ffffff` だが current conservative render fill は `#111111` なので、black fill を source-backed と誤表示せず placeholder だけを除去する。
- [x] 同じ title-art front-fill mismatch gate を `getPageLayerTree` にも mirror する。`frontPaintCandidate` は `renderFillColor:"#111111"`、`renderFillColorSource:"conservative-front-fill-fallback-source-paint-mismatch"`、`renderFillColorSourceBacked:false`、`sourcePaintColorMatchesRenderFill:false`、`renderFillColorPromotionBlockedReason:"source-paint-color-does-not-match-render-fill"` を持つようになり、SVG attributes と同じ helper を使うため、layer-tree consumers が source white paint candidate を black face fill と誤解しない。
- [x] `success_data-test` title-art front texture ownership を explicit per-path `0x82.word5` records だけでなく、inherited EmbeddedPress current paint state に基づく selector へ generalizeする。`success_data_test_title_art_front_texture_paths` は main-state/front-owned textures 0 件のまま、source-order interstitial front-erase gate は別経路で 530 texture paths を candidate として扱う。これにより future main-state texture promotion は source state spans に依存し、current sample の interstitial front erase は candidate-only に限定される。
- [x] `success_data-test` の source-order interstitial front-erase texture block を、SVG/PDF transparency ではなく PDFKit-safe な precomposited front-face texture として visible promote する。source gate は main-state/front-owned texture paths が 0 件であることを保持しつつ、`frontEraseTexturePathCount:530`、`candidateBasis:"source-order-interstitial-front-erase-texture"`、`visibleRenderPathCount:530`、`renderPromoted:true` を expose する。SVG は `data-title-texture-rendering="source-opacity-front-erase-precomposited-filled-source-paths"`、`data-title-texture-opacity="0.470"`、`data-title-texture-render-opacity="1.000"`、`data-title-texture-render-fill="#818181"` を mirror する。これは interstitial texture block、`0x82.word5=0x2f`、JSFart/EmbeddedPress white paint candidates に基づく source-backed rendering であり、sample-local color tune ではない。direct opacity probe は `/Group` transparency constructs を生成し PDF safety scanner に reject され、fill-rule-consistent nonzero side-strip probe も active title RMS をわずかに悪化させたため reject した (`92.359 -> 92.594`)。landed precomposited probe は active 144dpi reference comparison を table crop には影響せず改善する: page 1 whole RMS `46.356 -> 45.174`、title-wide `69.779 -> 60.428`、title-glyph `92.359 -> 78.328`、table unchanged `70.459`。regenerated `openjtd-samples/pdf-output/ichitaro-20030228030923-success-002-success_data-test.pdf` は PDF 1.4 のまま `/Group`、`/SMask`、`/ExtGState`、pattern、shading tokens を含まず、Poppler、`sips`、PDFKit、CoreGraphics で nonblank render できる。
- [x] `success_data-test` title-art に render-preserving な `titleTexturePaintPhaseGate` diagnostics を追加し、EmbeddedPress `0x46.word0` を paint-phase candidate として expose する。gate は interstitial texture paths が `textureRecord46Word0Values:["0x00"]`、shadow/main outlines がどちらも `["0x00","0x01"]` であることを記録する。`0x01` は outline-only candidate だが、`0x00` は texture と main outlines にも共有されるため、`renderPromoted:false`、`visibleRenderPathCount:0`、`renderPromotionBlockedReason:"record46-paint-phase-semantics-unproven"` のままにする。
- [x] 同じ title-art paint-phase gate で EmbeddedPress `0x60.word0` と `0x65.word0` が current sample の role separator ではないことを固定する。shadow outlines、interstitial textures、main outlines はどれも両 record family で `["0x10","0x11"]` を持つため、layer tree は `record60SharedAcrossRoles:true`、`record65SharedAcrossRoles:true`、`record60/65SeparatesTextureFromOutlines:false` を expose し、future paint promotion がこの値を誤用しないようにする。
- [x] 同じ `success_data-test` title-art `titleTexturePaintPhaseGate` を explicit promotion-proof gate として強化する。record48 value sets、`record46SeparatesTextureFromOutlines:false`、`record48SeparatesTextureFromMain:false`、`record46PromotionProofReady:false`、shared record46 zero と shared record60/65 state を含む `promotionProofBlockedReasons` を記録する。rendering は変更せず、outline-only `0x46=1` hint を source state role separation の証明なしに paint-phase promotion へ使えないようにする。
- [x] `success_data-test` title-art に render-preserving な EmbeddedPress `0x70` role-separation diagnostic `titleShadowPaintWordGate` を追加する。gate は `0x70.word0`、`word3`、`word7` が shadow outlines と interstitial texture/main outlines を分けることを示す (`0x2c/0x0a/0x1c` vs `0x1c/0x00/0x00`) が、`word1` は全 role で共有 (`0x11`) され、これらの field は interstitial texture と main outlines を分けないことも固定する。current shadow effect candidate は source-backed のまま (`word0=44`, `#8f8f8f`) だが、interstitial texture effect candidate (`word0=28`, `#676767`) は `record70-separates-shadow-but-not-interstitial-texture-from-main` として diagnostic-only に残す。regenerated `success_data-test.pdf` は 144dpi Poppler metrics (`whole RMS 45.068641`, `title_wide RMS 68.497339`, `title_bbox RMS 77.257278`) が変わらず、`sips`/PDFKit で nonblank のまま。
- [x] `success_data-test` title-art に render-preserving な `titlePaintRoleSeparationMatrix` diagnostic を追加し、model regression assertions で固定する。matrix は shadow outlines、interstitial texture block、main outlines の全 EmbeddedPress state record word を比較し、28 candidate record-word columns、18 shared、4 shadow-only separators、0 texture-only separators、1 main-only separator を報告する。texture/main が disjoint な唯一の column は `0x82.word5` で、shadow と texture はともに `0x2f`、main は `0x10` なので、main-vs-shadow-state texture は証明するが interstitial-texture-only paint phase は証明しない。rendering は変更せず `no-record-word-separates-interstitial-texture-from-both-outline-roles` で block し、regenerated `success_data-test.pdf` は PDFKit-safe のまま (`/Group`、`/SMask`、`/ExtGState`、`/Pattern`、`/Shading` は 0、`/Subtype /Form` と `/FormType 1` はともに 2、final `%%EOF` は 1 件)、Poppler page 1 `103585` non-white pixels と Apple `sips` page 1 `35353` non-white pixels で nonblank render できる。
- [x] `success_data-test` title-art に render-preserving な `horizontalPlacementGate` を追加し、JSFart frame の horizontal candidate を output を動かさず expose する。current rendering は `frameRecordContentOffsetAnchor` (`frameX/pathX=76.687`) のまま維持し、rejected source-backed candidate として `candidateFrameX=72.907`、`candidatePathX=80.995`、`candidateBasis:"jsfartFrameOuterEdgePlusFrameRecordContentOrigin"`、`strokeWidthCandidateSourceUnits:100`、`strokeOuterAdjustmentCssPx:3.780` を記録する。split candidate を A/B render すると left frame edge は近づくが reference comparison が悪化した (`whole RMS 45.069 -> 46.288`、`title_stack 56.475 -> 65.388`、`title_face 79.072 -> 91.836`、`title_lower_shadow 84.976 -> 102.382`) ため、`frame-content-split-horizontal-semantics-unproven` として diagnostic-only に残す。
- [x] `success_data-test` title-art に render-preserving な `frontEraseTextureDirectGrayCandidate` を追加する。layer tree と SVG は `0x82.word5=0x2f` を direct gray channel (`#787878`) として解釈する probe を、active precomposited opacity fill (`#818181`) とは別に expose する。fine gray sweep では局所的な微改善にとどまり (`gray 0.485` title_stack RMS `56.421` vs current `0.506` RMS `56.475`; `gray 0.470` whole RMS `45.062` vs current `45.069`)、mid-gray 過多の texture distribution 問題を解決しないため、`direct-gray-channel-probe-not-proven-as-paint-semantics` として diagnostic-only に残す。
- [x] `success_data-test` title-art に render-preserving な `frontEraseTextureSourcePaintCandidate` と `frontEraseTextureSpanCoverageProbe` を追加する。layer tree と SVG は source-backed solid white paint candidate (`#ffffff`) と active PDFKit-safe precomposited fill (`#818181`) を並べて expose し、11 個の explicit texture state span が 530 interstitial texture paths 全体へ inherit する分布 (`[16,36,91,55,16,55,73,22,64,52,50]`) も compact に記録する。solid-white front texture probe は mid-gray を減らすが black title face を消しすぎて visual verdict で reject した (`title_crop RMS 58.808 -> 72.734`, `face_core RMS 81.282 -> 109.804`) ため、rendering は変更せず `span-density-and-clip-semantics-unproven` として block する。
- [x] 最新の visual review に基づき、`success_data-test` の source-order interstitial front-erase texture と `0x70.word0=0x1c` texture-effect fill を visible rendering から demote する。SVG/layer tree はどちらも diagnostic-only として保持し、`frontEraseTextureRoleGate.visibleRenderPathCount:0`、`renderPromoted:false`、`renderPromotionBlockedReason:"front-erase-texture-over-main-face-semantics-unproven"`、`interstitialTextureEffectCandidate.renderPromoted:false`、`record70-separates-shadow-but-not-interstitial-texture-from-main` を報告する。生成 title は gray `#818181`/`#676767` promoted overpaint を使わず、`data-title-texture-render-fill` は conservative front fill `#111111` に戻す。visual verdict は partial (`0.74`) のまま: gray duplicate-shadow は減ったが title が黒く solid に寄りすぎるため、次は front erase の再有効化や任意 gray tuning ではなく source compositing/knockout semantics を decode する。`#8f8f8f`、light gray、texture removed の A/B probes は title metrics または visual balance を悪化させたため reject した。regenerated PDF は Poppler、Apple `sips`、Quick Look thumbnail、PDFKit/CoreGraphics smoke tests で render 済み。
- [x] merge 後に戻っていた title-art texture-effect diagnostic/render inconsistency を修正し、demote 済みの interstitial `0x70.word0=0x1c` candidate が promoted と報告/描画されないようにする。`interstitialTextureEffectCandidate` と `extrusionTextureEffectRenderPromoted` はどちらも `false` と blocker `record70-separates-shadow-but-not-interstitial-texture-from-main` を出し、SVG extrusion texture は candidate metadata を保持しつつ `#676767` ではなく conservative `#111111` で render する。
- [x] dev branch merge 後の current 819-path title front texture candidate を再確認する。全 inherited interstitial/front-erase texture paths を white で front face 上に render すると過度に白くなり、source-opacity gray で全 path を render すると crop RMS は下がるが rejected gray front-face overpaint を再現する。explicit state-tagged texture paths 15 件のみでは sparse すぎる。true front highlights と interstitial/shadow texture inheritance を分離する source field が見つかるまで、front texture candidate は diagnostic-only のままにする。
- [x] `success_data-test` title-art に render-preserving な `frontErasePaintTransitionGate` を追加する。layer tree は source-order bracket (`shadowLastPathIndex:10`、interstitial texture paths `11..540`、`mainFirstPathIndex:541`) と state separator を記録する。`0x48` は shadow と texture/main を分けるが、`0x48` と `0x70.word0` は interstitial texture と main を分けない。texture/main を分けるのは `0x82.word5` だけで、これは shadow state と一致する。SVG も同じ gate を `data-title-front-erase-*` attributes として mirror し、title face を再塗装せず `front-erase-transition-boundary-main-state-not-separated` で promotion を block する。
- [x] `success_data-test` title-art に render-preserving な `frontPaintArbitrationGate` diagnostic を追加し、front-fill selection が単一の fallback mismatch reason に潰れないようにする。gate は conservative `#111111` front fill を維持しつつ、JSFart/EmbeddedPress paint、direct-gray、source-paint、state-span probes 由来の 4 candidates と source-order transition blocker を記録する。SVG も同じ `data-title-front-paint-arbitration-*` attributes を mirror し、rendering は `front-paint-candidate-arbitration-unproven` のまま変更しない。
- [x] `success_data-test` title-art に render-preserving な `extrusionTextureClipGate` diagnostics を追加し、visible interstitial texture clip が暗黙のまま残らないようにする。layer tree と SVG は current promoted texture clip が `source-shadow-outline` であること、`long-shadow-side-sweep` alternative は historical Poppler crop A/B で reject 済みであること、exact front-face knockout/clip semantics は `texture-clip-and-knockout-semantics-unproven` として未解読であることを記録する。rendering と PDF output は変更しない。
- [x] `success_data-test` title-art に source-backed な `titleTextureGeometryRoleGate` diagnostics を追加する。layer tree と SVG は reference PDF を authority にせず、texture、shadow-outline、main-outline、long-shadow side-sweep の source bbox を比較する。texture bbox は front/shadow bbox の両方と `0.903` overlap するが、どちらか一方だけには contain されず、side-sweep bbox には contain される。bbox overlap は geometric relation の証拠に留まり、exact front-face knockout/clip semantics の証明ではないため、rendering は変更せず `texture-source-bbox-relation-is-bbox-only-not-knockout-proof` で block する。
- [x] `success_data-test` title front-fill winding を `frontFillWindingGate` の下で `nonzero` から `evenodd` へ promote する。gate は source contour evidence（main outline paths `11`、multi-contour paths `11`、opposite signed contours を持つ paths `4`）と active Poppler title crop A/B（`nonzero RMS 78.059`、`evenodd RMS 76.034`、improvement `2.025`）を記録する。SVG front-fill paths は `fill-rule="evenodd"` と `data-title-fill-rule-source="embedded-press-evenodd-boundary-contours"` を使う。別の source-backed horizontal pathX candidate は title-tight RMS (`76.034 -> 81.223`) と top crop RMS (`51.191 -> 52.741`) を悪化させたため、現時点では reject する。
- [x] promoted evenodd front fill の下で front-erase texture を再検査し、render-preserving な `frontEraseVisibleProbeGate` diagnostics を追加する。530 件すべての front-erase texture paths を visible にすると raw RMS は改善する（title-tight `76.034 -> 67.651`、top crop `51.191 -> 48.814`）が、見た目は distressed knockout ではなく gray overpaint として reject する。11 件の explicit-state texture paths だけを render する probe はほぼ neutral（`76.034 -> 76.016`）である。visible front-erase は `front-erase-visible-rms-improvement-is-not-knockout-proof` として block し続ける。
- [x] `success_data-test` title-art に render-preserving な `sourcePaintRenderTrace` diagnostics を追加する。layer tree と SVG は raw JSFart paint words（`styleWord1=0x02141030`、`styleWord2=0x02141018`、`paintColorCandidate=0x00ffffff`、`paintFlagCandidate=0x00000001`、`effectWordCandidate=0x0000000a`）を、選択中の conservative render fill `#111111` と並べて expose する。trace conclusion は `source-paint-present-but-render-fill-not-promoted` で、source paint は white だが active fill と一致せず、利用可能な texture path source も `front-erase-texture-over-main-face-semantics-unproven` のまま block される。
- [x] `success_data-test` title-art に render-preserving な `sourceFrameRenderTrace` diagnostics を追加する。この trace は JSFart frame candidate と `/Frame` row が frame ref/object id `1`、outer width `13260`、outer height `1327` で一致することを示しつつ、content dimensions `13031x1054`、selected `jsfartContentLeft` placement、blocked alternate frame-edge placement (`candidateFrameX=72.907`) を保存する。frame/content split はまだ `frame-content-split-horizontal-semantics-unproven` で block されるため、rendering は変更しない。
- [x] `styleWord1/styleWord2/effectWordCandidate` に semantics を割り当てる前に local JSFart2Contents candidates を sweep する。8 local files が `/JSFart2Contents` object-path candidates を持つが、current `MSTUDIO...` magic gate で `jsfartArt` として decode されるのは `success_data-test` の 2 streams のみである。decode された 2 streams は `paintColorCandidate=0x00ffffff`、`paintFlagCandidate=0x00000001`、`effectWordCandidate=0x0000000a` を共有し、`styleWord1/styleWord2` だけが異なる（`0x02141030/0x02141018` と `0x02021030/0x02021018`）。これは useful evidence だが、JSFart variants 全体の render authority として解釈するには不足している。
- [x] non-`MSTUDIO.OCX` variants を含め、すべての `/JSFart2Contents` streams に decoded-false `jsfartStreamProfile` を保存する。profile は prefix family（`mstudio-ocx-utf16le`、`jsfart-object-utf16le`、`zero-prefix` など）、`magicFamilyHex`、UTF-16 preview、header prefix、`structuredArtCandidatePresent` を記録しつつ、`diagnosticOnly:true`、`sourceBacked:true`、`renderable:false`、明示的な promotion blocker を保持する。これにより non-MSTUDIO streams は `jsfartArt` や rendered title art に silently upgrade されず、検索可能な source evidence として残る。`rjtd object-stream-candidates` も corpus sweep 用に同じ profile family/count を report する。
- [x] `success_data-test` title-art の do-not-render chain を regression tests に固定する。model test は `sourcePaintRenderTrace`、`frontEraseVisibleProbeGate`、`titleTextureGeometryRoleGate` が、source white paint、RMS 改善の all-visible texture probe、bbox overlap evidence をすべて diagnostic-only かつ blocked として扱うことを要求する（`source-paint-present-but-render-fill-not-promoted`、`front-erase-visible-rms-improvement-is-not-knockout-proof`、`texture-source-bbox-relation-is-bbox-only-not-knockout-proof`）。これにより future patch が gray overpaint を silently re-enable したり、bbox overlap を front-face knockout authority と誤認したりすることを防ぐ。
- [ ] blocked source-order front-erase texture candidate、main-vs-shadow EmbeddedPress word5 gate、conservative `0x70.word0` shadow fill、source-path evenodd front fill / nonzero clip rule、word-column diagnostics を超えて、`success_data-test` title-art の remaining exact paint semantics を decode する。interstitial `0x70.word0=0x1c` texture-fill promotion は悪化したため reject 済み。active 144dpi crop set では追加 A/B も reject した: shadow/extrusion 側の duplicate interstitial texture replay を skip すると title RMS が `+5.866/+6.270` 悪化し、white `0x82.word3` front paint を full opacity にすると `+13.645/+15.460` 悪化し、hardcoded front fill を `#111111` から `#000000` にすると MAE は少し改善したが title RMS が `+0.751/+0.574` 悪化し visual review でも不採用、texture paths を filled `TextureBezier` ではなく strokes として render すると `+8.924/+7.911` 悪化した。render を変えない layer-tree diagnostic `titleArtPaintStateSequence` は source-order paint-state runs を raw EmbeddedPress record offsets/payload words と inherited spans ごと保持し、`552` paths、`33` explicit transitions、path-kind runs `outline 0..10`、`texture 11..540`、`outline 541..551`、interstitial texture inherited spans `16,36,91,55,16,55,73,22,64,52,50`、path `11` shadow/front-erase `0x82.word5=0x2f` offset `18780`、path `541` main-outline `0x82.word5=0x10` offset `94960` を expose する。full `0x46/0x48/0x60/0x65/0x70` state transitions、exact face/shadow fill colors、texture paint order、front-face halftone semantics はまだ unresolved である。
- [ ] parse 時に decoded `TextLayoutStyle`、`DocumentEditStyles`、`DocumentViewStyles`、または関連 streams から paragraphs と text runs に style IDs を infer/attach する。11 件のテストサンプルの `/DocumentEditStyles` 初期分析では：(1) ヘッダーのワード 2 がサンプル間で異なる（`0x2001` vs `0x2000`）；(2) UTF-16BE の CJK ペア `0x7b2c/第`、`0x6761/条`、`0x9805/項`、`0x56f3/図` が早い位置に出現し、自動番号スタイルテンプレートの可能性；(3) ASCII `{n}` プレースホルダー（`0x007b 0x0034 0x007d` = `{4}`）がCJK 用語と共存；(4) `標準`（U+6A19 U+6E96）はオフセット 524 以降に繰り返し出現し、主要な名前付きスタイルとして確認済み — 前段の CJK セクションは自動番号フォーマット文字列のエンコードの可能性が高い。ストリーム構造とレコード境界は未解読。`/TextLayoutStyle` クロスサンプル分析：`sample-reference` 系は 25 レコードの豊富なスタイルリスト（目次1–3、本文、本文2、小見出し1–5、中見出し、大見出し、個条書き、個条書きの説明、日付･署名、文書ﾀｲﾄﾙ等）；`sample-outline`/`sample-draft` 系は 13 レコード（ラベル付き 2 件: 脚注(標準)/脚注ｴﾘｱ(標準)、匿名 7 件: `payloadPrefix=0003ffff000N` N=1–7、汎用 3 件+JTD autostyle 1 件）；匿名レコードの順番 N 値が JTD 内部スタイル ID に対応する可能性；`sample-table-b` と `sample-table-c` は 10 レコードで完全一致；`sample-table` は record 12 に `本文(ｵｰﾄｽﾀｲﾙ)` を持つ。`sample-draft-b` は TextLayoutStyle が 3 レコード（標準/Default/Normal Table）のみであるにもかかわらず w7 が 0/2/3/4/6/10 を取るため、`w7` は TextLayoutStyle レコードインデックスと直接対応しない。`sample-outline`/`sample-draft` の匿名 `0003ffff000N` レコード（N=1..7）が w7=1..7 に対応する可能性はあるが、w7=8（附則）と w7=10（理由）には対応する匿名レコードが存在しないため完全な対応ではない。TextLayoutStyle レコードインデックスと w7 段落スタイル ID の対応は未証明。
- [ ] 対応する stream mutation format が証明されたら、style edits を decoded JTD style/body streams に保存する。
- [ ] redistributable samples が利用可能になったら fixture-based expected JSON outputs を追加する。
- [ ] inner `DocumentText` structure が理解できたら、decompressed `.jttc` template text/control placeholders を meaningful model blocks に変換する。
- [x] `success_data-test` abc table の column widths を、各 cell text-span 幅ではなく source line-header cell slot 幅から derive する。renderer は各 cell provenance が選ぶ `/DocumentText` line-header と同じ header を使い、その offset から next header offset までを slot として測るため、`columnWidthBasis:"documentTextLineHeaderCellSlotUnits"` と widths `[49.370,56.423,56.423,56.423,56.423]` を expose する。144dpi の rule detection では generated widths が `[73.5,73.5,88.5,88.5,88.5]` から `[74.0,84.5,84.5,85.0,84.5]` になり、reference `[74.0,84.5,84.5,84.5,84.5]` とほぼ一致した。active table/problem crop RMS は `64.786 -> 64.009` に改善し、regenerated PDF は `/Group`、`/SMask`、`/ExtGState`、pattern、shading tokens 0 のまま PDFKit-safe である。
- [x] `success_data-test` abc table の horizontal solver に source-backed `sourceFrameAdmissionGate` を追加し、active render frame と未証明の PageMark raw-field agreement を分離する。renderable frame は `page-body-frame+documentTextLineHeaderUnitTransform` (`x=78.251,width=275.063`) のまま維持し、strongest PageMark raw agreement は `x=171,width=548` を選ぶ。gate は residual `-92.749px/-272.937px`、`pageMarkRawAgreementConflictsWithRenderFrame:true` を記録し、PageMark raw fields は `page-mark-horizontal-field-semantics-unproven` のまま block する。
- [x] `success_data-test` ABC table の cell whitespace placement diagnostics を render-preserving に追加する。144dpi reference/generated crop では grid は近く (generated grid は約 `2.5px` left、vertical は `~0.5px` 以内) だが、cell glyph ink は多くの cell で `6..8.5px` 右へ寄っている。current SVG は `"  １"` のような raw source text を `xml:space="preserve"` のまま center anchor に置くため、`whitespacePlacementProbe` が per-cell leading/trailing whitespace counts、trimmed candidate text、`centeredWhitespaceMayShiftInk` を記録する。SVG も `data-whitespace-placement-probe`、`data-cell-text-centered-with-source-whitespace`、`data-render-text-leading-whitespace-chars`、`data-render-trim-candidate-*` を mirror する。centered-cell trim render probe は ABC crop RMS を悪化させたため reject した (`71.933 -> 72.565`)。rendering は意図的に不変 (`abc_crop_rms_final 71.933`) で、trim candidate は `table-cell-whitespace-position-semantics-unproven` として block する。
- [x] `success_data-test` ABC table の cell text placement を、source-derived font-unit diagnostics に基づいて改善する。PDF-level A/B では `font-weight:400` が active table crop を `126.606 -> 126.171` に改善し、centered-cell baseline を `rowHeight*0.72` から `rowHeight*0.77` へ移すと `125.576` まで改善した。さらに source-derived font-unit stroke width と同じ左 x adjustment (`-1.750px`, basis `documentTextLineHeaderFontUnitPxStrokeCompensation`) を使うと mean glyph-center x residual が `+2.73px` から `+0.18px` に減り、final crop RMS は `125.508` になった。layer tree と SVG は `cellTextXAdjustment`、`cellTextBaselineFactor`、`cellTextFontWeight` と各 basis を expose し、trim candidate は未 promote のまま維持する。
- [x] その ABC table text metric adjustment を、homogeneous line-header font units を持つ source-derived table layout に限定する。現在の PDF-backed local samples 8 件を scan した結果、同じ visible source-derived centered table path を通る 2 例目は見つからなかった。`tsaiten` は table group diagnostics を emit するが visible table text は reference-backed のままで、他の PDF-backed samples は inspected pages で source-derived visible table text を emit しない。font-unit metrics が decode されていない future source-derived table は、ABC で測定した correction を継承せず、従来の centered baseline/weight fallback に留める。
- [x] source-rendered table cell font size を常に global app font size にするのではなく、homogeneous `/DocumentText` line-header `fontSizeUnits` から derive する。ABC table では `12` source units を app baseline に scale して visible size `13.300px` を維持しつつ、layer tree と SVG metadata に `cellTextFontSize` / `cellTextFontSizeBasis:"documentTextLineHeaderFontSizeUnitsScaledToAppFont"` を expose する。異なる homogeneous source font units を持つ future source-derived tables は、ABC の visual font size を silently 継承しない。
- [ ] `success_data-test` abc table unit range `519..924` の remaining physical table geometry を decode する。active projection は source-derived `lineMarkPageGrid` evidence から出ており、visible column widths は source line-header cell slot widths に従い、source-derived cell font size は homogeneous line-header `fontSizeUnits` に gate されている。remaining proof は reference PDF を render authority にせず、cross-sample horizontal origin/total-width semantics と、trailing line-header candidates (`76..88`, `92..174`) + source-backed top-text anchors が visible table placement をどう決めるかに集中する。
- [x] direct `/DocumentText` control-run table cells の rendered text では raw source whitespace を保持し、normalized model text は変えない。`success_data-test` page 1 の `segment.text()` は `ａ`/`１`/`０.８` のまま、`getPageLayerTree` は `renderText`、`renderTextBasis:"documentTextSourceRangePreservedWhitespace"`、`preservesSourceWhitespace` を expose し、SVG/PDF は `xml:space="preserve"` と source range attributes を mirror する。regenerated `openjtd-samples/pdf-output/ichitaro-20030228030923-success-002-success_data-test.pdf` は Poppler と macOS Quartz/PDFKit/CoreGraphics で nonblank。active 144dpi crop では table RMS がほぼ横ばいから微悪化 (`wide-actual-table` `91.864 -> 92.202`) したため、remaining mismatch は text-loss ではなく table line weight、font metrics、line-header slot semantics の未解読が支配的である。
- [x] Chrome では表示されるが Preview.app では blank に見える `success_data-test` PDF を再確認する。current on-disk `openjtd-samples/pdf-output/ichitaro-20030228030923-success-002-success_data-test.pdf` は 2 pages のまま、`com.apple.quarantine` xattr はなく、real `%%EOF` は 1 件だけで、`/Group`、`/S /Transparency`、`/SMask`、`/ExtGState`、`/Pattern`、`/Shading` tokens も含まない。同じ artifact は Poppler (`107617` page-1 non-white pixels at current debug scale)、Apple `sips` (`516x728`, `33056` non-white pixels)、Swift PDFKit (`page 1:33056`, `page 2:30672`)、CoreGraphics (`page 1:32036`, `page 2:27710`)、QuickLook thumbnailing (`725x1024`, `54897` non-white pixels) で nonblank render できる。fresh regenerated temp PDF だけでなく committed `openjtd-samples/pdf-output` artifact 自体を PDFKit で開く `local_pdf_output_artifacts_render_visible_content_with_macos_pdfkit_when_available` を追加した。PDFKit/CoreGraphics/QuickLook が通るため、newly named regenerated file でも再現しない限り direct Preview.app blankness は app cache/UI-path issue として扱い、local Preview-family gate は artifact-level PDFKit + `sips`/CoreGraphics を使う。
- [x] PDF export 中に embedded CMap の `%%EOF` comments を scrub し、Preview 系 reader が見る file EOF marker を trailer の 1 件だけにする。`svgs_to_pdf` は internal `%%EndResource\n%%EOF\nendstream` markers を同じ長さの `%%EOD` comments に書き換え、final `%%EOF` は保持する。tests は exported PDFs の `%%EOF` が 1 件だけであることを固定する。regenerated `success_data-test.pdf` は 2 pages、`eof_count:1`、`eod_count:6`、`/Group`/`/S /Transparency`/`/SMask`/`/ExtGState`/`/Pattern`/`/Shading` tokens 0、Apple `sips` `516x728` / `34017` non-white pixels、PDFKit page 1 `33056` non-white pixels で nonblank。
- [x] PDF wrapper が書く SVG Form XObject すべてに explicit `/FormType 1` を付与し、local PDF artifacts も再生成する。regenerated `openjtd-samples/pdf-output` は local `.jtd`/`.jtt`/`.jttc` 61 source すべてに same-stem PDF を持ち、各 artifact の `/Subtype /Form` count と `/FormType 1` count が一致し、final `%%EOF` は 1 件だけである。代表 artifacts (`a5`、`a6`、`b6`、`tsaiten`、`success_data-test`、`shanai_lan`、`shinsyo`) は `/Group` と `/SMask` token hits 0 のまま。`rjtd-export` には conditional on-disk artifact sweep を追加し、この Preview/PDFKit compatibility field が committed sample output から欠落した場合に検出できるようにした。
- [x] `success_data-test` ABC table の reference-layout fallback を visible render path から外し、reference bbox diagnostics は保持する。current table は source page-space solver に promote された `source_derived_table_grid_overlay_layout` で引き続き render される (`data-reference-backed="false"`、`data-source-derived-layout-candidate="true"`) が、`success_data_test_table_grid_overlay_layout` 単独では visible table を出せない。併せて `success_data-test` top-text static slots も `/DocumentText+/LineMark+/PageMark` placement がない場合は `referenceFallback` として render しない source-grid gate を通す。current SVG は `data-source-grid-render-required="true"`、`data-source-grid-rendered-slot-count="21"`、`data-unbacked-slot-count="0"`、`data-reference-fallback-rendered-count="0"` を report する。regenerated output は active 144dpi table crop で視覚的に変化しない (`RMS 64.009`、changed ratio `0.159629`) まま、Poppler と Apple `sips` で nonblank render できる。
- [x] `success_data-test` formula frame の vertical placement fallback を、対応する top text の `/DocumentText+/LineMark+/PageMark` placement から得た source-grid anchor に置き換える。SVG は 4 件すべての JSEQ formula projection で `data-frame-y-basis="topTextSourceGrid"` を報告し、`lineAnchorFallback` は 0 件になった。さらに cone diagram の visible projection は `/FigureData/main_data/FDMText` の nearby FDMText bbox corroboration を gate にし、local sample は FDMVector cone span に一致する text bbox 3 件が minimum 2-source threshold を超える場合にだけ render される。regenerated output は PDFKit-safe のまま (`/Group`、`/SMask`、`/ExtGState`、`/Pattern`、`/Shading` はすべて 0)、Poppler `103585` non-white pixels、Apple `sips` `35353` non-white pixels で nonblank render し、previous Poppler render との差分は `RMS 1.233502` / changed ratio `0.000668` に収まる。
- [x] hardcoded `answerSheetReferenceFrame` を置き換える前段階として、`success_data-test` answer sheet に diagnostic-only の `sourceFrameCandidate` を追加する。sparse table の section anchors `５`/`６` と `/LineMark+/PageMark` row tops から y-scale/top candidate を出し、records `59`/`70` が local sheet y `205..377pt` を source y `471..702px` に対応させることを記録する。これにより `sourcePxPerSheetPtY:1.343023`、derived frame top `195.680px`、height `613.762px` が得られ、current reference frame top `190.674px` / height `609.358px` に対する residual を expose する。SVG には `data-source-frame-*` attributes を mirror するが、rendering は reference frame のままで、`answer-sheet-x-width-and-local-schema-source-fields-undecoded` として block する。
- [x] 同じ `success_data-test` answer-sheet `sourceFrameCandidate` に source-backed な `pageMarkDisambiguationGate` を追加する。layer tree と SVG は section `５`/`６` anchors が同じ `/PageMark` entry と page-index candidate を共有していることを明示し (`disambiguationReady:true`、`disambiguationClass:"same-page-mark-entry-and-page-index-candidate"`)、rendering は reference frame のまま維持しつつ remaining blocker `answer-sheet-x-width-and-local-schema-source-fields-undecoded` を保持する。
- [x] `success_data-test` answer sheet の local rule segment schema を SVG loop 内の hidden literal から取り出し、layer-tree/SVG diagnostics の `localRuleSchemaCandidate` として expose する。source-backed な sparse table facts (`39` rows、max `11` columns、`6` section anchors と source ranges) と、まだ reference-backed な local rule positions (`19` segments、horizontal `13`、vertical `6`、x `[0,27,168,237,307,445]`、y `[0,31,63,79,110,142,173,205,236,268,377,409,457]`) を分離して記録する。`xSchemaSourceBacked:false`、`yScaleSourceBacked:true`、`ruleStyleSourceBacked:false` を明示し、render promotion は `answer-sheet-local-rule-schema-source-fields-undecoded` で block する。
- [x] `tsaiten` cross-table row-boundary offset diagnostics に source-backed PageMark line-domain evidence を追加する。combined previous-row-span `/LineMark` records `[7,9,11,13,21,23,25,27,32,34,36]` はすべて `/PageMark` entry `0` (`pageLineStart:0`, `pageLineEnd:42`) に入り、page start からの offsets も monotonic である。各 related compact table も per-table `pageMarkLineOffsetsFromEntryStart` を report する。これは row-span record family の page-domain ordering を証明するが、stable `-82` boundary offset はまだ page-y origin を decode しないため render promotion は block のままにする。
- [x] 同じ `tsaiten` cross-table row-boundary offset evidence に、diagnostic-only の PageMark u16 preview と page-line-gap y projection を追加する。shared PageMark entry は `pageMarkU16FieldCount:137` と `[0,0,1,0,0,0,0,42,...]` で始まる 24-word preview を expose し、body-line-gap projection は pitch `23.310px` と combined y tops `[235.167,281.786,328.405,375.024,561.500,608.119,654.738,701.357,817.905,864.524,911.143]` を報告する。この projection は table y-origin を decode しないため、`page-line-gap-projection-does-not-decode-table-y-origin` として non-rendering のまま残す。
- [x] `tsaiten` に cross-table source-unit-to-PageMark-line affine fit diagnostic を追加する。4 つの compact table candidates 全体で `rowSourceStartUnits` `[304,476,654,832,1135,1307,1467,1627,1857,2026,2170]` は previous-row-span `/LineMark` record indexes に slope `0.016`、intercept `1.220` で近似できるが、residual は最大 `1.706` record indexes に達する。そのため single source-unit-to-page-line transform は exact ではなく、`source-unit-to-page-line-affine-fit-not-exact` として block のままにする。
- [x] 同じ `sourceUnitToPageLineIndexFit` diagnostic に row-level witness を追加し、外部 script なしで non-exact residual pattern を見えるようにする。現在の最大 miss は table candidate `0` row `3` (`source 832`, actual record `13`, fitted `14.706`, residual `-1.706`) と table candidate `1` row `0` (`source 1135`, actual record `21`, fitted `19.618`, residual `1.382`) で、table candidate `3` row `2` は比較的近い (`source 2170`, actual `36`, fitted `36.394`, residual `-0.394`)。これにより次の y-origin work は single global source-unit affine transform ではなく、segment/table-family transition の説明に向ける。
- [x] table page-space diagnostics に source-only の reference-calibration replacement gate を追加し、`tsaiten` が reference table bbox を置き換えられる axis と残 blocker を明示するようにする。scoring table は source-derived layout candidate がないため replacement gates がすべて false、lower error table は internal column/row evidence (`sourceColumnSplitReady:true`, `rowHeightSolverReady:true`) は ready だが、full page-space replacement は `pageSpaceHorizontalTransformReady:false`、`yOriginSolverReady:false` のままで、`table-horizontal-page-space-transform-incomplete` と `source-page-y-transform-not-decoded` により block される。regression coverage は lower table の source-only horizontal unit transform (`matched-cells`、`0..100` units、slot widths `[20,80]`) も固定しつつ、`tsaitenReferenceProjection` rendering は変えない。
- [x] `table-page-space-v1` の下に source-only `pageSpaceHorizontalTransformGate` を追加し、internal column readiness と decoded page-space x/width placement を混同しないようにする。`tsaiten` scoring table は source layout candidate absent として報告され、lower table は `sourceColumnSplitReady:true`、`xUnitAllRowsAgree:true`、`fullExtentUnitsPresent:true`、`xUnitRange:0..100`、`fullExtentUnits:144` を報告する一方、`sourceFrameDecoded:false`、`page-space-horizontal-frame-not-decoded`、`line-mark-record-stride-to-page-y-transform-unproven` で block される。これにより lower table の useful な line-header unit evidence は保持しつつ、hardcoded/reference bbox は promote しない。
- [x] 同じ horizontal gate に diagnostic-only `sourceFrameHypotheses` を追加し、reference PDF を admission rule にせず candidate page frame を比較できるようにする。`tsaiten` lower table では page-body frame hypothesis が `selectedX:72.000` / `selectedWidth:451.181`、media-box hypothesis が `selectedX:0.000` / `selectedWidth:551.181` を出すが、current reference bbox `x=174,width=554` はまだ説明できないため、それぞれ `page-body-frame-not-proven-for-table` と `page-media-box-not-proven-for-table` のまま block する。これにより次の作業は `tsaiten_table_grid_overlay_layout` を外す前に missing source-backed horizontal frame/inset を見つけることに絞られる。
- [x] stride-only の source layout でも `originResidualEvidence` に `PageMark` selected u16 fields を引き継ぎ、`pageMarkSelectedFields:null` で終わらないようにする。`tsaiten` lower table は reference bbox の横で `word14=194` と `word21=564` を expose し、diagnostic-only `pageMarkRawFieldReferenceComparison` として `word14-referenceX=20px` が first column slot (`20` units) 1 個、`word21-referenceWidth=9.999px` がその half-slot に一致することを示す。これはまだ reference-backed であり `page-mark-raw-horizontal-field-semantics-unproven` として block するが、次の source-backed x/width decoder は opaque sample constants ではなく `PageMark` raw horizontal fields と line-header slot semantics に絞られる。
- [x] source-only の `PageMark` raw-field hypotheses を `pageSpaceHorizontalTransformGate.sourceFrameHypotheses` に追加する。`tsaiten` lower table では `page-mark-word14-word21-direct` が `selectedX:194.000` / `selectedWidth:564.000`、line-header first-slot adjusted candidate が `word14 - firstColumnSlotUnits` と `word21 - firstColumnSlotUnits/2` から `selectedX:174.000` / `selectedWidth:554.000` を出す。この candidate はまだ promote せず、`page-mark-raw-horizontal-field-semantics-unproven` として block する。reference calibration を置き換える前に、同じ式を scoring table または別 sample で validate する必要がある。
- [x] `tsaiten` の horizontal PageMark field probe を visible table family 全体へ generalize し、rendering には promote しないまま保持する。`sourceDerivedHorizontalFieldAdjustmentProbe` は selected PageMark fields `10/13/14/15/16/17/18/19/20/21` を各 table reference bbox の x/width/right と比較する。scoring table は source layout candidate が absent でも `word15=423` が reference width `421` に最も近い direct width field (`2px` residual) として expose され、lower table は line-header slot adjusted の `word14 -> x` と `word21 -> width` matches (`0.000px` / `0.001px` residual) を維持する。これにより horizontal decoder は fixed `word14/word21` rule ではなく、複数 PageMark fields と line-header slot semantics を合わせて解く必要がある。
- [x] 同じ `tsaiten` table family に source-only の cross-table horizontal field consensus diagnostic を追加する。`sourceOnlyHorizontalFieldConsensus` は sparse sibling の source-derived layouts `[1,2,3]` から stable unit frame (`firstColumnSlotUnits:20`, `firstMatchedCellSpanUnits:16`, `firstIntercellGapUnits:4`, `xUnitRange:0..100`, `fullExtentUnits:144`) を証明し、reference bbox を見ずに shared PageMark fields へ適用する。scoring table では source-only frame hypotheses として `word14 - firstSlot => x=174`、`word15 - halfGap => width=421`、`word21 - halfSlot => width=554` を出し、lower table では同じ consensus が `word21 - halfSlot` width candidate を維持する。rendering は `cross-table-horizontal-field-semantics-unproven` のまま block されるが、remaining decoder work は opaque page coordinates の探索ではなく table family ごとの width-field semantics 選択に絞られた。
- [x] `tsaiten` horizontal selector に source-only `sourceWidthFieldRoleGate` を追加し、現在の width-field split が chosen frame basis の中に隠れないようにする。scoring table は 3-column selector として `word15 - halfGap` (`selectedWidthWordIndex:15`, role `compact-three-column-visible-width`) を報告し、lower 2-column table は `word21 - halfSlot` (`selectedWidthWordIndex:21`, role `compact-two-column-visible-width`) を報告する。この gate は diagnostic-only のままで、`width-field-role-semantics-needs-cross-sample-validation` として block し、column-count/field-role rule がこの table family 外で検証されるまで reference fallback は維持する。
- [x] 同じ source-only cross-table horizontal consensus を `pageSpaceHorizontalTransformGate.sourceFrameHypotheses` にも mirror する。これにより scoring table は own source-derived layout が absent でも actual horizontal promotion gate 内に `x=174,width=421` candidates を expose する。lower table は既存の body/media/PageMark raw hypotheses を保持したうえで同じ consensus candidates を append し、すべて `cross-table-horizontal-field-semantics-unproven` として block したままにする。
- [x] `tsaiten` horizontal transform gate の下に `sourceFrameCandidateAgreementGate` を追加し、candidate agreement と render admission を分離する。scoring table は source-only x/width agreement が unique ではないことを明示する (`3` candidates、`3` groups、best support `1`)。一方 lower table は `page-mark-word14-word21-first-slot-adjusted` と `page-mark-word14-first-slot-word21-half-slot` の両方が `x=174,width=554` に合意することを報告する。gate は `selectionReady:false` のまま、`source-horizontal-field-semantics-unproven` で block するため、reference layout を黙って置き換えずに decoder を絞れる。
- [x] 同じ consensus の横に diagnostic-only の `sourceOnlyHorizontalFieldSelector` を追加する。入力は PageMark fields、sparse-sibling source unit frames、compact table topology だけに限定し、scoring table は 3-column family として `page-mark-word14-first-slot-word15-half-gap` (`x=174,width=421`) を、lower table は 2-column family として `page-mark-word14-first-slot-word21-half-slot` (`x=174,width=554`) を選ぶ。selector は `referenceBBoxUsedForSelection:false`、`selectionReady:false` を記録し、`cross-table-horizontal-field-semantics-unproven` として block したままにする。
- [x] topology selector を isolated JSON のままにせず、`sourceFrameCandidateAgreementGate` の source-only support に戻す。scoring table は consensus candidate と topology selector により `x=174,width=421` が unique best horizontal agreement になり、lower table は raw PageMark hypothesis、consensus candidate、topology selector により `x=174,width=554` が 3-support になる。`selectionReady:false` は維持し、rendering は unresolved horizontal semantics と separate page-y transform gate で block したままにする。
- [x] transition-based y-origin promotion の前に、`tsaiten` page-y gates へ source-only の `sourceGapToPageLineGapReadinessHints` を追加する。同じ `/PageMark` transition evidence を `sourceOnlyPageYOriginHypothesis` と `transitionSemanticsReadiness` の両方で summary 化し、3 transitions はすべて同じ PageMark entry にある一方で direct source-range gaps は page-line gaps から最大 `182` units、row-source-start gaps は `295`、最も良い segment-offset candidate でも `105` ずれ、ratio spread は `29.875` であることを記録する。scoring-table hypothesis は `source-gap-to-page-line-gap-transform-not-stable` を explicit blocker として持つが、rendering は reference calibration のままにする。
- [x] `pageMarkScopedYTransformProbe` の下に `absoluteVsSpanLineageGate` を追加し、reference-backed な absolute table-top probes と source-backed な subrecord span probes を 1 箇所で相関できるようにする。ただし、どちらも decoded page-y origin としては promote しない。`tsaiten` scoring table は reference top `301.005` と selected post-row-gap spans `[65,65,65,67]`、previous/compact spans `[107,113,113,113]` を記録し、lower table は reference top `768.014` と selected spans `[58,52,54]`、previous/compact spans `[111,92,91]` を記録する。どちらも `sourceOnlyAbsoluteTopDecoded:false` のまま、`absolute-top-evidence-reference-backed-span-evidence-non-positional` で promotion を block する。
- [x] `sourceOnlyPageYOriginHypothesis` に `yOriginReadinessClass` を追加し、source-only y candidates を page-space promotion 前に分離する。`tsaiten` scoring table は `cross-table-line-domain-only` として分類され、line-domain-to-page-space transform、non-exact piecewise fit、source/subrecord order regression、decoded page-y origin missing を blocker として保持する。lower table は `stride-only` として分類され、`line-mark-page-origin-stride-present`、`stride-origin-needs-direct-line-origin-rule`、`direct-line-mark-page-origin-absent`、`decoded-page-y-origin-missing` を明示する。どちらも `originDecisionReady:false` のままにする。
- [x] `sourceOnlyPageYOriginCandidateAgreementGate` に table-family coverage diagnostics を追加し、source-only y-origin agreement が実際より強く見えないようにする。`tsaiten` scoring table の current best group は unique だが related table candidate 4 件のうち table candidate `[0]` だけを cover する (`bestSupportedTableCandidateCount:1`、`bestSupportedCoversMultipleTableCandidates:false`、`crossTablePreviousRowSpanBestGroupCoversMultipleTables:false`、`crossTablePreviousRowSpanBestGroupTableCoverageRatio:0.250`)。gate は `crossTablePreviousRowSpanTableCandidateIndexes:[0,1,2,3]` と `crossTablePreviousRowSpanSupportFragmentedByTable:true` を expose し、reference calibration replacement の前に `source-page-y-origin-best-support-not-cross-table` と `cross-table-previous-row-span-support-fragmented-by-table` で block する。
- [x] 同じ `tsaiten` y agreement gate に diagnostic-only の `sourceOnlyPageYOriginSelector` を追加する。selector は reference bbox coordinates ではなく `tableCandidateIndex` と origin-basis evidence から best-supported source-only y group を deterministic に選ぶ。scoring table candidate `0` は `cross-table-combined-previous-row-span-first-record` を `selectedY:235.087`、`selectedRowHeight:23.298` として選ぶが、support は table `[0]` のみなので `selectionReady:false` のまま `cross-table-previous-row-span-support-fragmented-by-table` で block する。near-reference coincidence を promote せず、将来の renderer admission rule を明示する。
- [x] その y selector に `tsaiten` lower-table candidate 用の single-support diagnostic fallback を追加する。multi-support agreement がない場合でも `null` にせず、最も強い source-only hypothesis を記録する。lower table candidate `3` は PageMark absolute slot candidate `page-mark-absolute-y-slot-field2-tail-block16-word11` を `selectedY:768.000` として選ぶが、`singleSupportFallback:true`、`selectionReady:false` のまま、`page-mark-absolute-y-slot-semantics-unproven` と `single-source-y-origin-support-unproven` で non-rendering に保つ。
- [x] PageMark absolute-slot y fallback を candidate-local に保ち、`fallbackTextAnchors` diagnostic tables へ漏れないようにする。`tsaiten` の中間 2-row candidates はそれぞれ自身の raw `/LineMark` stride y (`534.000`、`618.000`) に fallback し、PageMark absolute slot `768.000` を選択するのは `lineMarkPageGridStrideRawRecordIndex` authority を持つ lower table だけになった。
- [x] global affine fit の横に diagnostic-only の `sourceUnitToPageLineIndexPiecewiseFit` evidence を追加する。同じ previous-row-span `/LineMark` records を related compact table ごとに group 化し、`rowSourceStartUnits -> lineMarkRecordIndexes` を per-table に fit して、piece residuals と transitions を rendering 変更なしで保持する。global fit は non-exact のままだが、per-table residual は table `0` max `0.027`、table `3` max `0.106` まで縮み、4 related tables 間の transition は record gaps `8`、`2`、`5` を expose する。これにより y-origin decoder は single global source-unit affine transform ではなく table/segment transition semantics を解く必要があることを固定し、`piecewise-fit-does-not-decode-page-y-origin` として block したままにする。
- [x] 同じ fit の横に source-only の `piecewiseRecordFamilyGapYDiagnostic` を追加し、selected-spacing と previous-row-span の `/LineMark` families を reference PDF なしで比較できるようにする。`tsaiten` related table family 全体で selected spacing records は previous row-span records の常に 1 record 後 (`stableSelectedMinusPreviousRecordIndexGap:1`) で、PageMark 1 line-pitch 分の y delta (`23.310px`) を持つ。table 間 transition record gaps も両 family で一致する (`8`、`2`、`5`、delta `0`)。これにより selected records は post-row gaps、previous records は compact row spans を encode することを固定できたが、absolute page-y origin はまだ decode していないため `piecewise-family-gap-y-comparison-blocks-page-y-origin` として render promotion を block する。
- [x] `tsaiten` cross-table row-boundary diagnostics の横に source-only の `sourceOnlyPageMarkSlotScopedSubrecordYSequenceProbe` を追加する。既存の PageMark raw u16 subrecord slot grouping (`fieldIndex+tailBlock16WordIndex`) を再利用しつつ、reference table bbox ではなく source-derived PageMark line-domain y projection と比較する。`referenceBBoxUsed:false`、`selectionReady:false` を記録し、combined previous-row-span `/LineMark` sequence `[7,9,11,13,21,23,25,27,32,34,36]` を保持する。これにより次の y-origin decoder は source-backed slot/line-domain evidence に絞られるが、absolute page-y origin はまだ decode していないため `page-mark-source-y-slot-candidates-do-not-decode-page-y-origin` として render promotion を block する。
- [x] `sourcePageYTransformGate` 内に `sourceOnlyPageYOriginHypothesis` を追加し、promotion 前の source-only y-origin evidence を summary 化する。`tsaiten` scoring table は shared PageMark line-domain / cross-table row-boundary evidence から `cross-table-page-line-domain` hypothesis を報告し、lower error table は stride row tops から `line-mark-page-origin-stride` hypothesis を報告する。どちらも `referenceBBoxUsed:false`、`selectionReady:false` のまま、`source-page-y-origin-inference-pending` として block するため、reference coordinates を hardcode せずに残り promotion blockers を inspect できる。
- [x] その hypothesis の横に diagnostic-only の `sourceOnlyPageYOriginCandidateAgreementGate` を追加し、render promotion 前に source-derived `selectedY` と row height で y-origin candidates を group 化する。`tsaiten` scoring table は cross-table combined previous-row-span projection と table-0 previous-row-span y top が `selectedY:235.167` / `rowHeight:23.310` で一致することを報告し、lower table は stride raw-record-index と stride-collapsed の first-row candidates に加えて、blocked PageMark absolute-y slot support `selectedY:768.000` を別候補として保持する。gate は `referenceBBoxUsed:false`、`selectionReady:false` のまま、`source-page-y-origin-agreement-unproven` として block する。
- [x] cross-table previous-row-span y support と selector readiness を分離し、agreement が origin decoder と誤読されないようにする。scoring table は `crossTablePreviousRowSpanSelectorPresent:true` と support count `5` を記録するが、previous-row-span subrecord coverage incomplete、cross-table ordering regression、offset transform requirement、non-exact piecewise fit、decoded page-y origin absent により `crossTablePreviousRowSpanSelectionReady:false` のままにする。agreement gate も `crossTablePreviousRowSpanUniqueBestSupported:true` だが `crossTablePreviousRowSpanReady:false` とし、blocker reasons として `cross-table-row-boundary-offset-transform-required` と `page-line-gap-projection-does-not-decode-table-y-origin` を expose する。
- [x] `sourceOnlyPageYOriginDomainGate` を追加し、同じ `235.167` の agreement が page-space table origin ではなく PageMark/LineMark line-domain evidence であることを明示する。scoring table は `crossTableLineDomainRecordCount:11`、`piecewiseTransitionRecordGaps:[8,2,5]`、stable selected-minus-previous gap `1` / `23.310px`、`pageSpaceOriginDecoded:false` を記録し、`line-domain-to-page-space-origin-transform-required`、`table-family-transition-semantics-undecoded`、`source-page-y-line-domain-not-page-space-origin` で block する。
- [x] `table_grid_overlay_layout` で `tsaiten` reference constants より source-derived table fallback geometry を優先する。未証明 rendering は promote しないが、diagnostic bbox generation の reference-first fallback path は 제거した。source-derived layout がある場合は source bbox/row height/column width を使い、source evidence が 없을 때だけ `tsaiten_table_grid_overlay_layout` に fallback する。`success_data-test` の unproven-source regression でこの precedence を固定し、visible reference projection gates は変更しない。
- [x] `tsaiten` table page-space solver に `sourceOnlyAxisAdmissionGate` を追加し、source-only x/width agreement と source-only y-origin selection を reference calibration removal の前に一緒に評価する。scoring table は `x=174,width=421` の unique horizontal agreement を保持しつつ、y selector が table candidate `[0]` のみへ fragmented していることを記録する。lower table は `x=174,width=554` の unique horizontal agreement を保持しつつ、y selector が `selectedY=768` の single-support PageMark absolute-slot fallback であることを記録する。両 axis は non-rendering のままで、`source-page-space-axis-selector-coupling-unproven` として block する。
- [x] selected source-only y candidate が render admissibility と混同されないよう、`tsaiten` `sourceOnlyAxisAdmissionGate` を強化する。gate は `ySelectorSupportCount`、`ySelectorCrossTableSupportPresent`、`ySelectorAgreementAdmissible`、`ySelectorAdmissionBlockedReason` を emit する。scoring table は `source-y-origin-selector-fragmented-by-table-not-render-admissible`、lower table の PageMark absolute-slot fallback は `source-y-origin-selector-single-support-fallback-not-render-admissible` として明示的に block される。
- [x] `tsaiten` の line-domain plus post-row-gap projection probe 内に `sourceOnlyProjectionDomainGate` を追加する。parent probe は diagnostic として reference PDF と比較し続けるが、nested gate は source-backed only (`referenceBacked:false`) とし、実際の blockers として line-domain/subrecord-gap units の混在、selected records が post-row-gap records であること、不完全または ordered-unique でない span coverage、missing page-y transform を記録する。これにより scoring table の near-reference projection (`235.087 + 65 -> 300.087`) を render authority と誤読しない。
- [x] `sourceOnlyPageMarkAbsoluteYSlotGate` を generic hardcoded blocker ではなく source disagreement を報告する形にする。gate は line-domain plus post-row-gap projection に対して最良の PageMark absolute-y slot を計算し、source candidates が一致する場合だけ `page-mark-absolute-y-slot-semantics-unproven` を解除する。current `tsaiten` candidates は lower table で `875.539` projected vs `768.000` absolute slot と一致しないため、rendering はより具体的な `line-domain-projection-disagrees-with-page-mark-absolute-y-slot` で block される。
- [x] その PageMark absolute-slot disagreement を dedicated gate だけでなく source-only y selector supports にも伝搬する。lower `tsaiten` table は引き続き `selectedY:768.000` を最も強い single-support diagnostic として記録するが、`sourceOnlyPageYOriginSelector` と agreement group は `page-mark-absolute-y-slot-semantics-unproven` の横に `line-domain-projection-disagrees-with-page-mark-absolute-y-slot` も含める。これにより raw `768` source value が line-domain agreement path を満たさないまま後で promote されることを防ぐ。
- [x] source-gap-to-page-line transform readiness を `sourceOnlyPageYOriginDomainGate` と `sourceOnlyPageYRenderAdmissionGate` の両方に mirror する。new `sourceGapToPageLineGapTransformAdmissionGate` は source-backed only (`referenceBacked:false`) で、`transformDomain:"source-unit-gap-to-page-mark-line-index-gap"`、`canDecodeSourceTransform:false`、`tableFamilyTransformStable:false`、best candidate `segment-offset-gap`、max delta `105`、blockers `source-gap-to-page-line-gap-transform-not-stable`、`source-gap-to-page-line-gap-transform-unstable-across-table-family`、`source-gap-to-page-line-gap-transform-undecoded` を報告する。これにより render admission path は reference table bbox ではなく同じ transition evidence に接続される。
- [x] 同じ y-support blockers と source-gap transform gate を `sourceOnlyAxisAdmissionGate` にも mirror し、combined x/y selector が依存先の y path よりきれいに見えないようにする。`tsaiten` scoring table の axis gate は fragmented cross-table blockers と transform gate を持ち、lower table は PageMark absolute-slot disagreement、residual `107.539`、selected raw slot `768.000`、source-gap blockers を持つ。Axis coupling は引き続き `source-page-space-axis-selector-coupling-unproven` で block される。
- [x] `sourceGapToPageLineGapReadinessHints` と `sourceGapToPageLineGapTransformAdmissionGate` に candidate-level transform taxonomy を追加する。gates は candidate count、exact-match count、best-candidate transition coverage、best-candidate spread、lowest-spread candidate、full candidate summaries、declined transform candidates を expose する。current `tsaiten` evidence は best max-delta candidate が `segment-offset-gap` (`105`) だが lowest-spread candidate は `direct-source-range-gap` (`12.250`) であることを示すため、単一 metric だけでは transform を promote できない。
- [x] `sourceOnlyAxisAdmissionGate` 配下に diagnostic-only `sourceOnlyAxisCandidateBBox` を追加する。best source-only horizontal selector と selected source-only y candidate を reference bbox なしで結合する。scoring table は `x=174,y=235.087,width=421,height=93.192`、lower table は `x=174,y=768,width=554,height=63` を emit する。どちらも `referenceBacked:false`、`selectionReady:false` のまま `source-page-space-axis-selector-coupling-unproven` で block され、`tsaiten` reference calibration を置き換える前の concrete comparison target として使う。
- [x] `sourcePageYTransformGate` 配下に `sourceOnlyPageYRenderAdmissionGate` を追加し、reference fallback removal のための source-only y admission contract を 1 か所に集約する。この gate は既に source-rendered な ABC table では `lineMarkPageGrid` origin、exact `/LineMark` rows、decoded y solver により `admissionReady:true` を emit し、selector fallback diagnostics は non-authoritative のまま保持する。`tsaiten` では reference coordinates を使わずに rendering を block する。scoring table は direct origin missing、page-space origin ではない cross-table line-domain evidence、fragmented y selector support、source/subrecord ordering contradiction、PageMark absolute-slot disagreement で block され、lower table は stride-only origin、non-exact line rows、single-support y fallback、non-unique selected post-row-gap coverage、同じ PageMark disagreement で block される。
- [x] local PAGE01 right/down probe family を diagnostic-only admission evidence として固定する。right-tick files は `/DocumentText` line-header の X delta が線形 (`firstCellOffsetDelta=2/4/6/8`) であることを示すが、horizontal selector と x/y coupling は render-admissible ではないため `sourceOnlyAxisAdmissionGate.admissionReady:false` のままにする。down files は `/LineMark` stride correlation を expose するが、editor は following text を押し下げずに table-only Y movement を独立させられないため、layer tree は `sourceOnlyPageYAdmissionClass:"flow-y-stride-only-diagnostic"`、`pageOriginAuthority:"fallbackTextAnchors"`、`sourceOnlyPageYRenderAdmissionGate.admissionReady:false` を保持する。したがってこれらの samples は blocker shape を固定するだけで、source-only render promotion の根拠にはしない。
- [x] table layer JSON と SVG diagnostics に `referenceFallbackAdmissionGate` を expose し、legacy reference-table fallback を同じ source-only admission contract から audit できるようにする。gate は既存の visible-render boolean を再利用する。`success_data-test` ABC table は source-derived layout で render される一方、`referenceFallbackAllowed:false`、`referenceFallbackUsed:false`、`sourceOnlyPageYAdmissionReady:true`、`blockedReason:"active-source-layout-admission-suppresses-reference-fallback"` を報告する。current `tsaiten` visible tables は引き続き reference fallback を使う (`referenceFallbackAllowed:true`、SVG で `referenceFallbackUsed:true` group が 2 件) が、`sourceReplacementBlockedReason` として `source-derived-layout-not-renderable` や `source-page-y-render-admission-not-ready` を持つため、出力を変えずに残りの removal work が明示される。
- [x] generic `table_grid_overlay_layout` fallback path から `tsaiten_table_grid_overlay_layout` を外し、explicit な `reference_table_grid_overlay_layout` / diagnostic reference probes は維持する。これにより visible な `tsaiten` calibration は legacy reference-only path に隔離され、unrelated generic table diagnostics の silent default bbox source ではなくなる。`tsaiten` regression は `render_page_svg` が引き続き `tsaitenReferenceProjection` を expose することを固定しつつ、scoring table に対する direct `table_grid_overlay_layout` が `(174,301,421,32.2)` reference constants ではなく generic page body frame に fallback することを証明する。
- [x] `pageMarkScopedYTransformProbe` 自体の top level に reference-target diagnostic であることを明示する。probe は source-backed PageMark/LineMark record matches の横に `referenceBBoxUsed:true`、`referenceTargetBasis:"referenceTableBBox.rowTopTargets"`、`sourceOnlyReplacementBlockedReason:"page-mark-scoped-y-transform-targets-reference-backed"` を emit する。`tsaiten` assertions は candidate `0` (`lineMarkRecordIndexes:[8,10,12,14]`) と candidate `3` (`[33,35,37]`) でこの split を固定する。scoped probe は reference row tops と比較してよいが、source-only render admission は `sourceOnlyPageYOriginSelector`、`sourceOnlyPageMarkAbsoluteYSlotGate`、`lineHeaderLineMarkCouplingEvidence` を通す必要がある。
- [x] ABC `sourceOnlyAxisAdmissionGate` を active source-derived page-space solver と整合させる。table が source-backed line-header units、exact `/LineMark` rows、`lineMarkPageGrid` origin、source y-origin solver によってすでに renderable な場合、selector-only fallback 側に horizontal selector absent や single-support y selector が残っていても、gate は `activeSourceLayoutAdmissionReady:true` と `admissionReady:true` を報告する。これらの fallback facts は blocker ではなく diagnostics として保持し、renderable active source layout を持たない `tsaiten` tables は fragmented または single-support y selector evidence によって引き続き block する。
- [x] ABC table の top-text placement coherence を horizontal `sourceFrameAdmissionGate` に mirror する。source-frame gate は `sourceTopTextPlacementCoherenceMirror` として `sourceTopTextPlacementEvidencePresent:true`、`sourceTopTextPlacementReady:true`、readiness blockers 空、`renderPromotionBlockedReason:null` を持ち、visible selected-width source frame (`page-body-frame` plus `documentTextLineHeaderUnitTransform`) を `topTextTableSourceGapEvidence` だけでなく source-only instruction-anchor / trailing-header / page-grid readiness chain に直接接続する。
- [x] 同じ source-only placement coherence が存在する場合、ABC table の `totalWidthSemanticsGate` handoff を閉じる。width gate は selected visible range `0..88` に対して `sourcePlacementCoherenceGateEvidencePresent:true`、`sourcePlacementCoherenceGateResolved:true`、coherence blockers 空、`renderPromotionNextGate:null`、`renderPromotionBlockedReason:null` を出す一方、`tsaiten` など unresolved tables は既存の decoder/coherence blockers のままにする。これにより raw PageMark horizontal fields を decoded 扱いせず、selected-width/full-extent split を source-backed に保つ。
- [ ] `tsaiten` で table/page-grid geometry を generalize してから reference table calibration を外す。lower error table は raw-record-index stride-Y candidate を representative source-derived layout として保持するようになったが、sparse-sibling-derived candidate は `sparse-sibling-derived-candidate-render-ineligible` のまま render-ineligible に保つ必要があり、X/width semantics と non-reference page-Y transform はまだ未証明。scoring table には compact-to-sparse source topology evidence があるものの、record-index affine diagnostics は raw `/LineMark` stride slope が reference row tops に対して速すぎることを示し、`lineMarkRowGapSequenceEvidence` は selected records が post-row spacing records で直前 records が compact row spans であることを示す。新しい per-family row-source-start affine diagnostic は実際の `tsaiten` transform candidate (`x = 143/6*y + 671/6`、residual `<= 1`) を記録し、decoded `success_data-test` ABC table でも独立に整合する (`1/3` residual) が、`hyo` は `tsaiten` formula と `51.667` ずれて矛盾し、probe corpus は別 family (`1103/17*y + 276/17`) に fit するため、`affine-row-source-start-gap-family-transform-authority-unproven` が non-rendering のまま保持する。worker-10 の PageMark slot sweep も broad raw-slot promotion を block する: lower `tsaiten` table の `768.000` slot は reference top `768.014` と一致するが、scoring table は raw slot `1024.000` と reference top `301.005` が矛盾し、ABC table には同じ slot が存在しない。alternate previous-row-span PageMark probe でも same-header y fields は得られず、`sourcePageYTransformGate` は lower table の PageMark span hits が row-unique ではない (`[174,734,174]`)、source-range coverage が incomplete (`3/4` scoring rows, `0/3` lower rows)、かつ shared sparse sibling 下の cross-table ordering が regresses する (`rawScanIndexes:[3,3,3,2,3,2,2,6,2]`) ことを証明したため、span correlation はまだ page-y origin decoder として使えない。追加 source fields から scoring-table page-space transform を derive し、y transform を両方の visible table families で validate し、その evidence が stable になるまで `/PaperMark` semantics は diagnostic-only のまま維持してから `tsaiten_table_grid_overlay_layout` を置き換える。
- [ ] 新しい `primitiveOwnershipComparison` role candidates が source cohorts や reference PDFs と矛盾しない形で FDMIndex row ownership と paint-order semantics から説明できるまで、Q4/Q5 FDM primitive ownership を promote しない。
- [x] Q4/Q5 FDM primitive role groups に source-only の `paintOrderContinuityProfile` diagnostics を追加し、rendering は変更しない。Q4 の `main-circle-anchor` role は command offsets `308..504` を span するが non-role commands が `4` 件 interleave する (`commandCountInSpan:7`、continuity score `0.429`)。一方 Q5 の `line-candidate` role は `1992..2024` を span し、interleaved command はない (`commandCountInSpan:2`、score `1.000`)。どちらも `ownershipProven:false` のまま、interleaved spans は `role-span-interleaved-non-role-commands` で block され、contiguous spans は FDMIndex row order が paint-order authority として証明されるまで `role-paint-order-authority-unproven` として non-rendering のまま残る。
- [x] Q4/Q5 `primitiveOwnershipComparison` diagnostics に source-backed な `ownershipGate` を明示的に追加する。gate は row-command direction、one-to-one row mapping、multi-command single-row evidence、raw/segment cohort mixing、command-offset p95 gaps、active row/index ownership blockers を記録し、rendering は変更しない。Q4 は `mixed-raw-and-segment-cohorts` (`rowCommandGapP95:240.000`) で block され、Q5 は `multi-command-single-index-row`、`mixed-raw-and-segment-cohorts`、`row-command-reference-not-one-to-one` を記録する (`rowCommandGapP95:132.000`)。role と paint-order の未解決性はこの ownership blocker list ではなく role/profile/admission gates に残す。
- [x] 同じ Q4/Q5 `primitiveOwnershipComparison` diagnostics に source-backed な `offsetFieldAuthorityGate` を追加する。gate は FDMIndex の `bbox.left` references が command-relative offsets に一致するのか source-segment-relative offsets に一致するのかを分離し、raw/segment command cohorts を記録する。render promotion は `fdm-index-offset-field-authority-mixed-command-and-segment-fields` で block する。現在 Q4 は command-relative `18`、source-segment `2`、Q5 は command-relative `1`、source-segment `6` なので、authoritative offset namespace が decoded されるまで promote しない。
- [x] Q4/Q5 FDM primitive ownership に source-backed な `rowFanoutSegmentOwnerGate` を追加する。Q4 は `20` row references / `20` unique rows で fanout なし (`maxRowFanout:1`) だが offset namespace が mixed と明示される。Q5 は fanout rows `[40,41]`、`fanoutRowCount:2`、`fanoutReferenceCount:6`、`maxRowFanout:4`、`fanoutRowsUseSourceSegmentOffsetFields:true` を記録する。rendering は Q5 で `fdm-index-row-fanout-segment-owner-multi-command-single-row`、Q4 で `fdm-index-row-fanout-segment-owner-offset-namespace-mixed` として block する。
- [x] Q4/Q5 `indexRowReferenceRoleCandidateGroups` の各 entry に source-backed role-level `roleFanoutSegmentOwnerGate` diagnostics を追加する。Q5 `line-candidate` role は references `[1992,2024]` が両方とも FDMIndex row `40` に map すること、`fanoutRowCount:1`、`maxRowFanout:2`、`fanoutRowsUseSourceSegmentOffsetFields:true`、`renderPromotionBlockedReason:"fdm-index-role-row-fanout-multi-command-single-row"` を expose する。これにより remaining ownership work は projection-level fanout ではなく、render されそうな具体的 role candidate まで絞られる。
- [x] Q4/Q5 `primitiveOwnershipComparison` diagnostics に source-backed な `primitiveOwnershipAdmissionGate` を追加する。gate は `ownershipGate`、`offsetFieldAuthorityGate`、`rowFanoutSegmentOwnerGate`、role-level `roleFanoutSegmentOwnerGate`、未解決の paint-order continuity を集約し、primitive ownership を promote する前に render-admission blocker list として固定する。Q4 は mixed raw/segment cohorts、mixed offset namespaces、valid vector-offset role references missing、paint-order continuity で block される (`roleGroupCount:6`)。Q5 は multi-command single-row ownership、row fanout、role fanout、valid vector-offset role references missing、paint-order continuity で block される (`roleGroupCount:4`, `roleFanoutBlockedGroupCount:3`)。admission list は paint-order の未解決性を `ownershipGate` 由来の generic primitive-role blocker ではなく、specific な `role-paint-order-continuity-unproven` blocker として保持する。
- [x] Q4/Q5 role paint-order diagnostics を source-span continuity blockers と authority-pending candidates に分離する。`paintOrderContinuityProfile` は `spanContiguousCandidate`、`continuityBlocked`、`paintOrderAuthorityPending` を報告し、interleaved roles は `role-span-interleaved-non-role-commands`、contiguous roles は non-rendering のまま `role-paint-order-authority-unproven` として block される。Q4 は引き続き 6 roles が continuity-blocked だが、Q5 solid-diagram は 4 roles 全部を continuity failure と扱わず、2 continuity-blocked roles (`arc-candidate`, `connector-candidate`) と 2 authority-pending roles (`line-candidate`, `surface-boundary-candidate`) に分離する。
- [x] generic な `indexRowOrderPromotionGate` primitive-role blocker を source-backed row-order blocker list に置き換える。Q4 は monotonic one-to-one row-command evidence を保持しつつ、`fdm-index-row-order-valid-vector-offset-missing`、mixed offset namespace、role paint-order continuity blockers を記録する。Q5 は valid-vector/namespace/continuity/authority blockers の前に、より厳しい row-order failures (`fdm-index-row-order-reference-not-one-to-one`, `fdm-index-row-order-single-row-backs-multiple-commands`) を先頭に出す。SVG diagnostics も同じ `data-index-row-order-render-promotion-blocked-reasons` を mirror し、visible rendering は変更しない。
- [x] Q4/Q5 role fanout gates の前に role-level `roleVectorOffsetAuthorityGate` diagnostics を追加する。この gate は role references が FDMIndex offset fields から source-backed で見つかる一方、現在一致する role references の `FDMIndex.vectorOffset` はすべて invalid であることを記録する (`validVectorOffsetReferenceCount:0`, `allReferencesHaveInvalidVectorOffset:true`)。top-level admission gate は legacy な `fdm-index-role-valid-vector-offset-missing` の前に、より具体的な `fdm-index-role-vector-offset-authority-valid-vector-offset-missing` blocker を持つようになったため、role references を render ownership に使う前に authoritative FDMIndex field を証明する必要がある。
- [x] `shanai_lan` same-row axis-rule connector diagnostics に source-backed FDMIndex segment membership gate を追加する。各 `fdmConnectorCandidateDiagnostic` は `sourceVectorRelativeOffset` と `sourceSegment` を保持し、row cohort は `fdmIndexSegmentGate` として FDMIndex vector offset/length、source-segment match counts、image signature counts、dual-endpoint image-bearing counts を expose する。current leading row 33 は vector offset `142284`、length `7222`、image signatures `3`、dual-endpoint candidates `7`、dual image-bearing connectors `7` なので `fdm-index-image-bearing-segment-role-unproven` で render block する。non-image-bearing rows `3/0/37/2/10` は `fdm-index-segment-ownership-and-paint-order-unproven` のまま block する。
- [x] 同じ `shanai_lan` row cohorts に source-backed FDMIndex bbox relation gate を追加する。`fdmIndexBboxRelationGate` は各 connector command source bbox と owner FDMIndex bbox を比較し、描画を変えずに contained/internal strokes と disjoint external-connector candidates を分離する。current evidence では row 37 の dual-endpoint connectors 3 件はすべて FDMIndex bbox 内にあり `fdm-index-bbox-contained-internal-stroke-role-unproven` で block する。rows 3、0、2、10 は dual-endpoint connectors がすべて FDMIndex bbox と disjoint であり、`fdm-index-bbox-disjoint-connector-ownership-unproven` として次の ownership/paint-order decode 対상に残る。row 33 は connector bbox relation では disjoint だが image-bearing segment gate で 먼저 block する。
- [x] visible wiring promotion の前に、同じ `shanai_lan` row cohorts へ diagnostic-only の FDMIndex connector-composition gate を追加する。`fdmIndexConnectorCompositionGate` は parse 済みの `ObjectFdmIndexEntryCandidate` と row-cohort counts から `vectorCommandCount`、`connectorCandidateCount`、non-connector command count、connector density、row-cohort matched coverage、row-cohort dual-endpoint coverage を aggregate する。current evidence では render は未 promote のまま: row 33 は image-bearing (`85` commands、`14` connector candidates、density `164` permille、dual coverage `500`)、rows 3/0/2/10 は disjoint ownership/paint-order candidates のまま、row 37 は contained/internal のままである。row 0 は coverage denominator を first FDMIndex entry connector count (`11`) ではなく row-cohort connector count (`24`) にすべきことも固定する。
- [x] paint-order promotion の前に、`shanai_lan` row cohorts へ source-backed axis-rule source-order gate を追加する。`axisRuleSourceOrderGate` は dual-endpoint connector の normalized parent relative offset を same-row axis-rule matches の normalized parent offset span と比較する。current evidence では row 0 の dual connectors 4 件はすべて axis-rule parent span の中 (`7644..9734` inside `3460..12022`)、row 10 の single dual connector は axis-rule parent span より前 (`1298` before `1376..2056`)、rows 3/33/37 は mixed であり、inspected rows はすべて source-order evidence backed かつ unclassified 0 件である。connector lines はまだ描画하지 않는다.
- [x] source-order `shanai_lan` connector classes の straight-line render probe を reject する。144dpi Poppler baseline は `mean=24.536415`、`RMS=71.637773`、`changed>32=273818`。width 2 で row 0 between-span connectors を描くと `24.553023/71.672634/273874`、row 2 between-span は `24.539779/71.647012/273813`、row 10 before-span は `24.547586/71.658740/273897`、row 3 mixed は `24.562030/71.689283/273965`、rows 0+2 は `24.556386/71.681868/273869` に悪化する。row 2 は changed-pixel 数だけわずかに減るが mean/RMS は悪化するため、endpoint straight-line drawing は diagnostic-only のままにする。次の wiring work は projected endpoints の promotion ではなく、FDMIndex ownership/paint order または compound path semantics の decode に進む。
- [x] tight/non-diagonal/dual-endpoint な `shanai_lan` same-row FDM axis-rule candidates に対して、per-connector `sameRowFdmOpenStrokeAxisRuleConnectorOrderTrace` layer diagnostic を追加する。trace は row number ではなく既存の source-backed readiness predicate で選択し、各 connector の FDMIndex row facts (`indexOffset`, `vectorOffset`, `vectorLength`, `kindHex`, normalized bbox, image-bearing flags, entry connector count)、connector provenance (`sourceSegment`, parent compound, source bbox, entry-connector membership)、endpoint nearest owners、per-rule normalized parent relative offset 付き same-row axis-rule matches、derived owner/axis/bbox relations、sorted `sourceOrderNodes` を emit する。wiring はまだ描画하지 않으며、straight-line endpoint rendering reject 後の promotion blocker を connector 粒度で inspect できるようにする。
- [x] future render promotion が individual connector traces を cherry-pick しないよう、`sameRowFdmOpenStrokeAxisRuleConnectorOrderTrace` に source-backed relation-count `summary` を追加する。summary は trace count、source-segment-to-FDMIndex matches、entry connector membership、image-bearing segment count、parent-normalized ordered owner count、bbox relation counts、connector-vs-axis-rule parent-span counts、owner-vs-axis-rule parent-span counts を記録する。さらに image-bearing traces を FDMIndex bbox relation、connector-vs-axis-rule parent span、complete payload extraction、connector offset relation to segment image-signature range で分け、各 trace は image signature offsets、payload extraction status、nearest signature distance、per-signature containing FDMVector command contexts、bbox/source-order blockers を持つ `imageBearingSegmentGate` を expose する。current local regression は `promotionReady:false`、`readinessBlockedReason:"image-signature-fragment-role-unproven"`、`imageBearingSegmentCount:5`、`imageBearingCompletePayloadSegmentCount:0`、`imageBearingSignatureWithoutPayloadSegmentCount:5`、`imageBearingBboxRelationCounts:{disjoint:5}`、`imageBearingConnectorVsAxisRuleParentSpanCounts:{before:4,after:1}`、`imageBearingConnectorVsSegmentImageSignatureRangeCounts:{inside:4,after:1}`、`parentNormalizedOrderedSameRowSameConnectorCount:0` を固定している。row 33 segment signatures は complete image payload ではなく FDMVector command records 内にあることも証明した: local offset `2453` は command 10 と synthetic nested command 10001 の内部、`4389` は command 25 の内部、`5957` は command 45 の内部である。signature-fragment command roles と connector/owner/axis ordering が global に説明されるまで wiring promotion を block する。
- [x] 次の connector promotion 前に、全 compound FDMVector command diagnostics へ source-backed `compoundChildLayoutGate` を追加する。gate は prefix child-offset table が最初の child と正確に一致すること、offsets が strict increasing であること、marker-valid child record の declared length が parent 内に収まり次の child と overlap しないことを検証する。current `shanai_lan` compound commands は layer-tree と object-stream JSON でこれらの boundary facts を expose するが、`compound-child-boundaries-do-not-prove-connector-ownership-or-paint-order` により diagnostic-only のままとする。構造的な child boundaries だけでは connector role、endpoint owner、page placement、paint order を割り当てられないためである。これにより次の source-order investigation から marker-scan ambiguity を除きつつ、A4 landscape PDF は変更しない。
- [x] strict な `sameRowFdmOpenStrokeAxisRuleConnectorOrderTrace` readiness summary を top-level `fdmConnectorGraphDiagnosticSummary` に promote するが、wiring は描画しない。graph summary は同じ non-hardcoded predicate (`tightDualEndpoint`, non-diagonal, same-row FDM axis-rule matches) で `sameRowFdmOpenStrokeAxisRuleConnectorOrderTraceSummary` を持ち、より具体的な blocker `image-signature-fragment-role-unproven` を graph level で確認できるようになった。row cohort はさらに source-backed `markerStyleAgreementGate` を expose し、tight non-diagonal connector と same-row axis-rule の marker/style dominance を比較する。current evidence は connector と axis-rule の marker/style dominance がまだ一致しないため non-rendering のまま維持する。
- [x] `shanai_lan` single-page diagram projection から exact filename gate を除去する。`project_fdm_single_page_diagram` は `document_has_shanai_lan_fdm_command_evidence` のみを使うため、同じ source-backed `/DocumentText` と FDMVector command-density evidence を持つ renamed LAN diagram も fallback text pagination を 1 diagram page に truncate できる。local regression は同じ parsed sample を `renamed-lan-diagram.jtd` として構成し、page count と FDM primitive layer output を確認する。
- [x] runtime path から exact filename-based layout/rendering hints を除去する。`DocumentCore::from_document` と `set_file_name` は `source_document_layout_hint(document, decoded_layout)` を通るようになり、Ginga vertical/front-matter documents、`shanai_lan` FDM command/frame evidence、`success_data-test` projection evidence、`tsaiten` table evidence、`fax02` VisualList evidence など parsed document evidence を再利用する。filename-only synthetic inputs は default/source-decoded layout のまま維持し、A5/A6/B6、`shanai_lan`、`success_data-test`、`tsaiten` の renamed local samples は writing mode、page size、page count、代表 layer/SVG projections が同じく維持される。
- [x] `shanai_lan` background/page-fill shape investigation の前に、non-hardcoded FDM page-paint coverage gate を追加する。各 `fdmVectorPrimitiveProjection` の `paintCoverage` は projected bbox、closed/fill evidence、既存の large-span filter (`FDM_VECTOR_PATH_DIAGNOSTIC_MAX_SPAN_RATIO = 0.28`) から計算され、固定の `pageFillCandidate:false` ではなくなった。`fdmConnectorGraphDiagnosticSummary` は `pagePaintCoverageSummary` を aggregate し、large-span filtered primitives は引き続き `renderable:false` のまま、source role、page extent、paint order が decode されるまで `fdm-page-fill-source-evidence-unproven` で block される。current PDF-backed `shanai_lan` regression は `pageFillCandidate:true` が出ないことを確認しているため、visible LAN primitives と SVG/PDF pixels はこの diagnostic gate では変わらない。
- [x] selected `shanai_lan` `/DocumentText` line-header candidates の full-span rendering を reject し、diagnostic-only として保存する。144dpi Poppler probe では 7 件の selected skipped-inline horizontal line headers を full-width rules として描くと reference comparison が `mean=24.536415`、`RMS=71.637773`、`changed>32=273818` から `25.839653/73.920877/284015` に悪化した。`documentTextLineHeaderProjectionCandidateSummary` は `fullSpanRenderPromotionGate` と `fullSpanRenderableCandidateCount:0` を expose し、各 candidate は source evidence から segment clipping、endpoint ownership、paint order が decode されるまで `line-header-segment-clipping-and-endpoint-ownership-unproven` で block される。
- [x] `/DocumentViewStyles` の first sequential record `0x1001` signal を diagnostic writing-mode evidence として保持し、render authority には promote しない。`0x1001` は vertical Ginga samples に出るが、reference PDFs で horizontal な `tsaiten`、`tmogi3_2`、`success_data-test`、`shanai_lan` にも出るため、`DocumentCore::from_document` は source document layout hints または horizontal default から writing mode を選択する。`getDocumentInfo` は `writingModeDecision`、`writingModeCandidateFromDocumentViewStyles`、`writingModeCandidateFromDocumentViewStylesSourceBacked`、first record code/hex を expose し、full style semantics が decoded-false の間も heuristic を audit できる。同じ document-info JSON は `rjtd document-info <file.jtd>` からも取得できるため、app/wasm glue なしで local sample comparison が selected writing-mode source を確認できる。`writingModeDecision` は source document layout hints、DocumentViewStyles、PaperMark candidates が selected writing mode と矛盾するかどうかも記録する。
- [x] image-payload render admission を強化しつつ visible rendering は promote しない。`imagePayloadDiagnostic` の layer/overlay JSON と SVG data attributes は、source ownership、`/Frame` reference-row geometry candidates、`EmbeddingInfo -> /Frame` source-frame traces、source frame-record geometry、candidate frame bbox (`candidateFrameBBox`)、payload-to-frame aspect-fit diagnostics を、まだ block している page-assignment/paint-order proof から分離して expose する。Synthetic coverage は ownership、frame-row evidence、`EmbeddingInfo`、nonzero `/Frame` geometry、source-derived candidate bbox、best payload aspect candidate が存在しても `renderable:false` が維持され、`image-payload-frame-geometry-present-but-page-assignment-and-paint-order-unproven` で block されることを固定する。local PDF-backed samples でも少なくとも 1 つの payload がこの final gate に到達することを確認した。
- [x] `shanai_lan` raw line-rule projections に per-rule / per-component render admission gates を追加し、wiring は描画しない。各 `documentTextLineRuleProjection` は `renderAdmissionGate` として component membership、`/LineMark` coverage、orthogonal/topology readiness、endpoint text-attachment candidates、`document-text-grid-origin-reference-backed`、`line-rule-topology-partial-orthogonal-coverage`、`line-rule-component-topology-unproven`、`line-rule-text-attachment-pair-absent`、`line-rule-endpoint-ownership-unproven`、`line-rule-component-endpoint-ownership-unproven`、`line-rule-component-line-mark-coverage-incomplete`、`line-rule-style-role-unproven`、`line-rule-component-style-role-unproven`、`line-rule-paint-order-unproven`、`line-rule-component-render-admission-not-ready`、`line-rule-text-attachment-pair-unproven` などの blockers を持つ。projection summary も `lineRuleRenderAdmissionGate` を aggregate し (`ruleCount:16`、`componentCount:6`、`orthogonalComponentCandidateCount:3`、`bothEndpointAttachmentWithinLineHeightRuleCount:0`)、今後の wiring work が raster 上たまたま改善する rule を cherry-pick できないようにする。rendering は引き続き `line-rule-render-admission-not-ready` で block する。
- [x] `tsaiten` source-gap-to-page-line transform gates に table-family stability blocker を追加する。transform admission gate は best current candidate と family-stable transform を分離し、全 transitions が同じ PageMark entry にあり selected source-gap transform の max delta が 0 になった場合だけ page-y / axis admission が decoded として扱える。current scoring/lower tables は nested transform gates と final source-only admission blockers の両方で `source-gap-to-page-line-gap-transform-unstable-across-table-family` を expose し、visible rendering は reference fallback のままにする。
- [x] 同じ source-gap transform gates に diagnostic-only の `affineRowSourceStartGapFit` を追加する。この fit は 1 つの PageMark/table family に scoped された exact rational source evidence であり、`tsaiten` では `slope=143/6`、`intercept=671/6`、`maxAbsResidual:1.000`、`sampleCount:3`、`familyScoped:true`、`fitStable:true` を emit する。ただし transform candidate は常に `affine-row-source-start-gap-family-transform-authority-unproven` で declined され、diagnostic candidate count だけを増やし、`canDecodeSourceTransform:false`、`admissionReady:false`、render blockers は変えない。
- [ ] `/DocumentViewStyles` 以外に残る一太郎の縦書き flags、特に `/PaperMark` flag `0x00010011` を decode する。`getDocumentInfo` は bit-level PaperMark evidence を expose するようになった: bit-0 rows には `paperMarkFlagBit0VerticalCandidate:true`、bit-17 rows には `paperMarkFlagBit17IndexStepCandidate:true`、Ginga vertical samples には `paperMarkWritingModeCandidateEvidence:["paper-mark-flag-bit0-vertical-corpus-consistent"]`、そして `writingModeCandidateDecoded:false` のまま blocker `paper-mark-writing-mode-flag-semantics-unproven` を出す。current corpus evidence では bit 0 は parsed vertical-rl Ginga samples (`46.jtd`, `a5.jtd`, `b6.jtd`) のみに出現する。一方 bit 17 は landscape bit ではなく、portrait `dousoukai.jtd` が counterexample で、landscape `shanai_lan` には存在しない。bit 17 は bit-16-only step `1` に対する PaperMark index step `2` を追跡している。この item は three vertical samples だけでは cross-version semantics を証明できないため unchecked のままにし、bit 0 が stable writing-mode flag、section transition marker、page-layout state のどれかを render authority に使う前に証明すること。
- [ ] `rjtd-export` の local PDF artifact regression で known divergence として固定した、新しく判明した local reference PDF の pagination/orientation gaps を decode する。fallback-pagination divergence は 7 件 (`iwata_file`, `resume`, `fujimoto_file`, `christmas_2001`, `syokuhin`, `natsu`, `asobinin_24`)、`seminar2004` の landscape orientation divergence は 4 件 (`shortcutkey1`, `shortcutkey2`, `toolbox`, `shortcutkey3`)。source-backed pagination と paper-orientation semantics が fallback output を置き換えるまで、この lock を drift evidence として維持する。
- [ ] renderer が non-text visual primitives を emit できるようになったら、app compatibility layer に real canvas/layer paint operations を実装する。
- [ ] `/LineMark`、`/PageMark`、`/PaperMark`、style、object semantics が証明されたら、fallback cursor/hit-test geometry を real JTD layout geometry に置き換える。
- [ ] true JTD paragraph records、inline controls、styles が decoded されたら、text-flattening body edit fallback を structure-preserving edits に置き換える。
- [ ] 対応する JTD streams が decoded されたら、real page/section/page-border mutation を実装する。
- [ ] default formatting fallback JSON ではなく、real character、paragraph、style、numbering、bullet streams を実装する。
- [ ] table/cell no-hit fallback APIs を decoded JTD table structures、nested cell paths、geometry、style、formula semantics に置き換える。
- [ ] picture/shape/equation/bookmark/form no-hit fallback APIs を decoded JTD object/control structures と real binary/image/form data に置き換える。
- [ ] common document model と JTD object semantics が十分 mature になったら、true HTML paste/export と HWP/HWPX/common-format export を実装する。
- [ ] selection/clipboard APIs を body plain text から拡張し、inline controls、tables、cells、pictures、shapes、fields、footnotes、unknown objects を preserve する。
- [ ] full rhwp Studio feature parity に必要な table、cell、header/footer、footnote、picture、shape、field、style、undo/redo、clipboard、search APIs を実装する。
- [ ] JTD または common document format への save/export back を実装する。current `HwpDocument` compatibility は read/render oriented である。

## M4: Markdown Export

Goal: document model layer 経由で `rjtd export <file> --format md` を実装する。

Status: minimal model-based Markdown export implemented.

Completed:

- [x] `Document` を consume する Markdown exporter を実装する。
- [x] `rjtd export <file> --format md` を wire する。
- [x] local `a5.jtd` が `一、午后の授業` のような recovered section titles を生成することを確認する。

Remaining:

- [ ] record parser がそれらを expose した後、headings、lists、tables、ruby、layout semantics を保存する。
- [x] model が stable block/inline semantics を持った後に HTML export を追加する。

## M5: WASM Viewer on Cloudflare Pages

Goal: `rjtd-wasm` を `wasm-pack` でビルドし、ブラウザで JTD ファイルをドラッグ&ドロップして閲覧できる静的 Web ビューアを Cloudflare Pages にデプロイする。

Status: MVP デプロイ済み（2026-06-24）。
- https://openjtd.pages.dev
- https://jtdview.pages.dev
- https://jtd-438.pages.dev（初期テスト用）

背景:
- `rjtd-wasm` クレートは `cdylib` + `wasm-bindgen` 構成済みで、`renderPageSvg()`、`renderPageHtml()`、`renderPageToCanvas()`、`plainText()`、`pageCount()` などのブラウザ API を公開している。
- レンダリング精度は M2/M3 でアクティブに更新中のため、ビューア実装はレンダリング改善を妨げない範囲に限定する。

前提条件:
- `wasm-pack` の `--target web` ビルドが通ること（`cfb` crate 等の I/O 依存を WASM で確認する）。
- `rjtd-core` が `std::fs` を直接使わず、バイト列インターフェース経由で CFB を読める状態であること。

Immediate tasks:

- [x] `wasm-pack build --target web rjtd/crates/rjtd-wasm` が通るか確認し、WASM 非対応の依存（`std::fs`、`image` crate のデスクトップ feature など）を特定する。→ 追加対応不要で一発通過。
- [x] `openjtd.github.io/` に最小限の HTML フロントエンドを作成する（ファイルドロップ → `HwpDocument.new(bytes)` → `renderPageSvg()` を `<div>` に挿入する MVP）。デジタル庁デザインシステムのカラートークン・タイポグラフィ適用済み。
- [x] ページナビゲーション UI（前ページ・次ページボタン、ページ番号表示）を追加する。
- [x] `plainText()` 出力タブを追加する。
- [x] `wasm-pack` 成果物（`.wasm` + JS glue）と HTML を Cloudflare Pages にデプロイする。
- [x] Cloudflare Pages の GitHub Actions デプロイパイプラインを設定し、`rjtd-wasm` ソース更新時に自動ビルド・デプロイできるようにする。

Constraints:
- ファイルはブラウザ内で処理し、サーバーへ送信しない（プライバシー要件）。
- レンダリング品質は M2/M3 の進捗に依存するため、MVP は `decoded:false` 状態のまま公開してよい。
- `rjtd-model`、`rjtd-core` の解析コードはビューア実装のために変更しない。ビューア側が WASM API に適応する。
